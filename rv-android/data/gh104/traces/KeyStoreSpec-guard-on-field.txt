# guard-on-field: getKey() reads `currentKSType` on a store whose getInstance the
# monitor never saw, so the guard tests the empty string while the object itself
# carries PKCS12
bind ks = KeyStore.getInstance("PKCS12")
ks.load(null)
ks.getKey("alias", chars)

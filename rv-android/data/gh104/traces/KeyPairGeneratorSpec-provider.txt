# gh109 task 8.9, the separating half: the same two-argument route with an admitted algorithm.
# `RSA` is one of the five `KeyPairGenerator.crysl:28` names, so `g2` takes the call, binds
# `algorithm`, and `g4`'s negated condition is false. 2048 is one of the four RSA key sizes the
# rule's CONSTRAINTS admit, the ORDER is `Get, Init, Gen`, and nothing is drawn.
#
# What the pair shows is that 8.9 added an accuser and not a blanket: before it, BOTH halves of
# this pair were silent, and the silence meant two different things.
KeyPairGenerator.getInstance("RSA", "SunRsaSign") -> kpg
kpg.initialize(2048)
kpg.generateKeyPair()

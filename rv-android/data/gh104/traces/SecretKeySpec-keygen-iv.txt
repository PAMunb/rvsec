# The F2 window this pass opens, and the witness that closes it at task 4.14.
#
# The same chain as SecretKeySpec-encoded-iv.txt with the other producer of
# `generatedKey`: KeyGenerator.generateKey() (KeyGenerator.cryptsl), whose write is still
# on the old substrate until task 4.14. So the read here answers NOT_OBSERVED, nothing is
# staged, and the IV is accused of an origin nobody observed -- about a program that
# breaks no clause.
#
# It is a window and not a regression: measured over the whole ErrorCollector, this
# program drew the same single IVPARAMETERSPEC-NOBS-00 before the pass, because the write
# it suppresses went to a store no reader of `randomized` has used since task 4.4. The
# report count is identical on both sides and the reason for it is not.
bind km = bytes(16)
KeyGenerator.getInstance("AES") -> kg
kg.init(128)
kg.generateKey() -> key
key.getEncoded() -> enc
new IvParameterSpec(enc) -> iv

# task 9.14. Two key stores alive at once -- the case a process-wide monitor cannot tell
# apart. Before: the specification declares a parameter no event binds, so the generator
# emits one monitor for the whole process, the second getInstance arrives where the `ere`
# expects a `load`, and the program is accused of a wrong call sequence. After: one monitor
# per store, and each store's `g1 load` is accepted on its own.
KeyStore.getInstance("PKCS12") -> ks1
KeyStore.getInstance("JKS") -> ks2
ks1.load(null)
ks2.load(null)

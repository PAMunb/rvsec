# violating branch: key material nothing randomised. The file name is historical --
# gh104 task 2.4 removed the algorithm half of the guard, so what is left to violate
# is the predicate read. Before gh105 task 3.4 this fired the orphan c3; after the
# fusion it fires c1, which accuses from its body.
new SecretKeySpec(bytes, "DES") -> spec

# The silent half of ledger #36: a key store of a type the rule admits, really loaded, handed
# to TrustManagerFactory.init. The clause `generatedKeyStore[keyStore]`
# (TrustManagerFactory.cryptsl:42) is satisfied and the read stays quiet.
KeyStore.getInstance("PKCS12") -> ks
bind ready = ks.load(null)
ks.load(null)
TrustManagerFactory.getInstance("PKIX") -> tmf
tmf.init(ks)

# task 9.16, the one file of the five that had no two-argument getInstance at all and needed
# a new event. Before: nothing is dispatched for the call, the store reaches `load` with the
# monitor at state 0, and `load[0]` is fail. After: `g3` fires and `g1 | g3` opens the `ere`
# the same way, so `load` is accepted.
bind p = Security.getProvider("SUN")
KeyStore.getInstance("JKS", p) -> ks
ks.load(null)

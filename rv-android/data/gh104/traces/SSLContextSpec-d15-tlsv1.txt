# D-15 (task 11.9). The api30 anchor admitted this value; the expert anchor rejects it.
# The corpus cannot supply the case, so it is replayed here: no published number moves,
# which is exactly why it would otherwise have travelled on as an unwitnessed false negative.
#
bind ctx = SSLContext.getInstance("TLSv1")
ctx.init(null, null, null)

# the four-argument constructor: the second `event c1` of :34, which the `ere`
# names `c2` and the file never declares (G-ERE's single hit on the frozen set)
bind iv = bytes(16)
SecureRandom.getInstance("SHA1PRNG") -> r
r.nextBytes(iv)
new GCMParameterSpec(128, iv, 0, 16) -> spec

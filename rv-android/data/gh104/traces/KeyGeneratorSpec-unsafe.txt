# violating branch g3: an algorithm outside the allow-list. The ordering reports at `init` and
# `gk1` are KEYGENERATOR-ORDER-02, label `creation-refused`: the refused `getInstance` (g3)
# sets `creationRefused`, and the automaton admits no use of a generator it refused.
KeyGenerator.getInstance("DES") -> kg
kg.init(56)
kg.generateKey() -> key

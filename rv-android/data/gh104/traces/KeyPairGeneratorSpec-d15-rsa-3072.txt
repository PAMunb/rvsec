# D-15 (task 11.9). A value D-10 narrowed away and the re-anchoring restores. Against the
# frozen jca this trace MUST class `unchanged` -- that is what proves the narrowing was
# undone rather than merely described in a record.
#
KeyPairGenerator.getInstance("RSA") -> kpg
kpg.initialize(3072)
kpg.generateKeyPair() -> kp

# gh109 task 2.4, the violating half: a 1024-bit prime. What the rule states about the subprime
# is a separate clause, and the sibling is conforming here so the accusation is unambiguous.
new DSAParameterSpec(bits(1024), bits(2048), bits(2048)) -> params

# gh109 task 3.7, the twin of DigestInputStreamSpec-forbidden-on.txt: `DigestOutputStream.crysl`
# states `on(boolean)` FORBIDDEN, and the automaton makes the forbidden event a SELF-LOOP so the
# `write` that follows still reaches its state and the `close()` still accepts. One FORB code and
# no ORDER code beside it.
#
# The digest comes from an observed `getInstance`, so the `generatedMessageDigest` read at `c1`
# -- the predicate task 1.3(b) taught `MessageDigestSpec` to write -- answers SATISFIED and
# nothing is drawn there.
MessageDigest.getInstance("SHA-256") -> md
new DigestOutputStream(null, md) -> dos
dos.on(false)
dos.write(0)
dos.close()

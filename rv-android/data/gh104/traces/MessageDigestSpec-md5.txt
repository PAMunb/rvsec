# violating branch: MD5 reaches g4, and the accusation lands on the call that
# consumes the algorithm (update), not on getInstance. The ordering reports at `update` and
# `d1` are MESSAGEDIGEST-ORDER-02, label `creation-refused`: the refused `getInstance` (g4)
# sets `creationRefused`, and the automaton admits no use of a digest it refused.
MessageDigest.getInstance("MD5") -> md
md.update(bytes)
md.digest() -> out

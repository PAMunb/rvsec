# gh104 E6 — the identity discontinuity

Generated by `scripts/gh104_identity_discontinuity.py` (task 9.1). Every number carries its numerator, its denominator, the unit it counted and the ids of the definitions that produced it; the definitions themselves are at the foot of this file.

The question is narrow and it decides whether task 9.2 lands: does adding `code` and `event` to `ErrorSummary`'s identity separate reports that the five-field identity collapses? If it does not, the change is a no-op and design D-5 is re-opened.

## Verdict

- **Deciding corpus**: differential harness (`data/gh104/evidence/harness/*.md`)
- **Discontinuity, whole corpus**: 26 identities the 7-tuple adds — but this corpus holds both eras, so part of that number is the sentinel standing beside a named event rather than a cause being separated
- **Discontinuity, enveloped records only**: **6** — the conservative figure, and the one E6 is decided on
- **Separated by `event` alone**: 3 of those, with `code` identical on both sides
- **E6 lands**: yes

E6 lands only if the discontinuity is non-zero on a corpus whose records carry `ev=`, counting only the records that carry an envelope - so that the sentinel cannot pass the gate on its own. On comp162 the discontinuity is zero by construction and is not the gate.

## Why the deciding number does not come from comp162

comp162 is the corpus the published numbers came from, and it is the corpus this file must state the baseline on. It is **not** the corpus that decides E6, because its records predate the envelope: 0 of its 19,664 rows carry a `v=1` envelope, so every `code` and every `event` parses to the sentinel `UNSPECIFIED` and the two identities cannot possibly differ. Its zero is a property of the input, recorded and labelled, never read as a result about the identity (INV-CORE-57).

## comp162 — the published corpus, zero by construction

Reader: frozen 11-column reader of `scripts/gh104_baseline.py`. Rows: 19,664.

| quantity | measured | definitions | expected | agrees |
|---|---|---|---|---|
| distinct identities under the 8-tuple `(apk, rep, tool, spec, class, method, source, message)` | 6,344 / 19,664 identities / rows = 32.26 % | `identity8` | 6,344 | yes |
| distinct identities under the `ErrorSummary` 5-tuple `(spec, error, class, method, location)` | 409 / 19,664 identities / rows = 2.08 % | `identity5`, `error_type` | 409 | yes |
| distinct identities under the 7-tuple `(spec, error, class, method, location, code, event)` | 409 / 19,664 identities / rows = 2.08 % | `identity7`, `error_type`, `envelope_parse` | 409 | yes |
| records whose message carries a `v=1` envelope | 0 / 19,664 rows = 0.00 % | `envelope_parse` | 0 | yes |
| identities the 7-tuple adds over the 5-tuple | 0 / 409 identities / identities = 0.00 % | `identity5`, `identity7`, `envelope_parse` | 0 | yes |

- `identities_8tuple` — Group 1's freeze item, reproduced here so the two tuples are never interchanged.
- `identities_5tuple` — the era every published deduplicated count of this corpus belongs to.
- `identities_7tuple` — equal to the 5-tuple count by construction: this corpus predates the envelope.
- `records_carrying_an_envelope` — zero is the reason the discontinuity below is zero, and it is a property of the corpus, not a result about the identity.
- `discontinuity` — zero **by construction** - labelled so, not read as a failure (INV-CORE-57).

## The differential harness — the corpus that decides

These are the replay records of the E4, E1 and S rounds, the only inputs in the tree whose accusations carry `ev=`. `TraceRunner` resolves every trace line through one reflective frame and its record carries no stack frame at all, so `class`, `method` and `location` are constant across the corpus and are held fixed. That is what makes it the right instrument rather than a limitation of it: with the site constant, the difference between the two identities is exactly what `code` and `event` add **at one site** — the case INV-INS-126 names.

Records per round: `e1` 62, `e4` 250, `s` 57.

| quantity | measured | definitions | expected | agrees |
|---|---|---|---|---|
| violation records read from the harness reports | 369 / 369 records = 100.00 % | `harness_corpus` | - | - |
| records whose message carries a `v=1` envelope | 281 / 369 records = 76.15 % | `envelope_parse` | - | - |
| distinct `ev=` values inside the envelopes | 17 / 281 events / records = 6.05 % | `envelope_parse` | - | - |
| distinct `code=` values inside the envelopes | 26 / 281 codes / records = 9.25 % | `envelope_parse` | - | - |
| distinct identities under the 5-tuple, with the site held constant | 25 / 369 identities / records = 6.78 % | `identity5`, `harness_site` | - | - |
| distinct identities under the 7-tuple, with the site held constant | 51 / 369 identities / records = 13.82 % | `identity7`, `harness_site`, `envelope_parse` | - | - |
| identities the 7-tuple adds over the 5-tuple | 26 / 25 identities / identities = 104.00 % | `identity5`, `identity7`, `harness_site`, `envelope_parse` | - | - |
| identities the 7-tuple adds over the 5-tuple, counting only enveloped records | 6 / 23 identities / identities = 26.09 % | `identity5`, `identity7`, `harness_site`, `envelope_parse` | - | - |
| five-part identities that the seven-part identity splits into more than one | 6 / 23 identities / identities = 26.09 % | `identity5`, `identity7`, `harness_site` | - | - |
| of those, the ones whose separated causes share a single `code` | 3 / 6 identities / identities = 50.00 % | `identity5`, `identity7`, `harness_site`, `envelope_parse` | - | - |

- `records` — excluded: the `selftest*` reports of the harness self-test
- `records_carrying_an_envelope` — the remainder are pre-E1 messages replayed as the A side of the E1 rounds; they parse to the sentinel exactly as a legacy device record would.
- `discontinuity` — the whole-corpus figure, which spans both eras: the A sides of the E1 rounds replay a pre-envelope snapshot, so some of this number is a sentinel standing beside a named event. E6 is decided on `discontinuity_envelope_only` below, not on this one.
- `discontinuity_envelope_only` — the number that decides E6: it must be non-zero, or `code` and `event` add nothing to the identity and design D-5 is re-opened. It counts only records carrying an envelope, so the sentinel cannot pass the gate on its own.
- `discontinuity_envelope_only` — excluded: records whose message carries no envelope
- `five_part_identities_split` — excluded: records whose message carries no envelope
- `split_by_event_alone` — these are the reports `event` separates on its own, with `code` identical - the measured form of design D-5's claim that `code` alone would refine nothing.
- `split_by_event_alone` — excluded: records whose message carries no envelope

### The five-part identities the seven-part identity splits

Each row is one `(spec, error)` pair that the five-field identity reports as a single record at a given site, and the causes the seven-field identity separates it into.

Only records carrying an envelope are counted, so a row here is never the era boundary showing through. The last column marks the rows where the separated causes share one `code` and `event` is therefore doing the separating on its own.

| spec / error | causes separated | by `event` alone |
|---|---|---|
| `MessageDigestSpec/InvalidSequenceOfMethodCalls` | `code=MESSAGEDIGEST-ORDER-00 ev=reset`; `code=MESSAGEDIGEST-ORDER-00 ev=update` | yes |
| `MessageDigestSpec/UnsafeAlgorithm` | `code=MESSAGEDIGEST-ALG-00 ev=update`; `code=MESSAGEDIGEST-ALG-02 ev=g4` | no |
| `PBEKeySpecSpec/ForbiddenMethod` | `code=PBEKEYSPEC-FORB-00 ev=f1`; `code=PBEKEYSPEC-FORB-01 ev=f2` | no |
| `PBEKeySpecSpec/UnsatisfiedConstraint` | `code=PBEKEYSPEC-CONSTR-00 ev=err1`; `code=PBEKEYSPEC-CONSTR-01 ev=err2` | no |
| `SignatureSpec/InvalidSequenceOfMethodCalls` | `code=SIGNATURE-ORDER-00 ev=i1`; `code=SIGNATURE-ORDER-00 ev=s1` | yes |
| `TrustManagerFactorySpec/InvalidSequenceOfMethodCalls` | `code=TRUSTMANAGERFACTORY-ORDER-00 ev=gtm1`; `code=TRUSTMANAGERFACTORY-ORDER-00 ev=init` | yes |

### Why 17 distinct events and not 19

A `grep` for `ev=` over `data/gh104/evidence/harness/*.md` returns 19 distinct values. This file measures 17, and the difference is the instrument, not the data: the `grep` reaches the 24 `selftest*` reports, which replay a synthetic mutant and are excluded here, and it also matches the harness's own record header — `spec=…,ev=…,type=…` — which is what `TraceRunner` knows it dispatched, not what the message carries. The identity is built from the message envelope alone, because that is the only field `ErrorDescription` will have on the device.

## The 8.15 reversion becomes measurable — a candidate, not a decision

Task 8.15 added `__RESET;` to `KeyPairGeneratorSpec`'s `@fail` handler. It is correct by construction — the other 20 handlers of the set reset, and `Category_fail` is sticky — but the harness classified `unchanged` on all 62 traces and the task's rule reverted it. The cause was measured and recorded in `evidence/e4_automata.md`: the replay has one call site, so every report carries the same `location`, and the five-field identity collapsed the repetition inside `ErrorCollector`'s `HashSet` before it could reach a row. The edit was reverted and the chain written into `data/jca_android/conformance_record.csv`.

The seven-field identity changes that premise. A handler re-running at one site under a **different event** no longer collapses, so a repeat that 8.15 removes can now become visible to the same instrument. This file records that as a **candidate for re-measurement**, against the corpus named here — the differential-harness traces of `data/gh104/traces`, replayed after task 9.2 has landed — and nothing more. 8.15 stays closed with the record it has; re-opening it is a decision for the researcher, and it needs its own before/after evidence, not this measurement.

One limit is worth stating with it, because it bounds what a re-measurement could show: the seven-field identity separates two *different* events at one site, but two runs of the *same* event at the same site still collapse. That repeat is exactly what 8.15 removes in production, and staging it still needs either a replay that gives each trace line its own call site or a device run — which is task 10.4's, not this group's.

## Era

**five-part identity** — identity `(spec, error, class, method, location)`, `unique_msg` of 5 parts. Applies to every deduplicated count published before this change, including the E3 trial `experimento-comp162` and the journal-article dataset.

**seven-part identity** — identity `(spec, error, class, method, location, code, event)`, `unique_msg` of 7 parts. Applies to every count computed from a run whose monitors were generated from `jca_android` after task 9.2 landed.

A count of one era MUST NOT be compared to a count of the other without the discontinuity stated beside the comparison (INV-CORE-41, INV-CORE-57).

## Inputs

| key | path | sha256 | size |
|---|---|---|---|
| `comp162_shards` | `rvsec/rv-android/experimento-comp162/results/*/*/errors.csv` | `099b1b3a50e07d36…` | 19,664 |
| `harness_reports` | `rvsec/rv-android/data/gh104/evidence/harness/*.md` | `a2e29c607c2b4d52…` | 369 |

## Definitions

### `envelope_parse`

`code` and `event` are the `code=` and `ev=` values of a `v=1` message envelope. A message carrying no envelope yields the sentinel `UNSPECIFIED` for both, never an empty string.

**Value**: {"marker": "v=1", "sentinel": "UNSPECIFIED"}

**Why**: It is the rule `ErrorDescription.createErrorSummary` will apply on the device, so the measurement and the production path read the same field the same way. The sentinel keeps a legacy record's key readable and distinguishable from an envelope record whose event was named (INV-CORE-25).

### `error_type`

The `ErrorType` of a row is `unique_msg.split(':::')[3]`, never parsed from `message`.

**Value**: `unique_msg.split(':::')[3]`

**Why**: `unique_msg` is the machine field: `class:::method:::spec:::ErrorType:::message`. `message` is the free text this change is rewriting, so deriving the type from it would make the baseline move when the messages move - the one thing a baseline may not do.

### `harness_corpus`

The deciding corpus is every `## Envelopes` record of `data/gh104/evidence/harness/*.md` whose file name does not begin with `selftest`; both replay sides (A and B) are read.

**Value**: {"dir": "data/gh104/evidence/harness", "excluded_prefix": "selftest"}

**Why**: The self-test reports replay a synthetic mutant written into scratch to prove the harness can reach all four verdicts; they measure the instrument, not the specification set, and counting them would inflate the corpus with authored differences. Both sides are read because the A side of the E4 rounds is itself a post-E1 snapshot and carries envelopes.

### `harness_site`

On the harness corpus `class`, `method` and `location` are constant across every record and are held fixed, because `TraceRunner` resolves every trace line through one reflective frame and its record carries no frame at all (`spec`, `ev`, `type`, `msg` - `TraceRunner.java:320-324`).

**Value**: `one call site, held constant`

**Why**: It is what makes this corpus the right instrument rather than a limitation of it: with the site fixed, the difference between the two identities is exactly what `code` and `event` add at one site - the case INV-INS-126 names, two events reported at one location.

### `identity5`

The identity era in force before this change: `ErrorSummary.equals`/`hashCode` compare `(spec, error, classQualifiedName, methodName, location)`. On comp162 `location` is the `source` column and `error` is part 4 of `unique_msg`.

**Value**: `spec`, `error`, `class`, `method`, `location`

**Why**: It is the identity every deduplicated count published before this change was computed under, so it is the baseline the discontinuity is measured against - not a candidate design.

### `identity7`

The identity era this change introduces: the five fields of `identity5` plus `code` and `event`, both read from the message envelope. The message free text stays outside the identity in both eras.

**Value**: `spec`, `error`, `class`, `method`, `location`, `code`, `event`

**Why**: `code` alone would refine nothing - every specification of the set has at most one `@fail`, so `code=<SPEC>-ORDER-00` is a function of `spec` (design D-5). It is `event` that separates causes that share a call site, which is why the measurement that decides E6 must be taken on a corpus carrying `ev=`.

### `identity8`

A repeated report is counted under the 8-tuple `(apk, rep, tool, spec, class, method, source, message)`. The `ErrorSummary` identity is a different, coarser 5-tuple: `(spec, error, class, method, location)`.

**Value**: `apk`, `rep`, `tool`, `spec`, `class`, `method`, `source`, `message`

**Why**: The two tuples answer different questions and give different numbers on the same rows - 6,344 against 409 - so every repetition figure states which one produced it. `location` in the `ErrorSummary` tuple is the `source` column.


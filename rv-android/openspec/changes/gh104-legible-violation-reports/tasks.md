<!-- Subagent dispatch hints (this change touches ~70 files across three trees: rv-android, the sibling
     rvsec Java reactor, and MetaCrySL read-only):
     - Read design.md D-12 first, then the pivot brief (successor set = `jca_android`, 21 specifications,
       no predicates, allow-lists transcribed from `MetaCrySL/generated/api30/*.cryptsl`). Every group heading
       below points to tasks/<GROUP>.md — the per-group execution file with the file inventory, per-site tables,
       commands, expected values and the subagent brief. A subagent dispatched for a group reads ONLY its group
       file (plus the spec deltas it cites); the orchestrator reads only this file and ticks the boxes here,
       immediately after each task.
     - MetaCrySL is READ-ONLY in this change. `generated/api30/*.cryptsl` is the oracle; nothing under
       `MetaCrySL/samples/` or `MetaCrySL/src/` is edited, not even the `KeyPairGenerator` defect of D-c —
       it is recorded as a divergence, not repaired.
     - Wave 1 (parallel, disjoint file sets, six subagents): Group 1 (E0 baseline: scripts/ + data/gh104/ + docs/),
       Group 2 (S seed: the `git mv` that archives the reproved derived set as
       rvsec-mop/.../jca_android_bug_predicate/, then rvsec-mop/jca_android/ + two new rvsec-core classes —
       the api30 Cipher utility and the Conscrypt alias table — + rv-experiment + data/jca_android/), Group 3 (G __EVENTNAME: rv-monitor generator only), Group 4 (E2 weaver counter:
       Java dexlib2 only), Group 5 (E3 transport: rvsec-core collectors, rv-monitor-rt, rv-coverage,
       rv-android-core, rv-platform, aperv-tool), Group 6 (EV validation: NEW files only — tests/parity/,
       scripts/gh104_*, rvsec-mop/src/test).
     - Groups 3 and 5 both live under rv-monitor/ but in different submodules: Group 3 edits rv-monitor/rv-monitor
       (the generator), Group 5 edits rv-monitor/rv-monitor-rt (ViolationRecorder). Disjoint; no lock needed.
       Groups 2 and 5 both touch rvsec-core but different packages: Group 2 adds `jca/util/`, Group 5 edits
       `eh/` collectors. Disjoint.
     - The enumeration does NOT grow: `jca`, `jca_android`, `generic`, `custom` stay the four accepted values.
       `jca_android` is re-pointed at the successor set; the reproved derived set moves to
       `jca_android_bug_predicate/` and is not selectable by any value.
     - Group 7 (E1 messages) starts as soon as Groups 2 AND 3 have committed (the set exists in its final
       allow-list/predicate-free shape and __EVENTNAME expands); it edits only jca_android/*.mop, codes.csv
       and rvsec-core `ErrorType.java`; run it as TWO subagents on disjoint file halves (7.a files A–K,
       7.b files M–T; see tasks/E1-messages.md) with `ErrorType.java` owned by 7.b alone.
     - Group 8 (E4 structural repairs) starts after Groups 6 AND 7 are complete (hard prerequisite: the gates
       and the harness must exist, and every report site must already carry its envelope). It is now seven
       named repairs across six files, not a replay: one owner per file, sequential is fine.
     - Group 9 (E6 identity) after Groups 5 and 7 — it may overlap Group 8 (rvsec-core ErrorSummary + Python;
       no .mop file).
     - Group 10 (integration & verification) last, after everything.
     - Critical path: (2 ∥ 3) -> 7 -> 8 -> 10. Groups 1, 4, 5, 6 are off the critical path; Group 9 is off it
       unless Group 8 slips. Group 2 is the long pole of wave 1 (one `git mv`, then 21 specifications ×
       three mechanical passes, plus two new Java classes), so dispatch it first and give it the largest budget.
     - Never start, stop or manage an emulator by hand. Task 10.4 is the only device task and it runs
       `rv-experiment run --specification-set jca_android`. Everything else is JVM/pytest/scripts.
     - Tests: `uv run pytest --import-mode=importlib -o "addopts=" <path>`. Java: `mvn -q test` inside the
       submodule under ../rvsec/rvsec/rvsec-android/rvsec-instrumentation-dexlib2/ (never at the reactor root
       without -pl); Group 3 runs `mvn -q test -pl rv-monitor/rv-monitor` from the reactor root. Monitor
       generation needs RVSEC_HOME and TMPDIR off tmpfs. -->

## 1. E0 — Baseline and definitions (`tasks/E0-baseline.md`)

- [ ] 1.1 Write `scripts/gh104_baseline.py`: reads `experimento-comp162/results/*/*/errors.csv` through `aperv_tool.analysis.violations.read_errors_csv` (11-column header accepted only until Group 5 lands — pin the pre-change reader by import path in the script header) and the article dataset through a declared 10-column reader in the same file; every definition (mute row, site 4-tuple vs 3-tuple, nine-prefix classifier, shard) is a required freeze item (`FreezeItemUnset` if absent); every number leaves as an `Envelope` (value, numerator, denominator, definition id)
- [ ] 1.2 Reproduce and record in `data/gh104/baseline.md`: article 97,018 / 19 messages / 70,760 `unknown` = 72.93 % / 8,843 `but found .`; comp162 19,664 / 15,714 mute / 296 sites / 101 mute-legible twin sites (3,950 rows) / 12 mute-mute (838) / per-spec mute top-8 / third-party 78.49–82.67–85.44 % under the three prefix lists; identities 6,344 (five-field) — each with its command
- [ ] 1.3 Register the definitions and the instrument discontinuity (the article dataset has no `source`; `read_errors_csv` rejects it by design) in `data/gh104/definitions.md`
- [ ] 1.4 Record the publishable Android tier the pivot rests on (11,409 events / 84 misuses of 454 = 18.5 %, five spec×ErrorType×value rows) with its source `ase-journal/docs/20260806_owasp_cwe_mapping_report.md`, and the API-30 / Android-11 / Conscrypt `android11-release` context fixation — this is the denominator the allow-list transcription of Group 2 is measured against
- [ ] 1.5 Add `tests/parity/test_gh104_baseline.py::test_baseline_reproduces_byte_identical` (rerun → identical `baseline.json`) and `::test_freeze_items_required`
- [ ] 1.6 Run `/rv-test-run tests/parity/test_gh104_baseline.py`

## 2. S — Archive the reproved set, seed and shape the successor set `jca_android` (`tasks/S-seed.md`)

- [ ] 2.1 Archive the reproved derived set **before** creating anything: `git mv rvsec-mop/src/main/resources/jca_android rvsec-mop/src/main/resources/jca_android_bug_predicate` — a pure rename of the 23 `.mop` (`git status` must show `R100` for every file, zero content hunks). The name states why it is archived: the 2026-08-08 audit reproved it 22/22 on its predicate machinery. The archived directory is **not selectable** — no enumeration value points at it and none is added (task 2.9). Record the rename, the name's reason and the audit reference in `data/jca_android/README.md` and as the set's first divergence entry (kind `set-archived`)
- [ ] 2.2 Create `rvsec-mop/src/main/resources/jca_android/` as a byte-identical copy of the frozen `jca/`, then delete `RandomStringPassword.mop` and `SecretKeySpec.mop` (pure predicate propagators: 0 `new ErrorDescription(`, no `@fail`) → **21 `.mop`**; add `codes.csv` with header only; decide and record whether `MultiSpec_1MonitorAspect.aj` travels with the set; commit as one seed hunk
- [ ] 2.3 Remove every use of `ExecutionContext` from the 21 specifications — `validate(...)` in `condition()`, `setProperty(...)`, `remove(...)` — over the inventory of 24 predicate-touching events (13 guards, 11 accusers) in the group file; the 13 guards are a net gain (today they silence the spec), the 11 accusers are a declared loss with the five report sites they take with them; every touched site gets a row in `data/jca_android/predicate_removal.csv` (file, line, class `guard`/`total-loss`/`partial-loss`/`provenance`, the accusation it used to raise) and each removal is one divergence entry
- [ ] 2.4 Transcribe the allow-lists literally from `MetaCrySL/generated/api30/*.cryptsl` `CONSTRAINTS`, spec by spec, over the 13 `Arrays.asList` declarations of the seed; one conformance-record row per list with its `.cryptsl` file and clause. **The api30-admits case is decided, do not re-open it**: if the rule admits a value, the specification accepts it — one oracle, no second anchor. For `MessageDigestSpec` (`MessageDigest.cryptsl:63` admits `MD5` and `SHA-1`) that costs **5,892 of the 6,048** `MessageDigestSpec/UnsafeAlgorithm` rows of the published dataset — 3,552 `MD5` + 2,340 `SHA-1`/`SHA1`/`SHA` — i.e. 97.4 % of that specification and 6.1 % of the 97,018-row corpus; write that figure into the conformance record as the declared cost, not as a question. For `SignatureSpec` (`Signature.cryptsl:75`, 20 entries) it means `MD5withRSA`, `SHA1withRSA` and `SHA1withDSA` are accepted
- [ ] 2.5 Declare the normalisation rule in `data/jca_android/README.md` and implement it in the 21 specifications: comparison is case-insensitive everywhere (today case-sensitive in 6 specs, `.toUpperCase()` in 3), plus the Conscrypt `android11-release` alias table. Import the finished table from `backup/gh104-analise/alias_table_conscrypt_android11.csv` into `data/jca_android/alias_table.csv` (158 alias rows in services that have a spec, of which 114 map onto an api30 allow-list entry; columns `service,alias,canonical,openssl_provider_line,in_api30_allowlist`) — **do not redo the extraction**, sample-check ~10 rows against `backup/gh104-analise/OpenSSLProvider.java` and record which rows were checked. Resolution at run time does **not** read the CSV: a new utility class under `rvsec-core/src/main/java/br/unb/cic/mop/jca/util/` carries the table as code and the `jca_android` specs name that class directly (INV-INS-112 — no runtime switch, and the frozen `jca` never references it, so its verdicts cannot move); one Java test asserts the in-code table is row-for-row equal to `data/jca_android/alias_table.csv`
- [ ] 2.6 Add `EC` (keySize 256) to the `KeyPairGeneratorSpec` allow-list against the literal api30 rule, with the D-c divergence entry naming the MetaCrySL defect (`samples/jca/base/KeyPairGenerator.cryptsl:27` writes the literal list where `KeyGenerator.cryptsl:27` writes `alg in ${algorithm}`, so `samples/jca/android/11plus/KeyPairGenerator.ref:2` `define algorithm = {"EC"}` is discarded in silence) and stating that the root fix is not executed here. This is the **api30-omits** case of the asymmetric rule: the platform has it, the rule does not list it, the set adds it and records the divergence
- [ ] 2.7 Same case, second occurrence: add `SHA1withECDSA`, `SHA256withECDSA`, `SHA384withECDSA` and `SHA512withECDSA` to the `SignatureSpec` allow-list. `Signature.cryptsl:75` lists only `SHA224withECDSA` among the ECDSA algorithms, but Conscrypt registers all five on API 30 — `OpenSSLProvider.java:270,271` (`ECDSA`, `ECDSAwithSHA1` → `SHA1withECDSA`), `:286` (`SHA256/ECDSA`), `:293` (`SHA384/ECDSA`), `:300` (`SHA512/ECDSA`) — which is primary-source proof that the four exist in the platform. One divergence entry, same shape as task 2.6
- [ ] 2.8 Create the new Java allow-list class for the Cipher under `rvsec-core/src/main/java/br/unb/cic/mop/jca/util/` with the lists transcribed from `generated/api30/Cipher.cryptsl` `CONSTRAINTS`, and point `jca_android/CipherSpec.mop`'s static import at it; `CipherTransformationUtil.java` (69 l., frozen with `jca`) and `AndroidCipherTransformationUtil.java` (262 l., now under `jca_android_bug_predicate`) are read-only — neither may be edited; add its Java unit test
- [ ] 2.9 Confirm — do not extend — the enumeration: the four accepted values stay `jca`, `jca_android`, `generic`, `custom`, so `click.Choice` (`__main__.py:443`), `valid_spec_sets` (`config.py:432-437`) and `SPEC_SET_JCA_ANDROID` (`constants.py:97`) are unchanged in shape. What changes is meaning: the mapping `jca_android → .../resources/jca_android` (`config.py:688-700`) now resolves to the successor set, so update the comments and help text that still describe it as "the same 23 specifications derived against generated CrySL rules" (`config.py:248,385,429,648,678`; `__main__.py:368,656,1193`). Add `test_jca_android_bug_predicate_not_selectable` (the archived directory name is rejected exactly like any other unknown value) beside the existing `test_jca_android_spec_set_valid` / `test_jca_android_spec_set_resolves_paths` / `test_near_miss_spec_set_still_rejected`; the invalid-value message keeps listing **four** values
- [ ] 2.10 Create `data/jca_android/{divergence_record.csv, conformance_record.csv, alias_table.csv, gate_allowlist.csv, predicate_removal.csv}` with headers and `README.md`, and `scripts/gh104_divergence_record.py` (parametrised copy of `scripts/gh101_divergence_record.py`: base = `jca/`, target = `jca_android/`, `--check` fails on an unrecorded hunk); there is no `predicate_omissions.csv` in this set — that record justifies a `Property` written and never read, and this set writes none (INV-INS-128); `predicate_removal.csv` is the record that does exist, and it names what left
- [ ] 2.11 Add `tests/parity/test_gh104_specset_gates.py::test_jca_android_hunks_all_recorded`, `::test_jca_android_has_no_execution_context` and `::test_predicate_removal_record_complete` (33 rows: the 24 predicate-touching events plus the 9 `remove(Property)` sites); confirm the existing five gh101 gates still pass (nothing under `jca/` moved) and that `jca_android_bug_predicate/` is byte-identical to the pre-rename `jca_android/`
- [ ] 2.12 Run `/rv-test-run modules/rv-experiment` and `/rv-test-run tests/parity`

## 3. G — `__EVENTNAME` in the monitor generator (`tasks/G-eventname.md`)

- [ ] 3.1 Red test first (INV-INS-108 discipline): in `rv-monitor/rv-monitor/src/test/` add `EventNameMacroTest` with the three cases of INV-INS-120 — `__EVENTNAME` in an event body becomes the literal `"<name>"`; `__EVENTNAME` in a `@fail` resolves to the event that last transitioned the monitor, exercised once per monitor shape; a monitor with no transition yet renders `none`; commit the red output under `evidence/`
- [ ] 3.2 Emit the event-name table: in `BaseMonitor.toString()`'s `// state declaration` block (`:806-812`, where `Prop_N_transition_*` lands) write one table per monitor class from `this.events` (`:122`, populated `:179`) in `getIdNum()` order — one table per class, not per property, because `getIdNum()` is spec-wide; reserve its name in `RVMNameSpace` beside `RVM_lastevent`
- [ ] 3.3 Expand the macro in the event-body path: `BaseMonitor.printEventMethod` (`:350-368`, beside the existing `__RESET`/`__LOC`/`__ACTIVITY` substitutions), using the `EventDefinition` already in scope → string literal
- [ ] 3.4 Expand the macro in the handler path: `HandlerMethod` constructor (`:36-48`), reading the last-event value shape-aware — `this.getLastEvent()` when `isAtomicMoniorUsed()` (`BaseMonitor.java:114-118`), the `RVM_lastevent` field otherwise, reusing the branch at `BaseMonitor.java:1044-1048` / `MonitorTermination.java:73`; index `-1` → `none`; mirror the substitution in `RawMonitor.java:90-105` so no emission path can leak the literal
- [ ] 3.5 Fail-closed check: generation aborts naming file and line if the literal `__EVENTNAME` survives in any generated Java; regenerate the two sets that are frozen and independent of Group 2's timing — `jca` (against `results/gh101_group8_jca_frozen_control/monitors/`) and the archived `jca_android_bug_predicate` (against its own recorded control) — and diff; the only admissible differences are the new table and the expanded macro; record both diffs in `evidence/g_regeneration.md`. The successor `jca_android` is not a control here: it does not exist yet when this group runs, and Group 7 regenerates it under task 7.7
- [ ] 3.6 Red test first for INV-INS-129: in `rv-monitor/rv-monitor/src/test/` add a case that generates a monitor whose handler throws, invokes the dispatcher twice, and asserts the second call completes — it must fail today, because the first call leaves `MultiSpec_1_RVMLock` held; commit the red output under `evidence/`
- [ ] 3.7 Emit the dispatcher's guarded region so the lock is released on every exit path (INV-INS-129). Baseline to beat, measured on `results/gh101_group8_jca_frozen_control/monitors/MultiSpec_1RuntimeMonitor.java`: one `ReentrantLock` at `:9005`, 134 `tryLock()` spin acquisitions, 134 `unlock()` calls, **0** `finally` blocks in the file. Change the framing only — not which advices fire, not the lock's identity, not the `Thread.yield()` spin
- [ ] 3.8 Regenerate `jca` and the archived `jca_android_bug_predicate` and diff against their controls (same run as 3.5): the only admissible differences are the lock framing, the event-name table and the expanded macro; assert acquisitions still equal releases and that every acquisition now sits inside the framing; record in `evidence/g_regeneration.md`
- [ ] 3.9 Rebuild the reactor (`mvn -q install -DskipTests -DskipMopAgent=true` from the root, ~12 min) and run `mvn -q test -pl rv-monitor/rv-monitor`

## 4. E2 — Weaver `args()` arity, counter mode (`tasks/E2-weaver-arity.md`)

- [ ] 4.1 Implement the three-clause rule as a **measurement only** in the `WrapperEmitter.generate` grouping loop (`:246-274`): read `ArgsPC` from the parsed expression, `types()` length with `trailingRest`/`headCount` semantics, absent `args()` = admit; count the advice/overload pairs the rule would exclude into `advicesExcludedByArity`; **no advice is removed from any wrapper** — emission is byte-for-byte what it is today
- [ ] 4.2 Return the count: `WrapperEmitter.generate` gains an `EmitResult { List<WrapperEntry> wrappers; int advicesExcludedByArity; }`; `cli/.../BatchRunner.java:199-201` puts `advicesExcludedByArity` in `counts` beside `wrappersGenerated`; `ResultsJsonReportingTest` asserts the key reaches `instrument_results.json`
- [ ] 4.3 Surface it in Python: `modules/rv-instrumentation-dexlib2/.../dexlib_instrumentation.py::_parse_results_json` carries `advicesExcludedByArity` into `weave_counts`; add the surface test in `modules/rv-instrumentation-dexlib2/tests/test_dexlib_instrumentation.py`
- [ ] 4.4 Keep the existing emission tests green **by construction** (nothing is filtered): `EmissionParityTest`, `EmissionCardinalityTest`, `WrapperRegistryGuardTest`, `WrapperEmitterTest`, `MonitorCallsPremiseContractTest` — run `mvn -q test` in `advice-emitter`, `cli`, `dex-mutator`, `validator` and record that no assertion needed changing
- [ ] 4.5 Record the reach of the rule in `evidence/e2_reach.md` from the frozen descriptor's 115 advices: 48 `after` non-constructor with `args()` measured; 44 without `args()` unaffected by clause 1; 9 `before` with `args()` and 14 `after`-on-constructor with `args()` outside the loop entirely — with both name lists, the reproduction command, and the sentence that in counter mode the number is a diagnosis, not a change in what fires
- [ ] 4.6 Re-weave the frozen `jca` descriptor (`results/gh101_group8_jca_frozen_control/monitors/MultiSpec_1MonitorAspect.json`) and record `advicesExcludedByArity` and `wrappersGenerated` in `evidence/e2_reweave.md`; `wrappersGenerated` must be unchanged from the pre-change run — if it moved, the filter leaked and the group is wrong; rebuild `lib/` jars from the reactor root and record the sha256 of `modules/rv-instrumentation-dexlib2/lib/instr-cli.jar`
- [ ] 4.7 Run `/rv-verify rv-instrumentation-dexlib2`

## 5. E3 — Honest transport (`tasks/E3-transport.md`)

- [ ] 5.1 Logcat `ErrorCollector.java` (`rvsec-android/rvsec-logger-logcat`): enable escaping (`\n`→`\\n`, keep commas), `null` expecting → sentinel envelope; align the CSV collector's `escape()`; new `ErrorCollectorTest` (newline, null, comma)
- [ ] 5.2 `ViolationRecorder.makeRelevantList` (`rv-monitor-rt`, `:87-105`): a monitoring-runtime frame with `fileName == null` is excluded, not included; new `ViolationRecorderTest`
- [ ] 5.3 `rv-coverage` `logcat_parser.py`: `ParserDiagnostics` with the 13 counters carried on `LogcatRepository.parser_diagnostics`; envelope fields on `RvErrorLog` (`code, event, obj, val, exp, msg, truncated`); sentinels; unclosed quote → truncated; Format-1 regex failure no longer falls into the comma path; `parse_logcat_file` re-raises with the line number; property tests (`,`, `\'`, `\n` split → `continuation_lines`, `:::`, 4068-byte cut) in `test_logcat_parser.py`
- [ ] 5.4 `rv-android-core` `domain/log.py`: `unique_msg` = seven parts with `code`/`event` (sentinel `UNSPECIFIED`); `test_log.py` updated; grep gate `tests/parity/test_gh104_unique_msg_built_once.py` (only `log.py:RvErrorLog.unique_msg` composes it)
- [ ] 5.5 `rv-platform` `result_processor.py`: 13-column `errors.csv` header (`…,source,code,event,message,unique_msg`); delete the three `unique_msg` fallbacks (`:631,:999,:1038`); count write failures (`:654-655,:1046-1047`) into the task result and log as errors; `test_result_processor.py` header + failure tests
- [ ] 5.6 `aperv-tool` `analysis/violations.py` + `clock_logcat_join.py`: `ERRORS_CSV_HEADER` 13 columns; `unique_msg` seven parts (`code=parts[4]`, `event=parts[5]`); `parse_payload` reads the envelope, `shape_ok=False` + `envelope_truncated`/`envelope_malformed` counters; one payload parser for bundle and run join; `test_violations.py` (+4); update the sha256-pinned fixture manifest only if a fixture changes (it should not — cmp162 stays 11-column and is read by the E0 reader, not here)
- [ ] 5.7 Delete the other `unique_msg` constructors: `scripts/regenerate_results/regenerate_container.py:244` → import from core; fix `scripts/rv_oracle_common.py:73-81` to require seven parts
- [ ] 5.8 Consumer matrix `data/gh104/consumer_matrix.md`: every reader of `message`/`unique_msg`/`errors.csv` named with its verdict (migrated / frozen-with-reason): `logcat_parser`, `log.py`, `result_processor`, `violations.py`, `clock_logcat_join.py`, `rv_oracle_common.py`, `regenerate_container.py`, `TraceComparator.parseObserved`, `validator/oracles/cryptoapp-oracle.yaml`, `ase-journal/docs/20260806_owasp_cwe_mapping_gen.py:47-54` (frozen: reads the published dataset), `experimento-gov/scripts/consolidate_gov.py:26-37`, `.claude/skills/rv-experiment-compare/scripts/consolidate_compare.py:35`, `scripts/gh91_compare_consolidation.py`
- [ ] 5.9 Run `/rv-qa-lint-fix rv-coverage`, `/rv-test-run modules/rv-coverage`, `/rv-test-run modules/rv-android-core`, `/rv-test-run modules/rv-platform`, `/rv-test-run modules/aperv-tool`

## 6. EV — Validation toolkit (`tasks/EV-validation.md`)

- [ ] 6.1 `scripts/gh104_gates.py`: reuse `gh101_monitor_transition_check.py`'s parser (both monitor shapes) and add G-2a, G-2b′, G-2c, G-2d, G-6′; `--allowlist data/<set>/gate_allowlist.csv`; JSON report
- [ ] 6.2 Correct G-2 before shipping it: an event that reaches `fail` from every state is a defect only if it has no CrySL clause behind it. 17 of the frozen set's 18 orphans are the correct encoding of `CONSTRAINTS`/`REQUIRES`/`FORBIDDEN`, which by definition never enter the `ORDER`; the single real orphan is `MessageDigestSpec:74-76 reset`. Record while implementing it that this orphan is not dormant — it carries no `condition()` either, so its row `{4,4,4,4,4}` accuses on every woven `reset()`; the only other ungated orphans are `PBEKeySpecSpec.f1`/`f2`, which encode a `FORBIDDEN` clause and are meant to. G-2 therefore takes the spec's api30 `.cryptsl` as a second input and reports `orphan-with-clause` (a note) separately from `orphan-without-clause` (a failure)
- [ ] 6.3 Add gate G-ERE — every symbol named in an `ere`/`fsm` expression has an event declaration. Zero false positives by construction; on the frozen `jca` it reports exactly one hit, `GCMParameterSpecSpec:48` `c2` (declared nowhere because `:23` and `:34` both declare `c1`)
- [ ] 6.4 Add gate G-CONF — allow-list conformance: for each of the 21 specifications, the `Arrays.asList` allow-list of the `.mop` (and, for the Cipher, the lists of the new Java class from task 2.8) equals the corresponding `CONSTRAINTS` set of `MetaCrySL/generated/api30/<Rule>.cryptsl` modulo the declared normalisation of task 2.5 — the gate reads the alias rows from `data/jca_android/alias_table.csv`, the auditable registry, never from the Java class (the class-versus-CSV equality is the Java test of task 2.5, not this gate's job); every difference must have a row in `data/jca_android/conformance_record.csv` (the `EC` of task 2.6 and the four ECDSA algorithms of task 2.7 among them) or the gate fails. The gate also asserts that no file under `jca/` names the alias class (INV-INS-112: the frozen set's verdicts must not move)
- [ ] 6.5 Add gate G-PRED — `jca_android` contains zero occurrences of `ExecutionContext`, `Property.`, `validate(`, `setProperty(`, `.remove(Property`; the frozen `jca` reports 134 `ExecutionContext` hits and is the negative control
- [ ] 6.6 `tests/parity/test_gh104_structural_gates.py`: parametrised over `jca` (frozen control monitor — expected G-2 18 hits in 10 specs of which 17 `orphan-with-clause` and 1 `orphan-without-clause`, G-2a 1 `SecretKeySpec.e1`, G-2b′ 8, G-2c 1, G-2d 2, G-6′ 1 `GCMParameterSpecSpec`, G-ERE 1, G-PRED 134) with `data/jca/gate_allowlist.csv` naming them, and over `jca_android` (generated in scratch, `RVSEC_HOME`). The `jca` control numbers for G-2a/G-2b′/G-2d shift once `SecretKeySpec` and `RandomStringPassword` leave the successor set — they stay as written for `jca`, which keeps both files; only the `jca_android` expectations drop them
- [ ] 6.7 `.mop` lint `scripts/gh104_mop_lint.py` + test: undeclared ERE/FSM symbol, duplicate event name (`GCMParameterSpecSpec:23,34`), unbalanced parentheses (`SecretKeySpecSpec:27-30`), three-argument `ErrorDescription` (INV-INS-119), hand-written event-name bookkeeping (forbidden by INV-INS-120 — the generator emits the name), reserved generator names in declarations; on the frozen `jca` the lint must report exactly the known hits and pass on `jca_android` only after Group 7
- [ ] 6.8 Message-property gate `scripts/gh104_message_gate.py` + test: numeric literals of a message equal those of its guard (INV-INS-121), `codes.csv` bijective with sites, `ErrorType` matches the site kind; on the frozen `jca` it must list `PBEKeySpecSpec:50` and `PBEParameterSpecSpec:50` (`1000` vs guard `10000`), the 25 three-argument sites, and the three wrong `ErrorType` values of D-a
- [ ] 6.9 Differential harness: `rvsec-mop/src/test/java/.../TraceRunner.java` (loads a generated `MultiSpec_1RuntimeMonitor` class set, replays a trace file of `spec event(args)` lines, records accusation/event/envelope), `scripts/gh104_diff_harness.py` (generate two snapshots in scratch, run, classify `moved`/`removed`/`unchanged`), `traces/<spec>.txt` for the 21 specifications of the successor set plus the two the set drops (so the seed side of every comparison has a trace)
- [ ] 6.10 Harness self-test: `jca` vs the **archived** `jca_android_bug_predicate` on `TrustManagerFactorySpec` (`getInstance("X509"); init(ks)` — accusation at `getInstance` before, `init` after → `moved`); the archived set is used because its behaviour is already measured and it exists independently of Group 2's progress; commit under `evidence/harness/selftest.md`
- [ ] 6.11 Run `/rv-test-run tests/parity`

## 7. E1 — Legible messages in `jca_android` (`tasks/E1-messages.md`) — after Groups 2 and 3; two subagents by file halves

- [ ] 7.1 `rvsec-core` `ErrorType.java` (six values today, `:4-9`) gains `ForbiddenMethod` and only that — `RequiredPredicate` does not enter, because predicates left the set in Group 2; Java test; KIND `FORB` enters the `codes.csv` vocabulary of design D-3
- [ ] 7.2 Recount the report sites of `jca_android` before editing anything and write the count into the group file: the seed's 51 (25 three-argument + 26 four-argument) are the `jca` figures and do not survive Group 2. Five sites are gone by construction (`IvParameterSpec` c3/c4 three-argument, `PBEKeySpecSpec` err2/err3 and `SecureRandomSpec` setSeed3 four-argument, all purely predicate-guarded); and the `SecretKeySpec` ruling of task 2.4 is now settled — `generated/api30/SecretKeySpec.cryptsl` declares only `length(keyMaterial) >= off + len`, so the algorithm list is removed and `SecretKeySpecSpec.c3` loses both halves of its condition, taking its site with it. That arithmetic points at 23 three-argument + 22 four-argument = **45** against the provisional 46 — either way the number is **A RECALCULAR na execução**, from the files, not from this line
- [ ] 7.3 Lying-message census applied first (precondition, INV-INS-121): `PBEKeySpecSpec:50` and `PBEParameterSpecSpec:50` (`1000`→`10000`, api30 declares `iterationCount >= 10000`); wrong `ErrorType` per D-a — `PBEKeySpecSpec:24,30` `InvalidSequenceOfMethodCalls`→`ForbiddenMethod` (they encode api30 `PBEKeySpec` `FORBIDDEN: PBEKeySpec(char[])`, `PBEKeySpec(char[], byte[], int)`) and `PBEParameterSpecSpec:49` `UnsafeAlgorithm`→`UnsatisfiedConstraint`; `MessageDigestSpec:70,92` list = the transcribed allow-list; `CipherSpec:61,76` drop the literal `...` and name the new Java utility of task 2.8; `KeyGeneratorSpec:64` and `KeyStoreSpec:68` (missing space); `MacSpec:62` (missing verb); `SecretKeySpecSpec:49` (array, not `.length`); leading spaces at `MacSpec:50`, `KeyManagerFactorySpec:55`, `KeyPairGeneratorSpec:72`, `SecretKeySpecSpec:49,56`; note `KeyPairGeneratorSpec:71-72` unreachable (Group 8 item)
- [ ] 7.4 Field → argument at every `but found` site that interpolates a monitor field instead of the bound argument (16 in the seed: `CipherSpec:61,76`; `KeyGeneratorSpec:64`; `KeyManagerFactorySpec:55`; `KeyPairGeneratorSpec:72`; `KeyStoreSpec:68`; `MacSpec:50,62`; `MessageDigestSpec:70,92`; `SignatureSpec:58,68,78,88`; `SSLContextSpec:58`; `TrustManagerFactorySpec:57`) — re-verify the list against the post-Group-2 files; un-comment and use `MessageDigestSpec:57-58`'s argument form
- [ ] 7.5 Envelopes: every four-argument value site rewritten to `v=1 code=… ev=__EVENTNAME obj=… val='…' exp='…' msg='…'` (`val` from the bound argument, capped at 512 chars); every three-argument site gains the fourth argument, composing `ev=` from `__EVENTNAME` before `__RESET`; `KeyPairGeneratorSpec` `@fail` gains `__RESET`; no bookkeeping field and no bookkeeping statement anywhere (INV-INS-120 forbids it — Group 3 emits the name); `codes.csv` filled, one row per site, KIND per design D-3
- [ ] 7.6 Divergence record: one entry per hunk (`scripts/gh104_divergence_record.py --check` green); lint, message gate and G-CONF/G-PRED green on `jca_android`; G-ERE still reports `GCMParameterSpecSpec` (Group 8 fixes it) — allowlisted with reason until 8.x
- [ ] 7.7 Generate `jca_android` monitors in scratch and confirm compilation and that no `__EVENTNAME` survives; harness before/after on every file (seed vs E1) → `evidence/harness/e1-<spec>.md`; classification must be `unchanged` for accusation, changed only in envelope
- [ ] 7.8 Run `/rv-test-run tests/parity`

## 8. E4 — Six structural repairs, two measured sites, in `jca_android` (`tasks/E4-automata.md`) — after Groups 6 and 7; per file

Every diagnosis below was re-confirmed on 2026-08-18 against the api30 rule, `javap` on `android-30`, the frozen-control monitor and the 97,018-row published corpus. Line numbers are the frozen `jca` seed's and have moved after Groups 2 and 7 — locate by symbol and record the number actually edited.

- [ ] 8.1 `GCMParameterSpecSpec`: the second `event c1` (`:34`) is renamed `c2`, which is what `ere : c1 | c2` (`:48`) has always referenced and what api30 `GCMParameterSpec:17,19,21` declares; G-ERE and G-6′ go to zero on this file. Record in the divergence entry that the behavioural effect is nil and why — the monitor is indexed by the constructed object, so each monitor sees at most one event and the `fail` state is unreachable before and after (`MultiSpec_1RuntimeMonitor.java:4232,4283,4300`); the spec accuses nothing today and accuses nothing after, and the corpus has 0 rows for it
- [ ] 8.2 `SecretKeySpecSpec:27-30`: **check before editing** — the surplus `)` is real in the text but inert in effect, because JavaMOP captures the pointcut as raw text (`javamop.jj:1441`) and parses it without requiring `<EOF>` (`aspectj.jj:49`), so the token is dropped in silence and the generated guard is already the intended one (`MultiSpec_1RuntimeMonitor.java:7725`). Group 2 tasks 2.3 and 2.4 remove both terms of that `condition` (api30 `SecretKeySpec:29` constrains only `length(keyMaterial) >= off + len`, nothing about `alg`), so the `condition` and its parenthesis may no longer exist. If the text is gone, close the task with that finding recorded and no edit; if it survives, remove the `)` and the lint's unbalanced-parentheses check goes clean
- [ ] 8.3 `MessageDigestSpec:74-76`: delete the `reset` event. It is the only true `orphan-without-clause` G-2 reports on the frozen set (api30 `MessageDigest` names it in no `EVENTS`/`ORDER`/`CONSTRAINTS`/`ENSURES`, and the rule has no `REQUIRES` or `NEGATES`) **and** the only one of the three ungated orphans that no `FORBIDDEN` clause justifies. It is not dead: its generated row is `{4,4,4,4,4}` against a `fail` of 4 (`:6369`, `:6504`), so every woven `reset()` accuses unconditionally and the handler's `__RESET` makes the following `digest()` accuse again. **This is the one repair of the group that removes accusations** — harness evidence is mandatory and its classification is `removed`, never `unchanged`. Magnitude is not derivable from the published corpus (`errors.csv` carries no event id; `class`/`method` are the call site): record the identifiable ceiling — 100 rows over 7 `MessageDigestSpec` call sites that raise `InvalidSequenceOfMethodCalls` and no `UnsafeAlgorithm`, inside a universe of 10,135 — and note that the archived `jca_android_bug_predicate` had already made this removal
- [ ] 8.4 `KeyPairGeneratorSpec:26`: `String algorithm` is declared without an initialiser. Correct the lineage's diagnosis in the divergence entry: the field is written by `g1`/`g2`/`g3` (`:44,:52,:59` — the `getInstance` events), **not** by the `init*` events, and the first read is `:29`'s `switch` inside `validate()`, not `:70`. A `null` there raises `NullPointerException`, it does not produce a false accusation, and `:70` is unreachable anyway (task 8.6). **Guard the read** (`if (algorithm == null) return false;` at the head of `validate`) rather than initialising to `""`: initialising would make `initError` fire where today the call raises, which adds accusations of the form `invalid key size for algorithm .`; guarding adds none. The exception's real damage is the dispatcher lock, which INV-INS-129 and tasks 3.6-3.8 handle
- [ ] 8.5 Dead pointcuts, **repair the two where the automaton is broken**: `SignatureSpec:99` and `:106` declare `sign()` returning `byte`; `javap` on `android-30` gives `public final byte[] sign()` and `public final int sign(byte[], int, int)`. `s1`/`s2` are the only transition into the accepting state of the signing branch (`Prop_1_transition_s1 = {8,8,8,8,8,8,6,6,8}`), so with them dead the monitor stalls in state 7 and a second `initSign`/`initVerify` on the same object accuses falsely. Fix `:99` to `byte[]` and `:106` to `int` (`s2` has no `returning` clause and writes its predicate from the argument, so the return type does not move its payload). Harness before/after must name `introduced` for every accusation a real call now produces, and record that the corpus has 0 rows attributable to either site and that all 990 `SignatureSpec` rows are the verification branch
- [ ] 8.6 `KeyPairGeneratorSpec:71-72`: **remove** the unreachable branch. The proof is a set equality, not a guess: `condition(validate(keySize))` compiles to `if (!(validate(keySize))) return false;` ahead of the body (`MultiSpec_1RuntimeMonitor.java:5364-5370`), and `validate` (`:29-36`) returns `true` exactly for the members of `safeAlgorithms` (`:22`), so `!safeAlgorithms.contains(algorithm)` is false whenever it is evaluated. Corroborated in the corpus: 0 of the 16 `KeyPairGeneratorSpec` rows are `UnsafeAlgorithm`, against 8,843 rows of that message shape in five other specifications. Drop the site's `codes.csv` row. Record that api30 `KeyPairGenerator:45` does support the accusation and that `g3` (`:55-60`) is the event that could host it — moving it there is behavioural and is **not** done here
- [ ] 8.7 Two further dead pointcuts are **measured, not repaired** (researcher decision, 2026-08-18): `SSLContextSpec:64` declares `createSSLEngine` returning `void` where the API returns `SSLEngine`, and `TrustManagerFactorySpec:63` declares `getTrustManagers` returning `KeyManager[]` where the API returns `TrustManager[]` (its `returning(TrustManager[][])` clause is a double array). Reviving either only adds accusations and the corpus cannot size that: `engine` is a `1→1` loop and acceptance already comes from `init` (api30 `SSLContext:39` makes `Engine?` optional), while `gtm1` sits in the `fsm` with `{3,0,3,3}` on the specification that carries 18,029 of the corpus's rows. Add both to `data/jca_android/conformance_record.csv` with the `javap` signature and the transition row, run the harness on each to record what reviving them would introduce, and change no `.mop` text. `TrustManagerFactorySpec:63` was not in the lineage's catalogue — say so in the entry
- [ ] 8.8 **Withdrawn, recorded not repaired**: `KeyGeneratorSpec:47` and `MessageDigestSpec:55` test `contains(currentAlgorithmInstance)` rather than the bound argument, and this is not a defect. `g1` is emitted into the same wrapper ahead of its sibling (`monitors/mop/MonitorWrappers.java:192-193`, `:357-358`), both resolve the same object-indexed monitor, and `g1`'s body writes the field before the sibling's `condition` runs; the field is initialised to `""` and never `null` (`:4749`, `:6362`), so the sibling fires exactly when `!contains(alg)`. Enter it in the conformance record as a checked non-defect with the fragility named — the equivalence depends on declaration order — and make no edit. `design.md` D-4's residues paragraph is already corrected to match
- [ ] 8.9 Per repair: one divergence-record entry naming the CrySL clause, the JDK signature or the generated-monitor line that proves it, one harness run before/after with the classification written (`unchanged` / `moved` / `removed` / `introduced`), and the `codes.csv` bijection preserved
- [ ] 8.10 Gates green on `jca_android`: G-2 `orphan-without-clause` = 0; G-6′ = 0; G-ERE = 0; G-CONF and G-PRED green; lint clean; `gate_allowlist.csv` carries every remaining G-2b′/G-2d hit with a reason; `CipherSpec` still generates (event count ≤ 17, INV-INS-115 — time and memory recorded)
- [ ] 8.11 Conformance record `data/jca_android/conformance_record.csv` complete: one row per specification against its api30 rule (single oracle, design D-10), including the two `api30-omits` divergences — the `EC` of task 2.6 and the four ECDSA algorithms of task 2.7 — the declared cost of the `api30-admits` rule recorded by task 2.4 (5,892 `MessageDigestSpec` rows), the two measured-not-repaired pointcuts of 8.7, and the withdrawn item of 8.8
- [ ] 8.12 Run `/rv-test-run tests/parity`

## 9. E6 — Identity (`tasks/E6-identity.md`) — after Groups 5 and 7

- [ ] 9.1 `scripts/gh104_identity_discontinuity.py`: recompute comp162 identities with `event` (from the envelope where present; sentinel otherwise) next to the five-field 6,344; record both in `data/gh104/identity_discontinuity.md`; comp162 predates E1 so its delta is zero by construction — the deciding corpus is the harness output of Group 6 or task 10.4's device logcat, and the group file says which one was used; if the delta is zero on a corpus that does carry `ev=`, stop and re-open design D-5
- [ ] 9.2 `rvsec-core` `ErrorSummary.equals/hashCode` += `code`, `event`; `ErrorDescription` carries them; `ErrorDescriptionTest.hashCodeMatchesEquals` rewritten for seven fields; logcat collector emits them in the envelope (already) — no line-format change
- [ ] 9.3 Confirm `errors.csv` `code`/`event` columns and `unique_msg` parts (Group 5) are fed from the collector line end to end with a recorded logcat fixture; declare the era in `data/gh104/identity_discontinuity.md`
- [ ] 9.4 Rebuild `lib/` jars; run `/rv-test-run modules/rv-coverage` and `/rv-test-run modules/aperv-tool`

## 10. Integration and verification (`tasks/E10-integration.md`)

- [ ] 10.1 Freeze and divergence: existing gh101 five gates green; `test_gh104_specset_gates.py` and `test_gh104_structural_gates.py` green on `jca` and `jca_android`; `jca/`, `CipherTransformationUtil.java` and `AndroidCipherTransformationUtil.java` byte-identical to their pre-change state, and the archived `jca_android_bug_predicate/` byte-identical to the pre-rename `jca_android/` (git records the move as `R100`, zero content hunks); `MetaCrySL/` untouched (`git status` clean in that tree)
- [ ] 10.2 Run `/rv-qa-lint-fix rv-coverage`, `/rv-qa-lint-fix rv-platform`, `/rv-qa-lint-fix aperv-tool`, `/rv-qa-lint-fix rv-android-core`, `/rv-qa-lint-fix rv-experiment`
- [ ] 10.3 Run `/rv-verify rv-coverage`, `/rv-verify rv-platform`, `/rv-verify aperv-tool`, `/rv-verify rv-android-core`, `/rv-verify rv-experiment`, `/rv-verify rv-instrumentation-dexlib2`
- [ ] 10.4 Device validation, one task, via `uv run rv-experiment run --tools monkey --specification-set jca_android --timeouts 180 --apks-dir <dir with com.owncloud.android_48000100, eu.opencloud.android_9, de.luhmer.owncloudnewsreader_196, com.etesync.syncadapter_20700>`: record `unknown` = 0, `but found .` = 0, envelope fields populated, `advicesExcludedByArity` in results JSON, parser counters, in `evidence/device_validation.md` (the platform manages the emulator; no manual emulator command)
- [ ] 10.5 Read the device evidence against the pivot's tier: the `TrustManagerFactorySpec` `X509` line, the `KeyStoreSpec` `AndroidKeyStore` line and the `SSLContextSpec` `TLS` line must no longer be reported as violations under the transcribed api30 allow-lists plus the normalisation rule of task 2.5 — record the shape of each, not counts (Monkey is stochastic)
- [ ] 10.6 Invoke `/rv-code-reviewer` via Skill tool
- [ ] 10.7 Run `/rv-docs-sync rv-coverage`, `/rv-docs-sync rv-instrumentation-dexlib2`, `/rv-docs-sync aperv-tool` if module docs need updating; update `openspec/specs/experiment/spec.md:87` sample comment at sync time

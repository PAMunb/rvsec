<!-- Subagent dispatch hints (this change touches ~130 authored files plus the 23-file rename and the
     evidence/trace files under data/gh104/, across three trees: rv-android, the sibling rvsec Java reactor,
     and MetaCrySL read-only):
     - Read design.md D-12 first, then D-11, then the pivot brief (successor set = `jca_android`, the seed's
       23 specifications, predicates carried over untouched, allow-lists transcribed from
       `MetaCrySL/generated/api30/*.cryptsl`). Every group heading
       below points to tasks/<GROUP>.md — the per-group execution file with the file inventory, per-site tables,
       commands, expected values and the subagent brief. A subagent dispatched for a group reads ONLY its group
       file (plus the spec deltas it cites); the orchestrator reads only this file and ticks the boxes here,
       immediately after each task.
     - MetaCrySL is READ-ONLY in this change. `generated/api30/*.cryptsl` is the oracle; nothing under
       `MetaCrySL/samples/` or `MetaCrySL/src/` is edited, not even the `KeyPairGenerator` defect of D-c —
       it is recorded as a divergence, not repaired.
     - Wave 1 (parallel, disjoint file sets, six subagents): Group 1 (E0 baseline: scripts/ + data/gh104/ + docs/),
       Group 2 (S seed: after the wave-0 `git mv` of task 2.1 has archived the reproved derived set as
       rvsec-mop/.../jca_android_bug_predicate/, rvsec-mop/jca_android/ + two new rvsec-core classes —
       `Api30CipherTransformationUtil` and `ConscryptAliasTable`, both under
       `rvsec-core/src/main/java/br/unb/cic/mop/jca/util/` — + rv-experiment + data/jca_android/), Group 3 (G __EVENTNAME: rv-monitor generator only), Group 4 (E2 weaver counter:
       Java dexlib2 only), Group 5 (E3 transport: rvsec-core collectors, rv-monitor-rt, rv-coverage,
       rv-android-core, rv-platform, aperv-tool), Group 6 (EV validation: NEW files only — tests/parity/,
       scripts/gh104_*, rvsec-mop/src/test).
     - Groups 3 and 5 both live under rv-monitor/ but in different submodules: Group 3 edits rv-monitor/rv-monitor
       (the generator), Group 5 edits rv-monitor/rv-monitor-rt (ViolationRecorder). Disjoint source sets.
       Group 2 adds `rvsec-core/.../jca/util/`; Group 5 edits the collectors, which live in
       `rvsec-android/rvsec-logger-logcat` and `rvsec-logger-csv` (not in rvsec-core — `ErrorDescription.java`
       is read, not edited, until Group 9). Disjoint. Group 6 creates `rvsec-mop/src/test/java` and adds the
       junit dependency to `rvsec-mop/pom.xml` (the pom is minimal today); Group 2 edits only
       `rvsec-mop/src/main/resources/`. Disjoint files, same module — commit separately.
     - Shared directories with per-group file names (no lock, but do not claim the directory): `scripts/gh104_*`
       (G1 `gh104_baseline.py`, G2 `gh104_divergence_record.py`, G3 `gh104_regen_diff.py`, G5 edits
       `regenerate_results/regenerate_container.py` + `rv_oracle_common.py`, G6 the rest); `tests/parity/`
       (G1 `test_gh104_baseline.py`, G2 `test_gh104_specset_gates.py` + edits `test_gh101_specset_gates.py`,
       G5 `test_gh104_unique_msg_built_once.py`, G6 `test_gh104_structural_gates.py`); `data/gh104/` (G1
       baseline/definitions, G5 `consumer_matrix.md`, G6/G3/G4/G2 under `data/gh104/evidence/`).
     - Roots: every `evidence/...` path in the group files is `data/gh104/evidence/...`; every `traces/...` path is
       `data/gh104/traces/...` (versioned; `results/` is gitignored). The sha256 manifest of task 3.0 is
       `data/gh104/jca_frozen_control.sha256`; there is no second control.
     - Two shared resources are SERIALISED BY THE ORCHESTRATOR (sequential dispatch — no lock files, nothing
       under data/gh104/ for it): (a) Maven — `mvn install` at the reactor root and every rebuild of `lib/`
       (3.9, 4.6, 9.4) run only between waves, never while a group is editing Java. Group 3's regeneration
       tasks (3.5, 3.8) consume the generator that 3.9 installs, so 3.9, 3.5 and 3.8 run together as ONE
       between-waves orchestrator step, after the group's wave-1 edits (3.1-3.4, 3.6, 3.7) have landed —
       the same shape as Group 4, which hands its re-weave to the same step; per-submodule `mvn -q test`
       may run inside a group (every module resolves rvsec-core from `target/` or the local Maven repository
       (`~/.m2/settings.xml` → `/home/pedro/desenvolvimento/repository`)); (b) monitor generation — 2.14,
       3.5/3.8, 6.6/6.10, 7.7, 8.9/8.10, 8.14-8.16 — the orchestrator dispatches at most one generating task at a time; a group
       file marks its generating tasks with `[GEN]` (3.0 is not one: it pins a control that already exists
       and generates nothing). Every Java/Maven command line is prefixed with
       `export JAVA_HOME=$HOME/.sdkman/candidates/java/21.0.12-tem; export PATH=$JAVA_HOME/bin:$PATH` (the
       default JDK is 25 and shell state does not persist between tool calls — prefix EVERY line, or run them
       in one shell line). **One exception, and it is load-bearing: monitor generation that will be
       byte-compared against the frozen control runs under JDK 25** (`25.0.3-tem`), the JDK the control was
       generated with — the state numbering of a generated monitor depends on it (tasks 3.5/3.8;
       `data/gh104/evidence/g_regeneration.md`). Building the reactor stays on 21, which is what the pom targets; every generation shell: `export TMPDIR=$HOME/tmp-gh104 && mkdir -p $TMPDIR` (`/tmp`
       and the session scratchpad are tmpfs). `rg` is not installed: use `grep -rE` where a task says `rg`.
     - Git: one repository (`git rev-parse --show-toplevel` = `…/rvsec`; rv-android is a subdirectory) with
       hundreds of dirty lines unrelated to this change. Every `git status`/`git diff` in a task carries a
       pathspec (`git status --short -- <paths>`; `git diff --cached --stat -- <paths>`); commits are made by ONE
       writer at a time — the orchestrator, after each group's summary, `git add <explicit pathspecs>` + commit;
       subagents do NOT commit unless the group file says so, and then only with explicit pathspecs; never
       `git add -A`/`git commit -a`.
     - Numbering is not execution order in Group 2: 2.15 (constraint table) runs between 2.4's transcription
       pass and 2.4's deferred-constant rows (the one ordering: 2.4 transcription → 2.15 → deferred-constant
       rows) and has no dependency on EV; 2.14 (harness before/after + promotion) runs LAST, once Group 6 task 6.9 has landed, and
       MUST be complete before Group 7 starts (2.14 edits `jca_android/*.mop`; two writers must not overlap).
     - `experimento-gh104/` (untracked campaign scaffold) hard-codes `jca_android`, `codes.csv`,
       `jca_android_bug_predicate/` and its own `scripts/gh104_gates.py` (campaign-readiness gates, 13-column
       header) — a different script from `scripts/gh104_gates.py` of task 6.1; the campaign runs only after
       Group 10; nothing in this change edits it.
     - The enumeration does NOT grow: `jca`, `jca_android`, `generic`, `custom` stay the four accepted values.
       `jca_android` is re-pointed at the successor set; the reproved derived set moves to
       `jca_android_bug_predicate/` and is not selectable by any value.
     - Group 7 (E1 messages) starts after the between-waves 3.9/3.5/3.8 step (the installed generator expands
       `__EVENTNAME` and the `jca` regeneration diff is recorded) AND after Group 2 has committed — including
       task 2.14, which is the last writer of `jca_android/*.mop` before this group, so the set exists in its
       final allow-list shape, predicates untouched — AND Group 6 tasks 6.1-6.5, 6.7, 6.8, 6.9 have landed (7.6 runs
       the lint, message gate, G-CONF, G-PRED and the G-2/G-ERE gates of 6.1-6.3 that its `gate_allowlist.csv`
       row answers to; 7.7 runs the harness). Inside the group, 7.1 (`ErrorType.java`) lands first and the
       orchestrator runs one serialised Maven step — `mvn install -DskipTests -DskipMopAgent -pl rvsec/rvsec-core` from the reactor root, JDK prefix as above — before 7.7, so the generated monitors
       that name `ForbiddenMethod` compile against the installed rvsec-core. It edits `jca_android/*.mop`,
       `codes.csv` and rvsec-core `ErrorType.java`, appends to `data/jca_android/{divergence_record.csv,
       conformance_record.csv, gate_allowlist.csv, README.md}` (the 7.2 recount, the guard-on-field
       declaration of 7.4, the 7.6 allow-list row) and writes `data/gh104/evidence/harness/e1-<Spec>.md` plus
       the harness traces of 7.4; run it as TWO subagents on disjoint file halves (7.a files A–K,
       7.b files M–T; see tasks/E1-messages.md) with `ErrorType.java` owned by 7.b alone. `codes.csv` and
       `data/jca_android/divergence_record.csv` are appended by both halves, so 7.a writes its rows into
       `codes.7a.part` and its divergence entries into `divergence.7a.part` (or hands them over in its summary)
       and 7.b, which owns the two files, merges them.
     - Group 8 (E4 structural repairs) starts after Groups 6 AND 7 are complete (hard prerequisite: the gates
       and the harness must exist, and every report site must already carry its envelope). It is six
       structural repairs across five files (GCMParameterSpecSpec, SecretKeySpecSpec, MessageDigestSpec,
       KeyPairGeneratorSpec, SignatureSpec), two measured sites, one withdrawal, three measured items
       inherited from E1 (8.14-8.16: the `g4` report of `MessageDigestSpec`, the `__RESET` in the `@fail` of
       `KeyPairGeneratorSpec`, the guard on the bound argument at the nine guard-on-field sites) and the
       record of the inherited divergences, not a replay: one owner per file, sequential is fine.
     - Two cross-wave hand-offs inside wave 1: Group 6 task 6.4 (G-CONF) waits for Group 2 task 2.15 (the
       constraint row table), and Group 2 task 2.14 (harness before/after on the seed edits) waits for Group 6
       task 6.9 (the harness) — 2.14 completes before Group 7 starts, and 6.4 lands before Group 7 starts. Four more hand-offs inside
       wave 1 are soft — the consumer reads what the producer committed and waits only if it is not there yet:
       6.6 ← 3.0 (the structural-gates test reads the control manifest `data/gh104/jca_frozen_control.sha256`
       that 3.0 commits), 6.9 ← 2.4 (the harness traces carry one sequence per value the transcription of 2.4
       newly admits), 5.9 ← 1.3 (the consumer matrix cites the instrument discontinuity 1.3 registers in
       `data/gh104/definitions.md`), Group 9 ← Group 1 (9.1 reuses the frozen 11-column reader of
       `scripts/gh104_baseline.py`). Task 2.4's transcription pass has NO dependency on
       EV: every clause the frozen `.mop` never tested is born a `deferred-constant` row; its promotion to a
       transcribed check is part of 2.14 (design D-10/D-30). Group 2 task 2.13 (repoint the gh101 gate scripts)
       runs immediately after 2.1, or two of the five gh101 gates go red.
     - Task 2.1 is the wave-0 barrier and the ORCHESTRATOR runs it (record `pre-rename-head`, `git mv`, commit
       with pathspec) BEFORE dispatching wave 1; 2.13 follows immediately (orchestrator, or the first act of
       Group 2). Reason: monitor generation writes `.rvm` into the source directory
       (`runtime_verification_generator.py:207-223`) before moving them, so a rename during generation corrupts
       both the run and the R100 record. Consequence: from wave 1 on the archived set is
       `rvsec-mop/src/main/resources/jca_android_bug_predicate/` — its bytes are identical to the pre-rename
       `jca_android` — and `jca_android/` is created by 2.2 as the seed and is NEVER the derived control; a task
       that finds `jca_android/` and no `jca_android_bug_predicate/` must stop: wave 0 has not run. The archived set is edited, regenerated and
       replayed by no task — Group 3 controls the generator against `jca` alone and Group 6's harness self-test
       uses a synthetic mutant of `jca`; it is read only by the gh101 gate scripts (repointed at it by 2.13) and
       by the identity checks (2.11's byte-identity against the pre-rename `jca_android`, the directory-count
       row of Group 1). It is a record, not an instrument, so it is not a cross-wave dependency of any kind. Wave-1 parallelism is preserved.
     - Group 9 (E6 identity) after Groups 1 (9.1 reads comp162 through the frozen reader of
       `scripts/gh104_baseline.py`), 5 and 7, with the harness output of 6.9 as its deciding corpus — it may
       overlap Group 8 (rvsec-core ErrorSummary + Python; no .mop file).
     - Group 10 (integration & verification) last, after everything.
     - Critical path: (2 ∥ 3) → 6.4/6.9 → 7 → 8 → 10; Group 6 is on it through 2.15→6.4→7 and 6.9→2.14→7.
       Groups 1, 4, 5 are off it; Group 9 is off it unless Group 8 slips. Group 2 is the long pole of wave 1
       (23 specifications × two mechanical passes — the allow-list transcription and the normalisation rule — plus two new Java classes), so dispatch it first and give it
       the largest budget.
     - Never start, stop or manage an emulator by hand. Task 10.4 is the only device task and it runs
       `rv-experiment run --specification-set jca_android`. Everything else is JVM/pytest/scripts.
     - Tests: `uv run pytest --import-mode=importlib -o "addopts=" <path>`. Java: `mvn -q test` inside the
       submodule under ../rvsec/rvsec-android/rvsec-instrumentation-dexlib2/ (never at the reactor root
       without -pl); Group 3 runs `mvn -q test -pl rv-monitor/rv-monitor` from the reactor root — every such
       line prefixed with `export JAVA_HOME=$HOME/.sdkman/candidates/java/21.0.12-tem; export PATH=$JAVA_HOME/bin:$PATH`.
       Monitor generation needs RVSEC_HOME and `export TMPDIR=$HOME/tmp-gh104 && mkdir -p $TMPDIR`. -->

## 1. E0 — Baseline and definitions (`tasks/E0-baseline.md`)

- [x] 1.1 Write `scripts/gh104_baseline.py`: reads `experimento-comp162/results/*/*/errors.csv` through its **own frozen 11-column reader** declared in the same file (an import of `aperv_tool.analysis.violations.read_errors_csv` is not a pin: task 5.6 changes `ERRORS_CSV_HEADER` in place, `violations.py:63-75`, and `read_errors_csv` raises on any other header, `:242-246`, so after Group 5 the shared reader rejects every comp162 file) and the article dataset through a declared 10-column reader in the same file; the comp162 input is pinned by the existing `modules/aperv-tool/tests/fixtures/cmp162_manifest.json` (465 entries, the 8 `errors.csv` among them) — reuse that pin, do not compute a second manifest; every definition (mute row, site 4-tuple vs 3-tuple, nine-prefix classifier, shard) is a required freeze item (`FreezeItemUnset` if absent); every number leaves as an `Envelope` (value, numerator, denominator, definition id)
- [x] 1.2 Reproduce and record in `data/gh104/baseline.md`: article 97,018 / 19 messages / 70,760 `unknown` = 72.93 % / 8,843 `but found .`; comp162 19,664 / 15,714 mute / 296 sites / 101 mute-legible twin sites (3,950 rows) / 12 mute-mute (838) / per-spec mute top-8 / third-party 78.49–82.67–85.44 % under the three prefix lists; identities 6,344 under the 8-tuple `(apk, rep, tool, spec, class, method, source, message)` (the `ErrorSummary` 5-tuple `(spec, error, class, method, location)` gives 409 — name the tuple, never "five-field"); and the footprint of two structural facts Group 8 records but does not repair, as ratios per specification on both corpora — `InvalidSequenceOfMethodCalls` against the specification's own labelled type (article: `SSLContextSpec` 17,510 vs 8,802 `UnsafeProtocol`, `TrustManagerFactorySpec` 9,015 vs 9,014 `UnsafeAlgorithm`; comp162: `TrustManagerFactorySpec` 2,855 vs 61, `SecureRandomSpec` 2,882 vs 0 — the second `@fail` report of every clause-encoding orphan and the DEX-path `g1`+`g2` double fire; design D-1, task 8.12) — each with its command, over the files `cmp162_manifest.json` pins
- [x] 1.3 Register the definitions and the instrument discontinuity (the article dataset has no `source`; `read_errors_csv` rejects it by design) in `data/gh104/definitions.md`
- [x] 1.4 Record the publishable Android tier the pivot rests on (11,409 events / 84 misuses of 454 = 18.5 %, five spec×ErrorType×value rows) with its source `ase-journal/docs/20260806_owasp_cwe_mapping_report.md`, and the API-30 / Android-11 / Conscrypt `android11-release` context fixation — this is the denominator the allow-list transcription of Group 2 is measured against
- [x] 1.5 Add `tests/parity/test_gh104_baseline.py::test_baseline_reproduces_byte_identical` (rerun → identical `baseline.json`) and `::test_freeze_items_required`; both skip with a named reason (`pytest.skip("comp162 results absent: <path>")`) when `experimento-comp162/results/` is absent and fail when it is present but disagrees with `cmp162_manifest.json`
- [x] 1.6 Run `/rv-test-run tests/parity/test_gh104_baseline.py`

## 2. S — Archive the reproved set, seed and shape the successor set `jca_android` (`tasks/S-seed.md`)

- [x] 2.1 **Wave-0 barrier, run by the orchestrator before wave 1 is dispatched** — archive the reproved derived set **before** creating anything: `git mv rvsec-mop/src/main/resources/jca_android rvsec-mop/src/main/resources/jca_android_bug_predicate` — a pure rename of the 23 `.mop` (`git status --short -- rvsec-mop/src/main/resources/jca_android rvsec-mop/src/main/resources/jca_android_bug_predicate` must show `R ` for every file — the similarity score is read with `git diff --cached --name-status --find-renames -- <the same pathspec>`, which must show `R100` for every file — zero content hunks), committed at once with that pathspec. The name states why it is archived: the 2026-08-08 audit reproved it 22/22 on its predicate machinery (22 of the 23 files were in its scope; `RandomStringPassword`, the `@fail`-less propagator, was outside). The archived directory is **not selectable** — no enumeration value points at it and none is added (task 2.9). Record the rename, the name's reason and the audit reference in `data/jca_android/README.md` and as the set's first divergence entry (kind `set-archived`). **Before running the `git mv`, record the Java tree's current `HEAD` SHA in `data/jca_android/README.md` as the freeze item `pre-rename-head`**: that SHA, not `7e7acb69`, is the base against which task 10.1 asserts the move is pure. `7e7acb69` predates gh101's edits to this directory (19 files, 720 insertions, 122 deletions measured on 2026-08-18), so a rename diff taken against it can never show zero content hunks
- [x] 2.2 Create `rvsec-mop/src/main/resources/jca_android/` as a byte-identical copy of the frozen `jca/` — all **23 `.mop`**, `RandomStringPassword.mop` and `SecretKeySpec.mop` included: they propagate predicates and the predicates stay (D-11); add `codes.csv` with header only; `jca/MultiSpec_1MonitorAspect.aj` is a gitignored generation leftover (`*.aj`) and does **not** travel — `codes.csv` is the only non-`.mop` file of the set (specs/instrumentation); record that in `data/jca_android/README.md`; commit as one seed hunk
- [x] 2.3 **Predicates are carried over untouched — this task removes nothing.** The seed's 134 `ExecutionContext` lines across the 23 files (23 `import`, 27 `validate(`, 49 `setProperty(`, 9 `remove(`, 25 `setObjectAsInAcceptingState`/`unsetObjectAsInAcceptingState`, 1 comment at `MessageDigestSpec.mop:25`) stay byte-for-byte, and so does every `condition()` that reads a predicate, every predicate-only accuser's declaration and every `@match`/`@match1` body. An earlier revision of this plan deleted all of it; that decision is withdrawn (D-11) because removal deletes detection at 11 of the 21 predicate-reading events with no before/after evidence, and repairing the graph is a change of its own. The task is a **check**: `grep -c ExecutionContext` over `jca_android/*.mop` equals the same count per file as over `jca/*.mop`, and `diff` of the predicate lines is empty. There is no `predicate_removal.csv` — nothing left to record
- [x] 2.4 *(executed as written; the anchor this task transcribed to was superseded on 2026-08-24 by design D-15 — Group 11 re-transcribes the value lists from the expert oracle)* Transcribe the allow-lists literally from `MetaCrySL/generated/api30/*.cryptsl` `CONSTRAINTS`, spec by spec, over the 13 `Arrays.asList` declarations of the seed; one conformance-record row per list with its `.cryptsl` file and clause. **The api30-admits case is decided, do not re-open it**: if the rule admits a value, the specification accepts it — one oracle, no second anchor. For `MessageDigestSpec` (`MessageDigest.cryptsl:63` admits `MD5` and `SHA-1`) that costs **5,892 of the 6,048** `MessageDigestSpec/UnsafeAlgorithm` rows of the published dataset — 3,552 `MD5` + 2,340 `SHA-1`/`SHA1`/`SHA` — i.e. 97.4 % of that specification's `UnsafeAlgorithm` reports (36.4 % of all its 16,183 reports) and 6.1 % of the 97,018-row corpus; write that figure into the conformance record as the declared cost, not as a question. For `SignatureSpec` (`Signature.cryptsl:75`, 20 entries) it means `MD5withRSA`, `SHA1withRSA` and `SHA1withDSA` are accepted. For `KeyPairGeneratorSpec`, api30 `KeyPairGenerator.cryptsl:51` gives RSA `{4096, 2048}` where the seed's `validate` (`:30`) accepts 3072 — transcribe `{4096, 2048}` and record the hunk (RSA-3072 dropped) in the conformance record; the seed's `DiffieHellman` (`KeyPairGeneratorSpec.mop:22,32`) leaves the same way, because `KeyPairGenerator.cryptsl:45` spells the algorithm `DH` only — record it as a narrowing hunk beside RSA-3072 (a `MOP-MAIS-PERMISSIVO` row in 2.15's `constraint_table.csv` plus a conformance-record note), not as a silent drop. **Every clause the frozen `.mop` files never tested is entered here as a `deferred-constant` row of `conformance_record.csv`, taken from the clause list of 2.15's `constraint_table.csv` (the one ordering: the transcription pass of this task → 2.15 → these deferred-constant rows; the pivot counted 30, the table gives the real number)** with the `.cryptsl` file, the clause, the reason and the statement that leaving it out adds no accusation (INV-INS-125, design D-30) — this pass has no dependency on EV; promotion of a row to a transcribed check happens in task 2.14, once the harness can size the accusations it adds. None is skipped in silence, and none is deferred to Group 8 — the transcription lives in this group. Also enter the OAEP case (`RSA/ECB/OAEPWithSHA1AndMGF1Padding`, 109 events; design D-10) in the divergence record with its evidence labelled `behavioural` and no alias row
- [x] 2.5 Declare the normalisation rule in `data/jca_android/README.md` and implement it in the 21 specifications that carry a value list: comparison is case-insensitive everywhere (today case-sensitive in 8 specs — Mac, Signature, SecureRandom, KeyGenerator, TMF, KMF, KeyStore, KeyPairGenerator — `.toUpperCase()` in 3), plus the Conscrypt `android11-release` alias table. Import the finished table from `backup/gh104-analise/alias_table_conscrypt_android11.csv` into `data/jca_android/alias_table.csv` (158 alias rows — 149 in services the 21 specs cover plus 9 for `AlgorithmParameters`/`SecretKeyFactory`, kept with the flag; columns `service,alias,canonical,openssl_provider_line,in_api30_allowlist`, where `in_api30_allowlist` is `yes` exactly when the canonical name is an entry of the successor set's allow-list after tasks 2.6 and 2.7 — post-divergence, so the `KeyPairGenerator → EC` rows and the 16 `SHA*withECDSA` rows are `yes`; the backup file flagged against the literal api30 lists, so the `yes`/`no` counts are recomputed under this definition and recorded by this task in the `alias_table.csv` line of `data/jca_android/README.md`, never carried over) — **do not redo the extraction**, sample-check ~10 rows against `backup/gh104-analise/OpenSSLProvider.java` and record which rows were checked. Resolution at run time does **not** read the CSV: `ConscryptAliasTable`, a new class under `rvsec-core/src/main/java/br/unb/cic/mop/jca/util/` (the package of the frozen `CipherTransformationUtil`), carries the table as code and the `jca_android` specs name that class directly (INV-INS-112 — no runtime switch, and the frozen `jca` never references it, so its verdicts cannot move); one Java test asserts the in-code table is row-for-row equal to `data/jca_android/alias_table.csv` (all 158 rows, flag included, under the flag definition above)
- [x] 2.6 Keep `EC` (keySize 256) in the `KeyPairGeneratorSpec` allow-list against the literal api30 rule: `EC` is already in the seed (`KeyPairGeneratorSpec.mop:22`, `validate` `:34`) and the literal transcription of task 2.4 would remove it, because api30 `KeyPairGenerator.cryptsl:45` lists `{DSA, DH, RSA}`; this task keeps it and records the D-c divergence entry naming the MetaCrySL defect (`samples/jca/base/KeyPairGenerator.cryptsl:27` writes the literal list where `KeyGenerator.cryptsl:27` writes `alg in ${algorithm}`, so `samples/jca/android/11plus/KeyPairGenerator.ref:2` `define algorithm = {"EC"}` is discarded in silence) and stating that the root fix is not executed here — a "kept against the rule" hunk, not an addition. This is the **api30-omits** case of the asymmetric rule: the platform has it, the rule does not list it, the set keeps it and records the divergence
- [x] 2.7 Same case, second occurrence: add `SHA1withECDSA`, `SHA256withECDSA`, `SHA384withECDSA` and `SHA512withECDSA` to the `SignatureSpec` allow-list. `Signature.cryptsl:75` lists only `SHA224withECDSA` among the ECDSA algorithms, but Conscrypt registers all five on API 30 — `OpenSSLProvider.java:270,271` (`ECDSA`, `ECDSAwithSHA1` → `SHA1withECDSA`), `:286` (`SHA256/ECDSA`), `:293` (`SHA384/ECDSA`), `:300` (`SHA512/ECDSA`) — which is primary-source proof that the four exist in the platform. One divergence entry, same shape as task 2.6
- [x] 2.8 Create `Api30CipherTransformationUtil` under `rvsec-core/src/main/java/br/unb/cic/mop/jca/util/` (the package of the frozen `CipherTransformationUtil`) with the lists transcribed from `generated/api30/Cipher.cryptsl` `CONSTRAINTS`, and point `jca_android/CipherSpec.mop`'s static import at it; `CipherTransformationUtil.java` (69 l., frozen with `jca`, byte-identical to `7e7acb69`) and `AndroidCipherTransformationUtil.java` (262 l., owned by the archived `jca_android_bug_predicate`; it does **not** exist at `7e7acb69` — gh101 added it, so its freeze base is `pre-rename-head`) are read-only — neither may be edited; add its Java unit test
- [x] 2.9 Confirm — do not extend — the enumeration: the four accepted values stay `jca`, `jca_android`, `generic`, `custom`, so `click.Choice` (`__main__.py:443`), `valid_spec_sets` (`config.py:432-437`) and `SPEC_SET_JCA_ANDROID` (`constants.py:97`) are unchanged in shape. What changes is meaning: the mapping `jca_android → .../resources/jca_android` (`config.py:688-700`) now resolves to the successor set, so update the comments and help text that still describe it as "the same 23 specifications derived against generated CrySL rules" (`config.py:248,385,429,648,657,678`; `__main__.py:368,656`; `experiment/workflow/pre_processor.py:132`, the comment that enumerates the sets; `__main__.py:1193` only lists the four values and stays). Add `test_jca_android_bug_predicate_not_selectable` (the archived directory name is rejected exactly like any other unknown value) beside the existing `test_jca_android_spec_set_valid` / `test_jca_android_spec_set_resolves_paths` / `test_near_miss_spec_set_still_rejected`; the invalid-value message keeps listing **four** values
- [x] 2.10 Create the five records `data/jca_android/{divergence_record.csv, conformance_record.csv, alias_table.csv, gate_allowlist.csv, constraint_table.csv}` with headers (`constraint_table.csv`: `spec,cryptsl_line,mop_line,verdict`, filled by task 2.15) and `README.md`, and `scripts/gh104_divergence_record.py` (parametrised copy of `scripts/gh101_divergence_record.py`: base = `jca/`, target = `jca_android/`, `--check` fails on an unrecorded hunk); there is neither a `predicate_omissions.csv` nor a `predicate_removal.csv`: the first justifies a `Property` written and never read, and the second recorded a removal this change no longer makes (D-11) — the set carries the seed's predicates unchanged, so there is nothing for either to name
- [x] 2.11 Add `tests/parity/test_gh104_specset_gates.py::test_jca_android_hunks_all_recorded` and `::test_jca_android_predicates_preserved` — the second asserts that every `ExecutionContext` site of the frozen `jca` is present in `jca_android` at the same event and unrewritten (134 lines over the 23 files: 23 `import`, 27 `validate(`, 49 `setProperty(`, 9 `remove(`, 25 accepting-state calls, 1 comment), which is the gate that would have caught the withdrawn removal going in; the two tests of the removal era (`test_jca_android_has_no_execution_context`, `test_predicate_removal_record_complete`) are deleted with it. Confirm the existing five gh101 gates still pass after task 2.13 repointed them (nothing under `jca/` moved) and that `jca_android_bug_predicate/` is byte-identical to the pre-rename `jca_android/`
- [x] 2.12 Run `/rv-test-run modules/rv-experiment` and `/rv-test-run tests/parity`
- [x] 2.13 Repoint the gh101 gate to the archive, immediately after 2.1: `scripts/gh101_divergence_record.py` (`--derived` default, `:152`), `scripts/gh101_predicate_pairing_check.py` (`--specs` default, `:156-160`) and `scripts/gh101_conformance_check.py` (`--specs` default, `:269`) default to `resources/jca_android`, and `tests/parity/test_gh101_specset_gates.py:110,122` invokes the first two with no path — after 2.1/2.2 the divergence check reports every recorded hunk as stale and the pairing check a stale inventory (simulated read-only on 2026-08-18: 106 and 9 problems), so two of the five gates go red. Change the three defaults (or the two test invocations) to `resources/jca_android_bug_predicate`, touch nothing in `data/gh101/`, and record in `data/jca_android/README.md` that the gh101 gate now guards the archive (INV-INS-118)
- [x] 2.14 Harness before/after for this group — the LAST task of the group, dispatched once Group 6 task 6.9 has landed and complete before Group 7 starts (it is the last writer of `jca_android/*.mop` before Group 7): replay the traces through the seed snapshot and the post-2.9 set (after 2.8 moved the Cipher list) for every touched file. Because the predicates are carried over unchanged (D-11), **every difference the harness sees is an allow-list difference** — there are no lost accusers and no recovered guards to classify. Admissible classes: every transcription difference that widens a list → `removed` with reason `corrected verdict (<api30 clause>)`; every one that narrows a list → `introduced — corrected verdict (narrowed by <api30 file:line>)` for a trace carrying a value the seed admitted and the api30 transcription does not (RSA-3072 and `DiffieHellman` of `KeyPairGeneratorSpec`, `SunX509`, `NativePRNG*`, `Windows-PRNG`, `PKCS11`, `JKS`/`JCEKS`/`DKS`, `HmacPBESHA1`, and bare `RSA`/`RSA/ECB` from the Cipher class), each citing its narrowing hunk in the conformance record; anything else stops the group → `evidence/harness/s-<Spec>.md`. Then promote to transcribed checks the `deferred-constant` rows of 2.4 whose added accusations the harness sized (design D-30); the rest stay deferred (INV-INS-124)
- [x] 2.15 Commit `data/jca_android/constraint_table.csv` immediately after the transcription pass of 2.4 and before 2.4 enters its deferred-constant rows (the one ordering: 2.4 transcription → 2.15 → deferred-constant rows; no dependency on EV; numbering is not order) and before Group 6 task 6.4 writes G-CONF: one row per api30 `CONSTRAINTS` clause of the 21 paired rules (Cipher through the Java class of 2.8) plus one per `.mop` value test with no clause behind it, columns `spec,cryptsl_line,mop_line,verdict` with the verdict vocabulary of the pivot (`CRYSL-NAO-IMPLEMENTADO`, `IGUAL`, `MOP-SEM-BASE`, `MOP-MAIS-PERMISSIVO`, `DIVERGENTE`, `MOP-MAIS-RESTRITIVO`); the pivot's summary 74 = 30/14/13/7/7/3 has no committed row table behind it and an independent reconstruction reached 65, so the table's totals replace the summary wherever it is quoted (`proposal.md`, `design.md` D-10, `tasks/EV-validation.md`); every row whose verdict is `CRYSL-NAO-IMPLEMENTADO` is a `deferred-constant` row that task 2.4 has already entered (born deferred; 2.14 promotes)

## 3. G — `__EVENTNAME` in the monitor generator (`tasks/G-eventname.md`) — wave 1 for 3.0-3.4, 3.6, 3.7; **3.9, 3.5 and 3.8 run in that order as one between-waves orchestrator step**, because the regeneration needs the generator that 3.9 installs

- [x] 3.0 **Before the generator is touched**: write `scripts/gh104_regen_diff.py` (`--specs-dir <path>` regenerates that directory's monitors into scratch with the current toolchain, diffs against `--control <dir>`, classifies each difference and exits non-zero on anything outside an `--expect` list) and pin the ONE control it compares against. `jca` already has it in the tree — `results/gh101_group8_jca_frozen_control/monitors/` — but it is unversioned: commit its sha256 manifest as `data/gh104/jca_frozen_control.sha256` (`results/` is gitignored, so the manifest is the versioned artefact; every test that reads the control directory skips with a named reason — `pytest.skip("control directory absent: <path>; regenerate per data/gh104/README.md")` — when it is absent, and fails, not skips, when it exists but its sha256 manifest disagrees, never with a missing-file traceback). Write `data/gh104/README.md` with the regeneration recipe for that control — the unmodified generator at the commit recorded there (the SHA before task 3.2) over `resources/jca` — and state that `MultiSpec_1MonitorAspect.json` is emitted by javamop `--emit-descriptor` in the same generation run (`runtime_verification_generator.py:210-213`) and is covered, while `mop/MonitorWrappers.java` is a product of the instrumentation step and is not, and `Coverage.aj` — copied into the same directory from the coverage aspects, not generated from the `.mop` sources (`runtime_verification_generator.py:227-230`) — is excluded as well, so the manifest names exactly the files it covers; note that `./clear.sh --clean-results` removes the control and plain `./clear.sh` removes `out/gh101_group8_apks` (the `apks_dir` of the control's `experiment_config.json`). **This task generates nothing**: the control it pins already exists, so 3.0 is not a `[GEN]` task and does not compete for the generation lock. The archived `jca_android_bug_predicate/` is NOT a control of this change and is regenerated by no task — Group 3 controls the generator against `jca` alone, which exercises the same emitter paths at a fraction of the cost
- [x] 3.1 Red test first (INV-INS-108 discipline): in `rv-monitor/rv-monitor/src/test/` add `EventNameMacroTest` with the three cases of INV-INS-120 — `__EVENTNAME` in an event body becomes the literal `"<name>"`; `__EVENTNAME` in a `@fail` resolves to the event that last transitioned the monitor, exercised once per monitor shape (atomic: `CipherSpec` or `SecretKeySpecSpec`; non-atomic: `HMACParameterSpecSpec` or `TrustManagerFactorySpec` — `TrustManagerFactorySpecMonitor` extends `AbstractSynchronizedMonitor`), asserting for each shape that the first dispatched event's handler sees that event's own name — the raw atomic `getLastEvent()` is `idnum+1`, so an expansion without the decoding of 3.4 names the *next* event in the 15 atomic classes — and that a handler run before any event sees `none` (raw `-1` non-atomic, `0` atomic); commit the red output under `evidence/`
- [x] 3.2 Emit the event-name table: in `BaseMonitor.toString()`'s `// state declaration` block (`:806-812`, where `Prop_N_transition_*` lands) write one table per monitor class from `this.events` (`:122`, populated `:179`) in `getIdNum()` order — one table per class, not per property, because `getIdNum()` is spec-wide; reserve its name in `RVMNameSpace` beside `RVM_lastevent`
- [x] 3.3 Expand the macro in the event-body path: `BaseMonitor.printEventMethod` (`:350-368`, beside the existing `__RESET`/`__LOC`/`__ACTIVITY` substitutions), using the `EventDefinition` already in scope → string literal
- [x] 3.4 Expand the macro in the handler path: `HandlerMethod` constructor (`:36-48`) expands `__EVENTNAME` to a call of a per-class helper `RVM_eventName()` — not to a direct table lookup. The generator emits the helper inside `BaseMonitor.toString()` after `checkIfAtomicMonitorCanBeEnabled()` (`:665`), where `isAtomicMoniorUsed()` is legal: it reads `this.getLastEvent()` (generated in the atomic shape; inherited from `rv-monitor-rt` `AbstractSynchronizedMonitor.java:21` in the other; declared on `IMonitor.java:19`) and decodes the shape — the atomic extracter (`BaseMonitor.java:1115-1140`, `getPairValueExtracter`) returns `pairValue >> numStateBits` where `calculatePairValue` stored `lastEvent+1`, so the raw atomic value is `idnum+1` and `0` before any event, while the non-atomic value (`BaseMonitor.java:427`) is `idnum` and `-1` — subtracts 1 in the atomic shape and returns `"none"` for the no-event value (`-1`/`0`) or the table entry otherwise. `HandlerMethod` itself stays shape-unaware, which is why the decoding lives in the helper: it is built in `initialize` (`BaseMonitor.java:226`) before `checkIfAtomicMonitorCanBeEnabled()` runs, so `isAtomicMoniorUsed()` throws `IllegalStateException` there, and `RVM_lastevent` does not exist in the atomic shape (design D-4); `RVM_eventName` is reserved in `RVMNameSpace` beside the table name; a non-outermost monitor (`isOutermost == false`) has neither and gets the literal `"none"` with the fail-closed check naming the case; mirror the substitution in `RawMonitor.java:90-105` so no emission path can leak the literal
- [x] 3.5 Fail-closed check: generation aborts naming file and line if the literal `__EVENTNAME` survives in any generated Java; regenerate `jca` — the one set that is frozen and independent of Group 2's timing — against `results/gh101_group8_jca_frozen_control/monitors/` and diff; the only admissible differences are the new table and the `RVM_eventName()` helper — no expanded macro, because the frozen `jca` contains no `__EVENTNAME`; record the diff in `evidence/g_regeneration.md`. Runs in the between-waves step, after task 3.9's `mvn install` has put the modified generator on the toolchain. The successor `jca_android` is not a control here: it does not exist yet when this group runs, and Group 7 regenerates it under task 7.7
- [x] 3.6 Red test first for INV-INS-129: in `rv-monitor/rv-monitor/src/test/` add a case that generates a monitor whose handler throws, invokes the dispatcher once on one thread (the throw) and once **from a second thread** with a bounded wait, and asserts the second call completes — it must fail today, because the first call leaves `MultiSpec_1_RVMLock` held and the lock is a `ReentrantLock` (a second call on the same thread re-enters and would pass before the fix); commit the red output under `evidence/`
- [x] 3.7 Emit the dispatcher's guarded region so the lock is released on every exit path (INV-INS-129). Baseline to beat, measured on `results/gh101_group8_jca_frozen_control/monitors/MultiSpec_1RuntimeMonitor.java`: one `ReentrantLock` at `:9005`, 134 `tryLock()` spin acquisitions, 134 `unlock()` calls, **0** `finally` blocks in the file. The framing is emitted in `Advice.java` (`rv-monitor/rv-monitor/src/main/java/.../output/combinedoutputcode/event/advice/Advice.java:176-177` acquire, `:254-256` release), where acquisition and release bracket the dispatch body — not in `GlobalLock.java:40-67`, whose fragments `BaseMonitor.java:543-587` also uses unbalanced. Change the framing only — not which advices fire, not the lock's identity, not the `Thread.yield()` spin; the exception still propagates to the application. The framing applies to the explicit `tryLock` form only: `GlobalLock.useImplicitLock` is hard-set `false`, so the implicit-lock branch emits nothing there is to frame
- [x] 3.8 Regenerate `jca` and diff against its control (same run as 3.5, in the between-waves step after 3.9's install): the only admissible differences are the lock framing, the event-name table and the `RVM_eventName()` helper (no expanded macro — the frozen `jca` has no `__EVENTNAME`); assert acquisitions still equal releases and that every acquisition now sits inside the framing; record in `evidence/g_regeneration.md`
- [x] 3.9 Rebuild the reactor (`mvn clean install -DskipMopAgent -DskipTests` from the root, ~1-12 min) and run `mvn -q test -pl rv-monitor/rv-monitor`; note in `evidence/g_regeneration.md` that `rvsec-agent/pom.xml:94-111` regenerates the JSE agent's monitor (`rvsec-agent/src/main/java/mop`) from `resources/jca` at every build without that flag, so the first such build after this group re-emits it with the new generator — run it once, diff it against the frozen control `results/gh101_group8_jca_frozen_control/monitors/MultiSpec_1RuntimeMonitor.java` (the regenerated file is gitignored — `rvsec-agent/src/main/java/mop/.gitignore` — so there is no committed source to diff against), and record the diff (table, helper, framing only)

## 4. E2 — Weaver `args()` arity, counter mode (`tasks/E2-weaver-arity.md`)

- [x] 4.1 Implement the three-clause rule as a **measurement only** in the `WrapperEmitter.generate` grouping loop (`:246-274`): read `ArgsPC` from the parsed expression, `types()` length with `trailingRest`/`headCount` semantics (three lines reimplemented in `WrapperEmitter` — in `PointcutMatcher.matchArgs :280-282` they are local variables of a private method, not helpers; `PointcutMatcher` is not edited), absent `args()` = admit; count the **advice/overload pairs** the rule would exclude into `advicesExcludedByArity` (unit = pairs; the same advice counts once per incompatible overload; expected on the frozen `jca` descriptor: 10 pairs over 4 advices — `TrustManagerFactorySpec_g2` ×1, `KeyManagerFactorySpec_g2` ×1, `SecureRandomSpec_g2` ×3, `SecureRandomSpec_g4` ×5 — design API); **no advice is removed from any wrapper** — emission is byte-for-byte what it is today
- [x] 4.2 Return the count: `WrapperEmitter.generate` gains an `EmitResult { List<WrapperEntry> wrappers; int advicesExcludedByArity; }` — this changes the 29 test call sites that receive the list (`WrapperEmitterTest` 25, `WrapperMergeTest` 3, `EmissionParityTest` 1) to `.wrappers()`, without changing any assertion; `cli/.../BatchRunner.java:199-201` puts `advicesExcludedByArity` in `counts` beside `wrappersGenerated`; `ResultsJsonReportingTest` asserts the key reaches `instrument_results.json` (it builds `counts` by hand, so it proves serialisation only; the origin of the count is what task 4.6's re-weave validates)
- [x] 4.3 Surface it in Python — a **test-only** task: `modules/rv-instrumentation-dexlib2/.../dexlib_instrumentation.py::_parse_results_json` already passes `weaveCounts` through whole (`dexlib_instrumentation.py:591-616`), so `advicesExcludedByArity` reaches `weave_counts` with no Python change; add the surface test in `modules/rv-instrumentation-dexlib2/tests/test_dexlib_instrumentation.py` asserting the key arrives from a results JSON that carries it
- [x] 4.4 Keep the existing emission tests green (nothing is filtered, so no existing **assertion** changes; the 29 call sites of 4.2 change mechanically): `EmissionParityTest`, `EmissionCardinalityTest`, `WrapperMergeTest`, `WrapperRegistryGuardTest`, `WrapperEmitterTest`, `MonitorCallsPremiseContractTest`; `WrapperMergeTest` gains three cases for the rule itself (design D-6) — an `args(a,*)` advice is counted **and** still emitted (count 1 on a TMF-only descriptor, the advice present in the wrapper), an advice with no `args()` is never counted, a trailing `..` is honoured (count 0 against every overload) — run `mvn -q test` in `advice-emitter`, `cli`, `dex-mutator`, `validator` and record that no existing assertion needed changing
- [x] 4.5 Record the reach of the rule in `evidence/e2_reach.md` from the frozen descriptor's 115 advices: 48 `after` non-constructor with `args()` measured; 44 without `args()` unaffected by clause 1; 9 `before` with `args()` and 14 `after`-on-constructor with `args()` outside the loop entirely — with both name lists, the reproduction command, and the sentence that in counter mode the number is a diagnosis, not a change in what fires
- [x] 4.6 Re-weave the frozen `jca` descriptor (`results/gh101_group8_jca_frozen_control/monitors/MultiSpec_1MonitorAspect.json`) and record `advicesExcludedByArity` and `wrappersGenerated` in `evidence/e2_reweave.md`; `wrappersGenerated` must be unchanged from the pre-change run — if it moved, the filter leaked and the group is wrong — and the sha256 of the re-woven `mop/MonitorWrappers.java` is recorded before and after the change (byte-identical expected): that hash **is** the parity instrument, because an unchanged `wrappersGenerated` alone cannot see a monitor call lost inside a wrapper; the re-weave command is the CLI with its real flags (`--descriptor`, `--android-jar`, the keystore, one APK from the corpus — as in `experimento-gh104/instrumentacao/README.md`), not a placeholder; rebuild `lib/` jars from the reactor root (between waves, serialised by the orchestrator per the dispatch hints) and record the sha256 of `modules/rv-instrumentation-dexlib2/lib/instr-cli.jar`
- [x] 4.7 Run `/rv-verify rv-instrumentation-dexlib2`

## 5. E3 — Honest transport (`tasks/E3-transport.md`)

- [x] 5.1 Logcat `ErrorCollector.java` (`rvsec-android/rvsec-logger-logcat`; the commented `escapeSpecialCharacters` call is `:39`, the dead method `:44-51`): enable escaping (`\n`→`\\n`, keep commas), `null` expecting → sentinel envelope; align the CSV collector's `escape()`; `rvsec-android/rvsec-logger-logcat/pom.xml` gains test-scope junit + surefire-junit4 (the stanza of `rvsec-core/pom.xml:23-29` — the module has no test dependency and no `src/test` today); new `ErrorCollectorTest` (newline, null, comma) targets a package-private `escape(String)`/line builder, not `addError`, because `addError` calls `android.util.Log.v` and the android stub throws `Stub!`
- [x] 5.2 `ViolationRecorder.makeRelevantList` (`rv-monitor-rt`, `:87-105`): a monitoring-runtime frame with `fileName == null` is excluded, not included; new `ViolationRecorderTest`
- [x] 5.3 `rv-coverage` `logcat_parser.py` plus `rv-android-core` `domain/coverage.py` and `rv-coverage` `tracker.py`: `ParserDiagnostics` (13 counters) is defined in `rv-android-core` `domain/coverage.py` next to `LogcatRepository` (`:451`), which gains the `parser_diagnostics` field there — rv-android-core cannot import rv-coverage; `parse_logcat_line` (`logcat_parser.py:214`) takes an optional `diagnostics: ParserDiagnostics | None` and its caller `tracker.py:411` passes the repository's, so the counters are shared with `CoverageTracker` on the live path (specs/analysis); envelope fields on `RvErrorLog` (`code, event, obj, val, exp, msg, truncated`), unescaping `\'`→`'` and `\\n`→`\n` (specs/instrumentation, `\n` restored); sentinels; unclosed quote → truncated; Format-1 regex failure no longer falls into the comma path; `parse_logcat_file` re-raises with the line number; property tests (`,`, `\'`, `\n` split → `continuation_lines`, `:::`, 4068-byte cut) in `test_logcat_parser.py`
- [x] 5.4 `rv-android-core` `domain/log.py` (**lands before 5.3**, with defaults — `code`/`event` = `UNSPECIFIED`, `obj`/`val`/`exp`/`msg` = `""`, `truncated = False` — so the parser's six-positional constructions at `logcat_parser.py:309-316,341-350,366-368` keep working until 5.3 fills them; fields are `str`, per specs/core and specs/analysis): `unique_msg` = seven parts with `code`/`event`; `test_log.py` updated; grep gate `tests/parity/test_gh104_unique_msg_built_once.py` (only `log.py:RvErrorLog.unique_msg` composes the error variant; the gate names `RvDiagnosticEvent.unique_msg` (`log.py:266-270`) as the second, permitted `:::{` composer — the 4-field diagnostic variant; the pattern is the f-string fragment `:::{`, which is what `result_processor.py:631,999,1038` and `regenerate_container.py:244` contain — a grep for the bare `':::'` misses them)
- [x] 5.5 `rv-platform` `result_processor.py`: 13-column `errors.csv` header (`…,source,code,event,message,unique_msg`); delete the three `unique_msg` fallbacks (`:631,:999,:1038`); count write failures (`:654-655,:1046-1047`) into the task result and log as errors; `test_result_processor.py` header + failure tests
- [x] 5.6 `aperv-tool` `analysis/violations.py` + `clock_logcat_join.py`: `ERRORS_CSV_HEADER` 13 columns; `read_errors_csv` already returns `(rows, CsvDiagnostics)` (`violations.py:215`) — extend `CsvDiagnostics` with `unique_msg_unparsed` and `unique_msg_disagrees` (specs/campaign-analysis); the `read_tagged_lines` callers that receive the envelope are `clock_logcat_join.py:489`, `monitored_ops.py:136` and the eight sites in `tests/test_clock_logcat_join_extraction.py`; `unique_msg` seven parts (`code=parts[4]`, `event=parts[5]`); `parse_payload` reads the envelope, `shape_ok=False` + `envelope_truncated`/`envelope_malformed` counters; `clock_logcat_join._parse_payload :454-461` deleted in favour of `violations.parse_payload` (one payload parser); `test_violations.py` (+4, one of them `unique_msg_disagrees`); **same commit as 5.7** (`read_logcat`'s new return shape breaks `step_bundle.py:286` otherwise); update the sha256-pinned fixture manifest only if a fixture changes (it should not — cmp162 stays 11-column and is read by E0's own frozen 11-column reader, never by this module's `read_errors_csv` after this task)
- [x] 5.7 `aperv-tool` `analysis/step_bundle.py`: `:286` already builds the step timeline's `violations[]` through `violations.read_logcat` (so the envelope reaches it once 5.6 lands); adapt that call to the new `(events, LogcatDiagnostics)` return and carry the counters onto the bundle (`BundleDiagnostics :106` has none today), so a step whose lines were discarded is distinguishable from a step that had none (INV-CAN-04, INV-CAN-25/26). This is the delta requirement `Step Bundle Places Every Logcat Stream on the Step Timeline` (`specs/campaign-analysis/spec.md`); the **existing** `tests/test_step_bundle.py` (422 l., 12 tests) gains two cases — the envelope fields present on a bundled violation, and the counters non-`None` on a bundle built from a stream carrying a discarded line
- [x] 5.8 Delete the other `unique_msg` constructors: `scripts/regenerate_results/regenerate_container.py:244` → import from core; fix `scripts/rv_oracle_common.py:73-81` to require seven parts
- [x] 5.9 Consumer matrix `data/gh104/consumer_matrix.md`: every reader of `message`/`unique_msg`/`errors.csv` named with its verdict (migrated / frozen-with-reason): `logcat_parser`, `log.py`, `result_processor`, `violations.py`, `clock_logcat_join.py`, `rv_oracle_common.py`, `regenerate_container.py`, `TraceComparator.parseObserved`, `validator/oracles/cryptoapp-oracle.yaml`, `ase-journal/docs/20260806_owasp_cwe_mapping_gen.py:47-54` (frozen: reads the published dataset), `experimento-gov/scripts/consolidate_gov.py:26-37`, `.claude/skills/rv-experiment-compare/scripts/consolidate_compare.py:35`, `scripts/gh91_compare_consolidation.py`; **and the readers the first inventory missed** (found by `rg` on 2026-08-18): `modules/rv-android-core/src/rv_android_core/domain/coverage.py:397,575,627` (`unique_errors` — the in-pipeline dedupe surface E6 changes), `execution_status.py:87` (a root-level untracked script of rv-android, not `domain/`), `scripts/consolida_comparacao_aperv.py:26`, `experimento-20260721/scripts/consolidate_compare.py:35`, `modules/aperv-tool/src/aperv_tool/analysis/loader.py:19`, `scripts/regenerate_results/regenerate_container.py:246` (writes a **10-column** `errors.csv` without `source` — a fourth layout), `scripts/derive_l3b_oracle.py:126,130` and `scripts/derive_l3c_oracle.py:118,127,221` (callers of `rv_oracle_common`), `scripts/regenerate_results/verify.py:122-142,208`, `scripts/jca557_quarantine_impact.py:104-110`, `scripts/jca557_vs_paper.py`, `experimento-gov/scripts/violations_detail.py:9,22` and `experimento-comp162-ajc/scripts/mop_diff.py:26` (both parse the `RVSEC :` logcat line, i.e. the message format E1/E3 change), the frozen `audit/20260808_validacao_jca_android/**/*.py`, and the tests `modules/rv-platform/tests/execution/test_resume.py`, `modules/rv-coverage/tests/parser/log/*`, `modules/rv-android-core/tests/domain/test_coverage.py`, `test_log.py`, `test_diagnostic_event.py`, `modules/aperv-tool/tests/fixtures/build_cmp162_manifest.py`; and the readers the 2026-08-19 audit added: `scripts/drive_cryptoapp.py:89-94`, `experimento-gh104/scripts/{consolidate.py,gh104_gates.py,msg_diff.py}`, `experimento-cal/tests/test_consolidate_verify.py:57`, and the tests `modules/rv-coverage/tests/**/test_tracker*.py`, `test_analyzer_branches.py`, `modules/rv-android-core/tests/**/test_logcat_manager.py`, `modules/rv-platform/tests/**/test_logcat.py`, `modules/aperv-tool/tests/test_clock_logcat_join*.py`, `test_monitored_ops.py`. The matrix closes with a **shared-runtime impact** section (spec scenario "Shared runtime code the frozen set references is repaired"): the four sites that change runtime the frozen `jca` also executes — 5.1 (collector escaping and the `null`-expecting sentinel), 5.2 (`ViolationRecorder` frame filter → `location`), 3.7 (dispatcher lock framing), 9.2 (`ErrorSummary` identity gains `code`/`event`) — each with its effect on a `jca` run written out (what a `jca` line looks like before and after, and that no `jca` verdict moves). The matrix is closed by a grep-backed check (`grep -rlE "errors\.csv|unique_msg|ERRORS_CSV_HEADER|read_errors_csv|RVSEC\s*:" modules/ scripts/ experimento-*/ audit/` — `RVSEC\s*:`, not `RVSEC :`, because `violations_detail.py`, `mop_diff.py` and `consolidate_gov.py` write `RVSEC   :` / `RVSEC\s+:`; `.venv` and `backup/` excluded; `rg` is not installed) whose every hit that is code has a row; data files (`*.csv`, `*.json`, `*.md`, `*.txt` hits under `experimento-*/` and `audit/`) are listed once as "data, not a reader"
- [x] 5.10 Run `/rv-qa-lint-fix rv-coverage`, `/rv-test-run modules/rv-coverage`, `/rv-test-run modules/rv-android-core`, `/rv-test-run modules/rv-platform`, `/rv-test-run modules/aperv-tool` (which covers `test_violations.py` and the new `test_step_bundle.py` of task 5.7)

## 6. EV — Validation toolkit (`tasks/EV-validation.md`)

- [x] 6.1 `scripts/gh104_gates.py`: reuse `gh101_monitor_transition_check.py`'s parser (both monitor shapes) and add G-2a, G-2b′, G-2c, G-2d, G-6′; `--allowlist data/<set>/gate_allowlist.csv`; JSON report
- [x] 6.2 Correct G-2 before shipping it: an event that reaches `fail` from every state is a defect only if it has no CrySL clause behind it. 17 of the frozen set's 18 orphans are the correct encoding of `CONSTRAINTS`/`REQUIRES`/`FORBIDDEN`, which by definition never enter the `ORDER`; the single real orphan is `MessageDigestSpec:74-76 reset`. Record while implementing it that this orphan is not dormant — it carries no `condition()` either, so its row `{4,4,4,4,4}` accuses on every woven `reset()`; the only other ungated orphans are `PBEKeySpecSpec.f1`/`f2`, which encode a `FORBIDDEN` clause and are meant to. G-2 therefore takes the spec's api30 `.cryptsl` as a second input and reports `orphan-with-clause` (a note) separately from `orphan-without-clause` (a failure). Record two more facts while writing it: every one of the 17 also carries an all-`fail` row, so each firing runs its body report **and** `@fail` (a second, unlabelled `InvalidSequenceOfMethodCalls` plus `__RESET`) — measured, deferred, not a hit; and the mechanical mapping leaves three clause-less orphans on the frozen `jca`, not one (`reset`; `PBEKeySpecSpec.err2`, which tests `RANDOMIZED[password]` where `PBEKeySpec.cryptsl:38-40` requires `randomized[salt]` only; `SecretKeySpecSpec.c3`, which tests an algorithm list `SecretKeySpec.cryptsl` does not declare). On `jca_android` the clearing set is `CONSTRAINTS`/`FORBIDDEN` only — the set encodes no `REQUIRES` (design D-7)
- [x] 6.3 Add gate G-ERE — every symbol named in an `ere`/`fsm` expression has an event declaration. Zero false positives by construction; on the frozen `jca` it reports exactly one hit, `GCMParameterSpecSpec:48` `c2` (declared nowhere because `:23` and `:34` both declare `c1`)
- [x] 6.4 Add gate G-CONF — allow-list conformance: for each of the 21 specifications that carry a value list — the two pure propagators `RandomStringPassword` and `SecretKeySpec` declare none — the `Arrays.asList` allow-list of the `.mop` (and, for the Cipher, the lists of `Api30CipherTransformationUtil` from task 2.8) equals the corresponding `CONSTRAINTS` set of `MetaCrySL/generated/api30/<Rule>.cryptsl` modulo the declared normalisation of task 2.5 — the gate reads the alias rows from `data/jca_android/alias_table.csv`, the auditable registry, never from `ConscryptAliasTable` (the class-versus-CSV equality is the Java test of task 2.5, not this gate's job); every value the successor list carries that the api30 rule omits must have an entry in `data/jca_android/divergence_record.csv` (exactly the `EC` of task 2.6 and the four ECDSA algorithms of task 2.7, nothing else) and every other difference a row in `data/jca_android/conformance_record.csv` (deferred-constant and api30-admits rows), or the gate fails; `ConscryptAliasTable` is never consulted — the CSV is. The gate also asserts that no file under `jca/` names the alias class (INV-INS-112: the frozen set's verdicts must not move). On the frozen `jca` the gate is a report that reproduces `data/jca_android/constraint_table.csv` row for row (task 2.15 — this task waits for it); the pivot's 74/30-14-13-7-7-3 summary is not the oracle
- [x] 6.5 Gate **G-PRED, predicate preservation** (INV-INS-128): every `ExecutionContext` site of the frozen `jca` — `validate`, `setProperty`, `remove`, `setObjectAsInAcceptingState`/`unsetObjectAsInAcceptingState`, the `Property` import — MUST be present in the set at the same event and unrewritten; the gate fails on a missing or altered site and reports the file, the event and the kind. It was first written to assert the *absence* of those sites; inverting it is what this task does, and the inversion is the gate that would have caught the withdrawn removal of D-11 going in. On `jca` it is trivially green; on `jca_android` it is green only while the seed's 134 lines survive
- [x] 6.6 `tests/parity/test_gh104_structural_gates.py`: parametrised over `jca` (frozen control monitor under `results/gh101_group8_jca_frozen_control/monitors/` — every generated-monitor line number this task cites is relative to that fixture — expected G-2 18 hits in 10 specs of which 15 `orphan-with-clause` and 3 `orphan-without-clause` under the mechanical mapping (`reset`, `PBEKeySpecSpec.err2`, `SecretKeySpecSpec.c3`; 17/1 only through `gate_allowlist.csv` rows, which the test must name), G-2a 1 `SecretKeySpec.e1`, G-2b′ 8, G-2c 1, G-2d 2, G-6′ 1 `GCMParameterSpecSpec`, G-ERE 1, G-PRED 134) with `data/jca/gate_allowlist.csv` naming them, and over `jca_android` (generated in scratch, `RVSEC_HOME`); the `jca` case skips with a named reason (`pytest.skip("control directory absent: <path>; regenerate per data/gh104/README.md")`) when the control is absent and fails when its manifest disagrees. The `jca` control numbers for G-2a/G-2b′/G-2d shift once `SecretKeySpec` and `RandomStringPassword` leave the successor set — they stay as written for `jca`, which keeps both files; only the `jca_android` expectations drop them
- [x] 6.7 `.mop` lint `scripts/gh104_mop_lint.py` + test: undeclared ERE/FSM symbol, duplicate event name (`GCMParameterSpecSpec:23,34`), unbalanced parentheses (`SecretKeySpecSpec:27-30`), three-argument `ErrorDescription` (INV-INS-119), hand-written event-name bookkeeping (forbidden by INV-INS-120 — the generator emits the name), reserved generator names in declarations; on the frozen `jca` the lint must report exactly the known hits and pass on `jca_android` only after Group 7
- [x] 6.8 Message-property gate `scripts/gh104_message_gate.py` + test: numeric literals of a message equal those of its guard (INV-INS-121), `codes.csv` bijective with sites, `ErrorType` matches the site kind, and a **self-contradicting envelope** — a site whose `val` can be a member of its own `exp` (guard on a monitor field, value from the object's getter: the nine guard-on-field sites task 7.4 declares) — is flagged, not passed; on the frozen `jca` it must list `PBEKeySpecSpec:50` and `PBEParameterSpecSpec:50` (`1000` vs guard `10000`), the 25 three-argument sites, and the three wrong `ErrorType` values of D-13
- [x] 6.9 Differential harness: `rvsec-mop/src/test/java/.../TraceRunner.java` plus its self-test `TraceRunnerTest.java` (loads a generated `MultiSpec_1RuntimeMonitor` class set, replays a trace file of API-call lines — `<Class>.<method>(<args>)` with an optional `-> <object>` binding — by resolving each line against the snapshot's generated **static event dispatchers** (`MultiSpec_1RuntimeMonitor.<Spec>_<event>Event(...)`), not against `mop/MonitorWrappers.java`, so that `before` advices (e.g. `MessageDigestSpec.reset`) and inline-woven events are replayable and two snapshots with different event alphabets replay one trace — records accusation/event/envelope; task 8.3's mandatory harness evidence depends on this; `rvsec-mop/pom.xml` has no dependency today, so the test scope gains junit, rvsec-core, rv-monitor-rt and the compiled control monitor on the test classpath), `scripts/gh104_diff_harness.py` (generate two snapshots in scratch, run, classify `moved`/`removed`/`unchanged`/`introduced`; its report flags every envelope whose `val` is a member of its `exp` as self-contradicting, beside the classification), `data/gh104/traces/<spec>.txt` for the 23 specifications of the successor set, including one sequence per value the allow-list transcription newly admits (specs/instrumentation harness requirement)
- [x] 6.10 Harness self-test (runs `TraceRunnerTest`): `jca` against a **synthetic mutant of `jca`** the test writes into scratch — one mutation per verdict the classifier must produce, so all four are exercised: `moved` (relocate `TrustManagerFactorySpec`'s accusation by moving the `condition()` from one event to the next), `removed` (delete one `new ErrorDescription(` site), `introduced` (add one), `unchanged` (a file mutated nowhere). Why a mutant and not the archived `jca_android_bug_predicate`: an authored difference is exactly known, reproducible, and covers every verdict, whereas the archived set supplies one accidental difference and drags a reproved specification set into the critical path of a test. The mutant is never committed and the self-test therefore depends on no other specification set and never waits on Group 2; commit the report under `evidence/harness/selftest.md`
- [x] 6.11 Run `/rv-test-run tests/parity`

## 7. E1 — Legible messages in `jca_android` (`tasks/E1-messages.md`) — after the between-waves 3.9/3.5/3.8 step and Group 2 (2.14 included), with Group 6 tasks 6.1-6.5, 6.7-6.9 landed; two subagents by file halves; accusation-neutral (the three behavioural items that used to sit here are E4 tasks 8.14-8.16)

- [x] 7.1 `rvsec-core` `ErrorType.java` (six values today, `:4-9`) gains `ForbiddenMethod` and only that — `RequiredPredicate` does not enter: the predicate sites report through the types they already use, and adding a type would change what is accused; Java test; KIND `FORB` enters the `codes.csv` vocabulary of design D-3
- [x] 7.2 Recount the report sites of `jca_android` before editing anything and write the count into `data/jca_android/README.md` (the group file quotes it). The seed's 50 live sites (25 three-argument + 25 four-argument; the 51st `new ErrorDescription(` of `jca` is the commented `g4` report at `MessageDigestSpec:57-58`, which stays commented through this group and is E4 task 8.14) **survive Group 2 intact**: the predicates are carried over (D-11), so the five purely predicate-guarded sites (`IvParameterSpec` c3/c4, `PBEKeySpecSpec` err2/err3, `SecureRandomSpec` setSeed3) keep their events and their reports, and `SecretKeySpecSpec.c3` keeps its predicate half while task 2.4 removes only its algorithm list — `generated/api30/SecretKeySpec.cryptsl` declares nothing about `alg` — so the site stays. That points at **50**, not the 45 an earlier revision computed from the removal; either way the number is **A RECALCULAR na execução**, from the files, not from this line
- [x] 7.3 Lying-message census applied first (precondition, INV-INS-121): `PBEKeySpecSpec:50` and `PBEParameterSpecSpec:50` (`1000`→`10000`, api30 declares `iterationCount >= 10000`); wrong `ErrorType` per D-13 — `PBEKeySpecSpec:24,30` `InvalidSequenceOfMethodCalls`→`ForbiddenMethod` (they encode api30 `PBEKeySpec` `FORBIDDEN: PBEKeySpec(char[])`, `PBEKeySpec(char[], byte[], int)`) and `PBEParameterSpecSpec:49` `UnsafeAlgorithm`→`UnsatisfiedConstraint`; `MessageDigestSpec:70,92` list = the transcribed allow-list; `CipherSpec:61,76` drop the literal `...` and name the new Java utility of task 2.8; `KeyGeneratorSpec:64` and `KeyStoreSpec:68` (missing space); `MacSpec:62` (missing verb); `SecretKeySpecSpec:49` (array, not `.length`) and `:49,56` leading spaces — **confirm absent** rather than edit: `c3` and its site leave in Group 2 (task 2.3/2.4); leading spaces at `MacSpec:50`, `KeyManagerFactorySpec:55`, `KeyPairGeneratorSpec:72`; `SecureRandomSpec:82` `String.join(" or ", …)` → `", "`; note `KeyPairGeneratorSpec:71-72` unreachable (Group 8 item)
- [x] 7.4 Field → target getter at every `but found` site that interpolates a monitor field (16 in the seed: `CipherSpec:61,76`; `KeyGeneratorSpec:64`; `KeyManagerFactorySpec:55`; `KeyPairGeneratorSpec:72`; `KeyStoreSpec:68`; `MacSpec:50,62`; `MessageDigestSpec:70,92`; `SignatureSpec:58,68,78,88`; `SSLContextSpec:58`; `TrustManagerFactorySpec:57`) — re-verify the list against the post-Group-2 files. None of the 16 reporting events binds the algorithm/type/protocol *argument* (only the `getInstance` events do — `MessageDigestSpec:57-58`'s argument form lives in `g4` and does not transfer), but every one binds the **target object**, so interpolate its getter: `c.getAlgorithm()` (Cipher), `k.getAlgorithm()` (KeyGenerator, KeyPairGenerator), `k.getType()` (KeyStore), `m.getAlgorithm()` (Mac), `digest.getAlgorithm()`, `s.getAlgorithm()` (Signature), `ctx.getProtocol()`, `mf.getAlgorithm()`/`k.getAlgorithm()` (the two factories) — all `public` on android-30 (`javap`); this also removes the mechanism behind the 8,843 empty labels (a field still `""` when `getInstance` was unobserved). `MessageDigestSpec:57-58` stays commented — reviving that `g4` report adds accusations and is E4 task 8.14. **Declare the guard-on-field case, do not repair it**: at the nine sites whose guard tests the monitor field while the message now prints the getter (`MessageDigestSpec:68`, `CipherSpec:59`, `SignatureSpec:56`, `SSLContextSpec:56`, `TrustManagerFactorySpec:55`, `KeyManagerFactorySpec`, `MacSpec`, `KeyStoreSpec`, `KeyGeneratorSpec`) an object with no observed `getInstance` (the 156 `MessageDigestSpec` `.` rows — Guava `clone()`, kmp-tor) makes the guard fire on `""` while the envelope prints `val='SHA-256' exp='…SHA-256…'`; write one row per site in `data/jca_android/conformance_record.csv` and one harness trace per site with no observed `getInstance` under `data/gh104/traces/`, so the message gate (6.8) and the harness report (6.9) flag the self-contradicting envelope; moving the guard to the bound argument/getter is behavioural and is E4 task 8.16
- [x] 7.5 Envelopes: every four-argument value site rewritten to `v=1 code=… ev=__EVENTNAME obj=… val='…' exp='…' msg='…'` (`val` from the bound argument, capped at 512 chars); every three-argument site gains the fourth argument, composing `ev=` from `__EVENTNAME` before `__RESET` where a `__RESET` already exists (`KeyPairGeneratorSpec`'s `@fail` has none and gains none here — adding it is E4 task 8.15); no bookkeeping field and no bookkeeping statement anywhere (INV-INS-120 forbids it — Group 3 emits the name); `codes.csv` filled, one row per site, KIND per design D-3; the `<SPEC>` token of `code=` is the full specification name without the `Spec` suffix in upper case (`MESSAGEDIGEST`, `TRUSTMANAGERFACTORY`, `GCMPARAMETERSPEC`), never an ad-hoc abbreviation, so 7.a and 7.b assign codes that cannot collide (design D-3 table)
- [x] 7.6 Divergence record: one entry per hunk (`scripts/gh104_divergence_record.py --check` green); lint, message gate and G-CONF/G-PRED green on `jca_android`; G-ERE still reports `GCMParameterSpecSpec` (Group 8 fixes it) — allowlisted with reason until 8.1
- [x] 7.7 Generate `jca_android` monitors in scratch and confirm compilation and that no `__EVENTNAME` survives; harness before/after on every file (the **post-Group-2 snapshot** vs E1 — the byte-identical seed would not classify `unchanged`, Group 2 already moved verdicts) → `evidence/harness/e1-<spec>.md`; classification must be `unchanged` for accusation, changed only in envelope and observed value
- [x] 7.8 Run `/rv-test-run tests/parity`

## 8. E4 — Six structural repairs, two measured sites, one withdrawal, three measured items inherited from E1 (8.14-8.16) and the inherited-divergence record, in `jca_android` (`tasks/E4-automata.md`) — after Groups 6 and 7; per file

Every diagnosis below was re-confirmed on 2026-08-18 against the api30 rule, `javap` on `android-30`, the frozen-control monitor and the 97,018-row published corpus. Line numbers are the frozen `jca` seed's and have moved after Groups 2 and 7 — locate by symbol and record the number actually edited.

- [x] 8.1 `GCMParameterSpecSpec`: the second `event c1` (`:34`) is renamed `c2`, which is what `ere : c1 | c2` (`:48`) has always referenced and what api30 `GCMParameterSpec:17,19,21` declares; G-ERE and G-6′ go to zero on this file. Record in the divergence entry that the behavioural effect is nil and why — the monitor is indexed by the constructed object, so each monitor sees at most one event and the `fail` state is unreachable before and after (`MultiSpec_1RuntimeMonitor.java:4232,4284,4301`); the spec accuses nothing today and accuses nothing after, and the corpus has 0 rows for it
- [x] 8.2 `SecretKeySpecSpec:27-30`: **check before editing** — the surplus `)` is real in the text but inert in effect, because JavaMOP captures the pointcut as raw text (`javamop.jj:1440`) and parses it without requiring `<EOF>` (`aspectj.jj:49`), so the token is dropped in silence and the generated guard is already the intended one (`MultiSpec_1RuntimeMonitor.java:7726`). Group 2 tasks 2.3 and 2.4 remove both terms of that `condition` (api30 `SecretKeySpec:29` constrains only `length(keyMaterial) >= off + len`, nothing about `alg`), so the `condition` and its parenthesis may no longer exist. If the text is gone, close the task with that finding recorded and no edit; if it survives, remove the `)` and the lint's unbalanced-parentheses check goes clean
- [x] 8.3 `MessageDigestSpec:74-76`: delete the `reset` event. It is the only true `orphan-without-clause` G-2 reports on the frozen set (api30 `MessageDigest` names it in no `EVENTS`/`ORDER`/`CONSTRAINTS`/`ENSURES`, and the rule has no `REQUIRES` or `NEGATES`) **and** the only one of the three ungated orphans that no `FORBIDDEN` clause justifies. It is not dead: its generated row is `{4,4,4,4,4}` against a `fail` of 4 (`:6369`, `:6504`), so every woven `reset()` accuses unconditionally, and because `__RESET` returns the monitor to state 0 with its fields retained — where every `update`/`digest` row is `fail` — every later `update`/`digest` on that object accuses again until a new `getInstance` is observed (unbounded in a loop); a bare `reset()` on an object with no observed `getInstance` creates its monitor and accuses too. `reset` is a `before` advice: on the DEX path it is woven inline (no wrapper in `MonitorWrappers.java` by construction, `WrapperEmitter.shouldWrap` admits `after` only), and the call site is present in the frozen control's instrumented APKs, so the accusation exists on both weaving paths. **This is the one repair of the group that removes accusations** — harness evidence is mandatory and its classification is `removed`, never `unchanged`. Magnitude is not derivable from the published corpus (`errors.csv` carries no event id; `class`/`method` are the call site): record the identifiable subset with no competing explanation — 100 rows over 7 `MessageDigestSpec` call sites that raise `InvalidSequenceOfMethodCalls` and no `UnsafeAlgorithm`, inside a universe of 10,135 — as a floor of evidence, not a ceiling (a `reset()` accusation can also land at one of the 28 sites that carry `UnsafeAlgorithm` rows, 10,035 rows), and note that the archived `jca_android_bug_predicate` had already made this removal
- [x] 8.4 `KeyPairGeneratorSpec:26`: `String algorithm` is declared without an initialiser. Correct the lineage's diagnosis in the divergence entry: the field is written by `g1`/`g2`/`g3` (`:44,:52,:59` — the `getInstance` events), **not** by the `init*` events, and the first read is `:29`'s `switch` inside `validate()`, not `:70`. A `null` there raises `NullPointerException`, it does not produce a false accusation, and `:70` is unreachable anyway (task 8.6). **Guard the three conditions, not `validate()`**: `init1` and `init2` become `condition(algorithm != null && validate(keySize))` and `initError` becomes `condition(algorithm != null && !validate(keySize))`, so a `null` field makes no event fire, nothing raise and nothing report — the same trace as today minus the exception. A guard inside `validate()` alone (or initialising to `""`) is **not** neutral: `initError`'s condition is `!validate(keySize)`, so it would fire from state 0 into its all-`fail` row `{4,4,4,4,4}` (`MultiSpec_1RuntimeMonitor.java:5266`) and produce `InvalidKeySize "invalid key size for algorithm null."` plus an `InvalidSequenceOfMethodCalls` on every such call. Record the second null door in the divergence entry: `getInstance(String, String)` with an algorithm outside the list (`g2` returns before writing the field). The exception's real damage is the dispatcher lock, which INV-INS-129 and tasks 3.6-3.8 handle
- [x] 8.5 Dead pointcuts, **repair the two where the automaton is broken**: `SignatureSpec:99` and `:106` declare `sign()` returning `byte`; `javap` on `android-30` gives `public final byte[] sign()` and `public final int sign(byte[], int, int)`. `s1`/`s2` are the only transition into the accepting state of the signing branch (`Prop_1_transition_s1 = {8,8,8,8,8,8,6,6,8}`), so with them dead the monitor stalls in state 7 and a second `initSign`/`initVerify` on the same object accuses falsely. Fix `:99` to `byte[]` and `:106` to `int` (`s2` has no `returning` clause and writes its predicate from the argument, so the return type does not move its payload). Harness before/after must name `introduced` for every accusation a real call now produces (`sign()` with no prior `getInstance`/`update`: `s1[0] = s1[1] = 8`) **and** `removed` for the false one it takes away (a second `initSign` after `sign`: `i1[7] = i2[7] = 8` today, `i1[6] = i2[6] = 1` once `s1`/`s2` fire — rows at `MultiSpec_1RuntimeMonitor.java:8375-8376`, `s1`/`s2` at `:8380-8381`) — both classes, per trace; record that the corpus has 0 rows attributable to either site and that all 990 `SignatureSpec` rows are the verification branch
- [x] 8.6 `KeyPairGeneratorSpec:71-72`: **remove** the unreachable branch. The proof is a set equality, not a guess: `condition(validate(keySize))` compiles to `if (!(validate(keySize))) return false;` ahead of the body (`MultiSpec_1RuntimeMonitor.java:5364-5366`; the dead branch is `:5368`), and `validate` (`:29-36`) returns `true` exactly for the members of `safeAlgorithms` (`:22`), so `!safeAlgorithms.contains(algorithm)` is false whenever it is evaluated. Corroborated in the corpus: 0 of the 16 `KeyPairGeneratorSpec` rows are `UnsafeAlgorithm`, against 15,444 `UnsafeAlgorithm` rows in five other specifications (`TrustManagerFactorySpec` 9,014, `MessageDigestSpec` 6,048, `SignatureSpec` 242, `CipherSpec` 109, `MacSpec` 31). Drop the site's `codes.csv` row. Record that api30 `KeyPairGenerator:45` does support the accusation and that `g3` (`:55-60`) is the event that could host it — moving it there is behavioural and is **not** done here
- [x] 8.7 Two further dead pointcuts are **measured, not repaired** (researcher decision, 2026-08-18): `SSLContextSpec:64` declares `createSSLEngine` returning `void` where the API returns `SSLEngine`, and `TrustManagerFactorySpec:63` declares `getTrustManagers` returning `KeyManager[]` where the API returns `TrustManager[]` (its `returning(TrustManager[][])` clause is a double array). Reviving either only adds accusations and the corpus cannot size that: `engine` is a `1→1` loop and acceptance already comes from `init` (api30 `SSLContext:39` makes `Engine?` optional), while `gtm1` sits in the `fsm` with `{3,0,3,3}` on the specification that carries 18,029 of the corpus's rows. Add both to `data/jca_android/conformance_record.csv` with the `javap` signature and the transition row, run the harness on each to record what reviving them would introduce, and change no `.mop` text. `TrustManagerFactorySpec:63` **is** in the lineage's catalogue — `data/gh101/divergence_record.csv:101` (hunk `038a616babd0`) lists its four defects, and `:105-106` the `g3` binding — so the entry cites that row and names the two defects a revival would also have to fix: `gtm1` and `g3` bind `k` where the specification declares `mf`, which sends both to the empty parameter slice (`TrustManagerFactorySpec__Map`, `MultiSpec_1RuntimeMonitor.java:16336,16464`, against `TrustManagerFactorySpec_mf_Map` for `g1`/`g2`/`init`) and broadcasts them to every live monitor, and the `returning(TrustManager[][])` at `:62` is a double array. Say in the entry that a harness run on the revived pointcut alone measures that broadcast, not the per-object row `{3,0,3,3}`
- [x] 8.8 **Withdrawn, recorded not repaired**: `KeyGeneratorSpec:47` and `MessageDigestSpec:55` test `contains(currentAlgorithmInstance)` rather than the bound argument, and this is not a defect. `g1` is emitted into the same wrapper ahead of its sibling (`monitors/mop/MonitorWrappers.java:192-193`, `:357-358`), both resolve the same object-indexed monitor, and `g1`'s body writes the field before the sibling's `condition` runs; the field is initialised to `""` and never `null` (`:4749`, `:6362`), so the sibling fires exactly when `!contains(alg)`. Enter it in the conformance record as a checked non-defect with the fragility named — the equivalence depends on declaration order — and make no edit
- [x] 8.9 Per repair: one divergence-record entry naming the CrySL clause, the JDK signature or the generated-monitor line that proves it, one harness run before/after with the classification written (`unchanged` / `moved` / `removed` / `introduced`), and the `codes.csv` bijection preserved
- [x] 8.10 Gates green on `jca_android`: G-6′ = 0; G-ERE = 0 (with the temporary allowlist row of task 7.6 removed); G-CONF and G-PRED green; lint clean; `CipherSpec` still generates (event count ≤ 17, INV-INS-115 — time and memory recorded); and `gate_allowlist.csv` carries **every** remaining hit with a reason, not only the G-2b′/G-2d ones. G-2 `orphan-without-clause` reaches **2, not 0**, and the reason is a conflict between two decisions of this change rather than an unfinished repair: D-7 counted three clause-less orphans and said all three would be absent from the successor by construction, because the predicate machinery was to be removed; **D-11, the later decision, withdrew that removal**, so `PBEKeySpecSpec.err2` and `SecretKeySpecSpec.c3` are carried over with the predicates they guard on. Repairing either removes accusations on a predicate the api30 rule does not state, which is the class of item task 8.12 records rather than repairs and which E4's criterion excludes, so both are allowlisted with that reason. The same ordering leaves G-2a 1, one of the eight G-2b′ hits and both G-2d hits alive on `SecretKeySpec.mop` and `RandomStringPassword.mop`, which D-7 measured as having no successor and D-11 keeps. `MessageDigestSpec.reset`, the third clause-less orphan, is deleted by task 8.3
- [x] 8.11 Conformance record `data/jca_android/conformance_record.csv` complete: one row per specification against its api30 rule (single oracle, design D-10), cross-checked against the two `api30-omits` entries of `data/jca_android/divergence_record.csv` — the `EC` of task 2.6 and the four ECDSA algorithms of task 2.7, which live there and not in this file — the declared cost of the `api30-admits` rule recorded by task 2.4 (5,892 `MessageDigestSpec` rows), the two measured-not-repaired pointcuts of 8.7, the withdrawn item of 8.8, the nine measured-not-repaired rows of 8.12, and the guard-on-field rows E1 task 7.4 declared, each closed by 8.16's verdict (repaired, or reverted with the reason)
- [x] 8.12 Record the inherited structural divergences as **measured, not repaired** rows of `data/jca_android/conformance_record.csv`, one per item, each with the monitor line, the api30 `ORDER`/binding it departs from and its corpus mass, no `.mop` text changed (researcher decision, 2026-08-18): (a) `KeyStoreSpec` declares `ks` and binds `k` in every event → one process-wide monitor (`KeyStoreSpec__Map`, `MultiSpec_1RuntimeMonitor.java:9082`; `g1 = {4,4,5,5,5,5}`, `:5805`; 8,655 sequence rows / 22 apps); (b) `CipherInputStreamSpec`/`CipherOutputStreamSpec` declare no parameter (`:9043,9045`; `c1 = {3,4,4,4,4}` / `{2,4,4,4,4}`; 0 rows); (c) empty-binding broadcasts — `MacSpec.f2` (`target(m)`, `m` unbound, `:12803`), `PBEKeySpecSpec.f1/f2` (`:13303,13335`), `SSLContextSpec.unsafe_protocol` (`:15920`), `TrustManagerFactorySpec.g3/gtm1` (`:16336,16464`); (d) `SecureRandomSpec` `end` omits `next2` (`{3,3,1,3}`, `:7909`) against api30 `Ins, Seeds?, Ends*`, and `next1`/`next3`/`ints` have no rule event (12,400 rows / 43 apps, all sequence); (e) `CipherSpec` `s3` has no `u1..u5` loop and `s2`/`end` no `i1`/`i2` (`u1[1] = 5`, `:3593`) against `Inits+, w+ | (FINWOU | (updates+, DOFINALS))+` (10,814 rows / 21 apps); (f) `KeyPairSpec` `ere: c1 (gpu | gpr)*` makes the constructor mandatory (`gpu = gpr = {2,1,2}`, `:5623`) against `co?, (pu*, pr*)*` (668 rows / 8 apps; `TLSClientHandshakeKt.generateECKeys` 430, `CryptoUtil.generateKeyPair` 218); (g) `getInstance(String, Provider)` bound in none of `KeyStoreSpec`, `SignatureSpec`, `MacSpec`, `KeyPairGeneratorSpec`, `SSLContextSpec` (`javap` shows the overload on all five; `KeyStore` also has `getInstance(String, String)` on android-30 — it is the `.mop` that lacks the two-argument form, not the platform), so the next event starts from state 0 into `fail`; (h) every clause-encoding orphan (the 17 of D-7) fires its body report **and** its all-`fail` row → a second `InvalidSequenceOfMethodCalls` and `__RESET` per hit (`SSLContextSpec` 17,510 vs 2 × 8,802; `TrustManagerFactorySpec` 9,015 vs 9,014); (i) on the DEX path the one-argument `getInstance` wrapper fires `g1`+`g2` (+`g3`/`g4`) — `MonitorWrappers.java:538-544` (TMF), `:429-434` (SecureRandom) — and `g2` from the post-`g1` state is `fail`, so every safe `TrustManagerFactory.getInstance("PKIX")`/`SecureRandom.getInstance("SHA1PRNG")` accuses and the following `init`/`nextBytes` accuses again (comp162: TMF 2,855 sequence vs 61 `UnsafeAlgorithm`, SecureRandom 2,882); AspectJ's `args(alg, *)` requires two arguments and does not do this. Then state in the E4 record what the sweep covered — the return types of all 141 `call(` pointcuts (137 lines; four lines carry two pointcuts and `PBEKeySpecSpec:22,28` write `call (`) of the 21-file seed against `android-30` — and that (a)-(i) are the divergences it did not cover
- [x] 8.13 Run `/rv-test-run tests/parity`
- [x] 8.14 Revive the `g4` report of `MessageDigestSpec` (`:57-58` of the seed, commented in `jca`; `g4` binds `alg`, so the envelope's `val` is the argument): un-comment it with the envelope of design D-3 and a `codes.csv` row; harness before/after with expected class `introduced` — every `getInstance(String)` outside the transcribed list now reports at the call — and record in the divergence entry the DEX-path double fire this revives (`g1`+`g4` on the one-argument wrapper, the same mechanism as 8.12(i)); it is behavioural, which is why it is measured here and not done in E1
- [x] 8.15 `__RESET` in the `@fail` handler of `KeyPairGeneratorSpec` (`:110-111` of the seed has none): today the monitor stays in `fail` and reports on every later event; with `__RESET` it returns to state 0 and a following `g1` reports nothing — it **removes** accusations, so the harness class is `removed`, never `unchanged`; divergence entry with the generated rows cited
- [x] 8.16 Guard on the bound argument/getter at the nine guard-on-field sites E1 task 7.4 declared (`MessageDigestSpec:68`, `CipherSpec:59`, `SignatureSpec:56`, `SSLContextSpec:56`, `TrustManagerFactorySpec:55`, `KeyManagerFactorySpec`, `MacSpec`, `KeyStoreSpec`, `KeyGeneratorSpec` — seed lines; locate by symbol): per site, harness before/after over the E1 traces with no observed `getInstance`; the only admissible class is `removed` and only for those traces (the self-contradicting envelope of `val ∈ exp` disappears); any other class on any trace → revert that site and record why in the conformance record; the message gate's self-contradiction check (6.8) must go to zero on every site that stays repaired

## 9. E6 — Identity (`tasks/E6-identity.md`) — after Groups 1 (the frozen 11-column reader of `scripts/gh104_baseline.py`), 5 and 7, with the harness output of 6.9 as the deciding corpus

- [x] 9.1 `scripts/gh104_identity_discontinuity.py`: recompute comp162 identities with `event` (from the envelope where present; sentinel otherwise) next to the 6,344 distinct 8-tuples `(apk, rep, tool, spec, class, method, source, message)`; record both in `data/gh104/identity_discontinuity.md`; comp162 predates E1 so its delta is zero by construction — the deciding corpus is the harness output of Group 6 (task 6.9, which exists before this group runs); task 10.4's device logcat reconfirms it afterwards and 10.5 records it; if the delta is zero on a corpus that does carry `ev=`, stop and re-open design D-5
- [x] 9.2 `rvsec-core` `ErrorSummary.equals/hashCode` += `code`, `event`; `ErrorDescription` carries them; `ErrorDescriptionTest.hashCodeMatchesEquals` rewritten for seven fields; logcat collector emits them in the envelope (already) — no line-format change
- [x] 9.3 Confirm `errors.csv` `code`/`event` columns and `unique_msg` parts (Group 5) are fed from the collector line end to end with a recorded logcat fixture; declare the era in `data/gh104/identity_discontinuity.md`
- [x] 9.4 Rebuild `lib/` jars; run `/rv-test-run modules/rv-coverage` and `/rv-test-run modules/aperv-tool`

## 10. Integration and verification (`tasks/E10-integration.md`)

- [ ] 10.0 (**before 10.4**) Static analysis reads the selected set: `get_static_analysis_config()` (`modules/rv-experiment/src/rv_experiment/config.py:942-951`) passes `mop_dir` = the directory `get_monitored_operations_config()` resolves for `specification_set` (`config.py:685-700`, the same `mop_specs_dir` — one resolution, one mapping), instead of leaving `RVStaticAnalysisConfig` to its literal default `resources/jca` (`modules/rv-static-analysis/src/rv_static_analysis/config.py:198-207`; audit G12 FAIL, `juizglobal_relatorio.md:234`). Without it every `jca_android` run's static view — monitored-operation targets, GATOR reachability, the coverage denominator — is computed against the 23 `jca` specs while the APK is instrumented with the 21 of the successor. Add `test_static_analysis_config_uses_selected_set` (four cases below) beside `test_jca_android_spec_set_resolves_paths` (`tests/test_config_jit.py`): for `jca_android` the `RVStaticAnalysisConfig.mop_dir` ends in `resources/jca_android`; for `jca` in `resources/jca`; for `custom` it is `custom_specs_dir`; `targets_file`, when set, still wins (INV-ANA-33 mutex). Task 10.4 then records in `evidence/device_validation.md` which directory the static analysis used
- [ ] 10.1 Freeze and divergence: existing gh101 five gates green; `test_gh104_specset_gates.py` and `test_gh104_structural_gates.py` green on `jca` and `jca_android`; `jca/` and `CipherTransformationUtil.java` byte-identical to `7e7acb69`; `AndroidCipherTransformationUtil.java` and the archived `jca_android_bug_predicate/` byte-identical to **`pre-rename-head`** (the class does not exist at `7e7acb69` — gh101 added it) — git records the move as `R100` with zero content hunks **against the `pre-rename-head` SHA task 2.1 froze**, never against `7e7acb69`, which predates gh101's edits to that directory and would show 19 files and 720 insertions no matter what this change does; `MetaCrySL/` untouched (`git status` clean in that tree)
- [ ] 10.2 Run `/rv-qa-lint-fix rv-coverage`, `/rv-qa-lint-fix rv-platform`, `/rv-qa-lint-fix aperv-tool`, `/rv-qa-lint-fix rv-android-core`, `/rv-qa-lint-fix rv-experiment`
- [ ] 10.3 Run `/rv-verify rv-coverage`, `/rv-verify rv-platform`, `/rv-verify aperv-tool`, `/rv-verify rv-android-core`, `/rv-verify rv-experiment`, `/rv-verify rv-instrumentation-dexlib2`
- [ ] 10.4 Device validation, one task, via `uv run rv-experiment run --tools monkey --specification-set jca_android --timeouts 180 --apks-dir <dir with com.owncloud.android_48000100, eu.opencloud.android_9, de.luhmer.owncloudnewsreader_196, com.etesync.syncadapter_20700>`: record `unknown` = 0, `but found .` = 0, envelope fields populated, `advicesExcludedByArity` in results JSON, parser counters, in `evidence/device_validation.md` (the platform manages the emulator; no manual emulator command)
- [ ] 10.5 Read the device evidence against the pivot's tier: the `TrustManagerFactorySpec` `X509` line, the `KeyStoreSpec` `AndroidKeyStore` line and the `SSLContextSpec` `TLS` line must no longer be reported as violations — under D-15 that means the expert allow-lists plus the `platform-value` layer of task 11.4 plus the normalisation rule of task 2.5 — record the shape of each, not counts (Monkey is stochastic). Runs only after Group 11
- [ ] 10.6 Invoke `/rv-code-reviewer` via Skill tool
- [ ] 10.7 Run `/rv-docs-sync rv-coverage`, `/rv-docs-sync rv-instrumentation-dexlib2`, `/rv-docs-sync aperv-tool` if module docs need updating; update `openspec/specs/experiment/spec.md:87` sample comment at sync time
- [ ] 10.8 Sync of invariants and data contracts (at sync/archive time): apply the `## Invariants` sections of the six deltas and the platform delta's Data Contracts (13-column `errors.csv`) to the upstream specs by hand — the openspec-sync-specs skill processes only ADDED/MODIFIED/REMOVED/RENAMED Requirements headers — then verify by grep that all 19 IDs (INV-INS-118…129 = 12, INV-ANA-62/63, INV-CORE-56/57, INV-CAN-25/26, INV-PLT-32) exist under `openspec/specs/`, that the restated INV-PLT-19 / INV-CORE-25 / INV-CORE-41 / INV-EXP-03 / INV-INS-09 / INV-ANA-08 / INV-CAN-04 carry the new text, that the upstream requirements "Derivation Provenance of the Android Specification Set" and "Cipher Transformation Tables of the Derived Set" are GONE (the delta removes them) and that their replacements "Provenance of the Archived Derived Specification Set" and "Cipher Transformation Tables of the Archived Derived Set" are present and scoped to `jca_android_bug_predicate`, and that INV-INS-113 is scoped to the archived set; the `openspec/specs/experiment/spec.md:87` sample comment of 10.7 is checked in the same pass

## 11. E11 — Re-anchor the value oracle to the expert-validated CrySL rules (D-15) — after Group 8, before tasks 10.4/10.5; one subagent per sub-block of 11.2, the rest sequential

<!-- The audit (`docs/20260824_auditoria_specs_jca_android.md`) measured that the api30 `.ref`
     tiers were derived from provider registries, so every refined list is an availability
     catalogue and a faithful transcription cannot accuse an insecure algorithm the platform
     ships. This group withdraws that anchor for value clauses only. ORDER, event alphabets
     and the gh105 predicate wiring keep theirs — do not touch them here.
     Method: the list the successor carries is the list the frozen `jca` carries, checked
     entry by entry against the pinned expert clause. Do NOT re-transcribe from the rule
     text; the frozen list IS the expert transcription and it is what the published numbers
     answered to. Spelling variants the frozen set hand-wrote stay, each with a
     `spelling-variant` note. Read design D-15 in full before the first edit. -->

- [x] 11.1 Freeze the oracle: `sha256sum` of the 49 `.crysl` of `RVSec-replication-package/tools/rules/` recorded as a freeze item in `data/jca_android/README.md`, with the provenance line the audit measured — the upstream checkout (`Crypto-API-Rules`, `master` at `6d844ab`, 2022-05-12) and `rvsec-cognicrypt/CrySL-Rules` are byte-identical; the replication copy differs in exactly one value, `"CCM"` added to the AES modes of `Cipher.crysl:97,113`, which is a **local RVSec addition** (`oracle-wart` row) — and the reason the copy is pinned rather than tracked: the current upstream `master` (checked 2026-08-24) still has no `CCM` and has meanwhile **dropped `CBC`/`PCBC` from the AES modes**, so re-anchoring on a live branch would accuse `AES/CBC/PKCS5Padding`, the corpus's most common transformation, on nobody's decision
- [x] 11.2 Re-transcribe the value lists, one sub-block per specification, each hunk carrying a `divergence_record.csv` row and a `conformance_record.csv` line naming the expert file and clause. The measured before → after, in `$RVSEC_HOME/rvsec/rvsec-mop/src/main/resources/jca_android/`:
  - **`MessageDigestSpec.mop:28`** `{MD5, SHA-224, SHA-256, SHA-1, SHA-512, SHA-384}` → the frozen list `{SHA-256, SHA-384, SHA-512, SHA256, SHA384, SHA512}` (`MessageDigest.crysl:algorithm in {"SHA-256","SHA-384","SHA-512"}`; the three unhyphenated entries are `spelling-variant`). `SHA-224` leaves as an `oracle-wart` row — Android offers it, the experts omit it, and correcting a wart privately is the failure this group undoes
  - **`SSLContextSpec.mop:36-37`** `{Default, TLSv1.2, TLSv1.1, SSL, TLSv1, TLS, TLSv1.3}` → `{TLSV1.2, TLSV1.3}` (frozen spelling) **+ `TLS`** as the one `platform-value` entry (11.4). `SSL`, `TLSv1`, `TLSv1.1` and `Default` return to being accused
  - **`MacSpec.mop:28-30`** the 12 api30 entries (which carry `HmacMD5`, `HmacSHA1`) → the frozen 17 (`Mac.crysl` names 9; the 8 extra are `spelling-variant`)
  - **`KeyGeneratorSpec.mop:35-36`** `{ChaCha20, ARC4, HmacSHA224, DESede, HmacSHA256, HmacMD5, HmacSHA1, HmacSHA512, AES, BLOWFISH, HmacSHA384}` → the frozen 10 (`KeyGenerator.crysl` names 4). `algorithm in {"AES"} => keysize in {128,192,256}` stays a **`deferred-constant`** row citing the expert clause — the frozen set never tested it and this group adds no accusation class (D-15)
  - **`SignatureSpec.mop:36-40`** the 24 api30 entries (which carry `NONEwithRSA`, `MD5withRSA`, `SHA1withRSA`, `SHA1withDSA`, `DSAwithSHA1`, `NONEwithDSA`) → the frozen 7, which are exactly `Signature.crysl`'s. `SHA224withECDSA` and `SHA1withECDSA` leave as `oracle-wart` rows; the `api30-omits` row for the four `SHA*withECDSA` is closed with a note, three of the four being expert entries
  - **`SecureRandomSpec.mop:36`** `{SHA1PRNG}` → the frozen 6. The five Android does not offer are inert, not removable (no-narrowing rule); the `MOP-MAIS-PERMISSIVO` rows D-10 wrote for them leave the constraint table
  - **`KeyStoreSpec.mop:36`** `{AndroidKeyStore, PKCS12, BKS, BouncyCastle, AndroidCAStore}` → the five expert types `{JCEKS, JKS, DKS, PKCS11, PKCS12}` **+** the four `platform-value` entries `{AndroidKeyStore, AndroidCAStore, BKS, BouncyCastle}` (11.4)
  - **`KeyManagerFactorySpec.mop:35`** and **`TrustManagerFactorySpec.mop:36`** `{PKIX}` → `{PKIX, SunX509}`. `X509` keeps resolving through the alias table; `SunX509` stops being accused
  - **`SecretKeySpecSpec.mop`** — the list D-10 **deleted outright** is restored: `{AES, HMACSHA256, HMACSHA384, HMACSHA512}` plus the frozen set's six `HMAC[-/]SHA*` spelling variants (`SecretKeySpec.crysl:keyAlgorithm in {"AES","HmacSHA256","HmacSHA384","HmacSHA512"}`). The check goes in the **bodies** of `c1` (`:76`) and `c2` (`:125`), beside the predicate reads gh105 put there, never back into `condition(...)` (INV-INS-141), setting `conforms = false` on the same footing as the other two failures; two new `codes.csv` rows `SECRETKEYSPEC-ALG-00` (c1) and `SECRETKEYSPEC-ALG-01` (c2), `error_type=UnsafeAlgorithm`, `site_kind=ALG`
  - **`KeyPairGeneratorSpec.mop:34,43-46`** `{DSA, DH, RSA, EC}` → `{RSA, EC, DSA, DiffieHellman, DH}`; RSA sizes `{4096, 2048}` → `{4096, 3072, 2048}`; `validate` gains `case "DiffieHellman": return keySize == 2048;`. The `api30-omits` row for `EC` is closed with a note — `EC` is an expert entry, so the exception no longer exists — and the two `MOP-MAIS-PERMISSIVO` rows for `3072` and `DiffieHellman` leave the constraint table
- [x] 11.3 `CipherSpec.mop` re-points at the frozen utility: the `import static br.unb.cic.mop.jca.util.Api30CipherTransformationUtil.*;` at `:17` becomes `CipherTransformationUtil.*`, and the `expectedTransformations` literal at `:29` names it. **`CipherTransformationUtil.java` is not edited** — the freeze (INV-INS-109/118, task 10.1) forbids editing it, not calling it, and task 10.1's byte-identity check MUST still pass afterwards. `Api30CipherTransformationUtil` is **not deleted**: it loses its only caller and stays as the record of the withdrawn anchor, with a javadoc paragraph saying so. This closes the finding this group's audit added to the previous one: `Api30CipherTransformationUtil` admits **`AES/ECB/PKCS5Padding`** — `ECB` is a member of the transcribed AES mode clause (`Cipher.cryptsl:137`) and no transcribed padding clause covers `ECB`, so `isValid` falls through to `return true`. Verified by executing both classes over the same inputs: `AES/ECB/PKCS5Padding`, `AES/ECB/NoPadding`, `DESede/CBC/PKCS5Padding`, `DESede/ECB/PKCS5Padding`, `BLOWFISH/ECB/NoPadding`, `ARC4` and `ChaCha20` are accused by the frozen class and admitted by the api30 one. A third class transcribing `Cipher.crysl` afresh is **not** written (P1: new unmeasured code for `CCM` and eight `PBEWithHmacSHA*AndAES_*` transformations no corpus app uses)
- [x] 11.4 The `platform-value` layer, closed and cited — five entries and no sixth: `SSLContextSpec` += `TLS`, cited to Conscrypt `android11-release` `OpenSSLProvider.java:81`, where `SSLContext.TLS` is bound to `classOpenSSLContextImpl + defaultSSLContextSuffix`, i.e. the TLSv1.2 or TLSv1.3 implementation — so an app asking for `TLS` on API 30 provably gets a protocol the expert clause admits; `KeyStoreSpec` += `{AndroidKeyStore, AndroidCAStore, BKS, BouncyCastle}`, each with its own citation. A candidate with no primary source is dropped and stays accused. **`SSL` gets no row** (researcher decision 2026-08-24), and the reason is written into its `behavioural` row rather than left implicit: `OpenSSLProvider.java:80` binds `SSLContext.SSL` to the same implementation as `TLS`, so the 103 accused events did get a TLS context at run time — but it is a `put`, not an `Alg.Alias`, so it earns no alias row either; the expert clause names `TLSv1.2`/`TLSv1.3` and nothing else; and asking a provider for `"SSL"` is the misuse the rule is about
- [x] 11.5 Record rows: `oracle-wart` for each transcribed quirk (`Cipher` admits `OAEPWithMD5AndMGF1Padding` and no SHA-1 OAEP variant; `MessageDigest` omits `SHA-224`; `Signature` omits `SHA224withECDSA` and `SHA1withECDSA`; the `CCM` addition of 11.1); closing notes on the two `api30-omits` rows (`EC`, the four `SHA*withECDSA`) saying the exception dissolved rather than deleting the row; `spelling-variant` notes on every frozen-set entry that duplicates an expert entry under case folding or an alias. The `RSA/ECB/OAEPWithSHA1AndMGF1Padding` row stays `behavioural` and its verdict is **unchanged** — it was accused under the api30 anchor too (that list spells the padding with the hyphen) and is accused under the expert one, which carries no SHA-1 OAEP variant at all; correct the D-10 bookkeeping that counted its 109 events among the rows a re-anchoring must keep silent
- [x] 11.6 Aliases: add the 11 multi-line Conscrypt registrations the single-line extraction missed (6 `Signature` composite OIDs → `SHA{224,256,384,512}withRSA` at `OpenSSLProvider.java:234-263`; 5 `Cipher.RSA/None/OAEP*` at `:339-355`) to both `data/jca_android/alias_table.csv` and `ConscryptAliasTable.ROWS`, with `ConscryptAliasTableTest` asserting row-for-row equality; recompute every row's `in_allowlist` flag against the **expert** lists after the 11.4 departures — the flag's definition names the set's own allow-list and that list has just changed, so leaving it would make the record assert the withdrawn anchor; recount and rewrite the per-flag line of `data/jca_android/README.md`. Note in the same pass which of the 6 `Signature` OID rows now resolve to a canonical name **outside** the expert list (`MD5withRSA`, `SHA1withRSA`): those rows make the accusation reach more calls, not fewer, which is the intended direction
- [x] 11.7 Re-derive `data/jca_android/constraint_table.csv` and `conformance_record.csv` with the expert rule as the anchor column: new denominator (the expert `CONSTRAINTS` clauses, not the api30 ones), `deferred-constant` rows quoting the **expert** clause text and never an api30 reconstruction — the audit proved `pre_len > pre_off` (`MessageDigest`) and `len > off` (the Cipher streams) are mangled reconstructions whose promotion would implement a bug, and the expert originals are `length[preInput] >= preLen + preOffset` / `length[output] >= len + offset` with their `>= 0` and `> 0` companions. Every row keeps a second column naming the api30 rule its ORDER and predicate clauses still answer to, so a reader can tell which oracle a verdict came from
- [x] 11.8 Re-point the gate: `scripts/gh104_gates.py` G-CONF takes the pinned expert copy as its value oracle (`--crysl` accepting the `.crysl` extension beside `.cryptsl`, and the dialect differences the parser must absorb: `length[x]`/`generatedKey[k, alg]` in brackets where api30 writes parentheses; `alg(transformation)`/`mode`/`pad` where api30 writes `part(n,"/",transformation)`), and `--cipher-util` names `CipherTransformationUtil`. The `ORDER`/predicate consumers of the api30 rules (G-2's second input, G-ORDER, G-PRED2) keep reading `generated/api30/` — verify by running both and reading which file each verdict cites. Update the expectations of `tests/parity/test_gh104_specset_gates.py` and `test_gh101_specset_gates.py`; `scripts/gh104_message_gate.py` expectations follow the new `exp=` strings automatically, since every one is `String.join(",", <list>)` — the one literal that does not is `CipherSpec`'s `expectedTransformations` (11.3)
- [x] 11.9 Regenerate the set and run the differential harness over the 131 traces, old snapshot × new, attributing **every** delta to a row of 11.2-11.5 — an unattributed delta is a defect, not a result. Add the traces the corpus cannot supply: one per transformation of 11.3 (`AES/ECB/PKCS5Padding` first), one per algorithm the api30 lists admitted and the expert lists reject (`MD5`, `SHA-1`, `HmacMD5`, `ARC4`, `DESede`, `BLOWFISH`, `NONEwithRSA`, `MD5withRSA`, `SSL`), and one per value D-10 narrowed and this group restores (RSA `3072`, `DiffieHellman`, `SunX509`, `NativePRNG*`, `Windows-PRNG`, `PKCS11`, `JKS`, `JCEKS`, `DKS`, `HmacPBESHA1`, the `SecretKeySpec` algorithms) — the last family MUST class `unchanged` against the frozen `jca`, which is what proves the narrowings were undone rather than merely described. **Measured, after the instrument repair of 11.11**: 159 traces (131 inherited + 28 written here), `unchanged 119 · moved 22 · removed 12 · introduced 6`, every delta attributed in `data/jca_android/evidence/d15_harness_attribution.md`. The first run of this task reported `unchanged 132 · moved 9` over the same two snapshots; the thirteen-row difference is the instrument and not the set, and 11.11 says how
- [x] 11.10 Re-answer C5 by family against the re-anchored set and check the two-sided acceptance criterion of D-15 as one measurement: **the detections return** — 5,892 `MessageDigestSpec` rows (3,552 `MD5`, 1,915 `SHA-1`, 424 `SHA1`, 1 `SHA`), 103 `SSLContextSpec` `SSL`, 4 `SignatureSpec` `NONEwithRSA` — **and the platform artefacts stay silent** — `TLS` 8,648, `AndroidKeyStore` 2,005, `X509` 643, `SHA256WITHRSA` 4. The `CipherSpec` OAEP 109 stay accused in both anchors (11.5) and are recorded as unchanged, not as either side. Rewrite the oracle section of `data/jca_android/README.md` and the `docs/20260824_auditoria_specs_jca_android.md` §"reparos R1-R8" disposition to say which of them this group executed; `codes.csv` gains the two `SECRETKEYSPEC-ALG-*` rows of 11.2; message-gate rerun
- [x] 11.11 Repair the differential harness, which could not witness this group's own work. Two defects, both measured on 2026-08-24 and both of the family the README draws its "a gate that stopped comparing must say so" lesson from. (a) `TraceRunner.envelope()` scanned the accumulated `ErrorCollector` set — a `HashSet`, unordered, accumulated over the whole trace — and returned the **first** error matching the specification, so a second accusation at one event was dropped and the recorded envelope could belong to an event that fired earlier; the previous committed evidence shows it plainly, all four `MacSpec-hmacpbesha1` envelopes carrying `ev=i1` inside the message while the outer `ev=` reads `update`, `updateBytes`, `f1`. It now diffs the sink against a snapshot taken before the advice and writes one envelope per accusation the event actually added, ordered by the message so two snapshots stay comparable. (b) `classify()` compared accusing event **names**; it now compares `(event, code)` pairs, and the report tables print `event:CODE`. The docstring's reason for excluding the message — a rewritten message must not read as a behavioural difference — is preserved, because a code is not prose. **What it recovers, all previously classed `unchanged`**: `d15-SignatureSpec-{md5withrsa,nonewithrsa,sha1withdsa}` add `i1:SIGNATURE-ALG-00`; `d15-SSLContextSpec-{ssl,tlsv1}` add `init:SSLCONTEXT-PROTO-00`; `d15-KeyGeneratorSpec-{desede,hmacmd5}` add `gk1:KEYGENERATOR-ALG-00`; `SecretKeySpecSpec-badalg` adds `c1:SECRETKEYSPEC-ALG-00`, which is the restored `SecretKeySpec` list of 11.2 witnessed by execution for the first time; `TrustManagerFactorySpec-sunx509`, `d15-TrustManagerFactorySpec-sunx509` and `d15-KeyManagerFactorySpec-sunx509` **drop** `init:*-ALG-00`, which is the no-narrowing-by-preference decision executed rather than described; `d15-MacSpec-hmacpbesha1` **drops** `i1:MAC-ALG-00`, because `Mac.crysl:44` names `HmacPBESHA1` outright and the re-anchoring admits it. The self-test still returns its three designed verdicts and cannot cover this case by construction (design, harness row). **One claim is withdrawn as measured-wrong**: the evidence file recorded eight traces as classing `unchanged` because they were "too short to reach the `*-ALG-*` site" — seven of the eight reach it. The single real limit is the platform's: `KeyGenerator.getInstance("ARC4")` and `Cipher.getInstance("ARC4")` both raise `NoSuchAlgorithmException` on this JVM, so `TraceRunner` falls back to `AES` and the value sites, which read `getAlgorithm()` off the object, correctly stay silent — lengthening those two traces changes nothing, and `ARC4` is witnessed by the C5 replay and by the two-class execution of 11.3 instead

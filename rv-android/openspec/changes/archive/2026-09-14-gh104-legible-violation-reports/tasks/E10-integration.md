# Group 10 — Integration and verification

Tracked checkboxes: `tasks.md` §10. Last; after every other group. Roots: `evidence/...` means `data/gh104/evidence/...`. Git: one repository (`git rev-parse --show-toplevel` is `…/rvsec`; rv-android is a subdirectory); every `git status`/`git diff` in this group carries a pathspec; the group does not commit — the orchestrator commits after the group's summary with explicit pathspecs (never `git add -A`/`git commit -a`). Environment: prefix every Java/Maven command line with `export JAVA_HOME=$HOME/.sdkman/candidates/java/21.0.12-tem; export PATH=$JAVA_HOME/bin:$PATH` (shell state does not persist between tool calls; the default JDK is 25). `experimento-gh104/` (the campaign scaffold) hard-codes `jca_android`, `codes.csv`, `jca_android_bug_predicate/` and its own `scripts/gh104_gates.py` — the campaign-readiness gates with the 13-column header, a different script from `scripts/gh104_gates.py` of Group 6 task 6.1; nothing in this change edits it. Tasks 10.4 and 10.5 read runs already executed (`experimento-gh104/`, 2026-09-06/07, and `experimento-estudo02/`, 2026-09-08/14) and start no emulator, no monitor generation and no weaving.

## Brief

- 10.0 (**first**, before 10.4) `get_static_analysis_config()` passes the resolved set directory as `mop_dir` — today `RVStaticAnalysisConfig` defaults it to `resources/jca` (`rv_static_analysis/config.py:198-207`) and `rv_experiment/config.py:942-951` overrides nothing, so a `jca_android` campaign's static view (targets, reachability, coverage denominator) is the `jca`'s (audit G12). Reuse the resolution of `get_monitored_operations_config()` (`config.py:685-700`) rather than duplicating the mapping; one test, `test_static_analysis_config_uses_selected_set` in `tests/test_config_jit.py` beside `test_jca_android_spec_set_resolves_paths`, four cases: `jca_android` → `mop_dir` ends in `resources/jca_android`; `jca` → `resources/jca`; `custom` → `custom_specs_dir`; `targets_file` set → it still wins (INV-ANA-33 mutex); 10.4 records the directory the static analysis used. Run it with `uv run pytest --import-mode=importlib -o "addopts=" modules/rv-experiment/tests/test_config_jit.py -q`.
- 10.1 Freeze and divergence: `uv run pytest --import-mode=importlib -o "addopts=" tests/parity/test_gh101_specset_gates.py tests/parity/test_gh104_specset_gates.py tests/parity/test_gh104_structural_gates.py tests/parity/test_gh104_baseline.py tests/parity/test_gh104_unique_msg_built_once.py -q` — all green. From the repository root, `git diff 7e7acb69 -- rvsec/rvsec-mop/src/main/resources/jca rvsec/rvsec-core/src/main/java/br/unb/cic/mop/jca/util/CipherTransformationUtil.java` must be empty; `git diff --cached --stat --find-renames <pre-rename-head> -- rvsec/rvsec-mop/src/main/resources/jca_android rvsec/rvsec-mop/src/main/resources/jca_android_bug_predicate` (pathspec always; the index equals `HEAD` for those paths at this point) must show the 23 files as pure renames (`R100`) plus the successor set as additions, with **zero** content hunks on the renamed side — where `<pre-rename-head>` is the SHA task 2.1 froze in `data/jca_android/README.md` as the freeze item `pre-rename-head`. **Do not use `7e7acb69` here**: it predates gh101's edits to that directory (19 files, 720 insertions, 122 deletions on 2026-08-18), so a rename diff against it can never show zero content hunks, whatever this change does. `7e7acb69` stays the base for the `jca` and `CipherTransformationUtil` half of this task, where the diff is empty today and must stay empty; and `.../jca/util/AndroidCipherTransformationUtil.java` must be unmodified against `<pre-rename-head>`. In the MetaCrySL tree, `git status --short -- generated/api30` must be clean — this change reads `generated/api30/` and writes nothing there. (The freeze is on the specification *sources*: a generated monitor that differs because Group 3 changed the generator is not a freeze violation; old measurements reproduce by pinning the toolchain, per design D-4.)
- 10.2 Lint, per module: `/rv-qa-lint-fix rv-coverage`, `/rv-qa-lint-fix rv-platform`, `/rv-qa-lint-fix aperv-tool`, `/rv-qa-lint-fix rv-android-core`, `/rv-qa-lint-fix rv-experiment`; record outputs under `evidence/integration/`.
- 10.3 Verify, per module: `/rv-verify rv-coverage`, `/rv-verify rv-platform`, `/rv-verify aperv-tool`, `/rv-verify rv-android-core`, `/rv-verify rv-experiment`, `/rv-verify rv-instrumentation-dexlib2`; record outputs under `evidence/integration/`.
- 10.4 Device validation, read from executed runs (researcher decision, 2026-09-14; the P6 pilot of `experimento-gh104/PRONTIDAO.md` was not run). Sources: the joint campaign's `experimento-gh104/consolidado/gh104_gates.json` (the message gates, run with `--codes-csv` over `jca_android/codes.csv`) and its results under `data/results/gh104_*`; estudo02's `data/results/estudo02_consolidado/errors.csv` and per-container `summary.csv`; the dataset's `instrument_results.json` that wove the campaign corpus. The four APKs gh101 task 8.1 used (`com.owncloud.android_48000100`, `eu.opencloud.android_9`, `de.luhmer.owncloudnewsreader_196`, `com.etesync.syncadapter_20700`; `com.etesync` reached no monitored call in 8.1 and is reported rather than dropped) are read from estudo02's Monkey runs at 180 s, identity by identity. Record in `evidence/device_validation.md`: rows, `unknown` count (expect 0), `but found .` count (expect 0), envelope fields populated (`ev`, `val`, `exp`, with every code family that legitimately carries an empty `val` or `exp` named against the `.mop` literal that writes it), `advicesExcludedByArity` and `wrappersGenerated` from the instrumentation results JSON with the sha256 of the four APKs checked against the campaign corpus, and the observable parser counters. Declared deviations, each with its source: the corpus was woven by the dataset funnel inside Docker, not with `lib/` jars rebuilt on the host; static analysis reused the funnel's `.apk.json` (the directory 10.0 resolves is not exercised on device; its test remains its evidence); estudo02's Monkey runs with `ignore_crashes`/`ignore_timeouts`; the `ParserDiagnostics` counters are written to no result artefact, so only `summary.csv`'s `unmatched_out_of_scope`/`unmatched_in_scope` are observable — a declared gap. Read shapes, not counts.
- 10.5 Read the evidence of 10.4 against D-15 (expert allow-lists of Group 11, the `platform-value` layer of task 11.4, the normalisation rule of task 2.5). Three lines are the criterion:

  | line | seen on the frozen `jca` | expected on `jca_android` | mechanism |
  |---|---|---|---|
  | `TrustManagerFactorySpec` `X509` | reported | not reported | normalisation rule of task 2.5 resolves it to `PKIX`, which the expert list carries |
  | `KeyStoreSpec` `AndroidKeyStore` | reported | not reported | `platform-value` entry of task 11.4 (the expert list is JSE-only) |
  | `SSLContextSpec` `TLS` | reported | not reported | `platform-value` entry citing Conscrypt `OpenSSLProvider.java:81` |

  For each line record the shape on comp162 (frozen `jca`), the shape on the two campaigns, and the reach of its call site — direct (another record of the same specification at the same source line) or only indirect (the enclosing method executed); an unreached site is not evidence of a repair, and a line with only indirect reach is recorded as a caveat. Record the bilateral half from the same corpora: the accusations D-15 gives back relative to the archived api30 set — `MD5`/`SHA-1` in `MessageDigestSpec`, `SSL` in `SSLContextSpec`, `AES/ECB` in `CipherSpec`, the `NOBS` family — noting which of them the frozen `jca` already accused; `RSA/ECB/OAEPWithSHA1AndMGF1Padding` stays accused (task 11.5).
- 10.6 `/rv-code-reviewer` via the Skill tool over the whole change (Java + Python).
- 10.7 `/rv-docs-sync` per module whose CLAUDE.md/architecture.md describes the parser, the collectors' line, `errors.csv` columns or the spec-set enumeration (`/rv-docs-sync rv-coverage`, `/rv-docs-sync rv-instrumentation-dexlib2`, `/rv-docs-sync aperv-tool`); at archive/sync time also fix `openspec/specs/experiment/spec.md:87` (`# "jca", "generic", or "custom"` sample comment) and the CLAUDE.md paragraph on specification sets, which must now name three predefined sets (`jca`, `jca_android`, `generic`) plus `custom` — `jca_android` naming the successor set, and the reproved derived set living at `jca_android_bug_predicate/` where nothing selects it.
- 10.8 Sync of invariants and data contracts (at sync/archive time). The openspec-sync-specs skill processes only the `## ADDED/MODIFIED/REMOVED/RENAMED Requirements` headers of a delta, so the `## Invariants` sections of the six deltas and the platform delta's Data Contracts (the 13-column `errors.csv`) are applied to the upstream `openspec/specs/*/spec.md` by hand in this task, then verified by grep:
  - all 19 IDs exist in `openspec/specs` (INV-INS-118…129 = 12, INV-ANA-62/63, INV-CORE-56/57, INV-CAN-25/26, INV-PLT-32): `grep -rn "INV-INS-118\|INV-INS-119\|INV-INS-120\|INV-INS-121\|INV-INS-122\|INV-INS-123\|INV-INS-124\|INV-INS-125\|INV-INS-126\|INV-INS-127\|INV-INS-128\|INV-INS-129" openspec/specs/instrumentation/spec.md` (12 hits at least), `grep -rn "INV-ANA-62\|INV-ANA-63" openspec/specs/analysis/spec.md`, `grep -rn "INV-CORE-56\|INV-CORE-57" openspec/specs/core/spec.md`, `grep -rn "INV-CAN-25\|INV-CAN-26" openspec/specs/campaign-analysis/spec.md`, `grep -rn "INV-PLT-32" openspec/specs/platform/spec.md`;
  - the restated invariants carry the new text: `INV-PLT-19` (platform), `INV-CORE-25`, `INV-CORE-41` (core), `INV-EXP-03` (experiment), `INV-INS-09` (instrumentation), `INV-ANA-08` (analysis), `INV-CAN-04` (campaign-analysis) — grep each ID and compare the paragraph with the delta's Invariants section;
  - the upstream requirements `Derivation Provenance of the Android Specification Set` and `Cipher Transformation Tables of the Derived Set` are GONE from `openspec/specs/instrumentation/spec.md` (the delta REMOVES them) and their ADDED replacements `Provenance of the Archived Derived Specification Set` and `Cipher Transformation Tables of the Archived Derived Set` are present and scoped to `jca_android_bug_predicate` (`grep -n "jca_android_bug_predicate" openspec/specs/instrumentation/spec.md` hits inside both), and `INV-INS-113` is scoped to the archived set (binds `jca_android_bug_predicate`; `jca_android` carries `conformance_record.csv` per INV-INS-125);
  - the platform spec's Data Contracts list the 13 columns of `errors.csv` (`grep -n "unique_msg" openspec/specs/platform/spec.md` at the Data Contracts section) and no 11-column list survives;
  - `openspec/specs/experiment/spec.md:87` carries the corrected sample comment (10.7).

## Commit and close

- Final commit message carries `closes #104`; PR body `Closes #104`.
- Archive via `/opsx:archive gh104-legible-violation-reports` (syncs the six deltas' requirement blocks); task 10.8 lands the invariants and data contracts and verifies that INV-INS-118..129, INV-ANA-62/63, INV-CORE-56/57, INV-PLT-32, INV-CAN-25/26 exist in the main specs.

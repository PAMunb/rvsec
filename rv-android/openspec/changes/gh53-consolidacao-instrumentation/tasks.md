<!-- Subagent dispatch hints (4-module structure):
     - Group 1 (rv-instrumentation-core foundation) MUST complete first — every other group needs the abstractions module to be importable.
     - Group 2 (atomic rename of ajc impl) is the heaviest — moves files, renames classes, updates internal imports. Sequential after Group 1. Adds `class AjcInstrumentation(Instrumenter)` inheritance (task 2.4).
     - Group 4 (dexlib2 update: imports from -core, inherit ABC) sequential after Group 1; CAN run in PARALLEL with Group 2 (no shared files). Adds `class DexlibInstrumentation(Instrumenter)` inheritance (task 4.3).
     - Group 3 (rv-instrumentation parent restructure: factory + re-exports + factory tests) sequential after Group 2 AND Group 4. Reason: tasks 3.5/3.7/3.10 invoke the factory and assert `isinstance(returned, Instrumenter)` for BOTH variants — needs both `AjcInstrumentation(Instrumenter)` (Group 2 task 2.4) and `DexlibInstrumentation(Instrumenter)` (Group 4 task 4.3) inheritance already wired. Running 3.5 before Group 4 would fail on the dexlib2 branch.
     - Group 5 (rv-experiment dispatch via factory) sequential after Group 3 (consumer of the factory).
     - Group 6 (Docker) and Group 7 (cleanup) are independent and can run in PARALLEL after Group 1.
     - Group 8 (documentation) sequential after Group 2 — task 8.3 migrates the legacy CLAUDE.md from `modules/rv-instrumentation/CLAUDE.md` (the path that pre-rename held the AjC impl) into `-ajc/CLAUDE.md`; this requires Group 2's `git mv` to have happened. Group 8 may run in parallel with Groups 3, 4, 5, 6, 7.
     - Group 9 (Verification) MUST run LAST — gates the entire change against design.md §D7 acceptance criteria.
     - Critical path: 1 -> 2 -> 4 -> 3 -> 5 -> 9. Group 4 still parallel-able with Group 2 to compress wall time; Groups 6, 7 parallel after Group 1; Group 8 parallel after Group 2.
     - ADR (`ADR-INSTRUMENTER-ABC.md`) + Risk register (`RISKS.md`) are Phase 3 deliverables already produced before `/opsx:apply` (verify pre-condition: `[ -f openspec/changes/gh53-consolidacao-instrumentation/ADR-INSTRUMENTER-ABC.md ] && [ -f openspec/changes/gh53-consolidacao-instrumentation/RISKS.md ]` true).
     - This change touches ~45 files (rename surface + new module bootstrap + Docker + docs) — use subagent orchestration (4 parallel dispatches: Groups 4, 6, 7 independent after Group 1; Group 8 after Group 2).
-->

## 1. Foundation: `rv-instrumentation-core` (NEW abstractions module)

- [ ] 1.1 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/` directory tree (`src/rv_instrumentation_core/`, `tests/`, `docs/`)
- [ ] 1.2 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/pyproject.toml` with `name = "rv-instrumentation-core"`, dependencies EXACTLY `["pydantic>=2.9.0", "rv-android-core"]`, `[tool.uv.sources]` `rv-android-core = { workspace = true }`. NO `[project.scripts]`. NO deps on `rv-instrumentation`, `rv-instrumentation-ajc`, `rv-instrumentation-dexlib2` (INV-INS-41 — would form a cycle)
- [ ] 1.3 Implement `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/src/rv_instrumentation_core/results.py` with `InstrumentationError` (copied from current `modules/rv-instrumentation/src/rv_instrumentation/config.py:102-124`) and `InstrumentationResults` (copied from `config.py:127-175`), bytes intact. Satisfies INV-INS-33. **Realidade do código** (verificada 2026-05-01): `InstrumentationResults.variant` usa `variant: str = Field(default="ajc", ...)` (campo `str` SEM `Literal` e SEM validator) — divergente da spec gh52 INV-INS-18 que exige (a) `variant` required (sem default) + (b) `model_validator(mode="before")` para retrocompat. Esta task copia o estado real byte-a-byte; fechar a divergência é dívida do gh52, fora do escopo. Ver design.md §"Dívida herdada gh52 INV-INS-18"
- [ ] 1.4 Implement `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/src/rv_instrumentation_core/instrumenter.py` with ABC `Instrumenter` declaring `instrument_apks(self, apks_dir, results_dir, force_instrumentation: bool = False, apk_paths: Optional[List[str]] = None) -> InstrumentationResults` as the SOLE `@abstractmethod` (design D2; INV-INS-35)
- [ ] 1.5 Implement `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/src/rv_instrumentation_core/__init__.py` re-exporting `Instrumenter`, `InstrumentationResults`, `InstrumentationError`; declare `__all__`
- [ ] 1.6 Add `rv-instrumentation-core` as workspace member in root `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/pyproject.toml`
- [ ] 1.7 Add `rv-instrumentation-core` to `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/clean.sh`, `modules/lock.sh`, `modules/test.sh` module lists
- [ ] 1.8 Add unit tests `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/tests/test_results.py` covering: round-trip serialization for `variant="ajc"` and `variant="dexlib2"`; legacy JSON without `variant` field deserializes with `variant == "ajc"` (via `Field(default="ajc")`). NOTE: NÃO incluir teste de "invalid variant raises ValidationError" — o campo é `variant: str` sem `Literal` e sem validator (estado atual herdado da gh52). Promover para tipo restrito é dívida da gh52, fora do escopo. Se gh52 fechar INV-INS-18 depois, esse teste é adicionado pela própria gh52
- [ ] 1.9 Add unit tests `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/tests/test_instrumenter.py`: synthetic subclass missing `instrument_apks` raises `TypeError` on instantiation; concrete subclass with `instrument_apks` overridden instantiates OK; `inspect.signature(synthetic_subclass.instrument_apks)` matches `Instrumenter.instrument_apks`
- [ ] 1.10 Run `uv sync` from root; verify `python -c "from rv_instrumentation_core import Instrumenter, InstrumentationResults, InstrumentationError; print('ok')"` exits 0
- [ ] 1.11 Run `/rv-doc-code modules/rv-instrumentation-core/src/rv_instrumentation_core/`
- [ ] 1.11b Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/docs/architecture.md` (~30 lines): purpose (pure abstractions module — types + ABC, no logic, no factory, no assets); dependency declaration (only pydantic + rv-android-core); public API (`Instrumenter`, `InstrumentationResults`, `InstrumentationError`); explain how variants depend on this module (NOT on parent)
- [ ] 1.11c Run `/rv-doc-architecture rv-instrumentation-core` to validate the new architecture.md
- [ ] 1.12 Run `/rv-test-run rv-instrumentation-core` (passes — no dep on impls)
- [ ] 1.13 Verify `-core` deps are exactly `pydantic` + `rv-android-core` via `python -c "import tomllib; deps = sorted(d.split('>=')[0].split('==')[0].strip() for d in tomllib.loads(open('/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/pyproject.toml','rb').read().decode())['project']['dependencies']); assert deps == ['pydantic', 'rv-android-core'], deps"` (INV-INS-41)

## 2. Atomic rename: `rv-instrumentation` impl → `rv-instrumentation-ajc`

- [ ] 2.1 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-ajc/` directory tree (`src/rv_instrumentation_ajc/`, `tests/`, `assets/`, `docs/`)
- [ ] 2.2 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-ajc/pyproject.toml` with `name = "rv-instrumentation-ajc"`, dependencies `["rv-instrumentation-core", "rv-android-core", "pyyaml>=6.0"]` (pydantic is transitive via `-core`; not declared directly to avoid duplication), `[tool.uv.sources]` for the workspace deps. `[project.scripts] rv-instrumentation-ajc = "rv_instrumentation_ajc.__main__:main"`. NO dep on `rv-instrumentation` (parent) or `rv-instrumentation-dexlib2` (INV-INS-41)
- [ ] 2.3 Move `modules/rv-instrumentation/src/rv_instrumentation/__main__.py` → `modules/rv-instrumentation-ajc/src/rv_instrumentation_ajc/__main__.py` via `git mv`; update internal imports from `rv_instrumentation` to `rv_instrumentation_ajc` and `rv_instrumentation_core` for shared types
- [ ] 2.4 Move `modules/rv-instrumentation/src/rv_instrumentation/rvandroid.py` → `modules/rv-instrumentation-ajc/src/rv_instrumentation_ajc/ajc_instrumentation.py` via `git mv`; rename `class RVInstrumentation` → `class AjcInstrumentation(Instrumenter)` (add inheritance from `rv_instrumentation_core.Instrumenter`); update internal imports
- [ ] 2.5 Move `modules/rv-instrumentation/src/rv_instrumentation/config.py` → `modules/rv-instrumentation-ajc/src/rv_instrumentation_ajc/config.py` via `git mv`; **REMOVE** the migrated classes (`InstrumentationError` lines 102-124 and `InstrumentationResults` lines 127-175) — they now live in `-core`; rename `class RVInstrumentationConfig` → `class AjcInstrumentationConfig`; preserve `ConfigurationError` re-export from `rv_android_core.util.error.exceptions`
- [ ] 2.6 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-ajc/src/rv_instrumentation_ajc/__init__.py`: export `AjcInstrumentation`, `AjcInstrumentationConfig`, etc.
- [ ] 2.7 Move `modules/rv-instrumentation/assets/weaving_excludes.yaml` → `modules/rv-instrumentation-ajc/assets/weaving_excludes.yaml` via `git mv` (asset AspectJ-específico; INV-INS-40)
- [ ] 2.8 Verify `[ -f modules/rv-instrumentation/assets/keystore.jks ]` still true (asset shared, MUST remain in parent — INV-INS-39); `[ ! -f modules/rv-instrumentation/assets/weaving_excludes.yaml ]` true (moved); `[ -f modules/rv-instrumentation-ajc/assets/weaving_excludes.yaml ]` true
- [ ] 2.9 Move test files: `modules/rv-instrumentation/tests/*` → `modules/rv-instrumentation-ajc/tests/` via `git mv`; update internal imports of all test files (from `rv_instrumentation` → `rv_instrumentation_ajc` for impl symbols; from `rv_instrumentation_core` for shared types); rename `RVInstrumentation` → `AjcInstrumentation` and `RVInstrumentationConfig` → `AjcInstrumentationConfig` everywhere
- [ ] 2.10 Move docs: `modules/rv-instrumentation/docs/architecture.md` → `modules/rv-instrumentation-ajc/docs/architecture.md` via `git mv`; update internal references
- [ ] 2.11 Add `rv-instrumentation-ajc` as workspace member in root `pyproject.toml`
- [ ] 2.12 Add `rv-instrumentation-ajc` to `modules/clean.sh`, `modules/lock.sh`, `modules/test.sh` module lists
- [ ] 2.13 Run `uv sync` from root; verify `python -c "from rv_instrumentation_ajc.ajc_instrumentation import AjcInstrumentation; from rv_instrumentation_ajc.config import AjcInstrumentationConfig; from rv_instrumentation_core import Instrumenter; assert issubclass(AjcInstrumentation, Instrumenter)"` exits 0
- [ ] 2.14 Run `/rv-doc-code modules/rv-instrumentation-ajc/src/rv_instrumentation_ajc/`
- [ ] 2.15 Run `/rv-doc-architecture rv-instrumentation-ajc` to validate the migrated architecture.md
- [ ] 2.16 Run `/rv-test-run rv-instrumentation-ajc`
- [ ] 2.17 Verify `-ajc` deps don't include parent or sibling: `python -c "import tomllib; deps = tomllib.loads(open('/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-ajc/pyproject.toml','rb').read().decode())['project']['dependencies']; names = [d.split('>=')[0].split('==')[0].strip() for d in deps]; assert 'rv-instrumentation' not in names, names; assert 'rv-instrumentation-dexlib2' not in names, names; assert 'rv-instrumentation-core' in names, names"` (INV-INS-41)

## 3. Restructure `rv-instrumentation` parent (factory + re-exports)

- [ ] 3.1 Verify post-Group-2 the parent `modules/rv-instrumentation/src/rv_instrumentation/` directory contains NO leftover impl files: `[ ! -f modules/rv-instrumentation/src/rv_instrumentation/config.py ] && [ ! -f modules/rv-instrumentation/src/rv_instrumentation/rvandroid.py ] && [ ! -f modules/rv-instrumentation/src/rv_instrumentation/__main__.py ]` true
- [ ] 3.2 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/pyproject.toml`: dependencies become EXACTLY `["rv-instrumentation-core", "rv-instrumentation-ajc", "rv-instrumentation-dexlib2"]` plus optional `pydantic`; `[tool.uv.sources]` for all 3 workspace deps; **REMOVE** `[project.scripts] rv-instrumentation = ...` (CLI moved to `-ajc`); INV-INS-41
- [ ] 3.3 Implement `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/src/rv_instrumentation/factory.py` with `get_instrumenter(variant, config) -> Instrumenter`: lazy imports for both branches (`ajc` → `from rv_instrumentation_ajc.ajc_instrumentation import AjcInstrumentation`; `dexlib2` → `from rv_instrumentation_dexlib2 import DexlibInstrumentation`); raise `ValueError` for unknown variant. Type return as `rv_instrumentation_core.Instrumenter` (design D5; INV-INS-36)
- [ ] 3.4 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/src/rv_instrumentation/__init__.py`: `from rv_instrumentation_core import Instrumenter, InstrumentationResults, InstrumentationError`; `from rv_instrumentation.factory import get_instrumenter`; declare `__all__ = ["Instrumenter", "InstrumentationResults", "InstrumentationError", "get_instrumenter"]`
- [ ] 3.5 Add unit tests `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/tests/test_factory.py`: `get_instrumenter("ajc", config)` returns `AjcInstrumentation` and does NOT import `rv_instrumentation_dexlib2` (snapshot `sys.modules` before/after); `get_instrumenter("dexlib2", config)` returns `DexlibInstrumentation` and does NOT import `rv_instrumentation_ajc`; `get_instrumenter("lspatch", config)` raises `ValueError` mentioning `ajc`, `dexlib2`; `isinstance(returned, Instrumenter)` for both valid variants. **Pré-condição**: Group 4 task 4.3 (inheritance `DexlibInstrumentation(Instrumenter)`) deve estar concluída — sem isso, o assert `isinstance` no branch dexlib2 falha
- [ ] 3.6 Recreate `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/docs/architecture.md` (parent canonical scope, ~50 lines: factory + re-exports + shared keystore; not the legacy ajc impl architecture)
- [ ] 3.7 Run `uv sync` from root; verify `python -c "from rv_instrumentation import Instrumenter, InstrumentationResults, InstrumentationError, get_instrumenter; from rv_instrumentation_core import Instrumenter as Core_Instrumenter; assert Instrumenter is Core_Instrumenter"` exits 0
- [ ] 3.8 Run `/rv-doc-code modules/rv-instrumentation/src/rv_instrumentation/factory.py`
- [ ] 3.9 Run `/rv-doc-architecture rv-instrumentation` to validate the recreated architecture.md
- [ ] 3.10 Run `/rv-test-run rv-instrumentation` (parent factory tests)
- [ ] 3.11 Verify parent deps are exactly the 3 workspace siblings: `python -c "import tomllib; deps = sorted(d.split('>=')[0].split('==')[0].strip() for d in tomllib.loads(open('/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/pyproject.toml','rb').read().decode())['project']['dependencies']); assert 'rv-instrumentation-core' in deps and 'rv-instrumentation-ajc' in deps and 'rv-instrumentation-dexlib2' in deps, deps"` (INV-INS-41)

## 4. Update `rv-instrumentation-dexlib2`

- [ ] 4.1 Edit `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-dexlib2/src/rv_instrumentation_dexlib2/dexlib_instrumentation.py:11-14`: change `from rv_instrumentation.config import (InstrumentationError, InstrumentationResults)` to `from rv_instrumentation_core import (InstrumentationError, InstrumentationResults, Instrumenter)`
- [ ] 4.2 Edit `dexlib_instrumentation.py:122-125`: same import change in the local scope inside `instrument_apks` apk_paths branch (drop `Instrumenter` here — only types needed)
- [ ] 4.3 Add inheritance: change `class DexlibInstrumentation:` → `class DexlibInstrumentation(Instrumenter):`
- [ ] 4.4 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-dexlib2/pyproject.toml`: replace dep `rv-instrumentation` with `rv-instrumentation-core` (verified 2026-05-01: current pyproject lists `rv-instrumentation`; this was the workaround for shared types). Verify NO dep on `rv-instrumentation` (parent) or `rv-instrumentation-ajc` (sibling) remains (INV-INS-41)
- [ ] 4.4b Verify dexlib2 dep topology (INV-INS-41): `python -c "import tomllib; deps = [d.split('>=')[0].split('==')[0].strip() for d in tomllib.loads(open('/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-dexlib2/pyproject.toml','rb').read().decode())['project']['dependencies']]; assert 'rv-instrumentation' not in deps, deps; assert 'rv-instrumentation-ajc' not in deps, deps; assert 'rv-instrumentation-core' in deps, deps"`
- [ ] 4.5 Verify INV-INS-34 dexlib2-side: `grep -rnE 'from rv_instrumentation[^_]|^import rv_instrumentation[^_]' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-dexlib2/src/` returns 0 hits (dexlib2 imports from `rv_instrumentation_core`, NOT from parent). Note: ajc-side INV-INS-34 check is in task 9.12 AC-IMP-03 (must run after Group 2 has created `-ajc/src/`)
- [ ] 4.6 Run `/rv-test-run rv-instrumentation-dexlib2` (suite passa com novos imports + herança)

## 5. `rv-experiment` dispatch via factory

- [ ] 5.1 Replace `if/else` block at `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/src/rv_experiment/experiment/workflow/pre_processor.py:188-207` with: `variant = getattr(self.config, "instrumentation_variant", "ajc")`; `instr_config = self.config.get_dexlib_instrumentation_config() if variant == "dexlib2" else self.config.get_rv_instrumentation_config()`; `instrumenter = get_instrumenter(variant, instr_config)`; preserve all surrounding logging, exception handling, and `_copy_original_apks()` fallback (design D5)
- [ ] 5.2 Add `from rv_instrumentation import get_instrumenter` import at top of `pre_processor.py`; remove direct imports of `RVInstrumentation` and `DexlibInstrumentation` if present
- [ ] 5.3 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/src/rv_experiment/config.py`. Realidade verificada (2026-05-01): linhas 33-34 importam `from rv_instrumentation.config import ConfigurationError as InstrumentationConfigError` e `from rv_instrumentation.config import RVInstrumentationConfig`. Pós-rename: ambos passam para `from rv_instrumentation_ajc.config import ...` (`ConfigurationError` continua re-exportado de `rv_android_core.util.error.exceptions` via `rv_instrumentation_ajc.config`; `RVInstrumentationConfig` renomeia para `AjcInstrumentationConfig`). Atualizar todas as referências dessa classe ao longo do arquivo (set/get methods). **Path keystore em `config.py:669` (`Path(rvsec_root) / "rv-android" / "modules" / "rv-instrumentation" / "assets" / "keystore.jks"`) STAYS UNCHANGED** (INV-INS-39)
- [ ] 5.4 Add `rv-instrumentation-dexlib2` AND `rv-instrumentation-ajc` to `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/pyproject.toml` dependencies. Realidade verificada (2026-05-01): hoje só `rv-instrumentation` está listada — falta dexlib2 (RISK-005) E falta ajc. Após o split, rv-experiment importa `AjcInstrumentationConfig` de `rv_instrumentation_ajc.config` E `DexlibInstrumentationConfig` indiretamente, então ambas as deps são necessárias
- [ ] 5.5 Update mocks in `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/tests/test_pre_processor_variant.py`: replace `patch("rv_instrumentation.RVInstrumentation")` (and dexlib2 equivalent) with `patch("rv_experiment.experiment.workflow.pre_processor.get_instrumenter")`
- [ ] 5.6 Add new test in `test_pre_processor_variant.py`: `_instrument_apks` propagates `ValueError` when `get_instrumenter` raises (mock to raise `ValueError`); confirm fallback to `_copy_original_apks()` triggers
- [ ] 5.7 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/scripts/validation/fase_a_preprocess.py` — 6 call sites (verificados 2026-05-01 via `grep -n "RVInstrumentation"`): linhas `:98` e `:148` (`from rv_instrumentation import RVInstrumentation, RVInstrumentationConfig` em duas funções distintas) → `from rv_instrumentation_ajc.ajc_instrumentation import AjcInstrumentation` + `from rv_instrumentation_ajc.config import AjcInstrumentationConfig`; linhas `:106` e `:159` (`config = RVInstrumentationConfig(...)`) → `config = AjcInstrumentationConfig(...)`; linhas `:113` e `:172` (`instr = RVInstrumentation(config)`) → `instr = AjcInstrumentation(config)`. Update calls to `prepare_instrumentation(results_dir)` e `check_if_instrumented(app)` continuam idênticos (métodos ajc-específicos preservados pela rename). Script importa direto (NOT via factory) porque precisa de métodos ajc-específicos não expostos pela ABC `Instrumenter`
- [ ] 5.7b Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/scripts/jca557_quarantine_impact.py`: line 14 (docstring "- Quarantine YAML: modules/rv-instrumentation/assets/weaving_excludes.yaml") and line 44 (`QUARANTINE_YAML = ROOT / "modules" / "rv-instrumentation" / "assets" / "weaving_excludes.yaml"`) — replace `rv-instrumentation` segment with `rv-instrumentation-ajc` (asset moved per INV-INS-40)
- [ ] 5.8 Smoke verify post-update: `uv run python /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/scripts/validation/fase_a_preprocess.py --help` exits 0 (script uses argparse — `--help` works). For `jca557_quarantine_impact.py`, smoke is `python -c "import importlib.util; spec = importlib.util.spec_from_file_location('jca557', '/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/scripts/jca557_quarantine_impact.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print(m.QUARANTINE_YAML)"` — verify path resolves to `-ajc/assets/`
- [ ] 5.9 Verify INV-INS-36: `grep -rnE 'def get_instrumenter|def make_instrumenter|def _select_instrumenter' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/{modules,scripts}/` returns hits ONLY in `modules/rv-instrumentation/src/rv_instrumentation/factory.py`
- [ ] 5.10 Run `/rv-test-run rv-experiment`

## 6. Docker unification

- [ ] 6.1 Delete `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/docker/rvandroid_dexlib2/Dockerfile` and remove the now-empty `docker/rvandroid_dexlib2/` directory (INV-INS-38)
- [ ] 6.2 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/docker/rvandroid/Dockerfile`: replace dead-code example comment at lines 8-9 (referencing `RVSEC_BRANCH=gh52-instr-dexlib2` and `0.8.0-dexlib2-base`) with a current-state example or remove the comment block (P4)
- [ ] 6.3 Add to `docker/rvandroid/Dockerfile` (after `mvn clean install` and `uv sync`) a build-time gate: `RUN test -f /opt/rvsec/rv-android/modules/rv-instrumentation-dexlib2/lib/instr-cli.jar || (echo "ERROR: instr-cli.jar missing — Maven D9 auto-copy did not run; check rvsec/rvsec-android/rvsec-instrumentation-dexlib2/cli/pom.xml" && exit 1)` (design D6; path verified per `docker/rvandroid_dexlib2/Dockerfile:46`)
- [ ] 6.4 Rewrite `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/docker/docker-compose.dexlib2-validation.template.yml`: both services use image `phtcosta/rvandroid:0.8.0`; service `ajc_00` sets `RV_INSTRUMENTATION_VARIANT=ajc`; service `dexlib2_00` sets `RV_INSTRUMENTATION_VARIANT=dexlib2`; preserve the two-service paired-comparison structure required by gh52 Phase 5
- [ ] 6.5 Verify `docker compose -f docker/docker-compose.dexlib2-validation.template.yml config --quiet` exits 0 and resolves both services to image `0.8.0` (AC-DOC-01)
- [ ] 6.6 Verify `find docker -name 'rvandroid_dexlib2*'` is empty (AC-DOC-02)
- [ ] 6.7 Verify `grep -rn '0\.8\.0-dexlib2' docker/ scripts/ docs/` returns 0 hits in functional code (AC-DOC-03)
- [ ] 6.8 Confirm `git diff --quiet docker/docker-compose.jca400-aperv.yml` exits 0 (no-op confirmed in proposal)

## 7. Cleanup operacional

- [ ] 7.1 Remove all 22× `ajcore.20260421.*.txt` files from the repo root: `find /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android -maxdepth 2 -name 'ajcore.*.txt' -delete`
- [ ] 7.2 Add line `ajcore.*.txt` to `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/.gitignore` (avoid duplicates)
- [ ] 7.3 Verify `find . -maxdepth 2 -name 'ajcore.*.txt'` returns empty (AC-CLN-01)
- [ ] 7.4 Verify `grep -nE 'ajcore' .gitignore` returns 1+ hit (AC-CLN-02)
- [ ] 7.5 Close gh52 leftover: add `rv-instrumentation-dexlib2` to `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/clean.sh:17`, `modules/lock.sh:17`, `modules/test.sh:17` module lists AND to the help text (`clean.sh:344`, `lock.sh:298`, `test.sh:399`). Verificado 2026-05-01: dívida pré-existente da gh52 — o módulo nunca foi adicionado às listas. gh53 fecha enquanto consolida (escopo já abrange edição desses 3 scripts via tasks 1.7 e 2.12). Pós-task: as listas contêm 4 entradas `rv-instrumentation*` (parent + `-core` + `-ajc` + `-dexlib2`)

## 8. Documentation

- [ ] 8.1 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/CLAUDE.md` (~25 lines): purpose (pure abstractions), API (Instrumenter ABC + types), no logic, no factory, no assets
- [ ] 8.2 Recreate `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/CLAUDE.md` (parent canonical role): purpose (factory + re-exports + shared keystore asset), API (`get_instrumenter` + re-exported types), points to `-core` for ABC and to variant modules for impls
- [ ] 8.3 Migrate `modules/rv-instrumentation-ajc/CLAUDE.md` from the legacy CLAUDE.md content (was at `modules/rv-instrumentation/CLAUDE.md` pre-rename) — adjust paths, names, references; document that `AjcInstrumentation` inherits from `Instrumenter` (`-core`)
- [ ] 8.4 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-dexlib2/CLAUDE.md` (file does not exist today; minimal version following the pattern of other module CLAUDE.md files)
- [ ] 8.5 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/CLAUDE.md` §"Three-Phase Workflow": document `get_instrumenter()` factory and `Instrumenter` ABC; mention closure of gh52 §15.4
- [ ] 8.6 Update root `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/CLAUDE.md` §System Modules: add `rv-instrumentation-core` E `rv-instrumentation-ajc`; restate role of `rv-instrumentation` as parent canonical; update §Module Dependencies to reflect the dep graph
- [ ] 8.7 Update root `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/README.md`: add `rv-instrumentation-core` and `rv-instrumentation-ajc` to the modules table. **Canonical count post-change = 16 production modules** (excluding `aperv-llm-validation` per memory). README's prior "13 independent modules" was wrong; PRD's "14 uv workspace modules" was correct pre-change. Reconcile README to "16"
- [ ] 8.8 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/openspec/config.yaml:8` context line: "14 uv workspace modules" → "16 uv workspace modules"
- [ ] 8.9 Verify `grep -n 'rv-instrumentation-core\|rv-instrumentation-ajc' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/CLAUDE.md` returns 2+ hits in §System Modules (AC-DCM-01)
- [ ] 8.10 Verify `[ -f modules/rv-instrumentation-core/CLAUDE.md ] && [ -f modules/rv-instrumentation/CLAUDE.md ] && [ -f modules/rv-instrumentation-ajc/CLAUDE.md ] && [ -f modules/rv-instrumentation-dexlib2/CLAUDE.md ]` (AC-DCM-02)
- [ ] 8.11 Verify `grep -n '16 uv workspace modules\|16 production modules' openspec/config.yaml CLAUDE.md README.md` returns 1+ hit per file (AC-DCM-03)

## 9. Integration & Verification

- [ ] 9.1 Run `/rv-qa-lint-fix rv-instrumentation-core`
- [ ] 9.2 Run `/rv-qa-lint-fix rv-instrumentation`
- [ ] 9.3 Run `/rv-qa-lint-fix rv-instrumentation-ajc`
- [ ] 9.4 Run `/rv-qa-lint-fix rv-instrumentation-dexlib2`
- [ ] 9.5 Run `/rv-qa-lint-fix rv-experiment`
- [ ] 9.6 Run `/rv-verify rv-instrumentation-core` (AC-BHV-01)
- [ ] 9.7 Run `/rv-verify rv-instrumentation` (AC-BHV-02)
- [ ] 9.8 Run `/rv-verify rv-instrumentation-ajc` (AC-BHV-03)
- [ ] 9.9 Run `/rv-verify rv-instrumentation-dexlib2` (AC-BHV-04)
- [ ] 9.10 Run `/rv-verify rv-experiment` (AC-BHV-05, AC-BHV-06)
- [ ] 9.11 Run full CI-mirror suite: `for m in /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/*/; do uv run pytest "$m/tests" --import-mode=importlib -o "addopts=" -m "not (slow or online or sglang or performance or dataset)" || break; done` (AC-BHV-07)
- [ ] 9.12 AC-IMP individual gates:
  - AC-IMP-01: `grep -rnE 'from rv_instrumentation\.config import (InstrumentationResults|InstrumentationError|RVInstrumentationConfig)' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/{modules,scripts}/` returns 0 hits (ajustado 2026-05-01: removido `tests/` do brace — `./tests/` no root não existe; testes vivem em `modules/*/tests/` e já são cobertos pelo glob de `modules`)
  - AC-IMP-02 (INV-INS-33): `grep -rnE '^class (InstrumentationResults|InstrumentationError)\b' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation*/src/` returns hits ONLY under `modules/rv-instrumentation-core/src/`
  - AC-IMP-03 (INV-INS-34): `grep -rnE 'from rv_instrumentation[^_]|^import rv_instrumentation[^_]' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-dexlib2/src/ /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-ajc/src/` returns 0 hits
  - AC-IMP-04: `grep -rnE 'from rv_instrumentation import RVInstrumentation|from rv_instrumentation\.rvandroid import RVInstrumentation|RVInstrumentation\(' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/{modules,scripts}/` returns 0 hits (ajustado 2026-05-01: removido `tests/` do brace; padrão expandido para cobrir `from rv_instrumentation.rvandroid import RVInstrumentation` que existe em `scripts/validation/fase_a_preprocess.py`)
  - AC-IMP-05: `python -c "from rv_instrumentation import Instrumenter, InstrumentationResults, InstrumentationError, get_instrumenter"` exits 0
  - AC-IMP-06: `python -c "from rv_instrumentation_core import Instrumenter, InstrumentationResults, InstrumentationError"` exits 0
  - AC-IMP-07: `python -c "from rv_instrumentation_ajc.ajc_instrumentation import AjcInstrumentation; from rv_instrumentation_ajc.config import AjcInstrumentationConfig"` exits 0
  - AC-IMP-08: `python -c "from rv_instrumentation_dexlib2 import DexlibInstrumentation; from rv_instrumentation_core import Instrumenter; assert issubclass(DexlibInstrumentation, Instrumenter)"` exits 0
  - AC-IMP-09 (INV-INS-36): `grep -rnE 'def get_instrumenter|def make_instrumenter|def _select_instrumenter' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/{modules,scripts}/` returns hits ONLY in `factory.py`
  - AC-IMP-10 (name collision sweep — Pydantic `InstrumentationError` em `-core` vs Exception `InstrumentationError` em `rv_android_core.util.error.exceptions`): `grep -rnE 'from rv_android_core\.util\.error\.exceptions import.*\bInstrumentationError\b' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/` deve retornar APENAS hits em `modules/rv-instrumentation-ajc/src/rv_instrumentation_ajc/ajc_instrumentation.py` (era `rvandroid.py`) e seus testes — confirma que call sites continuam usando a Exception correta pós-rename, sem confusão entre as duas classes homônimas. Verificação adicional: `python -c "from rv_android_core.util.error.exceptions import InstrumentationError as Exc; from rv_instrumentation_core import InstrumentationError as Pyd; assert Exc is not Pyd, 'collision'; print('ok')"` exits 0
- [ ] 9.13 Smoke `uv sync` from clean state: `rm -rf /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/.venv && uv sync` succeeds (AC-WSP-01)
- [ ] 9.14 Verify `python -c "import rv_instrumentation_core; import rv_instrumentation_ajc; import rv_instrumentation; print(rv_instrumentation_core.__file__, rv_instrumentation_ajc.__file__)"` resolves to module paths under `modules/rv-instrumentation-core/src/` and `modules/rv-instrumentation-ajc/src/` (AC-WSP-02)
- [ ] 9.15 Verify all 4 `rv-instrumentation*` modules listadas em cada script: `for f in /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/{clean,lock,test}.sh; do for m in rv-instrumentation rv-instrumentation-core rv-instrumentation-ajc rv-instrumentation-dexlib2; do grep -q "\"$m\"" "$f" || { echo "MISSING $m in $f"; exit 1; }; done; done; echo ok` (AC-WSP-03; ampliado 2026-05-01 para incluir `-dexlib2` — task 7.5 fecha a dívida gh52)
- [ ] 9.16 Verify `grep -n 'rv-instrumentation-dexlib2\|rv-instrumentation-ajc' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/pyproject.toml` returns 2+ hits (AC-WSP-04)
- [ ] 9.17 Verify dependency graph topology (INV-INS-41 / AC-WSP-05/06): no cycle; `python -c "import tomllib; ..."` checks each module's deps. Specifically: `-core` deps = `[pydantic, rv-android-core]`; `-ajc` deps include `rv-instrumentation-core` and exclude `rv-instrumentation` and `rv-instrumentation-dexlib2`; `-dexlib2` deps include `rv-instrumentation-core` and exclude `rv-instrumentation` and `rv-instrumentation-ajc`; parent deps include all three siblings
- [ ] 9.18 Verify `[ -f modules/rv-instrumentation/assets/keystore.jks ]` true (AC-AST-01); `[ -f modules/rv-instrumentation-ajc/assets/weaving_excludes.yaml ]` true (AC-AST-02); `[ ! -f modules/rv-instrumentation/assets/weaving_excludes.yaml ]` true (AC-AST-03)
- [ ] 9.19 Verify `grep -n 'rv-instrumentation/assets/weaving_excludes' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/scripts/jca557_quarantine_impact.py` returns 0 hits AND `grep -n 'rv-instrumentation-ajc/assets/weaving_excludes' scripts/jca557_quarantine_impact.py` returns 1+ hits (AC-AST-04, AC-AST-05)
- [ ] 9.20 Verify `grep -n 'rv-instrumentation/assets/keystore' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/src/rv_experiment/config.py` returns 1+ hit (AC-AST-06; path unchanged)
- [ ] 9.20b Verify entry-point script collision absence: after `uv sync`, confirm `[ ! -f .venv/bin/rv-instrumentation ] && [ -f .venv/bin/rv-instrumentation-ajc ]` (parent dropped its `[project.scripts]` entry; ajc inherited the CLI under new name — FIND-TSK-007)
- [ ] 9.20c Verify Dockerfile preserves `ARG RVSEC_BRANCH=modules`: `grep -n '^ARG RVSEC_BRANCH=modules' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/docker/rvandroid/Dockerfile` returns 1 hit (Phase 0 §4.3 decision; FIND-TSK-011)
- [ ] 9.21 Docker rebuild gate: `docker build -t phtcosta/rvandroid:0.8.0 /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/docker/rvandroid/` from clean clone of `modules` post-change exits 0; gate `instr-cli.jar` step passes (AC-IMG-01)
- [ ] 9.21b Verify rebuilt image carries all 4 modules (INV-INS-37): `docker run --rm phtcosta/rvandroid:0.8.0 bash -c "ls /opt/rvsec/rv-android/modules/ | grep -c '^rv-instrumentation'"` returns `4` (rv-instrumentation, rv-instrumentation-core, rv-instrumentation-ajc, rv-instrumentation-dexlib2 all present)
- [ ] 9.22 Docker smoke per variant (INV-INS-37): rebuild image carries 4 modules — `RV_INSTRUMENTATION_VARIANT=ajc` produces `instrument_errors.json` with `variant: "ajc"`; `=dexlib2` produces `variant: "dexlib2"` (AC-IMG-02). Use 1-APK fixture with `--skip-monitors --skip-static`
- [ ] 9.23 Verify `[ -f openspec/changes/gh53-consolidacao-instrumentation/ADR-INSTRUMENTER-ABC.md ]` (AC-DCM-04)
- [ ] 9.24 Invoke `/rv-code-reviewer` via Skill tool with args `Review gh53-consolidacao-instrumentation implementation`
- [ ] 9.25 Run `/opsx:verify gh53-consolidacao-instrumentation` to validate implementation matches delta specs
- [ ] 9.26 Run `/rv-docs-sync` (Group 8 modified 6 CLAUDE.md files — root + `-core` + parent + `-ajc` + `-dexlib2` + `rv-experiment` — plus README + openspec/config.yaml; sync is unconditional)
- [ ] 9.27 Final acceptance summary: confirm AC-IMP-01..10, AC-BHV-01..07, AC-WSP-01..06, AC-DOC-01..03, AC-IMG-01..02, AC-CLN-01..02, AC-DCM-01..04, AC-AST-01..06 from design.md §D7 — every AC must have an [x] checkbox or explicit deviation note

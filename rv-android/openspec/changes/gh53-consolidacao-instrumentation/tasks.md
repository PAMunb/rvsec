<!-- Subagent dispatch hints (4-module structure):
     - Group 1 (rv-instrumentation-core foundation) MUST complete first — every other group needs the abstractions module to be importable.
     - Group 2 (atomic rename of ajc impl) is the heaviest — moves files, renames classes, updates internal imports. Sequential after Group 1. Adds `class AjcInstrumentation(Instrumenter)` inheritance (task 2.4).
     - Group 4 (dexlib2 update: imports from -core, inherit ABC) sequential after Group 1; CAN run in PARALLEL with Group 2 (no shared files). Adds `class DexlibInstrumentation(Instrumenter)` inheritance (task 4.3).
     - Group 3 (rv-instrumentation parent restructure: factory + re-exports + factory tests) sequential after Group 2 AND Group 4. Reason: tasks 3.5/3.7/3.10 invoke the factory and assert `isinstance(returned, Instrumenter)` for BOTH variants — needs both `AjcInstrumentation(Instrumenter)` (Group 2 task 2.4) and `DexlibInstrumentation(Instrumenter)` (Group 4 task 4.3) inheritance already wired. Running 3.5 before Group 4 would fail on the dexlib2 branch.
     - Group 5 (rv-experiment dispatch via factory) sequential after Group 3 (consumer of the factory).
     - Group 6 (Docker) and Group 7 (cleanup) are independent and can run in PARALLEL after Group 1.
     - Group 8 (documentation) sequential after Group 2 — task 8.3 migrates the legacy CLAUDE.md from `modules/rv-instrumentation/CLAUDE.md` (the path that pre-rename held the AjC impl) into `-ajc/CLAUDE.md`; this requires Group 2's `git mv` to have happened. Group 8 may run in parallel with Groups 3, 4, 5, 6, 7.
     - Group 9 (Verification) MUST run LAST — gates the entire change against design.md §D7 acceptance criteria.
     - Critical path: 1 -> 2 -> 4 -> 3 -> 5 -> 9. Group 4 still parallel-able with Group 2 to compress wall time; Groups 6, 7 parallel after Group 1; Group 8 parallel after Group 2.
     - ADR (`ADR-INSTRUMENTER-ABC.md`) + Risk register (`RISKS.md`) are Phase 3 deliverables already produced before `/opsx:apply` (verify pre-condition: `[ -f openspec/changes/gh53-consolidacao-instrumentation/ADR-INSTRUMENTER-ABC.md ] && [ -f openspec/changes/gh53-consolidacao-instrumentation/RISKS.md ]` true).
     - This change touches ~45 files (rename surface + new module bootstrap + Docker + docs) — use subagent orchestration (4 parallel dispatches: Groups 4, 6, 7 independent after Group 1; Group 8 after Group 2).
-->

## 1. Foundation: `rv-instrumentation-core` (NEW abstractions module)

- [x] 1.1 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/` directory tree (`src/rv_instrumentation_core/`, `tests/`, `docs/`)
- [x] 1.2 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/pyproject.toml` with `name = "rv-instrumentation-core"`, dependencies EXACTLY `["pydantic>=2.9.0", "rv-android-core"]`, `[tool.uv.sources]` `rv-android-core = { workspace = true }`. NO `[project.scripts]`. NO deps on `rv-instrumentation`, `rv-instrumentation-ajc`, `rv-instrumentation-dexlib2` (INV-INS-41 — would form a cycle)
- [x] 1.3 Implement `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/src/rv_instrumentation_core/results.py` with `InstrumentationError` (copied from current `modules/rv-instrumentation/src/rv_instrumentation/config.py:102-124`) and `InstrumentationResults` (copied from `config.py:127-175`), bytes intact. Satisfies INV-INS-33. **Realidade do código** (verificada 2026-05-01): `InstrumentationResults.variant` usa `variant: str = Field(default="ajc", ...)` (campo `str` SEM `Literal` e SEM validator) — divergente da spec gh52 INV-INS-18 que exige (a) `variant` required (sem default) + (b) `model_validator(mode="before")` para retrocompat. Esta task copia o estado real byte-a-byte; fechar a divergência é dívida do gh52, fora do escopo. Ver design.md §"Dívida herdada gh52 INV-INS-18"
- [x] 1.4 Implement `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/src/rv_instrumentation_core/instrumenter.py` with ABC `Instrumenter` declaring `instrument_apks(self, apks_dir, results_dir, force_instrumentation: bool = False, apk_paths: Optional[List[str]] = None) -> InstrumentationResults` as the SOLE `@abstractmethod` (design D2; INV-INS-35)
- [x] 1.5 Implement `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/src/rv_instrumentation_core/__init__.py` re-exporting `Instrumenter`, `InstrumentationResults`, `InstrumentationError`; declare `__all__`
- [x] 1.6 Add `rv-instrumentation-core` as workspace member in root `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/pyproject.toml`
- [x] 1.7 Add `rv-instrumentation-core` to `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/clean.sh`, `modules/lock.sh`, `modules/test.sh` module lists
- [x] 1.8 Add unit tests `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/tests/test_results.py` covering: round-trip serialization for `variant="ajc"` and `variant="dexlib2"`; legacy JSON without `variant` field deserializes with `variant == "ajc"` (via `Field(default="ajc")`). NOTE: NÃO incluir teste de "invalid variant raises ValidationError" — o campo é `variant: str` sem `Literal` e sem validator (estado atual herdado da gh52). Promover para tipo restrito é dívida da gh52, fora do escopo. Se gh52 fechar INV-INS-18 depois, esse teste é adicionado pela própria gh52
- [x] 1.9 Add unit tests `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/tests/test_instrumenter.py`: synthetic subclass missing `instrument_apks` raises `TypeError` on instantiation; concrete subclass with `instrument_apks` overridden instantiates OK; `inspect.signature(synthetic_subclass.instrument_apks)` matches `Instrumenter.instrument_apks`
- [x] 1.10 Run `uv sync` from root; verify `python -c "from rv_instrumentation_core import Instrumenter, InstrumentationResults, InstrumentationError; print('ok')"` exits 0
- [x] 1.11 Run `/rv-doc-code modules/rv-instrumentation-core/src/rv_instrumentation_core/`
- [x] 1.11b Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/docs/architecture.md` (~30 lines): purpose (pure abstractions module — types + ABC, no logic, no factory, no assets); dependency declaration (only pydantic + rv-android-core); public API (`Instrumenter`, `InstrumentationResults`, `InstrumentationError`); explain how variants depend on this module (NOT on parent)
- [x] 1.11c Run `/rv-doc-architecture rv-instrumentation-core` to validate the new architecture.md
- [x] 1.12 Run `/rv-test-run rv-instrumentation-core` (passes — no dep on impls)
- [x] 1.13 Verify `-core` deps are exactly `pydantic` + `rv-android-core` via `python -c "import tomllib; deps = sorted(d.split('>=')[0].split('==')[0].strip() for d in tomllib.loads(open('/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/pyproject.toml','rb').read().decode())['project']['dependencies']); assert deps == ['pydantic', 'rv-android-core'], deps"` (INV-INS-41)

## 2. Atomic rename: `rv-instrumentation` impl → `rv-instrumentation-ajc`

- [x] 2.1 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-ajc/` directory tree (`src/rv_instrumentation_ajc/`, `tests/`, `assets/`, `docs/`)
- [x] 2.2 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-ajc/pyproject.toml` with `name = "rv-instrumentation-ajc"`, dependencies `["rv-instrumentation-core", "rv-android-core", "pyyaml>=6.0"]` (pydantic is transitive via `-core`; not declared directly to avoid duplication), `[tool.uv.sources]` for the workspace deps. `[project.scripts] rv-instrumentation-ajc = "rv_instrumentation_ajc.__main__:main"`. NO dep on `rv-instrumentation` (parent) or `rv-instrumentation-dexlib2` (INV-INS-41)
- [x] 2.3 Move `modules/rv-instrumentation/src/rv_instrumentation/__main__.py` → `modules/rv-instrumentation-ajc/src/rv_instrumentation_ajc/__main__.py` via `git mv`; update internal imports from `rv_instrumentation` to `rv_instrumentation_ajc` and `rv_instrumentation_core` for shared types
- [x] 2.4 Move `modules/rv-instrumentation/src/rv_instrumentation/rvandroid.py` → `modules/rv-instrumentation-ajc/src/rv_instrumentation_ajc/ajc_instrumentation.py` via `git mv`; rename `class RVInstrumentation` → `class AjcInstrumentation(Instrumenter)` (add inheritance from `rv_instrumentation_core.Instrumenter`); update internal imports
- [x] 2.5 Move `modules/rv-instrumentation/src/rv_instrumentation/config.py` → `modules/rv-instrumentation-ajc/src/rv_instrumentation_ajc/config.py` via `git mv`; **REMOVE** the migrated classes (`InstrumentationError` lines 102-124 and `InstrumentationResults` lines 127-175) — they now live in `-core`; rename `class RVInstrumentationConfig` → `class AjcInstrumentationConfig`; preserve `ConfigurationError` re-export from `rv_android_core.util.error.exceptions`
- [x] 2.6 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-ajc/src/rv_instrumentation_ajc/__init__.py`: export `AjcInstrumentation`, `AjcInstrumentationConfig`, etc.
- [x] 2.7 Move `modules/rv-instrumentation/assets/weaving_excludes.yaml` → `modules/rv-instrumentation-ajc/assets/weaving_excludes.yaml` via `git mv` (asset AspectJ-específico; INV-INS-40)
- [x] 2.8 Verify `[ -f modules/rv-instrumentation/assets/keystore.jks ]` still true (asset shared, MUST remain in parent — INV-INS-39); `[ ! -f modules/rv-instrumentation/assets/weaving_excludes.yaml ]` true (moved); `[ -f modules/rv-instrumentation-ajc/assets/weaving_excludes.yaml ]` true
- [x] 2.9 Move test files: `modules/rv-instrumentation/tests/*` → `modules/rv-instrumentation-ajc/tests/` via `git mv`; update internal imports of all test files (from `rv_instrumentation` → `rv_instrumentation_ajc` for impl symbols; from `rv_instrumentation_core` for shared types); rename `RVInstrumentation` → `AjcInstrumentation` and `RVInstrumentationConfig` → `AjcInstrumentationConfig` everywhere
- [x] 2.10 Move docs: `modules/rv-instrumentation/docs/architecture.md` → `modules/rv-instrumentation-ajc/docs/architecture.md` via `git mv`; update internal references
- [x] 2.11 Add `rv-instrumentation-ajc` as workspace member in root `pyproject.toml`
- [x] 2.12 Add `rv-instrumentation-ajc` to `modules/clean.sh`, `modules/lock.sh`, `modules/test.sh` module lists
- [x] 2.13 Run `uv sync` from root; verify `python -c "from rv_instrumentation_ajc.ajc_instrumentation import AjcInstrumentation; from rv_instrumentation_ajc.config import AjcInstrumentationConfig; from rv_instrumentation_core import Instrumenter; assert issubclass(AjcInstrumentation, Instrumenter)"` exits 0
- [x] 2.14 Run `/rv-doc-code modules/rv-instrumentation-ajc/src/rv_instrumentation_ajc/`
- [x] 2.15 Run `/rv-doc-architecture rv-instrumentation-ajc` to validate the migrated architecture.md
- [x] 2.16 Run `/rv-test-run rv-instrumentation-ajc`
- [x] 2.17 Verify `-ajc` deps don't include parent or sibling: `python -c "import tomllib; deps = tomllib.loads(open('/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-ajc/pyproject.toml','rb').read().decode())['project']['dependencies']; names = [d.split('>=')[0].split('==')[0].strip() for d in deps]; assert 'rv-instrumentation' not in names, names; assert 'rv-instrumentation-dexlib2' not in names, names; assert 'rv-instrumentation-core' in names, names"` (INV-INS-41)

## 3. Restructure `rv-instrumentation` parent (factory + re-exports)

- [x] 3.1 Verify post-Group-2 the parent `modules/rv-instrumentation/src/rv_instrumentation/` directory contains NO leftover impl files: `[ ! -f modules/rv-instrumentation/src/rv_instrumentation/config.py ] && [ ! -f modules/rv-instrumentation/src/rv_instrumentation/rvandroid.py ] && [ ! -f modules/rv-instrumentation/src/rv_instrumentation/__main__.py ]` true
- [x] 3.2 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/pyproject.toml`: dependencies become EXACTLY `["rv-instrumentation-core", "rv-instrumentation-ajc", "rv-instrumentation-dexlib2"]` plus optional `pydantic`; `[tool.uv.sources]` for all 3 workspace deps; **REMOVE** `[project.scripts] rv-instrumentation = ...` (CLI moved to `-ajc`); INV-INS-41
- [x] 3.3 Implement `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/src/rv_instrumentation/factory.py` with `get_instrumenter(variant, config) -> Instrumenter`: lazy imports for both branches (`ajc` → `from rv_instrumentation_ajc.ajc_instrumentation import AjcInstrumentation`; `dexlib2` → `from rv_instrumentation_dexlib2 import DexlibInstrumentation`); raise `ValueError` for unknown variant. Type return as `rv_instrumentation_core.Instrumenter` (design D5; INV-INS-36)
- [x] 3.4 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/src/rv_instrumentation/__init__.py`: `from rv_instrumentation_core import Instrumenter, InstrumentationResults, InstrumentationError`; `from rv_instrumentation.factory import get_instrumenter`; declare `__all__ = ["Instrumenter", "InstrumentationResults", "InstrumentationError", "get_instrumenter"]`
- [x] 3.5 Add unit tests `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/tests/test_factory.py`: `get_instrumenter("ajc", config)` returns `AjcInstrumentation` and does NOT import `rv_instrumentation_dexlib2` (snapshot `sys.modules` before/after); `get_instrumenter("dexlib2", config)` returns `DexlibInstrumentation` and does NOT import `rv_instrumentation_ajc`; `get_instrumenter("lspatch", config)` raises `ValueError` mentioning `ajc`, `dexlib2`; `isinstance(returned, Instrumenter)` for both valid variants. **Pré-condição**: Group 4 task 4.3 (inheritance `DexlibInstrumentation(Instrumenter)`) deve estar concluída — sem isso, o assert `isinstance` no branch dexlib2 falha
- [x] 3.6 Recreate `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/docs/architecture.md` (parent canonical scope, ~50 lines: factory + re-exports + shared keystore; not the legacy ajc impl architecture)
- [x] 3.7 Run `uv sync` from root; verify `python -c "from rv_instrumentation import Instrumenter, InstrumentationResults, InstrumentationError, get_instrumenter; from rv_instrumentation_core import Instrumenter as Core_Instrumenter; assert Instrumenter is Core_Instrumenter"` exits 0
- [x] 3.8 Run `/rv-doc-code modules/rv-instrumentation/src/rv_instrumentation/factory.py`
- [x] 3.9 Run `/rv-doc-architecture rv-instrumentation` to validate the recreated architecture.md
- [x] 3.10 Run `/rv-test-run rv-instrumentation` (parent factory tests)
- [x] 3.11 Verify parent deps are exactly the 3 workspace siblings: `python -c "import tomllib; deps = sorted(d.split('>=')[0].split('==')[0].strip() for d in tomllib.loads(open('/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/pyproject.toml','rb').read().decode())['project']['dependencies']); assert 'rv-instrumentation-core' in deps and 'rv-instrumentation-ajc' in deps and 'rv-instrumentation-dexlib2' in deps, deps"` (INV-INS-41)

## 4. Update `rv-instrumentation-dexlib2`

- [x] 4.1 Edit `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-dexlib2/src/rv_instrumentation_dexlib2/dexlib_instrumentation.py:11-14`: change `from rv_instrumentation.config import (InstrumentationError, InstrumentationResults)` to `from rv_instrumentation_core import (InstrumentationError, InstrumentationResults, Instrumenter)`
- [x] 4.2 Edit `dexlib_instrumentation.py:122-125`: same import change in the local scope inside `instrument_apks` apk_paths branch (drop `Instrumenter` here — only types needed)
- [x] 4.3 Add inheritance: change `class DexlibInstrumentation:` → `class DexlibInstrumentation(Instrumenter):`
- [x] 4.4 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-dexlib2/pyproject.toml`: replace dep `rv-instrumentation` with `rv-instrumentation-core` (verified 2026-05-01: current pyproject lists `rv-instrumentation`; this was the workaround for shared types). Verify NO dep on `rv-instrumentation` (parent) or `rv-instrumentation-ajc` (sibling) remains (INV-INS-41)
- [x] 4.4b Verify dexlib2 dep topology (INV-INS-41): `python -c "import tomllib; deps = [d.split('>=')[0].split('==')[0].strip() for d in tomllib.loads(open('/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-dexlib2/pyproject.toml','rb').read().decode())['project']['dependencies']]; assert 'rv-instrumentation' not in deps, deps; assert 'rv-instrumentation-ajc' not in deps, deps; assert 'rv-instrumentation-core' in deps, deps"`
- [x] 4.5 Verify INV-INS-34 dexlib2-side: `grep -rnE 'from rv_instrumentation[^_]|^import rv_instrumentation[^_]' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-dexlib2/src/` returns 0 hits (dexlib2 imports from `rv_instrumentation_core`, NOT from parent). Note: ajc-side INV-INS-34 check is in task 9.12 AC-IMP-03 (must run after Group 2 has created `-ajc/src/`)
- [x] 4.6 Run `/rv-test-run rv-instrumentation-dexlib2` (suite passa com novos imports + herança)

## 5. `rv-experiment` dispatch via factory

- [x] 5.1 Replace `if/else` block at `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/src/rv_experiment/experiment/workflow/pre_processor.py:188-207` with: `variant = getattr(self.config, "instrumentation_variant", "ajc")`; `instr_config = self.config.get_dexlib_instrumentation_config() if variant == "dexlib2" else self.config.get_rv_instrumentation_config()`; `instrumenter = get_instrumenter(variant, instr_config)`; preserve all surrounding logging, exception handling, and `_copy_original_apks()` fallback (design D5)
- [x] 5.2 Add `from rv_instrumentation import get_instrumenter` import at top of `pre_processor.py`; remove direct imports of `RVInstrumentation` and `DexlibInstrumentation` if present
- [x] 5.3 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/src/rv_experiment/config.py`. Realidade verificada (2026-05-01): linhas 33-34 importam `from rv_instrumentation.config import ConfigurationError as InstrumentationConfigError` e `from rv_instrumentation.config import RVInstrumentationConfig`. Pós-rename: ambos passam para `from rv_instrumentation_ajc.config import ...` (`ConfigurationError` continua re-exportado de `rv_android_core.util.error.exceptions` via `rv_instrumentation_ajc.config`; `RVInstrumentationConfig` renomeia para `AjcInstrumentationConfig`). Atualizar todas as referências dessa classe ao longo do arquivo (set/get methods). **Path keystore em `config.py:669` (`Path(rvsec_root) / "rv-android" / "modules" / "rv-instrumentation" / "assets" / "keystore.jks"`) STAYS UNCHANGED** (INV-INS-39)
- [x] 5.4 Add `rv-instrumentation-dexlib2` AND `rv-instrumentation-ajc` to `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/pyproject.toml` dependencies. Realidade verificada (2026-05-01): hoje só `rv-instrumentation` está listada — falta dexlib2 (RISK-005) E falta ajc. Após o split, rv-experiment importa `AjcInstrumentationConfig` de `rv_instrumentation_ajc.config` E `DexlibInstrumentationConfig` indiretamente, então ambas as deps são necessárias
- [x] 5.5 Update mocks in `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/tests/test_pre_processor_variant.py`: replace `patch("rv_instrumentation.RVInstrumentation")` (and dexlib2 equivalent) with `patch("rv_experiment.experiment.workflow.pre_processor.get_instrumenter")`
- [x] 5.6 Add new test in `test_pre_processor_variant.py`: `_instrument_apks` propagates `ValueError` when `get_instrumenter` raises (mock to raise `ValueError`); confirm fallback to `_copy_original_apks()` triggers
- [x] 5.7 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/scripts/validation/fase_a_preprocess.py` — 6 call sites (verificados 2026-05-01 via `grep -n "RVInstrumentation"`): linhas `:98` e `:148` (`from rv_instrumentation import RVInstrumentation, RVInstrumentationConfig` em duas funções distintas) → `from rv_instrumentation_ajc.ajc_instrumentation import AjcInstrumentation` + `from rv_instrumentation_ajc.config import AjcInstrumentationConfig`; linhas `:106` e `:159` (`config = RVInstrumentationConfig(...)`) → `config = AjcInstrumentationConfig(...)`; linhas `:113` e `:172` (`instr = RVInstrumentation(config)`) → `instr = AjcInstrumentation(config)`. Update calls to `prepare_instrumentation(results_dir)` e `check_if_instrumented(app)` continuam idênticos (métodos ajc-específicos preservados pela rename). Script importa direto (NOT via factory) porque precisa de métodos ajc-específicos não expostos pela ABC `Instrumenter`
- [x] 5.7b Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/scripts/jca557_quarantine_impact.py`: line 14 (docstring "- Quarantine YAML: modules/rv-instrumentation/assets/weaving_excludes.yaml") and line 44 (`QUARANTINE_YAML = ROOT / "modules" / "rv-instrumentation" / "assets" / "weaving_excludes.yaml"`) — replace `rv-instrumentation` segment with `rv-instrumentation-ajc` (asset moved per INV-INS-40)
- [x] 5.8 Smoke verify post-update: `uv run python /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/scripts/validation/fase_a_preprocess.py --help` exits 0 (script uses argparse — `--help` works). For `jca557_quarantine_impact.py`, smoke is `python -c "import importlib.util; spec = importlib.util.spec_from_file_location('jca557', '/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/scripts/jca557_quarantine_impact.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print(m.QUARANTINE_YAML)"` — verify path resolves to `-ajc/assets/`
- [x] 5.9 Verify INV-INS-36: `grep -rnE 'def get_instrumenter|def make_instrumenter|def _select_instrumenter' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/{modules,scripts}/` returns hits ONLY in `modules/rv-instrumentation/src/rv_instrumentation/factory.py`
- [x] 5.10 Run `/rv-test-run rv-experiment`

## 6. Docker unification

- [x] 6.1 Delete `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/docker/rvandroid_dexlib2/Dockerfile` and remove the now-empty `docker/rvandroid_dexlib2/` directory (INV-INS-38)
- [x] 6.2 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/docker/rvandroid/Dockerfile`: replace dead-code example comment at lines 8-9 (referencing `RVSEC_BRANCH=gh52-instr-dexlib2` and `0.8.0-dexlib2-base`) with a current-state example or remove the comment block (P4)
- [x] 6.3 Add to `docker/rvandroid/Dockerfile` (after `mvn clean install` and `uv sync`) a build-time gate: `RUN test -f /opt/rvsec/rv-android/modules/rv-instrumentation-dexlib2/lib/instr-cli.jar || (echo "ERROR: instr-cli.jar missing — Maven D9 auto-copy did not run; check rvsec/rvsec-android/rvsec-instrumentation-dexlib2/cli/pom.xml" && exit 1)` (design D6; path verified per `docker/rvandroid_dexlib2/Dockerfile:46`)
- [x] 6.4 Rewrite `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/docker/docker-compose.dexlib2-validation.template.yml`: both services use image `phtcosta/rvandroid:0.8.0`; service `ajc_00` sets `RV_INSTRUMENTATION_VARIANT=ajc`; service `dexlib2_00` sets `RV_INSTRUMENTATION_VARIANT=dexlib2`; preserve the two-service paired-comparison structure required by gh52 Phase 5
- [x] 6.5 Verify `docker compose -f docker/docker-compose.dexlib2-validation.template.yml config --quiet` exits 0 and resolves both services to image `0.8.0` (AC-DOC-01)
- [x] 6.6 Verify `find docker -name 'rvandroid_dexlib2*'` is empty (AC-DOC-02)
- [x] 6.7 Verify `grep -rn '0\.8\.0-dexlib2' docker/ scripts/ docs/` returns 0 hits in functional code (AC-DOC-03)
- [x] 6.8 Confirm `git diff --quiet docker/docker-compose.jca400-aperv.yml` exits 0 (no-op confirmed in proposal)

## 7. Cleanup operacional

- [x] 7.1 Remove all 22× `ajcore.20260421.*.txt` files from the repo root: `find /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android -maxdepth 2 -name 'ajcore.*.txt' -delete`
- [x] 7.2 Add line `ajcore.*.txt` to `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/.gitignore` (avoid duplicates)
- [x] 7.3 Verify `find . -maxdepth 2 -name 'ajcore.*.txt'` returns empty (AC-CLN-01)
- [x] 7.4 Verify `grep -nE 'ajcore' .gitignore` returns 1+ hit (AC-CLN-02)
- [x] 7.5 Close gh52 leftover: add `rv-instrumentation-dexlib2` to `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/clean.sh:17`, `modules/lock.sh:17`, `modules/test.sh:17` module lists AND to the help text (`clean.sh:344`, `lock.sh:298`, `test.sh:399`). Verificado 2026-05-01: dívida pré-existente da gh52 — o módulo nunca foi adicionado às listas. gh53 fecha enquanto consolida (escopo já abrange edição desses 3 scripts via tasks 1.7 e 2.12). Pós-task: as listas contêm 4 entradas `rv-instrumentation*` (parent + `-core` + `-ajc` + `-dexlib2`)

## 8. Documentation

- [x] 8.1 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-core/CLAUDE.md` (~25 lines): purpose (pure abstractions), API (Instrumenter ABC + types), no logic, no factory, no assets
- [x] 8.2 Recreate `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation/CLAUDE.md` (parent canonical role): purpose (factory + re-exports + shared keystore asset), API (`get_instrumenter` + re-exported types), points to `-core` for ABC and to variant modules for impls
- [x] 8.3 Migrate `modules/rv-instrumentation-ajc/CLAUDE.md` from the legacy CLAUDE.md content (was at `modules/rv-instrumentation/CLAUDE.md` pre-rename) — adjust paths, names, references; document that `AjcInstrumentation` inherits from `Instrumenter` (`-core`)
- [x] 8.4 Create `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-dexlib2/CLAUDE.md` (file does not exist today; minimal version following the pattern of other module CLAUDE.md files)
- [x] 8.5 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/CLAUDE.md` §"Three-Phase Workflow": document `get_instrumenter()` factory and `Instrumenter` ABC; mention closure of gh52 §15.4
- [x] 8.6 Update root `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/CLAUDE.md` §System Modules: add `rv-instrumentation-core` E `rv-instrumentation-ajc`; restate role of `rv-instrumentation` as parent canonical; update §Module Dependencies to reflect the dep graph
- [x] 8.7 Update root `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/README.md`: add `rv-instrumentation-core` and `rv-instrumentation-ajc` to the modules table. **Canonical count post-change = 16 production modules** (excluding `aperv-llm-validation` per memory). README's prior "13 independent modules" was wrong; PRD's "14 uv workspace modules" was correct pre-change. Reconcile README to "16"
- [x] 8.8 Update `/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/openspec/config.yaml:8` context line: "14 uv workspace modules" → "16 uv workspace modules"
- [x] 8.9 Verify `grep -n 'rv-instrumentation-core\|rv-instrumentation-ajc' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/CLAUDE.md` returns 2+ hits in §System Modules (AC-DCM-01)
- [x] 8.10 Verify `[ -f modules/rv-instrumentation-core/CLAUDE.md ] && [ -f modules/rv-instrumentation/CLAUDE.md ] && [ -f modules/rv-instrumentation-ajc/CLAUDE.md ] && [ -f modules/rv-instrumentation-dexlib2/CLAUDE.md ]` (AC-DCM-02)
- [x] 8.11 Verify `grep -n '16 uv workspace modules\|16 production modules' openspec/config.yaml CLAUDE.md README.md` returns 1+ hit per file (AC-DCM-03)

## 9. Integration & Verification

- [x] 9.1 Run `/rv-qa-lint-fix rv-instrumentation-core`
- [x] 9.2 Run `/rv-qa-lint-fix rv-instrumentation`
- [x] 9.3 Run `/rv-qa-lint-fix rv-instrumentation-ajc`
- [x] 9.4 Run `/rv-qa-lint-fix rv-instrumentation-dexlib2`
- [x] 9.5 Run `/rv-qa-lint-fix rv-experiment`
- [x] 9.6 Run `/rv-verify rv-instrumentation-core` (AC-BHV-01)
- [x] 9.7 Run `/rv-verify rv-instrumentation` (AC-BHV-02)
- [x] 9.8 Run `/rv-verify rv-instrumentation-ajc` (AC-BHV-03)
- [x] 9.9 Run `/rv-verify rv-instrumentation-dexlib2` (AC-BHV-04)
- [x] 9.10 Run `/rv-verify rv-experiment` (AC-BHV-05, AC-BHV-06)
- [x] 9.11 Run full CI-mirror suite: `for m in /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/*/; do uv run pytest "$m/tests" --import-mode=importlib -o "addopts=" -m "not (slow or online or sglang or performance or dataset)" || break; done` (AC-BHV-07)
- [x] 9.12 AC-IMP individual gates:
  - AC-IMP-01: `grep -rnE 'from rv_instrumentation\.config import (InstrumentationResults|InstrumentationError|RVInstrumentationConfig)' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/{modules,scripts}/` returns 0 hits (ajustado 2026-05-01: removido `tests/` do brace — `./tests/` no root não existe; testes vivem em `modules/*/tests/` e já são cobertos pelo glob de `modules`)
  - AC-IMP-02 (INV-INS-33): `grep -rnE '^class (InstrumentationResults|InstrumentationError)\b' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation*/src/` returns hits ONLY under `modules/rv-instrumentation-core/src/`
  - AC-IMP-03 (INV-INS-34): `grep -rnE 'from rv_instrumentation[^_]|^import rv_instrumentation[^_]' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-dexlib2/src/ /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-instrumentation-ajc/src/` returns 0 hits
  - AC-IMP-04: `grep -rnE 'from rv_instrumentation import RVInstrumentation|from rv_instrumentation\.rvandroid import RVInstrumentation|RVInstrumentation\(' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/{modules,scripts}/` returns 0 hits (ajustado 2026-05-01: removido `tests/` do brace; padrão expandido para cobrir `from rv_instrumentation.rvandroid import RVInstrumentation` que existe em `scripts/validation/fase_a_preprocess.py`)
  - AC-IMP-05: `python -c "from rv_instrumentation import Instrumenter, InstrumentationResults, InstrumentationError, get_instrumenter"` exits 0
  - AC-IMP-06: `python -c "from rv_instrumentation_core import Instrumenter, InstrumentationResults, InstrumentationError"` exits 0
  - AC-IMP-07: `python -c "from rv_instrumentation_ajc.ajc_instrumentation import AjcInstrumentation; from rv_instrumentation_ajc.config import AjcInstrumentationConfig"` exits 0
  - AC-IMP-08: `python -c "from rv_instrumentation_dexlib2 import DexlibInstrumentation; from rv_instrumentation_core import Instrumenter; assert issubclass(DexlibInstrumentation, Instrumenter)"` exits 0
  - AC-IMP-09 (INV-INS-36): `grep -rnE 'def get_instrumenter|def make_instrumenter|def _select_instrumenter' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/{modules,scripts}/` returns hits ONLY in `factory.py`
  - AC-IMP-10 (name collision sweep — Pydantic `InstrumentationError` em `-core` vs Exception `InstrumentationError` em `rv_android_core.util.error.exceptions`): `grep -rnE 'from rv_android_core\.util\.error\.exceptions import.*\bInstrumentationError\b' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/` deve retornar APENAS hits em `modules/rv-instrumentation-ajc/src/rv_instrumentation_ajc/ajc_instrumentation.py` (era `rvandroid.py`) e seus testes — confirma que call sites continuam usando a Exception correta pós-rename, sem confusão entre as duas classes homônimas. Verificação adicional: `python -c "from rv_android_core.util.error.exceptions import InstrumentationError as Exc; from rv_instrumentation_core import InstrumentationError as Pyd; assert Exc is not Pyd, 'collision'; print('ok')"` exits 0
- [x] 9.13 Smoke `uv sync` from clean state: `rm -rf /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/.venv && uv sync` succeeds (AC-WSP-01)
- [x] 9.14 Verify `python -c "import rv_instrumentation_core; import rv_instrumentation_ajc; import rv_instrumentation; print(rv_instrumentation_core.__file__, rv_instrumentation_ajc.__file__)"` resolves to module paths under `modules/rv-instrumentation-core/src/` and `modules/rv-instrumentation-ajc/src/` (AC-WSP-02)
- [x] 9.15 Verify all 4 `rv-instrumentation*` modules listadas em cada script: `for f in /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/{clean,lock,test}.sh; do for m in rv-instrumentation rv-instrumentation-core rv-instrumentation-ajc rv-instrumentation-dexlib2; do grep -q "\"$m\"" "$f" || { echo "MISSING $m in $f"; exit 1; }; done; done; echo ok` (AC-WSP-03; ampliado 2026-05-01 para incluir `-dexlib2` — task 7.5 fecha a dívida gh52)
- [x] 9.16 Verify `grep -n 'rv-instrumentation-dexlib2\|rv-instrumentation-ajc' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/pyproject.toml` returns 2+ hits (AC-WSP-04)
- [x] 9.17 Verify dependency graph topology (INV-INS-41 / AC-WSP-05/06): no cycle; `python -c "import tomllib; ..."` checks each module's deps. Specifically: `-core` deps = `[pydantic, rv-android-core]`; `-ajc` deps include `rv-instrumentation-core` and exclude `rv-instrumentation` and `rv-instrumentation-dexlib2`; `-dexlib2` deps include `rv-instrumentation-core` and exclude `rv-instrumentation` and `rv-instrumentation-ajc`; parent deps include all three siblings
- [x] 9.18 Verify `[ -f modules/rv-instrumentation/assets/keystore.jks ]` true (AC-AST-01); `[ -f modules/rv-instrumentation-ajc/assets/weaving_excludes.yaml ]` true (AC-AST-02); `[ ! -f modules/rv-instrumentation/assets/weaving_excludes.yaml ]` true (AC-AST-03)
- [x] 9.19 Verify `grep -n 'rv-instrumentation/assets/weaving_excludes' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/scripts/jca557_quarantine_impact.py` returns 0 hits AND `grep -n 'rv-instrumentation-ajc/assets/weaving_excludes' scripts/jca557_quarantine_impact.py` returns 1+ hits (AC-AST-04, AC-AST-05)
- [x] 9.20 Verify `grep -n 'rv-instrumentation/assets/keystore' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/modules/rv-experiment/src/rv_experiment/config.py` returns 1+ hit (AC-AST-06; path unchanged)
- [x] 9.20b Verify entry-point script collision absence: after `uv sync`, confirm `[ ! -f .venv/bin/rv-instrumentation ] && [ -f .venv/bin/rv-instrumentation-ajc ]` (parent dropped its `[project.scripts]` entry; ajc inherited the CLI under new name — FIND-TSK-007)
- [x] 9.20c Verify Dockerfile preserves `ARG RVSEC_BRANCH=modules`: `grep -n '^ARG RVSEC_BRANCH=modules' /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/docker/rvandroid/Dockerfile` returns 1 hit (Phase 0 §4.3 decision; FIND-TSK-011)
- [ ] 9.21 Docker rebuild gate: `docker build -t phtcosta/rvandroid:0.8.0 /pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec/rv-android/docker/rvandroid/` from clean clone of `modules` post-change exits 0; gate `instr-cli.jar` step passes (AC-IMG-01) — **DEFERRED**: depends on `modules` branch being pushed to GitHub (Dockerfile clones from there). Image build (~15 min) gated on push; AC-IMG-01 verified in CI on next push.
- [ ] 9.21b Verify rebuilt image carries all 4 modules (INV-INS-37): `docker run --rm phtcosta/rvandroid:0.8.0 bash -c "ls /opt/rvsec/rv-android/modules/ | grep -c '^rv-instrumentation'"` returns `4` (rv-instrumentation, rv-instrumentation-core, rv-instrumentation-ajc, rv-instrumentation-dexlib2 all present) — **DEFERRED**: blocked on 9.21 image rebuild.
- [ ] 9.22 Docker smoke per variant (INV-INS-37): rebuild image carries 4 modules — `RV_INSTRUMENTATION_VARIANT=ajc` produces `instrument_errors.json` with `variant: "ajc"`; `=dexlib2` produces `variant: "dexlib2"` (AC-IMG-02). Use 1-APK fixture with `--skip-monitors --skip-static`. **DEFERRED**: blocked on 9.21 image rebuild + needs 1-APK fixture and emulator device.
- [x] 9.22b Host-side E2E smoke per variant — `uv run rv-experiment run --tools aperv:sata_mop --apks-dir /tmp/gh53_smoke_apks --timeout 60 --specification-set jca --name gh53_smoke_ajc --instrumentation-variant ajc` exits 0 and `results/gh53_smoke_ajc/instrument_errors.json` (or equivalent variant tag) shows `variant: "ajc"`. Uses cryptoapp.apk (apks_examples/cryptoapp.apk). **Verified 2026-05-01**: ran in 4m44s; pre-processing → instrumentation → emulator boot → 60s aperv:sata_mop on cryptoapp; result 1/1 task successful, 50% activity coverage, 9.43% method coverage, 3.12% RV method coverage, 0 errors. `experiment_config.json` shows `"instrumentation_variant": "ajc"`.
- [x] 9.22c **SUPERSEDED by 9.22b4** — Host-side E2E smoke per variant (dexlib2). Primeiro run em 2026-05-01 com `--name gh53_smoke_dexlib2` produziu 0% coverage devido a bug pré-existente do gh52 (dexlib2 wrapper não populava runtime classpath; CLI Java exitava 0 silently). Bug consertado em 9.22f1-f7 (Template Method + wrapper guard); validado em 9.22b4 com `--name gh53_smoke_dexlib2_v2` (cov_act=75%, cov_method=22.64%, cov_rv_method=18.75%, 3 MOP violations detectadas em cryptoapp).

## 9.22f. Bug fix dexlib2 (Phase 4 amend — gh52 leftover surfaced by gh53 smoke)

Tasks descobertas durante Phase 4 (Implement) ao validar 9.22c/9.22b. Root-cause analysis (3 Explore agents + sequential-thinking, ver `/home/pedro/.claude/plans/dapper-leaping-teapot.md`):

1. **Java CLI dexa `--classpath` no APK final** (MonitorBuilder.java:114-129). Classpath serve para AMBOS javac (compile) E d8 (runtime no APK).
2. **AJC popula `lib_tmp/` via mvn copy-dependencies** com rv-monitor-rt + rvsec-core + rvsec-logger-logcat + aspectjrt; depois mescla esses .class no APK. Funciona porque `prepare_instrumentation()` roda Maven.
3. **Dexlib2 wrapper Python NÃO populava nada** — `prepare_instrumentation()` só validava descriptors. `extra_classpath` ficou vazio (rv-experiment/config.py apontava para thin agent jar de 124KB que não shada nada).
4. **Bug paralelo**: subcommand `instrument` do CLI Java exita 0 mesmo quando javac falha. Wrapper Python interpretava exit 0 como sucesso e reportava success_count=1/1 mesmo sem APK criado.

Decisão de design (template method na ABC `Instrumenter`): mover lógica `mvn dependency:copy-dependencies` para `_resolve_runtime_libs` em `rv-instrumentation-core`. AJC e dexlib2 usam o helper compartilhado, sem duplicação. Cada variante popula seu próprio `<working_dir>/lib_tmp/` (variantes ALTERNATIVAS, nunca compartilham — user roda uma OU outra).

- [x] 9.22f1 [ABC] Adicionar `_resolve_runtime_libs(rvsec_root, lib_tmp_dir) -> List[Path]` em `modules/rv-instrumentation-core/src/rv_instrumentation_core/instrumenter.py`. Roda `mvn -f rv-android/pom.xml -q dependency:copy-dependencies -DoutputDirectory=<lib_tmp_dir.absolute()> -DincludeScope=runtime`. Raises FileNotFoundError se pom missing, RuntimeError se mvn falha OU se rodou mas nenhum jar caiu. Retorna lista absoluta dos jars (ordem alfabética).
- [x] 9.22f2 [ABC tests] Adicionar 4 tests em `modules/rv-instrumentation-core/tests/test_instrumenter.py`: (a) mvn run com sucesso retorna jars, (b) pom missing raises FileNotFoundError, (c) mvn returncode != 0 raises RuntimeError, (d) mvn returncode 0 mas dir vazio raises RuntimeError.
- [x] 9.22f3 [Dexlib2] Atualizar `DexlibInstrumentation.prepare_instrumentation()` em `modules/rv-instrumentation-dexlib2/src/rv_instrumentation_dexlib2/dexlib_instrumentation.py:38-63`: após validações existentes (descriptor + cli_jar_path), chamar `self._resolve_runtime_libs(rvsec_root, working_dir/lib_tmp)` e **filtrar aspectjrt** (não é usado pelo dexlib2 — `MultiSpec_*RuntimeMonitor.java` que dexlib2 compila NÃO importa AspectJ; aspectjrt só serve para o weaving AJC). Filtro: `runtime_jars = [j for j in template_jars if not j.name.startswith("aspectjrt-")]`. Depois `self.config.extra_classpath = list(self.config.extra_classpath) + runtime_jars` (append, não overwrite). Os 3 jars que dexlib2 precisa: rv-monitor-rt, rvsec-core, rvsec-logger-logcat.
- [x] 9.22f4 [Dexlib2] Wrapper guard em `dexlib_instrumentation.py` (apk_paths path linhas 141-159 + batch path após `_parse_results_json`): após `_run_cli` retornar, verificar `(results_dir / apk_name).is_file()` antes de incrementar success. Se arquivo ausente, demote para erro com phase=`dexlib2_pipeline`, message="instr-cli reported success but APK not created — silent javac/d8 failure".
- [x] 9.22f5 [Dexlib2 tests] Adicionar 6 tests em `modules/rv-instrumentation-dexlib2/tests/test_dexlib_instrumentation.py`: (a) wrapper guard apk_paths demotes when APK missing, (b) wrapper guard apk_paths succeeds when APK present, (c) wrapper guard batch path demotes when results.json lies, (d) prepare_instrumentation calls _resolve_runtime_libs (spy), (e) prepare_instrumentation appends runtime jars to extra_classpath, (f) prepare_instrumentation **exclui aspectjrt** dos jars adicionados (regression para evitar reintroduzir AspectJ jar no classpath dexlib2).
- [x] 9.22f6 [AJC refactor] Substituir `AjcInstrumentation.__execute_maven()` (linhas 681-707) por chamada ao Template Method da ABC: em `prepare_instrumentation()` (linhas 385-422) trocar `self.__execute_maven()` por `self._resolve_runtime_libs(Path(self.config.rvsec_root or os.environ["RVSEC_HOME"]), Path(self.config.lib_tmp_dir))`. AJC consome **todos os 4 jars** (rv-monitor-rt, rvsec-core, rvsec-logger-logcat, aspectjrt) — sem filtro, AspectJ weaving precisa de aspectjrt. Remover o método `__execute_maven`.
- [x] 9.22f6-tests [AJC tests pós-refactor] Rodar `uv run pytest modules/rv-instrumentation-ajc/tests/ -v -m "not (slow or online)"` — 72 testes devem continuar passando após o refactor. Se algum quebrar, é regression do Template Method.
- [x] 9.22f6-parent-tests [Parent tests pós-refactor] Rodar `uv run pytest modules/rv-instrumentation/tests/ -v` — 3 factory tests devem continuar passando (factory dispatch não muda mas re-imports após mudanças na ABC).
- [x] 9.22f6-core-tests [Core tests pós-refactor] Rodar `uv run pytest modules/rv-instrumentation-core/tests/ -v` — após adicionar `_resolve_runtime_libs` os 7 testes existentes + 4 novos (9.22f2) = 11 testes passam.
- [x] 9.22f7 [rv-experiment] Em `modules/rv-experiment/src/rv_experiment/config.py:679-693`: remover bloco `agent_jar = ... extra_classpath = [agent_jar] if agent_jar.is_file() else []`. Substituir por `extra_classpath: list[Path] = []` com comentário explicando que runtime libs são populados pelo wrapper dexlib2 via Template Method.

## 9.22b. Validação em camadas (testar antes do E2E)

Tasks descobertas durante Phase 4 — validação isolada por camada antes do smoke E2E completo. Ordem importa: 9.22b1 → 9.22b2 → 9.22b3 (já feito) → 9.22b4.

### Dexlib2 layered

- [x] 9.22b1-dexlib2 [CLI Java direto] Após 9.22f1-f7 implementadas, invocar o CLI Java sem Python: `java -jar modules/rv-instrumentation-dexlib2/lib/instr-cli.jar instrument /tmp/gh53_smoke_apks/cryptoapp.apk --descriptor <descriptor.json> --output /tmp/cli_test/out --work-dir /tmp/cli_test --monitor-src-dir <monitors> --classpath <rt_jar>,<core_jar>,<logger_jar> --keystore modules/rv-instrumentation/assets/keystore.jks --keystore-pass password --key-alias server --key-pass password`. **3 jars no classpath (sem aspectjrt)**. Verificar: (a) exit 0, (b) `/tmp/cli_test/out/cryptoapp.apk` existe, (c) `unzip -p /tmp/cli_test/out/cryptoapp.apk classes*.dex | strings | grep -cE "RVMObject|ExecutionContext|ErrorCollector"` >= 3 (runtime classes dexadas no APK). ISOLA: bug do CLI Java vs bug do wrapper Python.
- [x] 9.22b2-dexlib2 [Wrapper Python isolado] Script ad-hoc invocando `DexlibInstrumentation.instrument_apks()` diretamente (sem rv-experiment), reusando monitors gerados pelo run anterior. Verificar: (a) `success_count == 1`, (b) APK output existe em results_dir, (c) `variant == "dexlib2"`, (d) `extra_classpath` populado pelo Template Method com 3 jars (não 4 — aspectjrt filtrado). ISOLA: bug do wrapper Python vs bug do rv-experiment.

### AJC layered (paridade — confirma que 9.22f6 refactor não quebrou)

- [x] 9.22b1-ajc [Wrapper Python isolado AJC] Script ad-hoc invocando `AjcInstrumentation.instrument_apks()` diretamente (sem rv-experiment) com 1 APK (cryptoapp). Verificar: (a) `success_count == 1`, (b) APK instrumentado existe, (c) `variant == "ajc"`, (d) `lib_tmp/` populado pelos 4 jars (rv-monitor-rt + rvsec-core + rvsec-logger-logcat + aspectjrt). Confirma que substituir `__execute_maven` por `_resolve_runtime_libs` não quebrou AJC.

  AJC não tem CLI Java único equivalente ao dexlib2 (orquestra dex2jar, ajc, d8, jarsigner — não dá pra `java -jar X instrument apk` direto). Por isso só "wrapper Python isolado", sem "CLI direto" para AJC.

### E2E completo

- [x] 9.22b3 [E2E rv-experiment ajc] Já validado em 9.22b. summary.csv: cov_act=50%, cov_method=9.43%, cov_rv_method=3.12%, errors=0. **Re-rodar após 9.22f6** para confirmar que refactor do AJC não regrediu.
- [x] 9.22b4 [E2E rv-experiment dexlib2] Após 9.22b1-dexlib2 + 9.22b2-dexlib2 passarem, rodar `uv run rv-experiment run --tools aperv:sata_mop --apks-dir /tmp/gh53_smoke_apks --timeout 60 --specification-set jca --name gh53_smoke_dexlib2_v2 --instrumentation-variant dexlib2`. Verificar: (a) exit 0, (b) `results/gh53_smoke_dexlib2_v2/instrumented_apks/cryptoapp.apk` existe, (c) summary.csv tem `cov_rv_method > 0` (runtime monitors disparam), (d) errors=0, (e) logcat contém eventos MOP (`grep -i "mop\|monitor" results/.../cryptoapp.apk__1__60__aperv:sata_mop.logcat | head`). Substitui o 9.22c original.
- [ ] 9.22d Docker E2E smoke per variant — once `phtcosta/rvandroid:0.8.0` image is rebuilt (9.21), run cryptoapp + aperv:sata_mop + 60s once with `RV_INSTRUMENTATION_VARIANT=ajc` and once with `RV_INSTRUMENTATION_VARIANT=dexlib2`; both exit 0 and produce results with the right variant tag. Confirms the unified image carries both variants and the entrypoint env-var dispatch works.
- [x] 9.23 Verify `[ -f openspec/changes/gh53-consolidacao-instrumentation/ADR-INSTRUMENTER-ABC.md ]` (AC-DCM-04)
- [x] 9.24 Invoke `/rv-code-reviewer` via Skill tool with args `Review gh53-consolidacao-instrumentation implementation`
- [x] 9.25 Run `/opsx:verify gh53-consolidacao-instrumentation` to validate implementation matches delta specs
- [x] 9.26 Run `/rv-docs-sync` (Group 8 modified 6 CLAUDE.md files — root + `-core` + parent + `-ajc` + `-dexlib2` + `rv-experiment` — plus README + openspec/config.yaml; sync is unconditional)
- [x] 9.27 Final acceptance summary: confirm AC-IMP-01..10, AC-BHV-01..07, AC-WSP-01..06, AC-DOC-01..03, AC-IMG-01..02, AC-CLN-01..02, AC-DCM-01..04, AC-AST-01..06 from design.md §D7 — every AC must have an [x] checkbox or explicit deviation note

## 9.22g. Bug fix dexlib2 wallclock timeout (Phase 5 amend — Docker E2E surfaced)

Tasks descobertas durante validação Docker em batch real (JCA-400, container 07, 2026-05-01) após push de gh53. Bug pré-existente do gh52 (commit `04f2dd0c`), exposto por gh53 (classpath cresceu — 1 thin jar 124KB → 3 jars reais ~250KB) e por o smoke do gh53 ter coberto apenas cryptoapp (4MB, 30k methods) sem testar APKs reais do dataset.

**Sintoma**: `subprocess.TimeoutExpired: Command [...instr-cli.jar instrument io.github.eucsoh.android_9.apk ...] timed out after 600 seconds` propagado por `except RuntimeError as ex:` (que não captura `TimeoutExpired` — herda de `SubprocessError`, não `RuntimeError`) → batch abortado → `pre_processor._copy_original_apks()` deixou 22 APKs **originais sem instrumentação** em `instrumented_apks/` → Phase 2 rodou aperv:sata_mop sobre binários sem MOP monitors → 0% MOP coverage.

**Baseline empírico (2026-05-01)**: `instr-cli.jar` direct sobre `io.github.eucsoh.android_9.apk` (28MB, 5 dex, 249k methods, 43k classes) sem timeout completou com sucesso em **14m14s** (`phase=signed`, `coverageInstrumented=80160`). 600s é insuficiente para APKs reais do JCA-400 corpus.

**Decisão de design**: remover wallclock timeout — paridade com AJC pipeline (que NÃO tem timeout em dex2jar/ajc/d8/jarsigner; só `zipalign` tem 60s SAFETY_KILL legítimo). Instrumentação é build operation, run-to-completion é semântica correta.

### Logging + observabilidade (3a-prelim do plano)

- [x] 9.22g1 [Logger setup] Adicionar `_logger = LoggingManager.get_instance().get_logger("rv_instrumentation_dexlib2.DexlibInstrumentation", {CONTEXT_COMPONENT: "DexlibInstrumentation"})` em `DexlibInstrumentation.__init__` (paridade com `AjcInstrumentation` em `ajc_instrumentation.py:90-100`). Atualmente DexlibInstrumentation tem ZERO logging — usuário só descobriu o timeout via traceback final, sem contexto de qual APK estava sendo processado nem onde gastou tempo.
- [x] 9.22g2 [Per-APK logging] Em `instrument_apks` (apk_paths branch e batch branch): logar `INFO` ao iniciar cada APK, `INFO` com duração ao completar, `ERROR` com phase + message ao falhar. Inclui `extra={"app_name": ..., "elapsed_seconds": ...}` para análise estruturada.
- [x] 9.22g3 [CLI invocation logging] Em `_run_cli`: logar `INFO` antes de invocar Java CLI com cmd args (truncados — primeiros 200 chars do classpath para não poluir log) e duração total no retorno. Permite correlacionar tempo Python-side vs Java-side.
- [x] 9.22g4 [Persist instrument_errors.json] Em `instrument_apks`: ao final, persistir `instrument_errors.json` em `results_dir` igual AJC faz (`ajc_instrumentation.py:340-360`). Permite post-mortem do batch sem precisar relogar.

### Fix do timeout (3a do plano)

- [x] 9.22g5 [Remove timeout config] Apagar `timeout_seconds: int = Field(default=600, ...)` em `modules/rv-instrumentation-dexlib2/src/rv_instrumentation_dexlib2/config.py:93-97`. Adicionar comentário no commit explicando que field foi removido (paridade com AJC, instrumentação é build).
- [x] 9.22g6 [Remove timeout from subprocess.run] Remover `timeout=self.config.timeout_seconds` do `subprocess.run(...)` em `dexlib_instrumentation.py:325-329`. Adicionar comentário inline: "No wallclock cap — instrumentation is a build operation; weave time scales with method count and is bounded only by APK content. Parity with AJC's mvn/dex2jar/ajc/d8/jarsigner invocations (no timeout). Real APKs from JCA-400 corpus require 10-30+ min legitimately (e.g. io.github.eucsoh.android_9.apk: 14m14s baseline 2026-05-01)."

### Defesa em profundidade (3b do plano)

- [x] 9.22g7 [Broaden except] Trocar `except RuntimeError as ex:` por `except (RuntimeError, subprocess.SubprocessError) as ex:` em `dexlib_instrumentation.py:226` (apk_paths branch). Mesmo com timeout removido, defende contra futuro re-introduction OU outras subprocess exceptions (CalledProcessError se alguém adicionar `check=True`). Per-APK demote-on-error em vez de matar batch.

### Tests (3c do plano)

- [x] 9.22g8 [Test: subprocess.run sem timeout] Adicionar test em `test_dexlib_instrumentation.py` que mocka `subprocess.run` e verifica que **NÃO** recebe `timeout=` kwarg (regression guard contra re-introduction).
- [x] 9.22g9 [Test: SubprocessError capturado] Test que `instrument_apks(apk_paths=[...])` com `_run_cli` raising `subprocess.SubprocessError` demote esse APK e continua com os demais (sem batch abort).
- [x] 9.22g10 [Test: existing tests pass without timeout_seconds] Atualizar tests que setam `timeout_seconds` no `DexlibInstrumentationConfig` (se houver) para refletir o field removido.
- [x] 9.22g11 [Test: logger called] Test que `instrument_apks` chama `_logger.info` ao iniciar/completar APK (verificação simples de observabilidade).

### Verification

- [x] 9.22g12 [Re-test cryptoapp] Re-rodar `/tmp/test_dexlib2_wrapper.py` (Python wrapper isolado) — confirma que mudanças não regrediram baseline cryptoapp.
- [x] 9.22g13 [Re-test eucsoh via wrapper] Adaptar script para `io.github.eucsoh.android_9.apk` (28MB) — confirmar que wrapper aguenta 14min+ sem timeout, completa com sucesso, e a instrumentation_errors.json fica vazia.
- [x] 9.22g14 [Suite completa] `uv run pytest modules/rv-instrumentation-dexlib2/tests/ modules/rv-instrumentation-ajc/tests/ modules/rv-instrumentation/tests/ modules/rv-instrumentation-core/tests/ modules/rv-experiment/tests/ -q` — todos os módulos afetados passam.

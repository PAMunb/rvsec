<!-- Subagent dispatch hints — OWN BY FILE, NOT BY DEFECT.
     The defect boundaries and the file boundaries do not coincide, and it is the file matrix that
     decides what can run in parallel. Files touched by more than one group:
       - static_analysis_parser.py  ← 2.0 (reads the key), 4.1/4.2 (removes the normalizer)
       - analysis/static/static_analysis.py ← 2.6 (wires the gate), 3.3 (source consumers), 3.8 (cache key)
       - RvsecAnalysisClient.java ← 1.4b (isAppClass) and 1.4(b)'s net count, which calls it — one owner
       - experiment/workflow/pre_processor.py ← 3.5 (three App( sites), 6.1-6.6, 7.2
         "different regions" does not save it: 3.5's App( at :488 sits INSIDE the :484-492 block 6.2 removes
       - modules/rv-static-analysis/CLAUDE.md ← 4.8, 7.1 and 7.3, on adjacent bullets
       - rv-experiment/__main__.py ← 3.4 (the CLI pair) and 6.5 (the resume paths)
       - tests/experiment/test_pre_processor.py ← 3.12 and 6.8
     Also shared: domain/task.py (Groups 2 and 5), result_processor.py (Groups 2 and 8), and 4.4
     deletes inside rv-android-core, which is Lane K's module — 4.4 belongs to K, not P.

     Four lanes, three concurrent from t=0:
       Lane J (Java)   — 1.1-1.9, including 1.4b/1.4c. One reactor build carries ALL THREE Java edits
                         (guard, writer/enricher, isAppClass). Zero Python — except 1.4(a)'s argv half,
                         which is one line in rv-static-analysis/config.py and belongs to whoever lands 1.4.
       Lane P (parser + static analysis) — owns static_analysis_parser.py and static_analysis.py:
                         4.1-4.3, 4.5, 4.7 at t=0, then 2.0 once J freezes the artefact contract,
                         then 2.4-2.8, then 3.3/3.7/3.8, 4.10.
       Lane C (workflow) — owns pre_processor.py and its test file: 7.2, 6.1-6.9, 3.5's sites in
                         that file, and 3.12 (same test file as 6.8).
       Lane K (core)   — rv-android-core domain + the rv-experiment entry point: 3.1/3.2/3.4/3.6,
                         3.9-3.11, 3.13/3.14, 4.4, then 5.x, then 2.1-2.3, 2.9-2.11 (blocked on P's 2.0).
     Serial tail, not a lane: the docs pass (4.8 + 7.1 + 7.3) after P lands 4.1-4.3; the measurement
     runs (4.11 then 3.15); 4.6, 4.9; and Group 8 in three sessions — 8a columns (8.1-8.7),
     8b integration + measurement (8.8-8.10), 8c verification + docs + sync gates (8.11-8.17).

     Attribution binds the MEASUREMENT RUNS (3.15, 4.11), not the commits — which is why D2 and D3 may
     be built in parallel and must be measured separately. Run 4.11 BEFORE 3.15: it needs neither the
     jar nor D2, so measuring D3 on pre-D2 artefacts makes its delta more cleanly attributable.

     Critical path: 1.1/1.4/1.4b -> 1.5 -> 2.0 -> 2.1 -> 2.2 -> 2.3 -> 2.4 -> 2.5 -> 2.6 -> 3.7 -> 8.1-8.3 -> 8.4/8.5 -> 8.10.
     1.4b is ON the critical path, not beside it: 1.4(b)'s net count is computed with isAppClass, so the
     denominators 1.7/2.5/8.10 assert are the post-1.4b ones and cannot be measured before it lands. -->

## 1. D9 — The GATOR guard reads the code key, and the artefact records what produced it (Java reactor, ONE build)

- [x] 1.1 Extract `static String resolveScopeKey(String clientParam, String manifestPackage)` in `rvsec/rvsec-android/rvsec-gator/sootandroid/src/main/java/presto/android/AnalysisEntrypoint.java` and call it from the guard at `:119` (INV-ANA-65). It MUST strip the `codePackage=` prefix — `Configs.getClientParamCode` returns the **whole** parameter string (`Configs.java:292-302`), and `RvsecAnalysisClient.getCodePackage():244-250` already carries the strip every caller repeats. Four cases: parameter present → prefix stripped; `null` → manifest; empty after `=` → manifest (never `""`, which would make `startsWith("")` promote every library class); non-prefix → manifest
- [x] 1.2 Add `AnalysisEntrypointTest` in `sootandroid/src/test/java/presto/android/` pinning those four cases — no Soot, runs in the existing surefire. `AnalysisEntrypoint` has zero JUnit coverage today, and the prefix mistake compiles clean and fails silently
- [x] 1.3 Leave the `<activity>` rescue at `:112` and the `libPackages.txt` demotion at `:121` unchanged; comment that the rescue is no longer load-bearing **for the denominator** — it still promotes into `Hierarchy.appClasses`, so it remains load-bearing for the reachability predicates
- [x] 1.4 **(hoisted from the old task 3.7)** Make the artefact record the effective key, its origin and `class_defs_under_key` (INV-ANA-66). Three sub-edits, none of them mere wiring — verified 30/08 that `topLevelMetadata()` today returns exactly `manifestPackage`, `codePackage`, `mainActivity`, and that the enricher is handed three strings and sees neither the APK nor the Scene:
  - **(a) the origin needs a channel that does not exist.** `-clientParam codePackage=` is the only key crossing into GATOR (`config.py:395-397`); `App.code_package_source` lives on the Python side in a log line the run never writes to disk (`static_analysis.py:243,:448`, and INV-CORE-62 is why the log is not on disk). Add `-clientParam codePackageSource=<manifest|manifest-neutralized|detector>` on the emitting side and a `getCodePackageSource()` accessor beside `getCodePackage():244-250` on the reading side, with the same prefix strip
  - **(b) the compiled count needs a source.** `class_defs_under_key` is the **net** count: the classes under the key that survive `isAppClass`. Read the universe from `Scene.v().getClasses()`, not `getApplicationClasses()` — the demotion calls `setLibraryClass()`, which reclassifies without removing, so `getClasses()` is the compiled universe regardless of what the guard did. Apply `isAppClass` itself; do NOT reimplement the strip, and do NOT record the raw count, which no consumer can correct (the gate receives an `int`, not names)
  - **(c) the writer and the schema.** Feed the two new values into `ReachabilityEnricher`, emit them from `client/.../json/JsonReportWriter.java`, add the members to `JsonSchema.Keys`, and update `ReachabilityEnricherTest::topLevelMetadataReturnsExactlyThreeKeys` — the metadata map goes from three keys to five, so that assertion and its name both change
  
  This is a `client` edit and MUST ship in the same reactor build as 1.1
- [x] 1.4b **Generated resource classes leave the denominator at every segment** (INV-ANA-71). `RvsecAnalysisClient.isAppClass:289-298` anchors its test at the scope key's root — `suffix = className.substring(filterPackage.length())`, then `suffix.equals(".R") || suffix.startsWith(".R$") || suffix.equals(".BuildConfig")` — so `app.pachli.R` is removed and `app.pachli.core.database.R` is not, and a key that is an *ancestor* of the resource namespace escapes entirely. Measured over the 162 corpus artefacts: **505** such classes are in the denominator today (117 in `app.pachli_50`, 33 in `com.blacksquircle.ui_10028`, 6 in `com.cointrend_10304`), carrying 547 methods of which **zero** are non-trivial. Move the test to the class name's **last segment**: `R`, `R$*`, `BuildConfig`, `Manifest`, `Manifest$*`, wherever it sits under the key. Do NOT extend it to annotation-processor output (`_Factory`, `_Impl`, `_MembersInjector`, `$$serializer`, `Hilt_*`, `Dagger*`, DataBinding) — 5,816 classes and 36,264 non-trivial methods that execute; that is a redefinition of the denominator, recorded as an open question. **BREAKING for measurement**, and it is what makes 1.4(b)'s net count and the parsed side agree by construction
- [x] 1.4c Extend `ExtractClassesFilterTest` for 1.4b — it is now a real guard (its 32 assertions all call `isAppClass`), and its existing cases pin only root-anchored resource classes (`com.gh4a.R`, `com.gh4a.R$layout`, `com.gh4a.BuildConfig`), so they pass unchanged and say nothing about the repair. New cases: a module-level `app.pachli.core.database.R`; an ancestor key (`com.github.cvzi` over `com.github.cvzi.screenshottile.R$string`); `Manifest` and `Manifest$*`; and a negative case pinning `_Factory` / `_Impl` / `$$serializer` **inside** the denominator, so a later widening by analogy trips a test
- [x] 1.5 Rebuild the reactor: `mvn clean install -DskipMopAgent` under JDK 21 — **without** `-DskipTests`. No gator pom sets `skipTests` (the reactor default is `false`; only `-Pcheck` skips), so that flag was the only thing suppressing `ReachabilityEnricherTest` and `ExtractClassesFilterTest`, the two suites a mistake in 1.1/1.4 would trip. Confirm `rv-android/lib/gator/rvsec-gator.jar` and `rvsec-analysis-client.jar` were refreshed via `main.basedir` — by **sha256**, not mtime, and against the right reference: record the sha256 of each **deployed** jar before the build and verify it changed after. **Before running the build, copy the two deployed jars to `openspec/changes/gh111-cadeia-medicao/evidence/jars-pre/`**: the `install` phase overwrites `lib/gator/`, and 1.8 needs the pre-change jar to produce the "before" leg of its assertion — a sha256 records that the file changed, it does not let you run the old one. Without the copy, the `21` and the control's invariance are unobtainable after this task. Do NOT compare `client/target/` against `lib/gator/`: the fat jar is not byte-reproducible between assemblies, so those two can differ even when content is identical (measured 28/08: same size, same 22,064 entries and CRCs, different sha). Group 1 always ends with `install`, never `package` — the copy is bound to the `install` phase
- [x] 1.6 Regenerate the cryptoapp baseline after the rebuild (`modules/rv-static-analysis/tests/resources/cryptoapp.apk.json` + the `regenerated_at` / `regenerated_against_jar_sha256` of `baselines/MANIFEST.json`), or `tests/parity/test_baseline_freshness.py::test_baseline_not_older_than_jar` fires by construction. A regeneration diff is large and mostly meaningless — GATOR assigns window/widget ids non-deterministically — so only a changed **count** is a regression
- [x] 1.7 Run `D9Probe` (`docs/20260828_d9_colapso_denominador/D9Probe.java`) for `br.com.colman.petals_3040000`, `com.github.livingwithhippos.unchained_60`, `com.nononsenseapps.feeder.play_4025` and `com.github.cvzi.screenshottile_148`, predicting 771 / 1971 / 3589 / 550 under the code key, and `app.pachli_50` at 6467 under both guards. These are **raw** `startsWith` counts — the probe does not apply the client's `isAppClass` filter, so its numbers sit above the pipeline's by the generated-class count. **The gap is no longer the root-only one**: after 1.4b the filter removes `R`, `R$*`, `BuildConfig`, `Manifest`, `Manifest$*` at every segment, so 762 / 1952 / 3578 — derived under the old root-only rule — are superseded. Record the decomposition this task measures (raw, minus root-anchored generated, minus segment-anchored generated) and carry **that** number into 2.5 and 8.10 rather than the old one. This is the **prediction**, not the acceptance: the probe imports nothing from `presto.android`, reimplements the demotion predicate and prints both guards without mutating the Scene, so editing `:119` changes its output by zero
- [x] 1.8 **Acceptance.** Run `rv-static-analysis analyze --apk <apk> --output <dir> --package-detector` on `com.github.cvzi.screenshottile_148` against the rebuilt jar and assert `len(reachability) == 550`, against `21` under the pre-change jar. Use **two distinct `--output` directories** for the pre/post runs: the cache treats mere existence as a hit (`static_analysis.py:327-335`, "We do not validate content") and the filename carries no key (`:197-200`), so an A/B in one directory returns the first run's artefact. (The bare `rv-static-analysis --package-detector` form does not analyse anything — without a subcommand `main()` prints help and returns 1, `__main__.py:543-545`.) The detector elects `com.github.cvzi`, so this exercises the repaired guard end to end with no D2 anywhere — and the asserted number is the one measured **after 1.4b**, not 550. The old 550 was the ancestor-key case of exactly the leak 1.4b closes: the elected key `com.github.cvzi` is an ancestor of the resource namespace, so `com.github.cvzi.screenshottile.R` and its `R$*` carried the suffix `.screenshottile.R`, which the root-only strip of `isAppClass` (`RvsecAnalysisClient.java:289-298`) did not match — they were **in** that 550. Pinning it would have made this change's D9 acceptance number a contaminated denominator. Measure the post-1.4b value on the pre-change and post-change jars alike and assert the **movement** (collapsed → full universe) plus the recorded value; the pre-change leg runs against the jars preserved in `evidence/jars-pre/` by 1.5. For the invariance control, do NOT use `app.pachli_50`: no `libPackages.txt` pattern matches `app.pachli.*` (the only `app`-initial entries are `apparat.*`), so the demotion loop never runs and the control cannot fail — and its 6467 is unreachable by this command anyway (the detector elects `app.pachli.core`, 2466 classes). Use one of the six live-guard controls named in `docs/20260828_d9_colapso_denominador.md:133-135` (e.g. `me.zhanghai.android.untracker`): code package matching a `libPackages.txt` pattern with **no** build-type suffix, so the demotion loop really runs and the pre/post keys coincide — assert `len(reachability)` invariance across the rebuild; this control fails if the repair widens the guard. Name the reproducibility inputs in the run record: the api-35 `android.jar`, and whether the APK is the original or the instrumented one (`#AppClasses` is 3711 vs 3963 on petals)
- [x] 1.9 Commit the probe outputs and the two acceptance run logs to `openspec/changes/gh111-cadeia-medicao/evidence/`; each MUST show the value asserted in 1.7/1.8, and each acceptance log MUST show the line `Executing analysis` (`static_analysis.py:338`) — never `Analysis result already exists, skipping` (`:329`), which would mean the run answered from cache

## 2. D1 — Accounting: discard counters and the denominator gate

- [x] 2.0 **(hoisted from the old task 3.7, parser half)** Make `StaticAnalysisParser` read the effective key, its origin and `class_defs_under_key` back from the artefact, and mirror the new keys in `_JK` at `static_analysis_parser.py:79` — `tests/parity/test_json_keys.py` compares the Java `JsonSchema.Keys` constants against that mirror and will go red otherwise. Blocked on 1.4/1.5: the parser reads the shape the writer emits
- [x] 2.1 Add `unmatched_out_of_scope`, `unmatched_in_scope` and `unmatched_unclassified` to `ParserDiagnostics` — which lives in `modules/rv-android-core/src/rv_android_core/domain/coverage.py:453`, **not** in `rv-coverage` (its docstring records why it cannot move: `rv-android-core` cannot import `rv-coverage`) — and include all three in `to_dict()` (INV-ANA-68)
- [x] 2.2 Give `LogcatRepository` (`domain/coverage.py:559`, **there is no `CoverageData` class in the tree**) the effective scope key at construction, sourced from the artefact's recorded key and never re-derived (INV-ANA-58, INV-CORE-60). Enumerate the **eight** no-argument construction sites across **three** modules — `domain/task.py:576,594,615,679`; `rv-coverage/analysis/coverage/analyzer.py:100`; `tracker.py:157`; `parser/log/logcat_parser.py:141`; `rv-platform/components/coverage.py:58` — several on the resume path where no artefact is loaded. The construction inside `parse_logcat_file` (`logcat_parser.py:141`, already counted) is reached by seven callers that must thread the key through its signature: `result_processor.py:377-378`, `rv-coverage/analysis/coverage/analyzer.py:378`, `domain/task.py:698,:708`, `rv-platform/components/coverage.py:142`, `scripts/regenerate_results/regenerate_container.py:151` and `scripts/migrate_logcat_files.py:84`. The key is `Optional[str]` **with default `None`** — declared without a default, every production and test site breaks at once; `None` is the state of all 162 existing artefacts
- [x] 2.3 Classify and count both discards at `domain/coverage.py:640-674`, keeping the `logger.debug` lines. A key of `None` counts as `unmatched_unclassified` — never silently as in-scope
- [x] 2.4 Create `check_denominator(...)` in `modules/rv-static-analysis/src/rv_static_analysis/analysis/static/denominator_gate.py` — a module-level function (not a gate class: the module has nine files, no `exceptions.py`, and every existing check is an inline guard — P1) raising `DenominatorImplausibleError`, defined in `denominator_gate.py` itself and subclassing `RVAndroidError`. It must NOT import `StaticAnalysisException` from `static_analysis.py`: that module imports the gate (2.6), so importing its error type back is a cycle. It reads `class_defs_under_key` **from the artefact**, where 1.4(b) already recorded it **net** — the compiled classes under the key that survive `isAppClass`, the same predicate that filtered the parsed side. The gate divides and MUST NOT subtract: it holds an `int`, and filtering by name needs the names (INV-ANA-66, INV-ANA-69)
- [x] 2.5 Fix the threshold at **0.15** on that corrected ratio and record the separation with a **calibration script over the 162 corpus APKs, offline**: parse `class_defs` straight from the DEX headers (`zipfile` + `struct`, ~1 min for the corpus) and apply the corrected ratio under the **neutralized** key — the 162 stored artefacts do not carry `class_defs_under_key` (only post-rebuild artefacts do, task 1.4), and re-running GATOR over 162 is 81–243 h, so the calibration input cannot come from where the production gate reads (2.4 stays as written: in production the gate is a pure artefact predicate). Pass criterion, not just a recorded value: it MUST refuse all 4 collapsed and admit all 158 others; record `min(admitted)` (measured 30/08: 1.0 — all 158 healthy sit exactly at 1.0 on the corrected ratio, because the corrected ratio *is* the client's own filter re-derived) and `max(refused)` (re-derive under the post-1.4b filter; it was 0.0393 for `com.github.cvzi.screenshottile` at 21/535 under the root-only rule). The degenerate example is 1 of the pipeline count for petals — take it from 1.7's decomposition, measured under the post-1.4b filter, not from the superseded 762 (which subtracted only the root-anchored generated classes). Note that the **separation is invariant to the strip rule**: both terms of the ratio answer to `isAppClass`, so widening it moves the absolute denominators and not the bands. The calibration holds **under the neutralized key**: under the literal manifest key 75 of 162 have no compiled class at all (0/0), which is why 2.6 lands warn-only. The raw (uncorrected) ratio does not separate the bands — the healthy floor is 0.5610 (`org.cry.otp_31`, 23/41), and `com.tananaev.passportreader` is 18/28 = 0.6429 because ten of its classes are `.R` and `R$*`
- [x] 2.6 Wire the gate at `StaticAnalyzer.analyze()`, which holds both the APK and the key; the parser stays keyless per INV-ANA-61. Note the wiring is not free: `analyze()` (`static_analysis.py:216-269`) does not read the artefact today — the parse happens only in `get_static_data()` (`:418`) — so this task adds an artefact read (of the recorded key and `class_defs_under_key`) that does not exist yet. Land it **warn-only** in this group and flip it to raise in Group 3, after D2 — between the two, 75 of 162 APKs have zero compiled classes under the manifest key and a raising gate would abort 46% of the corpus. The wiring has its own acceptance in 2.8 (`test_gate_warns_at_analyze`)
- [x] 2.7 Handle `compiled_under_key == 0` as its own named refusal, raised **before any division** — the same `DenominatorImplausibleError`, with a message naming the zero universe; the gate must never leak a bare `ZeroDivisionError`. It is the state of 75 of 162 APKs under the default policy
- [x] 2.8 Add unit tests: the three split counters, serialization, gate refuses empty, gate refuses degenerate (1 of 762), gate refuses zero-universe, gate admits a genuinely small app (`com.tananaev.passportreader`, whose artefact records the net 18 against 18 parsed — the gate divides, it does not subtract), legacy artefact with no recorded key counts unclassified. Plus the wiring tests, both exercising `analyze()` and not the bare function: `test_gate_warns_at_analyze` — a `StaticAnalyzer` whose artefact carries `class_defs_under_key = 762` — a **net** count, as 1.4(b) records it — and 1 parsed class MUST emit exactly one `logger.warning` naming parsed count, compiled count and key, and MUST still return success — and `test_gate_raises_after_flip` on the same fixture, added when 3.7 lands. Without them, 2.6 can be skipped entirely — or written and never called — and every other test here stays green
- [x] 2.9 Keep INV-ANA-62's identity intact — no restatement, no relocation. The three `unmatched_*` counters MUST NOT enter `ParserDiagnostics.discarded_lines` (`coverage.py:541-556`): they count lines that **did** become records, which is exactly the reason its docstring already excludes the sentinel and grammar counters ("those lines did become records, so adding them would double-count against lines read"). With the counters outside that sum, "records registered plus counted lines equals lines read" (`coverage.py:461-462`) holds unchanged. The guard tests live in `rv-coverage` — `tests/parser/log/test_logcat_parser.py:1089,:1104,:1113` — and MUST stay green; 2.11 runs that module
- [x] 2.10 Run `/rv-doc-code modules/rv-static-analysis/src/rv_static_analysis/analysis/static/denominator_gate.py`
- [x] 2.11 Run `/rv-test-run rv-android-core` (which is where both changed classes live), `/rv-test-run rv-static-analysis` and `/rv-test-run rv-coverage` — the last is where the INV-ANA-62 identity tests live (2.9) and where 2.2 edits three files

## 3. D2 — Build-type suffix neutralization as a run policy

- [x] 3.1 Create `modules/rv-android-core/src/rv_android_core/util/android/build_type_suffix.py` with `BUILD_TYPE_DENYLIST`, `MIN_SEGMENTS = 2` and `neutralize_build_type_suffix()` — lowercase comparison, repeated application, two-segment floor (INV-CORE-58)
- [x] 3.2 Add `strip_build_type_suffix: bool = False` to `App`; apply it in `code_package`; extend `code_package_source` with `"manifest-neutralized"`, reporting `"manifest"` when the policy removed nothing (INV-CORE-18 modified)
- [x] 3.3 Update the existing `code_package_source` consumers for the third value: `rv-static-analysis/analysis/static/static_analysis.py:88,235,243,448` and `rv-platform/tests/test_platform.py:144`
- [x] 3.4 Add the `ENV_STRIP_BUILD_TYPE_SUFFIX` constant to the core registry and the negatable CLI pair to `rv-experiment run` and to `rv-static-analysis`, resolved through the shared truthiness helper, precedence CLI > env > `False` (INV-EXP-35). Document it in `README.md` **and** `.env.example` in the same task — `scripts/check_env_vars_drift.py` check 3 (`:232-245`) concatenates the two files and reports one violation naming both when the value is documented in neither, so task 3.11's drift check fails otherwise
- [x] 3.5 Propagate the resolved value from `ExperimentConfig` to `PlatformConfig` and to every `App(` construction site **except** the ajc instrumenter (INV-EXP-36). **Ten** sites, and the six on the production path come first: `rv-experiment/src/rv_experiment/experiment/workflow/pre_processor.py:347,:469,:488` (pre-processing — `:488` sits inside the block 6.2 removes, one owner for both), `rv-platform/src/rv_platform/platform.py:233` (task construction) and `rv-static-analysis/__main__.py:347,:425` (the standalone CLI); then `rv-android-core/util/utils.py:315` (`get_apks`) and three in `scripts/` — `static_analysis_sweep.py:465`, `static_analysis_sweep_generic.py:465` (both read `code_package` and feed the GATOR argv; they are the tooling that produced the corpus this change exists to repair) and `gh51_smoke_test.py:114` (same shape: `App(apk_path)` at `:114`, `code_package` at `:115`, `codePackage=` at `:52`).

  **Two sites the earlier list carried and that MUST NOT be touched**, verified 30/08 by reading them: `scripts/sa_parallel.py:128`, whose own comment says *"Only sdk_target is read here, so the package policy is immaterial"* — passing the kwarg would contradict a correct comment (P4) and change nothing; and `scripts/validation/fase_a_preprocess.py:182`, whose `App` is handed straight to `AjcInstrumentation.instrument(app, …)` at `:184`, so propagating the policy there is a direct violation of INV-EXP-36, the very invariant this task cites. Its comment already records the reason: *"Instrumentation reads app.path, not code_package: the default applies."* (`scripts/gh91_sa_rerun.py` is **not** a site at all — its only `App(` is inside the docstring at `:10`, explaining why the script bypasses `App.code_package` and passes `Mneut` verbatim.)
- [x] 3.6 Record the resolved policy in `experiment_config.json`
- [x] 3.7 Flip the denominator gate from warn-only to raise, now that a correct key is available (completes 2.6)
- [x] 3.8 Make a stored analysis artefact reusable only under its own key: the cache check at `analysis/static/static_analysis.py:323-335` treats mere existence as a hit and says so — *"We do not validate content"* — the filename carries no key (`:198-200`), and `--force` is dead code (`rv-static-analysis/__main__.py:214,236`, declared and never read). A recorded key that differs from the run's effective key MUST regenerate or abort naming both (INV-ANA-70)
- [x] 3.9 Record the deliberate ajc divergence in `modules/rv-instrumentation-ajc/CLAUDE.md`, citing the inert anti-quarantine guard at `ajc_instrumentation.py:854-885`. Note the asymmetry the exclusion does not cover: ajc is excluded from this policy but not from `package_detector` (`ajc_instrumentation.py:193`), so the guard activation cited as the reason already happens today under `--package-detector`
- [x] 3.10 Add unit tests: single suffix, stacked `.qa.debug`, capitalized `.BETA`, two-segment floor, policy off by default, an uncovered suffix reaching the gate
- [x] 3.11 Add integration tests: policy reaches the GATOR argv; policy does not reach ajc; artefact records the key and `class_defs_under_key`; a key-mismatched artefact is not reused; `check_env_vars_drift.py` reports zero violations
- [x] 3.12 Update the tests this group's changes invalidate: six tests in `modules/rv-experiment/tests/experiment/test_pre_processor.py` (which has 29 in all) stub `App` with a fixed-arity `lambda app_path, package_detector=False:` (`:97,116,133,148,166,185`), so the new kwarg raises `TypeError` in those six; one of them additionally asserts exact call kwargs — `MockApp.assert_called_once_with(app_path=…, package_detector=False)` at `:190-192`, whose own docstring says "Asserted on the call, not the result" — so widening the lambda alone does not fix it; and `modules/rv-android-core/tests/util/test_utils.py:412-415,427-430` assert `App` call args exactly
- [x] 3.13 Run `/rv-doc-code modules/rv-android-core/src/rv_android_core/util/android/build_type_suffix.py`
- [x] 3.14 Run `/rv-verify rv-android-core`
- [ ] 3.15 Re-run the analysis over a **named** sample of the 75 suffixed-key APKs and record the delta against a **stated baseline** — this is the measurement attributable to D2. **Name the baseline explicitly, because the two candidates give opposite verdicts.** The baseline MUST be a fresh run under the default policy, whose artefact is **empty**: `static_analysis.py:277` passes `code_package=self.app.code_package`, which since gh98 (`553ae54a`, 05/08) is the declared applicationId, and `isAppClass` on a suffixed key matches no compiled class — so today's production path writes zero `reachability` entries for these APKs. It MUST NOT be the stored corpus artefacts: those carry 707, 203, … classes because they were produced when `code_package` ran `PackageDetector` unconditionally (pre-gh98), a pipeline configuration that no longer exists, so measuring against them shows D2 as a no-op for all but the four collapsed APKs. Against the stated baseline the predicted movement is 0 → N for every APK in the sample. The population is enumerable without a run: `openspec/changes/archive/2026-08-16-gh102-artifact-scoped-parse/corpus_verification.csv` has exactly 75 rows with `classes_before == 0`, and `evidence/calibration/calibration_rows.json` records **zero** compiled classes under the manifest key for every one of those 75 — so the baseline's zero is established over the whole population from the DEX headers, and the fresh run **demonstrates** it rather than discovering it.

  **The sample: 12 suffixed APKs plus one negative control.** The suffix literals present in the 75, measured 30/08, are `.debug` 61, `.dev` 7, `.beta` 4, `.BETA` 1, `.current` 1 and `.qa.debug` 1 — six literals, of which `.BETA` is the capitalized case and `.qa.debug` the stacked one, **each with exactly one member**. The 12 MUST cover all six, which forces `com.vrem.wifianalyzer_71` (`.BETA`), `eu.opencloud.android_9` (`.qa.debug`) and `app.pachli_50` (`.current`) into the sample. **The fourth family an earlier revision asked for — "at least one the denylist does not resolve" — does not exist in this population**: all 75 resolve. The APKs no string rule resolves live in the broad corpus, so that criterion becomes a **negative control drawn from outside the 75**: `de.grobox.liberario_131` from `APKS_INSTRUMENTED_jca_dexlib2_experimento-20260706_selected181`, whose manifest declares `de.grobox.liberario.debug` while its 9765 compiled classes carry **0** under `de.grobox.liberario` and 519 under `de.grobox.transportr`. The denylist **does** fire here and removes the `.debug` — and the neutralized `de.grobox.liberario` still matches nothing, because the residual mismatch is a package rename no string rule reaches. That is what makes it the right control: it shows D2 doing its work and still being insufficient, with the gate as the backstop, rather than showing D2 not running at all.

  **The predicted N is `compiled_net`** from `evidence/calibration/calibration_rows.json` — the compiled universe under the neutralized key, net of the generated classes the segment-anchored `isAppClass` of INV-ANA-71 removes. It MUST NOT be `classes_after` from the gh102 CSV, which is the root-anchored count and disagrees wherever a resource class sits below the key's root (`eu.opencloud.android_9` is 2600 and not 2644; `app.pachli_50` is 6336 and not 6453) — pinning a root-anchored prediction in a change whose subject is the denominator would ratify the very leak INV-ANA-71 closes, which is the reason task 1.8 abandoned its asserted 550. The warrant is that acceptance run: `compiled_net` predicts 535 for `com.github.cvzi.screenshottile_148` and the live run produced exactly 535.

  **Assertions.** At least **10 of the 12** suffixed APKs move 0 → N in the predicted direction; a case that does not move is a finding, not a pass. The negative control MUST **not** move — both legs 0 — and MUST be refused by the denominator gate, which is what makes it evidence for INV-ANA-69 rather than a gap in the denylist.

  **Cost.** Every leg is capped by `--analysis-timeout`, so the campaign's worst case is arithmetic rather than estimate. The two legs are radically asymmetric, and the asymmetry is the repair itself: the baseline runs under a key that matches nothing, so every class is demoted and Soot analyses almost nothing, while the treatment keeps the application's whole class universe in the Scene. **The gh91 anchor's "12–120 GB per APK" is not disk.** Measured 30/08 on this machine, peak scratch is the apktool extraction at 0–117 MB per run plus a 1.6 MB artefact; the figure is the JVM heap (`jvm_memory` defaults to `"12g"`). The binding constraint is therefore RAM and not the volume — 12 GB per concurrent leg against 123 GB — which makes the campaign parallelizable six ways and puts its 26 legs inside roughly 3 h of worst case, where a serial reading gives 6–18 h. Record the wall clock of every leg: the sample spans 51 to 7016 classes, so it is also the first measurement of how analysis cost tracks the surviving class count.

- [x] 3.16 Make `--force` real and specify it. The flag was declared on both subparsers (`modules/rv-static-analysis/src/rv_static_analysis/__main__.py:214,:236`) and read nowhere, so a run asking for a fresh analysis silently got the stored one — which is why INV-ANA-70 was able to cite it as *evidence that no invalidation path exists*. It is now honoured by `_discard_cached_artifact`, and the requirement says so rather than leaving a working flag that no artefact decides. The two invalidation paths are not substitutes and the spec scenario states why: the key comparison of INV-ANA-70 fires only when the keys **disagree**, so it cannot serve an operator re-measuring the *same* key against a rebuilt jar — which is exactly what tasks 1.8 and 3.15 do, and the trap both of them dodged by using two output directories instead. Test: `TestArtifactReusedOnlyUnderItsOwnKey::test_force_discards_an_artefact_the_cache_would_have_answered_with`

## 4. D3 — Remove the normalizer

- [x] 4.1 Back up `signature_normalizer.py` and its test module to `backup/` (tracked in git) **before** deleting — P3 mandates it and the convention is live (`backup/gh105-retired/`, `backup/gh51-deprecated-modules/`, `backup/gh62-disabled-stub-tests/`)
- [x] 4.2 Remove both normalizer calls in `modules/rv-static-analysis/src/rv_static_analysis/parser/static/static_analysis_parser.py` — `:371` (class names) and `:455` (window names) — together, plus the import at `:156` and the `self.normalizer` field at `:203` (INV-ANA-67)
- [x] 4.3 Delete the docstring claims at `:11`, `:24`, `:51`, `:183`, `:196` and `:370` describing the normalizer as a safety net — six sites, not three
- [x] 4.4 Delete `modules/rv-android-core/src/rv_android_core/util/android/signature_normalizer.py` and `modules/rv-android-core/tests/util/android/test_signature_normalizer.py`; confirm no re-export survives in any `__init__.py` (P3)
- [x] 4.5 Update the tests that break: five, not four — `test_dot_notation_normalized` plus the four of `TestNormalizerSafetyNet`, including `test_normalizer_is_noop_on_correct_json`, which monkeypatches the method and must be replaced by a test asserting the artefact spelling is preserved. Note that `test_normalizer_warns_on_change` is already vacuous (no `logger.warning` exists near `:371`), and that 4.4 additionally removes a module containing **50** tests
- [x] 4.6 Add the corpus witness test: `com.hwloc.lstopo_80283` — `com.hwloc.lstopo.ZoomView.ZoomView` must match the 1080 dotted `RVSEC-COV` events, and the dollar form must not occur. **Commit a trimmed fixture** — the 1080-event assertion is already exact, and a fixture is the only branch verifiable in CI; the 99 archived logcats under `RV_ANDROID_NOVO_DATASET` (outside the repo) stay as the manual cross-check, not the acceptance
- [x] 4.7 Add a grep gate asserting zero **imports or instantiations** of `SignatureNormalizer` across `modules/*/src` **and `scripts/`** — scope the pattern to import/instantiation lines: `scripts/e3_validate_emulator.py:254` mentions the name in a docstring and a prose-level match would trip on it (either scope the pattern or clean that docstring in the same task). Dispose of `scripts/gh91_gate.py`, which imports it at `:53`, instantiates at `:65` and calls it at `:80,:94`: migrate or retire it — **after 4.11 has run or captured its output** (the script is the instrument that produced the 465-class witness count). Its import guard raises `SystemExit("cannot import SignatureNormalizer … Run this under the workspace venv")`, so after the deletion it dies diagnosing a venv problem that does not exist
- [x] 4.8 Clear the deletion's documentation tail — `modules/rv-android-core/CLAUDE.md`, `modules/rv-android-core/docs/architecture.md` (one inventory line at `:797`; its decision blocks are AD-1…AD-8, no ADR lives there), `modules/rv-static-analysis/CLAUDE.md`, `modules/rv-static-analysis/README.md`, and `modules/rv-static-analysis/docs/architecture.md` — which is where **ADR-5: SignatureNormalizer as Defensive Safety Net** actually lives (`:71`), asserting the diagnosis this change inverts; also its INV-ANA-02 traceability line at `:25` and the test count at `:628`. Module `architecture.md` ADR blocks carry no Status field, so supersede ADR-5 with a dated correction note in place (or a proper `docs/adr/` record via `/rv-doc-adr`), not by silent deletion
- [x] 4.9 Record the four base-spec references that must be **hand-edited at sync/archive time** — `openspec sync`/`archive` cannot deliver them: the merge engine rebuilds only the `## Requirements` section of the base spec (verified 30/08 by executing the tool's `buildUpdatedSpec`; gh104's task 10.8 records the same limitation), and all four targets sit outside it. They are: the directory-tree line at `core/spec.md:43` (`## Purpose`), the key-concept item at `analysis/spec.md:96` (`## Purpose`) which states the inverted diagnosis as fact, INV-ANA-02's body at `analysis/spec.md:329` (`## Invariants`), and the word *normalized* in INV-ANA-60 (`analysis/spec.md:394`, `## Invariants`), which mandates the behaviour INV-ANA-67 forbids. Execution is task 8.17, alongside the invariants sync
- [x] 4.10 Run `/rv-test-run rv-static-analysis` and `/rv-test-run rv-android-core`
- [x] 4.11 Re-parse the 7 affected corpus artefacts and record the delta — the measurement attributable to D3. The seven, with their corrupted-class counts: `com.tk.quicksearch_65` (305), `com.afkanerd.deku_83` (75), `app.michaelwuensch.bitbanana_79` (35), `de.luhmer.owncloudnewsreader_196` (19), `com.gelakinetic.mtgfam_99` (15), `com.smartpack.packagemanager_79` (14), `com.hwloc.lstopo_80283` (2). Assert all 7 APKs and all 465 classes move in the predicted direction, and record the **window counts** too — the second removed call site (`:455`) governs ACTIVITY window admission, so window counts may move and an unrecorded shift would be unattributable later. **Run this before 3.15** (it needs neither the jar nor D2, so measuring D3 on pre-D2 artefacts makes its delta more cleanly attributable) and **before 4.7 retires `scripts/gh91_gate.py`**, the instrument that produced the 465 count

## 5. D4 — Serialize what is already counted, and make it reach disk

- [x] 5.1 Serialize `TaskResult.write_errors` in `to_dict()` and read it back in `from_dict()` in `modules/rv-android-core/src/rv_android_core/domain/task.py`. The field is `Dict[str, int]` (`:354`), a per-artefact loss **count** consumed by `result_processor.py:709` — not a `list[str]`; treating it as a list would destroy the count INV-PLT-32 exists to preserve (INV-CORE-61)
- [x] 5.2 Add the persistence step, without which 5.1 is inert: `_count_write_error` is called only from inside result processing (`result_processor.py:362,397,695,1085`), and no `task_storage` save happens after `Platform._process_results()` — `platform.py:181` saves per task at `:426`/`:461`, `:192` processes results, `:197` summarises, and nothing saves after. Add the save in `_process_results()` after `processor.execute({})`, and in the standalone `--process-results` path (`rv_platform/__main__.py:497-514`). `get_completed_tasks()` returns references (`task_storage.py:494-501`), so the mutations are already visible to the store; only the write is missing
- [x] 5.3 Create a production call to `LoggingManager.setup_file_logging` at an entry point; do not attempt to re-enable the self-referential guard at `manager.py:147`. Note that `configure_output` **does** have two production callers (`rv-experiment/__main__.py:137`, `rv-platform/__main__.py:200`) — both pass `file=False`, so the repair may be that argument plus an explicit call, not a missing caller (INV-CORE-62)
- [x] 5.4 Delete `LogcatRepository.diagnose()` under P3: zero production callers, and the diagnostics that matter now reach disk through `ParserDiagnostics.to_dict()` and the summary columns. Back it up alongside 4.1's backups and update any test that calls it
- [x] 5.5 Add tests: `write_errors` round trip, a legacy `tasks.json` without the key still loads (defaulting to `{}`), a write error recorded during result processing is present in `tasks.json` after the run, the same on `--process-results`, and the effective key appears in the run's log file. Note for the persistence assertion: `_count_write_error` fires once per generator (`result_processor.py:473,:643,:787,:890,:1067`), so assert `>= 1` per artefact family, not `== 1`
- [x] 5.6 Run `/rv-test-run rv-android-core`
- [x] 5.7 Add `write_errors` to the retry-invariant exclusion in `modules/aperv-tool/src/aperv_tool/analysis/tasks_record.py` — `_payload` (`:275-287`) excludes only `state_transitions`, and two tied `COMPLETED` records that differ only in `write_errors` would raise `IdentityCollisionUnresolved` (`:315-323`) and abort the whole load; `write_errors` is per-run I/O noise, exactly the category the docstring excludes "by construction". One line plus a test, and it belongs to this change because this change is what puts the field on the wire

## 6. D10' — Pre-processing flags stop failing silently

*(All paths in this group: `modules/rv-experiment/src/rv_experiment/experiment/workflow/pre_processor.py`.)*

- [x] 6.1 Make `--skip-instrument` combined with `--static-analysis` abort naming both the flag and the missing `instrumented_apks/` directory (INV-EXP-37), at `pre_processor.py:318`
- [x] 6.2 Make the logged and executed APK sets agree (INV-EXP-16 modified); remove or correct the false fallback message at `pre_processor.py:484-492`. Note that 3.5's `App(` site at `:488` sits inside this block — one owner for both
- [x] 6.3 Make `get_instrumented_apks()` stop filtering: an APK without `.apk.json` executes, and the log states it will run without a coverage denominator instead of claiming an exclusion that never happens (researcher decision, 29/08). The returned list MUST stay non-empty when APKs exist — `experiment_controller.py:275-277` treats empty as a fatal "No APKs available for execution"
- [x] 6.4 Emit one consolidated report at the end of pre-processing naming the count and listing the APKs that got no static analysis artefact; the run continues (INV-EXP-39)
- [x] 6.5 Add a provenance marker to `out/monitors` recording the specification set, and abort on mismatch under `--skip-monitors` (INV-EXP-38). Specify the absent-marker case explicitly as **warn**, not abort: `__main__.py:1268,1283` force `generate_monitors = False` on both resume paths and no existing `out/monitors` carries a marker, so aborting on absence makes every existing experiment unresumable
- [x] 6.6 Give `--skip-static` its requirement (researcher decision, 30/08): under `--skip-static` the consolidated pre-processing report of INV-EXP-39 MUST state that static analysis was skipped **by flag** and name the count of APKs that will run without a coverage denominator; their coverage cells are empty per INV-PLT-35 and `measured` is false. The flag was named in the proposal's Modified Capabilities and in the delta's Data Contracts with no requirement behind it; the scenario lives in the experiment delta's Three-Phase Workflow block
- [x] 6.7 Add tests: skip-instrument abort, logged set equals executed set in the mixed case, an APK without `.apk.json` executes and still contributes violations, the consolidated report names `2 of 50`, the `--skip-static` report names the flag and the affected count (6.6), wrong-set monitors abort, absent marker warns, same-set monitors proceed with a log line
- [x] 6.8 Update the tests this group invalidates: `test_excludes_apks_without_json` (`:102`, asserts `len(result) == 1`) and `test_falls_back_to_originals_when_no_apk_has_json` (`:140`, asserts the fallback path fires)
- [x] 6.9 Run `/rv-test-run rv-experiment`

## 7. D9b — Documentation that contradicts the code

- [x] 7.1 Correct `modules/rv-static-analysis/CLAUDE.md:26` and `:28` — `:26` is the SignatureNormalizer safety-net claim that states the inverted diagnosis D3 removes, and `:28` is the `jca_android` sanity ruler whose numbers are wrong by 74% (`:27`, the granularity bullet, is correct and stays)
- [x] 7.2 Correct the `process()` header at `modules/rv-experiment/src/rv_experiment/experiment/workflow/pre_processor.py:83-90`, which claims static analysis runs on original APKs and depends on no earlier step
- [x] 7.3 Record in `modules/rv-static-analysis/CLAUDE.md` the two granularities the chain carries: targets seeded by class+name without parameters (LENIENT), coverage matched by complete Soot signature

## 8. Report columns, integration and verification

- [x] 8.1 Add `classes_total` and `methods_total` to `summary.csv` in `modules/rv-platform/src/rv_platform/components/result_processor.py` (INV-PLT-33)
- [x] 8.2 Add `unmatched_out_of_scope` and `unmatched_in_scope` as separate columns, empty when diagnostics were not produced (INV-PLT-34)
- [x] 8.3 Add the `measured` boolean column (INV-PLT-36, researcher decision 29/08) **and make it live in the same task** (researcher decision, 30/08). Empty cells alone do not carry the distinction into aggregates: a `pd.read_csv` cell becomes `NaN` and `.mean()`/`.groupby()` skip it silently, while the two `trim_mean` consumers do **not** skip `NaN` and return `nan` — `scripts/aperv_objective.py:76-78` and `scripts/analyze_calibration.py:186-192,:253` (the second is the one no earlier revision named). Edit both to filter by `measured == True` (or an explicit `.dropna()` that reports the surviving `n`) before aggregating, make `scripts/verify_phase.py:385-386` compute its informational coverage means over measured rows only, and append `"measured"` to `_PAYLOAD_COLUMNS` in `modules/aperv-tool/src/aperv_tool/analysis/loader.py:93-103`. Acceptance: a test in which the aggregate verdict changes when one row flips to `measured=False`. Two stale-schema notes for whoever edits: `verify_phase.py` has **no** coverage gate today (its gate at `:115` is over `errors`; the coverage check is `passed=True  # Informational`), and `aperv_objective.py` already raises `KeyError` against the current schema — it asks `summary.csv` for `cov_rv_method`, a `coverage.csv` column — so this task is also what makes it runnable at all. Not every consumer is pandas: three scripts read summary-schema CSVs with `csv.DictReader` + `float()` (`select_compat_apks.py:90` — frozen paper dataset, unaffected in practice —, `regenerate_results/verify.py:107`, `gh91_compare_consolidation.py:128-129`), where an empty cell raises `ValueError` rather than becoming `NaN`
- [x] 8.4 Write **empty** coverage cells — not `0.00` — for a task whose APK has no static analysis artefact, across **all six** percentage columns (`cov_act`, `cov_class`, `cov_method`, `cov_reachable`, `cov_reaches_target`, `cov_directly_reaches_target`; all six are 0/0 because `calculate_metrics()` returns early on empty classes at `coverage.py:742-746`), while still writing its violation columns (INV-PLT-35). The discriminator MUST be `metrics_dict.get("total_classes", 0) == 0`, available at `:899` — **not** `self._unresolved_task_ids`, which is populated only on the resume path (`:316` ← `:379` ← `:889-892`) and never on the live path. It MUST also **not** be the presence of a recorded scope key: all 162 stored artefacts record none and all 162 carry a non-empty `reachability` (1 to 14,860 classes), so keying `measured` on that would empty the coverage of every resumed and reprocessed task in the existing campaign — the measurement INV-PLT-15/16 exist to recover. A missing key costs the row its two `unmatched_*` cells (INV-ANA-68) and nothing else
- [x] 8.5 Apply the same rule to the other two artefacts that publish coverage percentages: `coverage.csv` — **two mechanisms, both must write empty**: the four row-constant `cov_*_final` columns built with `round(metrics_dict.get(..., 0) or 0, 2)` at `:483-492` (`cov_class`, `cov_reachable`, `cov_reaches_target`, `cov_directly_reaches_target`) **and** the three per-row percentages (`cov_act`, `cov_method`, `cov_rv_method`) computed in the row loop at `~:496-545` and written at `:555-557` — and `results.json` (`:1025-1035`, and the stale-read `else` branch at `:1050-1064`). While here, verify whether `scripts/gh91_compare_consolidation.py:466-467,:490` — `float(cell)` and an md5 over `coverage.csv` `row[8:12]` — is reachable with an empty cell, and record the answer
- [x] 8.6 Reconcile the second writer — header **and values**: `scripts/regenerate_results/regenerate_container.py:78-91` hard-codes a `SUMMARY_HEADER` under the comment "Exact headers from `result_processor.py`" and writes it at `:385`, and its row builder emits `round(metrics_dict.get("class_coverage", 0) or 0, 2)` (`:198-203`) — after 8.1-8.5 it would silently produce a narrower CSV **carrying `0.00` exactly where rv-platform writes empty**, and `experimento-20260706/scripts/consolidate_offline.sh` regenerates exclusively through it, so a whole regenerated campaign would resurrect the pre-change semantics. Under P1, import `SUMMARY_HEADER` and the emptiness rule from `result_processor.py` instead of duplicating both — the script already computes `missing_static_apks` (`:377,:406-418`), so it already holds the information the rule needs
- [x] 8.7 Add tests: denominators present, counters not summed, empty cells when unmeasured, `measured` true and false, and the pair of rows that distinguishes "no denominator" from "denominator of 705 covered zero times". Update the tests this group invalidates: `test_result_processor.py:1283-1285` and `test_resume_integration.py:675` both do `float(...)` on cells that become empty, and `test_summary_csv_zeros_when_no_data:658` asserts `row[4] == "0"`
- [x] 8.8 Run the end-to-end integration check on a small APK set with the policy on, confirming the key flows entry point → `App` → GATOR argv → artefact → parser → CSV
- [x] 8.9 Run the reactor's integration tests once with `-DskipITs=false` (`client/pom.xml:18` sets `skipITs=true`, so `BaselineComparisonIT`, `GenericSubtypeMatchingIT` and `RvsecAnalysisClientIT` never run in a normal build), and `RV_GATOR_REQUIRED=1 uv run pytest tests/parity/`
- [x] 8.10 Record the combined D9+D2+D3 delta against the verification baseline: assert 4/4 denominators at the values 1.7 recorded under the **post-1.4b** filter, and state them in the run record. The earlier figures **762 / 1952 / 3578 / 550 are superseded**: they were pipeline counts under the root-only strip, and 550 in particular *included* the leaked `com.github.cvzi.screenshottile.R*` classes that the ancestor key let through — the leak INV-ANA-71 closes. What MUST be asserted without re-derivation is the shape: each of the four moves from its collapsed count (1 / 2 / 6 / 21) to the full universe under its key, and the six live-guard controls stay invariant — 0 of 162 parsing to zero, 0 of 465 classes carrying `$`
- [x] 8.11a Update `scripts/regenerate_results/verify.py` check C3 to the directional form (INV-PLT-37): today it asserts `summary cov_* == 0 whenever coverage_rows == 0` (`:164`, described at `:9`), which after INV-PLT-35 is false for every unmeasured row — the cells are empty, not `0`. The two directions: no denominator → zero rows **and** empty cells with `measured=false`; denominator with nothing covered → zero rows **and** `0.00` with `measured=true`. This is the one mandate INV-PLT-37 carries that no other task owned
- [x] 8.11b Update the resume-health WARNING wording at `modules/rv-platform/src/rv_platform/components/result_processor.py:206` to "Resume coverage health: N/M resumed tasks had unresolved static data — coverage cells left empty for those tasks" (INV-PLT-18 as amended). The current text says "coverage zeroed", which names exactly the behaviour INV-PLT-35 forbids
- [x] 8.11 Run `/rv-qa-lint-fix` for `rv-static-analysis`, `rv-android-core`, `rv-experiment`, `rv-platform`, `rv-coverage`
- [x] 8.12 Run `/rv-verify rv-static-analysis`, `/rv-verify rv-android-core`, `/rv-verify rv-experiment`, `/rv-verify rv-platform`. The starting line is **green**: measured 29/08 with the CI invocation, `tests/parity/` is 204 passed and all 16 modules pass, after the five pre-existing failures were repaired. Any red here belongs to this change
- [x] 8.13 Invoke `/rv-code-reviewer` via the Skill tool
- [x] 8.14 Run `/rv-docs-sync rv-static-analysis` and `/rv-docs-sync rv-android-core`
- [x] 8.15 Write **rev. 6** of `docs/20260828_cadeia_medicao_rvandroid.md` — rev. 5 already exists and already folds in the four corrections the previous version of this task named, so it is a no-op as written. Rev. 6 folds in what this change's adjudication established (`docs/20260829_adjudicacao_revisoes_gh111.md`) and what the 30/08 verification re-measured (`docs/20260830_verificacao_consistencia_gh111.md`): C1 narrowed to the default policy, the probe demoted to a prediction, the raw-vs-delivered denominator distinction (771/1971/3589 raw, delivered counts re-derived under D9c's segment-anchored generated-class filter — 762/1952/3578 was the root-only figure and is superseded), the D9c leak itself and why task 1.8 no longer pins 550, the delta-to-base spec failures, the gh104 archiving collision, and the `libPackages.txt` debt restated as covering three of six published denominators
- [ ] 8.16 **Archive-order gate.** `gh104-legible-violation-reports` MUST be archived before this change syncs: this change's `Result Generation (FR14)` block was copied from gh104's version (not the base), and its INV-PLT-19 restatement replaces gh104's text by name. Before applying anything to `openspec/specs/`, verify `grep -c 'code, event' openspec/specs/platform/spec.md` is non-zero and that the base INV-PLT-19 carries the thirteen-column `errors.csv` — if either fails, stop and archive gh104 first
- [ ] 8.17 **Hand-sync of what the merge engine cannot deliver** (in the mould of gh104's task 10.8; verified 30/08 by executing the tool's `buildUpdatedSpec`: `openspec sync`/`archive` rebuilds only the `## Requirements` section, and delta `## Invariants` sections are silently discarded). Apply by hand to `openspec/specs/`: the four `## Invariants` sections of this change's deltas (INV-ANA-65..71, INV-CORE-58..62, INV-EXP-35..39, INV-PLT-33..36 and INV-PLT-37 — **22** new ids), plus the restatements of INV-CORE-18, INV-PLT-19 and INV-EXP-16, and the **amendments** to INV-PLT-15, INV-PLT-16 and INV-PLT-18. The amendments are clause-level: apply only the clause each delta names and leave the rest of the base body standing — INV-PLT-15 keeps its directory derivation, memo and `_unresolved_task_ids` rules, INV-PLT-16 keeps the writer unification and the removed cascade, INV-PLT-18 keeps the round-trip equivalence. **INV-PLT-17 of the base MUST NOT be touched**: it says `cov_class` holds `class_coverage`, and the directional consistency rule this change adds is INV-PLT-37, deliberately a different number (the base misattributes C3 to INV-PLT-17 in a scenario bullet at `platform/spec.md:521`, which this delta's own scenario corrects); the platform delta's Data Contracts additions; the removal of INV-ANA-02 from `analysis/spec.md:329`; and the four hand-edits task 4.9 records (`core/spec.md:43`, `analysis/spec.md:96`, `analysis/spec.md:329`, INV-ANA-60's *normalized* at `analysis/spec.md:394`). Then verify by grep: all **22** new invariant ids exist under `openspec/specs/`; the restated and amended invariants carry the new text **and** the base clauses the amendments do not touch; `INV-PLT-17`'s base text (`cov_class` = `class_coverage`) is still present and unchanged; `INV-ANA-02` is gone; and no `## Invariants` bullet of any delta is missing from the base

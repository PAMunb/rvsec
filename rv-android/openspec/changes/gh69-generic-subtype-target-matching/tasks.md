<!-- Scope: Java (rvsec-mop-extractor + rvsec-gator commons/client), ~10-12 files — includes
     rvsec-mop-extractor/pom.xml (task 1.0: the module declares no test dependency today) and
     ReachabilityEngine, which must be extended to carry the declared Set<TargetMethod> to the bytecode
     scan (cascade; see design D-DataFlow §5). Below 20 files → no subagent orchestration needed.
     Critical path: 1 (extractor) → 2 (commons/source + helper) → 3 (wire 2 match points + cascade) →
     4 (ordered rebuild + IT on real scene) → 5 (verify + review).
     The rv-* component skills are Python-oriented; Java verification uses mvn/JUnit. /rv-code-reviewer
     is language-agnostic and is retained. Refs: proposal.md, specs/analysis/spec.md (INV-ANA-40..44),
     design.md (D1-D7), ADR docs/adr/0004, risk-register.md (RISK-001 High, RISK-003). GitHub #69. -->

## 1. Extractor: wildcard / `+` / name-pattern (rvsec-mop-extractor) — INV-ANA-40, INV-ANA-41

- [ ] 1.0 **Test infrastructure (prerequisite for 1.4–1.6)**: `rvsec-mop-extractor` has no `src/test` directory and declares no test dependency (verified 2026-08-21), so the unit tests below cannot run as written. Declare the `junit` dependency in `rvsec/rvsec-mop-extractor/pom.xml` — the version comes from the root reactor's `dependencyManagement` (junit 4.13.2), **do not pin it locally** — and create `src/test/java` + `src/test/resources`. Fixtures read the real spec directories under `rvsec/rvsec-mop/src/main/resources/{jca,jca_android,generic_new}` (RISK-012)
- [x] 1.0b **Spec repair (prerequisite for the 1.4 gate)**: `rvsec/rvsec-mop/src/main/resources/generic_new/CharSequence_NotInSet.mop` declared owner `Set+` (i.e. `java.util.Set`) while importing only `java.io.*`, `java.lang.*`, `java.nio.*`. Under the import-driven resolution rule of 1.3(b) that owner resolves to nothing, so the spec would contribute **zero** targets, net coverage would be 23/27 instead of 24/27, and the "skip-count == 0" gate of 1.4 and the "20 owners" gate of 4.3 would both be false. Repair: add `import java.util.*;` to that file (done 2026-08-21; re-verified: 20 non-constructor owners, **0** unresolvable). This is the only corpus edit in this change — do NOT touch `jca`/`jca_android`, which are frozen. Note the general lesson for RISK-006: an owner being a JDK class does not make it resolvable; its own spec must import the package
- [ ] 1.0c **Build infrastructure — the gator skips its own tests by default (measured 2026-08-21)**: `rvsec/rvsec-android/rvsec-gator/pom.xml:18` sets `<skipTests>true</skipTests>`, overriding the reactor root's `false` (`rvsec/pom.xml:21`) for `commons`, `sootandroid` and `client`. Maven's failsafe honours `skipTests` too, so `-DskipITs=false` alone does **not** enable the ITs. Measured from the reactor root under JDK 21: with `-DskipTests=false` the client suite reports `Tests run: 178, Failures: 0, Errors: 0, Skipped: 0` (BUILD SUCCESS); without it, `Tests are skipped.` + BUILD SUCCESS — a **false green** on every verification gate in this change. Two actions: (a) every gator maven command (tasks 2.5 / 3.3 / 4.3-4.8 / 5.1 / 5.1b) MUST pass `-DskipTests=false` — already applied below; (b) **delete the `<skipTests>true</skipTests>` line** from `rvsec-gator/pom.xml:18`, so the trap stops being something every future change must remember. Safety confirmed by the 178/178 green run: removing it breaks nothing. Provenance — the line arrived in commit `d94e33cc "starting rvsec-gator"` (2024-09-25), when the module was freshly vendored from upstream GATOR and held no RV-Android test; the project's own tests came later (gh27 2026-02-24: `BaselineComparisonIT`, `ReachabilityBfsTest`; gh60 2026-05-25: `TargetMethodTest`, `MopSpecsParityTest`, `JsonSchemaKeysTest`) and were born skipped. No automated path runs them today: CI (`ci.yml:30`) uses `-DskipTests`, its only `-DskipTests=false` (`:44`) is scoped to `-pl grammar-tests`, and `build.sh`, `build_docker_image.sh`, `docker/mop/entrypoint.sh` and every README skip as well. Task 1.6 (extractor) is unaffected — it inherits the root's `false`
- [ ] 1.1 `model/MopMethod.java`: add fields `includeSubtypes` and `nameIsPattern` (default false), with constructor/getters; keep className/name/params/signature intact. **Extend `equals`/`hashCode`/`toString` to include the two new fields** — `MopMethod` lives in a `HashSet<MopMethod>` (`UsedJcaMethodsVisitor.java:24`), so omitting the flags silently dedups two pointcuts that differ only by `+`/name-pattern (verified: current `equals`/`hashCode` use only the 4 original fields). **State the P3 treatment of the constructor explicitly, symmetrically with task 2.1**: `MopMethod` has a single 4-arg constructor (`model/MopMethod.java:15`) with **3** call sites outside the visitor, all in the gator client's test tree — `rvsec/rvsec-android/rvsec-gator/client/src/test/java/presto/android/gui/clients/BytecodeScanMatchTest.java:25,52,54` — which compiles in the same reactor build as tasks 4.2/5.1. Either extend the canonical constructor and migrate those 3 sites (the P3-consistent choice, matching 2.1's "no delegating overload"), or keep a 4-arg convenience constructor and say so. Silence is what lets the two POJOs diverge. The break would be a loud compile error, not a silent one — `skipTests` suppresses test *execution*, not test *compilation*
- [ ] 1.2 `visitor/UsedJcaMethodsVisitor.visit(ImportDeclaration)`: stop discarding `isAsterisk()` imports — register wildcard-import packages (`java.util`, `java.io`, `java.lang`, `java.net`, ...) in a packages map; keep recording explicit imports as today. **Do NOT seed the implicit `java.lang` package** — this reverses the earlier "seed by default as defense-in-depth" instruction, on measured grounds (2026-08-21): (a) `generic_new` does not need it — all **seven** specs with a `java.lang` `call()` owner (`Object_MonitorOwner`, `Comparable_CompareToNull`, `Comparable_CompareToNullException`, `CharSequence_UndefinedHashCode`, `Long_BadParsingArgs`, and — owner `Iterable` — `ListIterator_Set`, `Map_UnsafeIterator`) carry an explicit `import java.lang.*;`. **List corrected 2026-08-21**: an earlier draft said six and wrongly included `CharSequence_NotInSet`, whose `call()` owner is `Set` (`CharSequence` appears there only in `args()`); (b) seeding it would silently move the **frozen `jca` set**, because `String` — owner of the two `RandomStringPassword.mop` pointcuts — is the *only* unresolved owner in `jca`/`jca_android` (enumerated over the 144 `jca` and 130 `jca_android` `call()` pointcuts), taking `jca` 120→122 and `jca_android` 119→121; (c) MOP targets are LENIENT (class+name, signature ignored), so the resulting `String#valueOf` target matches every overload — measured over 3 corpus APKs: 74 call sites of `String.valueOf`/`toCharArray`, of which only 17 correspond to the signatures the aspect actually weaves (`valueOf(Object)`, `toCharArray()`), i.e. 57 false positives propagated transitively through `reachesTarget`. See INV-ANA-40 scope boundary (c) and `docs/20260821_handoff_gh69_coringas.md`
- [ ] 1.3 `visitor/UsedJcaMethodsVisitor.visit(MethodPointCut)`: (a) strip a trailing `+` from the owner and set `includeSubtypes=true`; (b) **[mitigates RISK-013 — High]** resolve simple owner name → FQN via explicit `imports` first, then `Class.forName(pkg + "." + simple)` over the registered wildcard packages, log+skip if unresolvable (D5). **The `log+` half is the point, not an aside**: today `:70-77` has no `else` and no log, which is why `RandomStringPasswordSpec` has contributed zero targets to the frozen `jca` set since the beginning without anything saying so. This line is what converts that class of hole from silent to reported, for `String` today and for any future spec with an unimported owner; (c) detect a wildcard method name and set `nameIsPattern=true`, preserving the pattern — handle **all 8** patterns present in `generic_new` (`add*`, `remove*`, `retain*`, `clear*`, `put*`, `offer*`, `write*`, and the **bare `*`** from `call(* Iterator.*(..))`); the bare `*` is stored as pattern `*` and is intentional match-all (D4); (d) constructor pointcuts (`call(Owner.new(..))` — `ServerSocket.new` ×2, `TreeMap.new` ×1) are extracted as `MopMethod(owner, "<init>")` — map, do **not** log+skip (this reverses the earlier draft; see design **D9** and phase **4b**, which owns the gate and the re-baseline). `new` is a Java keyword, so the rename is unambiguous, and no GATOR-side change is needed: `TargetResolver.java:53` compares names by equality and `SignatureFileTargetSource` already accepts `<init>`. The flags are `includeSubtypes=false` — constructor pointcuts carry no `+`; `staticinitialization(...)` pointcuts never reach `visit(MethodPointCut)`, so the 3 staticinit-only specs contribute 0 targets by design (INV-ANA-40 scope boundary)
- [ ] 1.4 Unit test `UsedMethodsGenericTest`: parse the 27 `generic_new` specs → assert a cardinality **fixed before implementation**, not pinned to whatever the code emits. The enumeration over the corpus gives **69** distinct `call()` pairs when the trailing `+` is part of the owner key and **67** when it is not — this **includes** the 3 constructor pointcuts, now mapped to `<init>` per 1.3(d)/D9 (they contribute 2 pairs: `(java.net.ServerSocket, <init>)`, distinct from the `ServerSocket+` entries, and `(java.util.TreeMap, <init>)`; under the `+`-less key the ServerSocket pair merges, hence 67 rather than 68). The pre-D9 figures were 67/66 with constructors excluded (the only owner used both ways is `Iterator`: `Iterator.next` in `Map_UnsafeIterator` vs `Iterator+.next` in `ListIterator_Set`),  State in the test which key it uses and assert that number. **Compare with exact string equality (or `LC_ALL=C`)**: a locale-aware collation treats `*` as ignorable and folds `Map+.put` into `Map+.put*`, yielding **66** — so a shell cross-check written as `sort -u` under a pt_BR locale fails this gate for the wrong reason (measured 2026-08-21); if parameter granularity raises it, explain the delta in the test comment rather than absorbing it. Also assert: that `Collection_UnsynchronizedAddAll` yields `java.util.Collection#addAll` with `includeSubtypes=true` and `add*` yields `nameIsPattern=true`; that the **20 owners reachable through non-constructor `call()` pointcuts** all resolve with **unresolved-owner skip-count == 0** — true only after the 1.0b repair, and the test MUST fail loudly if that repair is reverted (there are 21 distinct owners across the `call()` pointcuts, and `TreeMap` appears only in `call(TreeMap.new(Map))`, which D9 now maps to `<init>` rather than skipping — so the owner count is **21**, not the 20 of the pre-D9 draft (re-counted 2026-08-21, revised 2026-08-25): assert **zero** constructor-skip notices — D9 maps them instead of skipping; assert instead that the two constructor pairs `(java.net.ServerSocket, <init>)` and `(java.util.TreeMap, <init>)` are present with `includeSubtypes=false`); that `Object_MonitorOwner` resolves `java.lang.Object` (via its explicit `import java.lang.*;` wildcard registration); that a **synthetic fixture spec whose owner package is registered by no import** (an `Object+` owner in a spec without `import java.lang.*;`) is **logged and skipped**, never silently dropped — this fixture is authored under `src/test/resources` (task 1.0), NOT added to the real corpus, and it is the regression guard for the class of defect 1.0b repaired — this observable behavior replaces the rejected `java.lang` seed (task 1.2); and that `Iterator.*` yields the bare-`*` pattern (INV-ANA-40)
- [ ] 1.5 Unit test: parse the 23 `jca` specs → assert **exactly 120** targets, all `includeSubtypes=false` and `nameIsPattern=false` (INV-ANA-40 JCA half / INV-ANA-41). Re-measured 2026-08-21 with the shipped extractor: `jca` = 120 signatures / 68 `(class, method)` pairs / 22 owners; `jca_android` = 119/67/22. **Assert `jca` by literal; DERIVE `jca_android` by enumeration and do not pin it.** Note that D9 does **not** move the `jca` triple: 120/68/22 stays 120/68/22, because the 18 constructor rows already existed — only the emitted name changes. Add one assertion for it: the 11 constructor pairs MUST now read `<init>`, not `new` (`SecureRandom`, `KeyPair`, `CipherInputStream`, `CipherOutputStream`, `SecretKeySpec`, `IvParameterSpec`, `GCMParameterSpec`, `PBEKeySpec`, `PBEParameterSpec`, `DHGenParameterSpec`, `HMACParameterSpec`). Both sets share the visitor, so `jca_android` must be exercised — but it is **not frozen the way `jca` is**, and gh105 is editing it right now. The two changes use "frozen" with different scopes: gh101 (archived) froze `jca` alone and made `jca_android` the successor that carries the repairs ("not one byte of `rvsec-mop/src/main/resources/jca` ... Every correction to a specification lands in `jca_android` alone"), and gh105 is executing exactly that. Measured 2026-08-21: `call(` occurrences in `jca_android` went **144 → 130** between commit `42a3528` and the working tree, from gh105's F1 twin fusions. The 119/67/22 triple above is accurate **today**, against the already-partially-edited set — but gh105 has **39 of 74 tasks open**, including Group 5, which adds new `<Chain>Junction.mop` files to that same directory. A literal assertion will go red on a change that is doing the right thing. So: assert 120/68/22 literally for `jca` (which gh105 does not touch and gh101 froze), and for `jca_android` assert only the properties that must hold regardless of gh105 — all flags `includeSubtypes=false` and `nameIsPattern=false`, zero unresolved owners other than `String`, and a count derived by enumerating the directory rather than hard-coded. This is the discipline gh105 already imposes on its own gates ("each gate **derives** the count by enumeration and never asserts a literal"). Also assert that the `String` owner of `RandomStringPassword.mop` stays **unresolved and logged** (documented static false-negative, INV-ANA-40 scope boundary (c), **RISK-013 — High**). The `and logged` half is the assertion that keeps RISK-013 mitigated: unresolved-and-silent is the state that made it grave, and a test that only checks the count would pass in that state. **This literal count is the real JCA regression gate**: `MopSpecsParityTest` compares `MopSpecsTargetSource.load()` with `JavamopFacade.listUsedMethods()` on the same directory, so both sides run through this very visitor and it cannot detect an extractor-side JCA regression (its fixtures are only `CipherSpec`/`MessageDigestSpec`, owners `Cipher`/`MessageDigest`, no `String`)
- [ ] 1.6 Run the extractor unit tests from the reactor root, with JDK 21 in the prefix: `cd /home/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec && mvn -q -pl rvsec/rvsec-mop-extractor test`. Always use absolute `/home/pedro/...` paths — the `/pedro/...` alias does not open under the JVM. (The name `rvsec` is duplicated in the workspace: the module lives at `<reactor-root>/rvsec/rvsec-mop-extractor`.)

- [ ] 1.7 **Install the fresh extractor into `~/.m2` before starting phase 2** (D6/RISK-003 — the ordering the earlier task list violated): phase 2 changes `MopSpecsTargetSource` to read the two new `MopMethod` flags, so the gator will not even compile against the stale `.m2` copy. `cd /home/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec && mvn clean install -DskipMopAgent -DskipTests -pl rvsec/rvsec-mop-extractor -am`. Task 4.1 repeats this install after any further extractor edit — it is not redundant, it is the same gate applied twice

## 2. Matcher model + source + helper (rvsec-gator commons/client) — INV-ANA-41, INV-ANA-42, INV-ANA-43

- [ ] 2.1 `commons/.../target/TargetMethod.java`: add `includeSubtypes` and `nameIsPattern` fields (immutable) + getters; do not change `MatchPolicy` (orthogonal axis — *signature strictness* `LENIENT`/`STRICT`; see design D7 / ADR 0004). Extend `equals`/`hashCode`/`toString` to include the two new fields. **There is exactly one 5-arg constructor** (verified), used by `MopSpecsTargetSource`, `SignatureFileTargetSource`, and 15 test call sites (`TargetMethodTest`/`TargetResolverTest`/`TargetResolverParamsMatchTest` — 17 `new TargetMethod(` occurrences in the tree in total, re-counted 2026-08-21). Per P3 (no compat shim): add the two params to the canonical constructor and **update every call site** (17: `MopSpecsTargetSource` passes the real flags; `SignatureFileTargetSource` + tests pass explicit `false`/`false`). **No delegating 5-arg overload** — migrating all call sites is cheap and keeps a single constructor, so the JCA/STRICT/signature-file paths never reach `canStoreType` (INV-ANA-35)
- [ ] 2.2 `client/.../target/MopSpecsTargetSource.load()`: propagate `includeSubtypes`/`nameIsPattern` from each `MopMethod` to the `TargetMethod` (today it hard-codes `MatchPolicy.LENIENT` with no flags — verified) (INV-ANA-41)
- [ ] 2.3 New `client/.../target/TargetMatching.java` helper:
  - `nameMatches(TargetMethod, String)` — trailing-`*` prefix; bare `*` → prefix `""` matches all; no-`*` → `equals` (D4)
  - `matches(soot.Type callSiteType, String callSiteName, TargetMethod, FastHierarchy)` — **raw `(Type,String)` signature** (decided; closes the prior 3-option open question), so both match points call it with zero allocation (`SootMethod`/`SootMethodRef` both expose `getDeclaringClass().getType()`+`getName()`; no `makeRef()` per Scene method). Evaluate `nameMatches` **first** (short-circuit), then exact `equals` when `!includeSubtypes`, else `canStoreType(callSiteType, superType)` when `includeSubtypes` (INV-ANA-42). `STRICT` param-matching stays in `resolveInScene`, not in the helper
  - `forceResolveTargets(Set<TargetMethod>)` via `Scene.v().forceResolve(fqn, SootClass.SIGNATURES)` — **`SIGNATURES`, not `HIERARCHY`**. `canStoreType` only needs `HIERARCHY`, but resolving *at* `HIERARCHY` can introduce a class into the Scene below `SIGNATURES`, and `TargetResolver.resolveInScene` iterates `Scene.v().getClasses()` calling `cls.getMethods()` with no guard (`TargetResolver.java:48-50`); `SootClass.getMethods()` opens with `checkLevel(SIGNATURES)`, `Scene.doneResolving()` is `true` during the `wjtp` pack, and `ignore_resolving_levels` is never set in this build — so the next pass would **throw `RuntimeException`**, a crash rather than a silent false-negative. The realistic trigger is an owner absent from the APK's Scene, e.g. `java.net.ServerSocket` in an app that opens no sockets. `SIGNATURES` satisfies `HIERARCHY` and costs nothing here. Alternatively (or additionally) guard the loop with `if (cls.resolvingLevel() < SootClass.SIGNATURES) continue;`. Classify an owner as resolved ONLY if `!isPhantom() && resolvingLevel() >= HIERARCHY`; classify an owner as resolved ONLY if `!isPhantom() && resolvingLevel() >= HIERARCHY` (verified: under `allow_phantom_refs=true` an unresolvable type force-resolves to a phantom at `BODIES` level that passes `containsClass` but makes `canStoreType` return a definite, wrong `false` — it does NOT throw, so a try/catch would be dead code). Cache the resolved `superType` `RefType` per target; phantom/absent owners degrade to exact `equals`+log once per owner (INV-ANA-43)
- [ ] 2.4 Unit test `TargetMatchingTest`: `canStoreType` class→interface (`ArrayList <: Collection`) and **interface→interface (`List <: Iterable`** — the only case distinguishing A2 from A1, both forced into the Scene); `nameMatches` for `add*` (matches `add`,`addAll`; not `remove`), `write*`, and the **bare `*`** (matches any name); a non-trailing-`*` pattern → literal `equals` (no crash); **absent super-type AND phantom owner** → both degrade to `equals` + emit a warning (INV-ANA-42, INV-ANA-43 — both IDs written out so a mechanical `grep INV-ANA-43 tasks.md` finds this task)
- [ ] 2.4b Unit test `MopSpecsTargetSourceTest` (INV-ANA-41 — the orphan test referenced in design.md but previously missing from tasks): load a `generic_new` fixture → assert the emitted `TargetMethod` set carries `includeSubtypes=true`/`nameIsPattern=true` on the `+`/wildcard entries; load a `jca` fixture → assert all `TargetMethod` carry both flags `false`. This isolates the `MopMethod → TargetMethod` propagation boundary (the extractor tests 1.4/1.5 stop at `MopMethod`)
- [ ] 2.5 Run commons+client unit tests **from the reactor root** (never module-local: `main.basedir` is resolved by `directory-maven-plugin` against the `br.unb.cic:rvsec-parent` project and does not resolve standalone, and the client's failsafe config injects `RVSEC_HOME=${main.basedir}`): `cd /home/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec && mvn -q -pl rvsec/rvsec-android/rvsec-gator/commons,rvsec/rvsec-android/rvsec-gator/client -am test -DskipTests=false`. The earlier `cd ../rvsec/rvsec-android/rvsec-gator` was a broken path — the gator lives at `<reactor-root>/rvsec/rvsec-android/rvsec-gator`, and the workspace duplicates the name `rvsec` (same trap 1.6 warns about)

## 3. Wire the A2 predicate into the two match points (rvsec-gator client) — INV-ANA-42, INV-ANA-43, INV-ANA-64

- [ ] 3.1 `client/.../target/TargetResolver.resolveInScene`: when seeding the reverse-BFS set, apply `TargetMatching.matches(...)` against the declared super-type (replacing `equals(className) && equals(methodName)`) for `includeSubtypes` targets; keep the exact path for the rest. **`nameMatches` MUST be evaluated before `canStoreType`** — the current `equals(fqn)` fast-reject disappears for subtypes (verified: triple loop `Scene.getClasses()` × methods × targets), so the name short-circuit is the mandatory replacement (RISK-005, this match point too). Call `forceResolveTargets(...)` before building the `FastHierarchy`
- [ ] 3.2 **Cascade + hybrid scan** (verified: `findDirectTargetCallersByBytecodeScan(appClasses, Set<SootMethod>)` (~:574) and `ReachabilityEngine` receive only resolved methods, losing the declared owner + flags; `RvsecAnalysisClient.run()` already holds `targetSpecs` (`Set<TargetMethod>`) in scope):
  - extend `ReachabilityEngine`'s constructor/`run(...)` and `findDirectTargetCallersByBytecodeScan` to also carry `Set<TargetMethod> targetSpecs`, propagated from `run()`
  - make the scan **hybrid**: keep the `Set<String> class#method` key path for `!includeSubtypes` targets (JCA O(1) + parity), and for `includeSubtypes` targets iterate (grouped by distinct super-type) applying `TargetMatching.matches(...)` per invoke against the declared super-type — NOT pre-resolved keys
  - preserve `findDirectTargetCallers` (CG-based, ~:447) which consumes the resolved set (INV-ANA-42, BUG-INV-ANA-19)
- [ ] 3.2b **Seed the reverse BFS with the direct set** (INV-ANA-64, design D8) — `client/.../reach/ReachabilityEngine.run()`. Today the order is: `reachesTargetSet = multiSourceBfs(reversed, targets)` first, then `directTargetSet = findDirectTargetCallers(...) ∪ findDirectTargetCallersByBytecodeScan(...)`. Invert it: compute `directTargetSet` **first**, then `reachesTargetSet = multiSourceBfs(reversed, union(targets, directTargetSet))`. This restores the definitional containment `reachesTarget ⊇ directlyReachesTarget`, which the tree violates today on 14 flags / 6 distinct methods / 2 APKs out of the 269 `*.apk.json` present (12 of the 14 on methods with `reachable=false`, i.e. with no call-graph vertex at all). No signature changes and no defensive code: `multiSourceBfs` (`RvsecAnalysisClient.java:~423`) already calls `graph.addVertex(seed)` before its visited check, with a comment covering exactly this case. **Do NOT** implement this as a post-hoc `reachesTargetSet.addAll(directTargetSet)` — that asserts the containment without propagating it, leaving the callers of those methods unmarked (D8 rejects it explicitly). **Do NOT** add any gate, assertion or abort to `JsonReportWriter`: a residual case must let the analysis continue normally. Optional observability, in the existing idiom: extend the `[ReachabilityEngine] directlyReachesTarget: N (CG: x, bytecode: y, intersection: z)` line with the count of seeds contributed by the scan alone. After this lands, regenerate the golden fixture of task 3.2d — **not before**, or the bug gets committed as the golden
- [ ] 3.2c Tests for INV-ANA-64, in the two places that can actually hold them. **(a) Java unit, in the existing `client/src/test/java/presto/android/gui/clients/ReachabilityBfsTest.java`** (Scene-free, `String` vertices over the generic `multiSourceBfs<V>`; it already carries `testBfsSeedNotInGraph` at ~:91 — the precondition this repair leans on — and `testReverseBfsForReachesMop` at ~:118, so the new cases sit in the same idiom): a scan-only caller absent from the graph is marked when passed as a seed; **its own caller, present in the graph, is marked too** — the property a post-hoc `addAll` would miss and the reason D8 rejects that shortcut; an empty direct set yields a result byte-identical to seeding with `targets` alone (JCA path untouched, INV-ANA-35). **(b) End-to-end**: no new assertion is needed — `tests/parity/test_reachability_parity.py:163` (`test_directly_reaches_target_is_subset_of_reaches_target`) already asserts the containment, and its docstring already claims it holds "by construction". Today that claim is false and the test passes only because its single APK (`cryptoapp`, `jca` specs) has zero violations; after 3.2b it holds for real. Confirm it still passes and **do not weaken it**. **Scope limit to record**: `ReachabilityEngine.run()` calls `Scene.v().getCallGraph()` and is therefore not unit-testable — the synthetic test locks the BFS composition semantics, not the wiring inside `run()`. The wiring is covered only by the ITs (`RvsecAnalysisClientIT` / `BaselineComparisonIT`, task 5.1b) plus that tripwire; both need `-DskipTests=false -DskipITs=false` (task 1.0c / RISK-012), and the tripwire skips silently when `RVSEC_HOME` is unset
- [ ] 3.2d **Make the containment tripwire non-vacuous** (INV-ANA-64). `tests/parity/test_reachability_parity.py:163` runs GATOR fresh over `cryptoapp` alone, which has **zero** violations today (21 direct, 32 transitive) — so it passes by luck and is blind to the case that motivates this repair. Committing a second APK is not an option (`app.notesr_59.apk` is 35.4 MB against the 4.0 MB tracked `cryptoapp.apk`, and the `aegis_81` original is absent even from the 400-APK external corpus). Instead, add a **golden JSON fixture**: after 3.2b lands and the JARs are rebuilt (4.1/4.2), run GATOR over `app.notesr_59.apk` (the external corpus at `/home/pedro/desenvolvimento/RV_ANDROID_NOVO/JOAO/APKs/`, via `scripts/check_signature_file_subset.py --apk <path>` — the flag already exists) and commit the resulting `app.notesr_59.apk.json` (~688 KB) next to the existing `cryptoapp.apk.json` under `modules/rv-static-analysis/tests/resources/`. Add a parity test asserting containment over it. **Order matters and is the whole point**: the JSON produced by *today's* build violates (1 method — `app.notesr.core.security.crypto.AesCryptor.generatePasswordBasedKey`, `directlyReachesTarget=true` with `reachesTarget=false`), so generating the fixture **before** 3.2b would commit the bug as the golden. Generate it **after**, and record in the commit message that the pre-fix output of the same APK carried that one violation — that sentence is the evidence the gate is no longer vacuous. Note the residual limitation honestly: a golden fixture proves the recorded output satisfies the containment, not that a fresh run on an arbitrary APK does; the end-to-end half stays with `cryptoapp` and the ITs
- [ ] 3.3 Confirm `MopSpecsParityTest` still passes (JCA exact path unchanged), again from the reactor root: `cd /home/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec && mvn -q -pl rvsec/rvsec-android/rvsec-gator/client -am test -DskipTests=false -Dtest=MopSpecsParityTest` (INV-ANA-35). Remember this test cannot detect an extractor-side JCA regression — task 1.5 and the `BaselineComparisonIT` of 5.1b are the load-bearing JCA gates

## 4. Rebuild (ordered) + integration on the real scene — RISK-001, RISK-003, INV-ANA-44

- [ ] 4.1 Rebuild step 1 — extractor, **from the reactor root** (JDK 21 in the prefix): `cd /home/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec && mvn clean install -DskipMopAgent -DskipTests -pl rvsec/rvsec-mop-extractor -am`. Do **not** run `mvn install` from inside the module directory: `main.basedir` is resolved by `directory-maven-plugin` against the `br.unb.cic:rvsec-parent` project and does not resolve in a standalone module build — the literal directory `rvsec/rvsec-mop-extractor/${main.basedir}/rv-android/lib/mop-extractor/mop-extractor.jar` present in the tree is the residue of exactly that mistake (verified 2026-08-21). What the gator fat jar consumes is the `~/.m2` install; the copy into `rv-android/lib/mop-extractor/` only happens when `main.basedir` resolves. **Confirm both**: fresh timestamp on `rv-android/lib/mop-extractor/mop-extractor.jar` AND no new literal `${main.basedir}` directory — D6/RISK-003
- [ ] 4.2 Rebuild step 2 — gator, also **from the reactor root** (same `main.basedir` reason as 4.1): `cd /home/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec && mvn clean install -DskipMopAgent -DskipTests -pl rvsec/rvsec-android/rvsec-gator/sootandroid,rvsec/rvsec-android/rvsec-gator/client -am` (re-bundles the fresh extractor into `rvsec-analysis-client.jar`; `sootandroid` kept in `-pl` to preserve the `FlowgraphRebuilder` arity guard). Confirm both JARs land in `rv-android/lib/gator/` with fresh timestamps. **Do not** phrase the second check as "no literal `${main.basedir}` directory appeared under `rvsec-gator/`" — three already exist there (`client/`, `commons/`, `sootandroid/`), residue of past module-local builds, and the pattern recurs in 8 modules tree-wide. The usable check is that the **mtime** of those existing residue directories does not advance during this build
- [ ] 4.3 IT on a small APK against `generic_new` — **the ITs are skipped by TWO independent properties, so every IT command in phase 4 MUST pass `-DskipTests=false -DskipITs=false`** — `client/pom.xml:18` sets `<skipITs>true</skipITs>`, and the gator parent `rvsec-gator/pom.xml:18` sets `<skipTests>true</skipTests>`, which failsafe also honours (task 1.0c). `-DskipITs=false` on its own leaves them skipped. Assert log shows `Loaded N>0 MOP signatures`. **The load-bearing assertion is `directlyReachesTarget`, not `reachesTarget`** (RISK-004, measured 2026-08-21): with `generic_new` the transitive flag saturates at 84–94% of app methods and collapses onto `reachable`, so `reachesTarget>0` passes trivially and proves nothing; the direct flag moves from 0.0–0.3% (jca) to **2–12%**, which is the band to assert. Keep `reachesTarget>0` only as a smoke check, and record the measured ratio of both fields in the IT log; assert `canStoreType` runs inside the real `RvsecAnalysisClient` scene with **0 degrade warnings for the 21 owners that reach `forceResolveTargets`** — 21 distinct owners appear across the `call()` pointcuts, and `TreeMap` occurs only in a constructor pointcut, which D9 now maps rather than skips, so 21 carry targets after D9 (`TreeMap` joins, since it appears only in a constructor pointcut; the figure was 20 while constructors were suppressed, and 19 without the 1.0b repair). A degrade on any `includeSubtypes=true` owner is a **hard gate that blocks the sweep** (RISK-001; all of them are JDK types and MUST force-resolve non-phantom). Log which owners resolved and whether any is phantom. **Do not** assert that `forceResolveTargets` ran before the first `getOrMakeFastHierarchy()` of the process — SPARK materialises the hierarchy in the `cg` pack, long before any client analysis, so that ordering can never hold and the earlier draft's check was unsatisfiable. Assert instead the property that matters: after `forceResolveTargets`, a known subtype pair (e.g. `ArrayList <: java.util.Collection`) answers `true` through the `FastHierarchy` the matcher actually uses (design D2)
- [ ] 4.4 Schema invariance (`-DskipTests=false -DskipITs=false`): diff the JSON key-set of the `generic_new` run vs a `jca` run on the same APK → MUST be identical; only `reachesTarget`/`directlyReachesTarget` values differ (INV-ANA-44). **Also feed the `generic_new` JSON to `static_analysis_parser.py` and assert it parses without error** — the RISK-007 indicator, which a key-set diff alone does not cover
- [ ] 4.5 Canary (RISK-003): post-rebuild extractor target count for `generic_new` MUST be > 0 — proves the fresh extractor was bundled into the JAR
- [ ] 4.6 Negative E2E (`-DskipTests=false -DskipITs=false`, no over-match) — **name-axis** (because `generic_new` includes `Object+`, every type is a subtype, so a pure "non-subtype" example is impossible; the non-match must come from the method name): on the same IT APK against `generic_new`, assert a subtype-receiver call whose name does NOT match the declared pattern stays `reachesTarget=false`/`directlyReachesTarget=false` — e.g. `java.util.ArrayList.remove(...)` vs `Collection+.add*`, and `java.lang.String.length()` (`String <: Object+` but `length` ∉ {`wait`,`notify`,`notifyAll`}). Confirm `nameMatches` short-circuits before `canStoreType`. Acceptance is spot-check + sampled inspection (honest bounded criterion, not a completeness proof) — spec scenario "Non-target call site stays unmatched"

- [ ] 4.7 **Scan-only path (RISK-009 indicator, previously asserted by nothing)**: on the same IT APK against `generic_new`, identify a method reachable **only** through the direct bytecode scan — i.e. not present in the SPARK call graph — and assert it reports `directlyReachesTarget=true`. This is what proves the hybrid scan of 3.2 actually gained the subtype predicate, rather than the CG path covering for it (BUG-INV-ANA-19)
- [ ] 4.8 **Performance gate (RISK-005 hard trigger, previously measured by nothing)**: time `TargetResolver.resolveInScene`, `findDirectTargetCallersByBytecodeScan` **and `ReachabilityEngine.multiSourceBfs`** on the same APK, `generic_new` vs `jca`. All three MUST be **≤ 2× the JCA baseline** (INV-ANA-42 cost bound, NFR04). The BFS is in scope because it is the stage that grows least visibly: `resolveInScene` iterates `Scene.v().getClasses()`, so with `Collection+`/`Object+`/`Iterable+` the seed set it hands the BFS goes from ~120 JCA methods to every matching library method in the Scene. Timing only the two match points would miss that. This supersedes the register's older "within an order of magnitude" phrasing — the two thresholds must not both remain in circulation. If the gate fails, the first suspects are the lost `equals(fqn)` fast-reject and a `superType` `RefType` resolved per invoke instead of cached per target (design D-Risks)

## 4b. Constructor targets: `new` → `<init>` on the frozen ruler — INV-ANA-40 boundary (b), design D9, RISK-014

Sequenced **after** phase 4 has the subtype path green on a real scene, deliberately: this group moves values in
the frozen `jca` set, and if a parity gate goes red with both landed at once nothing separates the subtype matcher
from the constructor mapping. Its evidence is its own.

- [ ] 4b.1 **Record the before-state, from the current build, before touching anything.** Run the extractor over `jca`, `jca_android` and `generic_new` and save the CSVs; run the GATOR gate script over `cryptoapp` and save the JSON. These are the "before" half of the gate — without them, 4b.4 has nothing to diff against. Store under the change's evidence directory, not in `/tmp`
- [ ] 4b.2 Implement the mapping in `visitor/UsedJcaMethodsVisitor` (the same site as 1.3(d), already written by then — this task is the gate, not a second edit). Confirm the extractor now emits `<init>` for all constructor pointcuts in the three sets, and that the emitted `parameters`/`signature` columns are unchanged
- [ ] 4b.3 **Enumerate the effect on `jca`, do not summarise it.** Diff before/after: the triple MUST stay 120/68/22, and exactly the 11 pairs listed in 1.5 MUST flip from `new` to `<init>`. Any other delta is a defect, not a consequence — stop and investigate
- [ ] 4b.4 **Measure the frozen fixture and re-baseline with the enumeration written down.** Predicted from a dexdump of `cryptoapp.apk` (2026-08-25): 11 constructor call sites (`SecretKeySpec` ×5, `IvParameterSpec` ×4, `SecureRandom` ×2) across 10 methods, 8 of which are already flagged, so `directlyReachesTarget` goes **21 → 23**, the two new ones being `CryptoUtils.createSecretKeyFromBytes` and `CryptographyActivity.executeSecretKeyOperation`. **Verify the prediction against the real run rather than assuming it** — and measure the transitive axis, which was deliberately not predicted: a new seed propagates to its callers, so `reachesTarget` may move by more than the direct set. Update `modules/rv-static-analysis/tests/resources/cryptoapp.apk.json` and re-run `tests/parity/test_reachability_parity.py`; `G_paridade_targets` freezes the *set* of signatures, so it will go red until the fixture is updated — that is the gate working, not a failure. Write the full enumeration (every signature added, with why) into the commit message; a re-baseline whose diff is not enumerated is exactly what the gh101 freeze doctrine forbids
- [ ] 4b.5 `test_historical_methods_coverage.py:134` pins three methods by `directlyReachesTarget` and reads only the committed fixture (it does **not** run GATOR), so it moves only if 4b.4's re-baseline touches one of its three. Check; if it does, re-baseline it in the same commit with the same enumeration
- [ ] 4b.6 Re-run `BaselineComparisonIT` (`-DskipTests=false -DskipITs=false`, task 1.0c). Its `directlyReachesTarget` assertion is **exact**, not ±10% — so it moves with 4b.4 and must be updated in the same commit. `reachable` and `reachesTarget` carry ±10% tolerance and should absorb the change; if `reachesTarget` breaches 10%, that is the transitive propagation of 4b.4 and it is a finding worth recording in the risk register, not a number to quietly widen
- [ ] 4b.7 Record the outcome in `data/gh101/frozen_set_debt.md` ("What the frozen `jca` set knowingly retains"), which is where this project already keeps enumerated debt against the frozen instrument — this repair **removes** an item from that ledger rather than adding one, and saying so there is what keeps the ledger true. Cross-reference issue #101 per the convention gh100 used for its dependent

## 5. Verification & review

- [ ] 5.1 Full Java unit pass from the reactor root: `cd /home/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec && mvn -q -pl rvsec/rvsec-mop-extractor,rvsec/rvsec-android/rvsec-gator/commons,rvsec/rvsec-android/rvsec-gator/client -am test -DskipTests=false` (extractor unit + `TargetMatchingTest` + `MopSpecsTargetSourceTest` + `MopSpecsParityTest` green). Note this is surefire only — it does **not** reach the failsafe ITs, which is why 5.1b exists
- [ ] 5.1b **Run `BaselineComparisonIT`** — the end-to-end half of the JCA regression gate that `specs/analysis/spec.md` names but that no task previously executed. It exists at `rvsec/rvsec-android/rvsec-gator/client/src/test/java/presto/android/gui/clients/BaselineComparisonIT.java` and is skipped by default: `cd /home/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec && mvn -q -pl rvsec/rvsec-android/rvsec-gator/client -am verify -DskipTests=false -DskipITs=false -Dit.test=BaselineComparisonIT` (INV-ANA-35 / INV-ANA-40 JCA half)
- [ ] 5.2 Re-run the Estágio A smoke on 5–10 APKs (procedure per `docs/20260611_sweep_generic_new_400.md` **§5** "Escada de execução → Estágio A" — §10 is the execution record, not the procedure, and the document has no §11) → confirm `reachesTarget>0`; **APK source updated 2026-07-09, corrected 2026-08-21**: draw the smoke APKs from the new dataset repo at `/home/pedro/desenvolvimento/workspaces/workspaces-doutorado/workspace-rv/rvsec-dataset` — **not** from `apks_original/`, which holds only a `.gitkeep`; the APKs live in `head_apks/` (348), `built_apks/` (317) and `instrumented_apks/` (219). Use absolute `/home/pedro/...`, never the `/pedro/...` alias, which the JVM cannot open. The "219 curated apps" figure quoted elsewhere is also stale: the repo re-froze to 182 (Phase 10) and then 181 (Phase 11) on 2026-07-14 — the generic experiment will run on that corpus, superseding the 400-APK sweep corpus for dataset purposes (note: the rvsec-dataset ROADMAP is currently JCA-only; the generic-experiment planning there is a downstream change, not gh69 scope). Do NOT launch any full sweep (separate change)
- [ ] 5.2b **[RISK-004 — High] Decide what happens to `aperv-tool` before this ships.** It is the one consumer that reads `reachesTarget` from the raw JSON and is **already in production**, so it changes verdict in the same commit this change lands, not in a future dataset change: `sa_methods_reaches_mop` is its count-model offset (`modules/aperv-tool/.../analysis/static_artifact.py:13-17`) and degenerates toward `total_methods`, while the `hot`/`cold`/`unresolved` verdict (`static_artifact.py:288-296`) collapses to `hot` for practically every APK once the transitive flag saturates. Note this is triggered by *spec set*, so it bites only when something runs `aperv-tool` against `generic_new` output — `jca` runs are unaffected. Pick one and record it here: (a) move the tool to `directlyReachesTarget`, which stays discriminating at 2–12%; (b) gate its `hot`/`cold` on spec set and keep `jca` behaviour; (c) accept the collapse and document it in the tool. **Do not** reach for owner blacklisting — measured refutation in RISK-004: dropping 34 of the 67 pairs does not move `reachesTarget` on any medium or large APK. This task is a decision plus its record; the code change, if any, belongs to `aperv-tool`, not to this change
- [ ] 5.3 Invoke `/rv-code-reviewer` via the Skill tool: "Review gh69 generic-subtype-target-matching implementation (extractor + gator matcher A2)"
- [ ] 5.4 Run `/opsx:verify` to validate implementation against specs/analysis/spec.md (INV-ANA-40..44)
- [ ] 5.5 Update `docs/20260617_sa_generic_new.md` §13 status (implemented) and tick the issue #69 acceptance criteria
- [ ] 5.6 **[discharges RISK-013 — High]** Open the follow-up GitHub issue for the **`RandomStringPassword` static false-negative** (INV-ANA-40 scope boundary (c)). This is not housekeeping: it is the only exit from a High risk this change accepts rather than fixes — `RandomStringPasswordSpec` is 1 of the 23 `jca` specs and contributes zero static targets, so every published `cov_reaches_target` from the frozen ruler was computed over 22 of 23. The issue MUST carry that framing and the level, so it is not triaged as minor. Body, carrying the 2026-08-21 measurements so nobody re-measures: seeding `java.lang` alone is not the repair — it needs (i) owner visibility (explicit import, an FQN-owner rule in the visitor, or the seed), (ii) a **STRICT** policy for that target so `String#valueOf` stops matching `valueOf(int)`/`valueOf(long)` (74 call sites → 17 legitimate over 3 APKs), and (iii) `getParams()` resolving parameters to FQN (see design Non-Goals). The three go together; any one alone makes the measurement worse. `cryptoapp.apk` has 0 such call sites, so `BaselineComparisonIT` is unaffected either way
- [ ] 5.7 **Commit the four evidence documents this change cites as normative** (verified 2026-08-21: all four are untracked — not gitignored, simply never `git add`ed, so they exist only in one working tree): `docs/20260617_sa_generic_new.md`, `docs/20260611_sweep_generic_new_400.md`, `docs/20260821_handoff_gh69_coringas.md`, `docs/20260821_plano_correcao_analise_estatica.md`. The proposal opens by citing the first two as the root-cause analysis, and task 5.6 instructs the follow-up author to carry the 2026-08-21 measurements forward "so nobody re-measures" — which is impossible if the measurements are not in the repository. `docs/adr/0004-generic-subtype-target-matching-canstoretype.md` is already tracked and needs nothing
- [ ] 5.8 **Follow-up (not this change's code scope) — reconcile the `T+`-owner count between two synced specs**: `openspec/specs/instrumentation/spec.md:1504` records "§4.O — `T+` in `call()` owner (R11: **64** sites generic_new)", and INV-INS-93 (`:286`) makes that 64 a reproducibility pin asserted by `MatrixIntegrityTest`, counted "per-occurrence of the `+.` owner token". Measured 2026-08-21 over the same corpus: the `+.` token occurs **71** times in `generic_new`, all in `call()` owner position — matching this change's 71 and its 89-`call(` denominator. Once gh69 syncs, `analysis/spec.md` will carry 71 and `instrumentation/spec.md` 64 for the same token in the same corpus. Open an issue against the instrumentation matrix rather than editing INV-INS-93 from here

## Follow-up backlog herdado do gh60 (C2/C3) — NÃO é escopo do gh69

> Parqueado aqui a pedido do usuário (2026-06-17): em vez de abrir issues GitHub agora
> (gh60 tasks 10.1/10.2), o backlog C2/C3 do plano GATOR-targets
> (`docs/20260515_plano_gator_targets_generic.md` §10.2/§10.3) fica registrado nesta change
> ativa de GATOR. **Estes itens NÃO são tarefas do gh69** (sem checkbox, não contam no
> progresso) e não alteram o escopo do gh69 (matching subtype/wildcard). Viram issues GitHub
> quando C2/C3 forem efetivamente iniciados. Lembrete do plano: o analisador estático GATOR
> **só está completo após C2 e C3 mergearem** (gh60 é "C1 de 3").

### C2 — `hardening-package` (bug fixes + observabilidade)

**Title**: GATOR: hardening (cache, menu inheritance, integer-array, dead code expanded) + dual package + observability

- **Summary**: Bug fixes/hardening do review codex gh57 + análise multi-LLM 2026-05-15. Inclui
  dual-package (manifest vs code package) que corrige `am start` nos ~27,5% do corpus com apps
  game-engine/híbridos de pacotes divergentes; warning de fallback de widget-type p/ drift
  pós-ProGuard; sync do README de rv-static-analysis + expor flag CLI `--cg-algorithm`.
- **Scope**:
  - G6.2 cache em `resolveStringReference` (padrão `stringIdNameCache`)
  - G6.3 `findOnCreateOptionsMenu` percorre hierarquia de superclasses (corrige FN de menus em base classes)
  - G6.4 `parseArraysXml` cobre `<integer-array>` e `<array>` — **NOTA: já adiantado no gh60 §12 (G6.4)**; revalidar escopo restante
  - G6.5a-c dead code expandido: `client/.../wtg/model/` + `FlowgraphRebuilder.createDefineIntentContentOpNode` (bloco comentado) + `FlowgraphRebuilder.buildCallGraphLegacy` — **EXCLUÍDO** do dead-code: tem caller vivo em `FlowgraphRebuilder.java:980`; remoção exige decisão arquitetural sobre o branch `cgDelegation`
  - G6.6 log warn em `WidgetType.from_class_name` ao cair em OTHER
  - G11 dual package: campos top-level `manifestPackage` + `codePackage`
  - G5.7-G5.8 sync README (output path, Java version) + expor CLI `--cg-algorithm`
- **Pre-requisite**: C1 (gh60) mergeado/arquivado. Usa o `ReachabilityEnricher`/`JsonReportWriter` decompostos e as chaves JSON pós-rename (`JsonSchema.Keys`/`_JK`).
- **Gates**: paridade-delta por grupo (§5.4 do plano); `G_dual_package` (fixture ≥1 APK híbrido Godot/Unity); `G_menu_inheritance` + `G_integer_array` (fixtures sintéticas); `G_dead_code_flowgraph` zero; `G_widget_type_fallback` emite warning; `G_readme_sync`.
- **Refs**: `closes #<N+1>`, `follows #60`.

### C3 — `agent-enrichment` (JSON enrichment para APE-RV)

**Title**: GATOR: JSON enrichment for agent prioritization (widget/transition reachability + external exit + event types)

- **Summary**: Mover as junções widget×reachability e transition×reachability dos consumidores
  Python (onde a normalização de assinatura de inner-class é frágil) para o `JsonReportWriter`
  do GATOR (que tem acesso à Scene — normalização canônica). Marcar transições de saída externa
  p/ o agente evitar dead-ends (browser/dialer). Corrigir o parser Python p/ reconhecer os 14
  `EventType` (G12 confirmou Cenário A — Java já emite certo, gargalo era o parser).
- **Scope**:
  - G7 emitir `handlerReachesTarget`/`handlerDirectlyReachesTarget` por listener de widget (anotação no `ReachabilityEnricher`; writer continua puro)
  - G8.1 idem por evento de transição (per-event; agregado por-transição `targetReachesTarget` removido do JSON, derivado como `@property target_reaches_target` em `WindowTransition`) — **NOTA: o agregado por-transição, quando existir, DEVE chamar `transition_reaches_target_aggregate`**, pois `target_reaches_target` já é o `@property` de janela do gh60 (D10)
  - G9 marcar `externalExit=True`/`exitKind` p/ transições a `ACTION_VIEW`/`ACTION_SHARE`/`ACTION_DIAL`/`ACTION_SENDTO`/`ACTION_CALL`
  - G12 estender parser Python p/ todo o enum `EventType` (~10 LOC, sem mudança Java — G12.1 confirmou Cenário A)
- **Consumer**: APE-RV (aperv-tool) prioritizer + transition_picker + action_executor (§5.3 do plano); update do consumidor é PR de follow-up (não bloqueia C3).
- **Pre-requisite**: C1 (gh60) mergeado. C2 idealmente mergeado (independência técnica permite review paralelo).
- **Gates**: `G_widget_reachability` + `G_transition_reachability` zero discrepâncias; `G_external_exit` (fixture com ACTION_VIEW/ACTION_SHARE); `G_event_type_coverage` (cryptoapp emite `item_selected` p/ `spinnerMessageDigest`).
- **Final sweep**: pós-merge de C3, sweep 380 APKs com schema final.
- **Refs**: `closes #<N+2>`, `follows #60, #<C2 issue>`.

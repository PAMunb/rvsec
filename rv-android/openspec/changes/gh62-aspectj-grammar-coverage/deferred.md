# Deferred-by-Design — gh62-aspectj-grammar-coverage (round-8)

**Purpose**: snapshot of every AspectJ construction the gh62 round-8 closure series explicitly does NOT implement, with the demand evidence (zero at the pipeline stage that would consume it) and rationale per entry. Matches one-to-one with matrix rows whose `Verdict ∈ {EXPLICIT-NO-OP, NOT-NEEDED}`.

**Frozen-snapshot tripwire**: SHA-256 of this file is committed to `rvsec-android/rvsec-instrumentation-dexlib2/grammar-tests/src/test/resources/deferred.snapshot.sha256` per design D7 (round-7 reformulation). `MatrixIntegrityTest.testDeferredDocumentIsFrozenPostArchive` reads both files and fails the build if they diverge (positive enforcement of the "frozen post-archive" property). Round-8 fix: the snapshot is generated in the same commit as the final `deferred.md` edit (see tasks §1.4), eliminating the round-7 race condition flagged by cross-LLM review.

**Round-8 redesign — absorption-aware demand**: round-7 framed scope by **source-level `.mop`/`.aj` demand**. Round-8 reframes scope by **pipeline-level demand** — the demand that actually reaches the dexlib2 instrumenter after the upstream stages (JavaMOP compiler, AJC, `coverage-weaver`) have absorbed everything they can absorb. The Three Empirical Audits Appendix at the bottom of this file documents the evidence base.

Concretely: a `.mop` source may declare 64 `condition(...)` clauses in `jca/`, but the `.aj` produced by the JavaMOP compiler contains **zero** — the condition logic has been folded into the corresponding `*RuntimeMonitor.*Event(...)` method body that the dexlib2 instrumenter already invokes. The round-7 closure §4.G (`ConditionGuardEmitter`, ~80 LOC) was attacking a construction the instrumenter never sees. Round-8 catches seven such closures.

**What is NOT here**: every construction with `DemandCounter ≥ 1` at the pipeline stage that consumes it ships in-change per design D9 (round-8 demand-driven absorption, post round-7 absorption-aware refinement). The round-6 `Fix-now`/`Follow-up` ledger buckets remain eliminated. AspectJ extensions outside the closed enumeration (`lock`/`unlock` per OQ3; per-clauses, ITDs, `declare warning/error/soft`, annotation-style aspects, generics in type patterns per OQ4) are NOT in the matrix and NOT here — they are AUSENTE-JUSTIFICADO in design.md.

---

## 1. Deferred-by-design (EXPLICIT-NO-OP)

Production code raises `UnsupportedOperationException` AND a passing test asserts the throw. The matrix row's `Evidence` column cites both the test FQN AND the `file:line` of the no-op declaration. If a future MOP spec uses one of these constructions, the matrix row's `EXPLICIT-NO-OP` verdict will fail loudly via the UOE assertion test, signalling the need to open a sub-change implementing the closure.

| AspectJ syntax | Production no-op anchor | Assertion test FQN | Rationale |
|---|---|---|---|
| `around` advice | `EmitterDispatch.java:61-65` — `throw new UnsupportedOperationException("around advice not supported")` | `EmitterDispatchTest.java:54-59` — `aroundAdviceRejected` asserts `UnsupportedOperationException` | Zero demand across all four corpora (`DemandCounter` returns 0/0/0/0). Implementing `around` requires wrapper-method synthesis: the emitter creates a synthetic method that calls the advice, and the advice calls `proceed()` which invokes the original — a substantial DEX-level feature with no current use case. |
| `proceed(...)` keyword in around body | Same `EmitterDispatch.java:61-65` — `around` rejection covers `proceed` by reflection (the around emit never runs, so no separate code path exists for `proceed`) | Same `EmitterDispatchTest.java:54-59` | Subsumed by `around` deferral. The matrix row cites the same anchor; the EXPLICIT-NO-OP verdict applies by reflection. |

---

## 2. Deferred-by-design (NOT-NEEDED)

`DemandCounter` returns zero at the pipeline stage that would consume the construction. There are two paths under this verdict and the matrix `Evidence` column distinguishes them:

- **Path α** — zero source-level demand AND no implementation. The construction does not appear in any of the four corpora at any stage.
- **Path β** — non-zero source-level demand in `.mop` and/or `.aj`, but absorbed by an upstream pipeline stage (JavaMOP compiler, `coverage-weaver`, AJC weaver, `MonitorRuntime` dispatch loop) before reaching the dexlib2 pipeline. Pipeline-stage demand at dexlib2 is zero.

For both paths, the Evidence column cites an enabled passing test asserting the absence of pipeline demand. For path β, the test additionally asserts that the upstream-absorbed equivalent input is produced (or absorbed equivalently) regardless of upstream source syntax. If a future corpus introduces non-zero **pipeline** demand for any deferred row, `MatrixIntegrityTest.testPipelineDemandCountsReproducible` fails the build (the matrix's pipeline-demand cell will diverge from the helper's output), forcing amendment via a new sub-change.

### 2.1 NOT-NEEDED path α (zero source demand + no implementation)

| AspectJ syntax | Anchor | Assertion test FQN | Rationale |
|---|---|---|---|
| `get(FieldPattern)` field-access pointcut | `MISSING` in parser/matcher/emitter | `FieldAccessGrammarTest.getFieldAccessHasZeroCorpusDemand` | Zero demand. Implementation would require a new join-point family (iput/sget/iput-object/sget-object DEX opcodes). The earlier draft's 356/158 count was a substring grep conflating `call(* Foo.get(...))` method-name calls with field-access pointcuts; corrected to zero. |
| `set(FieldPattern)` field-access pointcut | `MISSING` in parser/matcher/emitter | `FieldAccessGrammarTest.setFieldAccessHasZeroCorpusDemand` | Same as `get(FieldPattern)`. |
| `this(name)` value-binding | `MISSING` in parser/matcher (no `ThisPC` class) | `ThisGrammarTest.thisBindingHasZeroCorpusDemand` | Zero demand. The dexlib2 binding-form `target(name)` covers the common receiver-binding case; `this()` distinguishes the bound aspect instance from the join-point receiver, which the corpora do not exercise. |
| `this(Type)` type-matching | `MISSING` in parser/matcher | `ThisGrammarTest.thisTypeMatchingHasZeroCorpusDemand` | Same as `this(name)`. |
| `withincode(...)` | `MISSING` in parser; falls into `NamedRefPC` always-match in matcher | `WithincodeGrammarTest.withincodeHasZeroCorpusDemand` | Zero demand. Would filter join points by enclosing method signature — niche use case. |
| `cflow(...)` | `MISSING` in parser; `NamedRefPC` always-match in matcher | `CflowGrammarTest.cflowHasZeroCorpusDemand` | Zero demand. Runtime flow tracking requires a thread-local stack of active frames and instrumentation in every method on the candidate flow — substantial runtime cost for zero current use. |
| `cflowbelow(...)` | Same as `cflow(...)` | `CflowGrammarTest.cflowBelowHasZeroCorpusDemand` | Same rationale as `cflow(...)`. |
| `initialization(...)` | `MISSING` in parser; `NamedRefPC` always-match | `InitializationGrammarTest.initializationHasZeroCorpusDemand` | Zero demand. `staticinitialization(...)` is in-change via §4.Y; constructor-related variants have zero demand. |
| `preinitialization(...)` | Same as `initialization(...)` | `InitializationGrammarTest.preInitializationHasZeroCorpusDemand` | Same as `initialization(...)`. |
| `@annotation(...)` AspectJ 5 | Parser-crash at `PointcutExpressionParser.isIdentPart()` (rejects `@` as ident char) | `AnnotationPointcutGrammarTest.atAnnotationHasZeroCorpusDemand` | Zero demand across all four corpora. Implementing requires fixing `isIdentPart` + adding `AnnotationPC` + DEX-level annotation table lookup. |
| `@target(...)` AspectJ 5 | Same parser-crash | `AnnotationPointcutGrammarTest.atTargetHasZeroCorpusDemand` | Same as `@annotation(...)`. |
| `@this(...)` AspectJ 5 | Same parser-crash | `AnnotationPointcutGrammarTest.atThisHasZeroCorpusDemand` | Same. |
| `@args(...)` AspectJ 5 | Same parser-crash | `AnnotationPointcutGrammarTest.atArgsHasZeroCorpusDemand` | Same. |
| `@within(...)` AspectJ 5 | Same parser-crash | `AnnotationPointcutGrammarTest.atWithinHasZeroCorpusDemand` | Same. |
| `@withincode(...)` AspectJ 5 | Same parser-crash | `AnnotationPointcutGrammarTest.atWithincodeHasZeroCorpusDemand` | Same. |
| Standalone `target(name)` / `args(name)` without `&&`-composition | Routes through `PointcutMatcher.java:106-108` for `Match.empty(pe)` resolution | `CompositionGrammarTest.standaloneTargetArgsHasZeroCorpusDemand` | All corpus uses are `&&`-composed with `call()`; standalone matching is not exercised. |
| `!cflow` parser specialization | `MISSING` in parser | `CompositionGrammarTest.negativeCflowHasZeroCorpusDemand` | Zero demand. Only `!within`/`!target`/`!args` negations appear in corpora. |
| `!cflowbelow` parser specialization | `MISSING` in parser | `CompositionGrammarTest.negativeCflowBelowHasZeroCorpusDemand` | Same. |
| `!if` parser specialization | `MISSING` in parser | `CompositionGrammarTest.negativeIfHasZeroCorpusDemand` | Same. |
| `!handler` parser specialization | `MISSING` in parser | `CompositionGrammarTest.negativeHandlerHasZeroCorpusDemand` | Same. |
| `T+` inside `!within(...)` | `matchesTypePattern` strips `+` before exact-match | `TypePatternGrammarTest.tPlusInsideNegativeWithinHasZeroCorpusDemand` | Zero demand. Positive `within(T+)` is also deferred in round-8 (Coverage.aj-only consumer; Coverage.aj absorbed by `coverage-weaver` — see §2.2-D below); the negative form has no use. |
| SignaturePattern modifier `!public` | `MISSING` in parser (positive visibility is stripped silently) | `SignatureModifierGrammarTest.negatedVisibilityHasZeroCorpusDemand` | Zero demand. |
| SignaturePattern modifier `static` | `MISSING` in parser | `SignatureModifierGrammarTest.staticModifierHasZeroCorpusDemand` | Zero demand. |
| SignaturePattern modifier `final` | `MISSING` in parser | `SignatureModifierGrammarTest.finalModifierHasZeroCorpusDemand` | Zero demand. |
| SignaturePattern modifier `throws ExceptionPattern` | `MISSING` in parser | `SignatureModifierGrammarTest.throwsModifierHasZeroCorpusDemand` | Zero demand. |

### 2.2 NOT-NEEDED path β (source-level demand absorbed upstream)

The construction may exist at source level in `.aj` or `.mop` files but is consumed by an upstream toolchain stage before reaching the dexlib2 pipeline. The matrix Evidence column cites: (a) the source-level demand counts (typically non-zero), (b) the upstream stage that absorbs the construction, (c) the `file:line`/RELATORIO citation that **proves the absorption empirically**, and (d) an enabled passing test asserting that the equivalent descriptor input is produced (or absorbed equivalently) regardless of upstream source syntax.

#### 2.2.1 Round-8 newly-reclassified entries (the round-7 closures that became NOT-NEEDED β)

These are the **seven closures** that round-7 planned to ship in-change but that round-8 reclassifies as NOT-NEEDED β based on the Three Empirical Audits. Each entry cites the audit evidence directly. The matrix rows for these constructions are NOT removed — they remain in the matrix with `Verdict = NOT-NEEDED` and the Evidence column quotes the audit finding.

| AspectJ syntax | Source-level demand | Upstream absorption stage | Empirical evidence | Assertion test FQN | Rationale |
|---|---|---|---|---|---|
| **A. `condition(...)`** JavaMOP MOP-extension guard | 64 jca + 10 generic_new = 74 sites | **JavaMOP compiler** — folds the condition into the `*RuntimeMonitor.*Event(...)` body | `results/gh53_smoke_dexlib2/monitors/MultiSpec_1MonitorAspect.aj`:212-218 shows `condition(isValid(transformation))` from `CipherSpec.mop:32` rewritten as `after (...) returning (...) : CipherSpec_g1(transformation) { MultiSpec_1RuntimeMonitor.CipherSpec_g1Event(transformation, c); ... }` — the condition has disappeared. Grep confirms `wc -l <(grep 'condition' MultiSpec_1MonitorAspect.aj)` = 0. | `ConditionGrammarTest.conditionAbsorbedByRuntimeMonitor` — asserts (a) source-level count via `DemandCounter.countMop("condition")` ≥ 1; (b) `DemandCounter.countCompiledAj("condition")` == 0; (c) the corresponding `*RuntimeMonitor.*Event` method exists in the descriptor with the condition logic in its body | Round-7 planned §4.G `ConditionGuardEmitter` (~80 LOC). The JavaMOP compiler had already moved the condition logic into the runtime monitor, where the dexlib2-emitted `invoke-static *RuntimeMonitor.*Event(...)` already invokes it. The dexlib2 instrumenter has nothing left to guard. |
| **B. `__STATICSIG`** JavaMOP macro | 3 sites in `generic_new/` (`Collection_HashCode.mop:23`, `Serializable_NoArgConstructor.mop:33`, `URLConnection_OverrideGetPermission.mop:21`) | **JavaMOP compiler** — expands the macro before emitting `.aj`; absent from compiled JCA AND generic_new aspects (audit PASS 2026-05-26) | `results/gh53_smoke_dexlib2/monitors/MultiSpec_1MonitorAspect.aj` (JCA-derived): zero `__STATICSIG`/`toLongString`/`thisJoinPointStaticPart` references. **Generic_new audit confirmed 2026-05-26**: `$RVSEC_HOME/rvsec/rvsec-mop/src/main/resources/generic_new/MultiSpec_1MonitorAspect.aj` (592 LOC) also has zero hits for the three markers. The compiler inlines `thisJoinPoint.getStaticPart().getSignature()` directly into the `*RuntimeMonitor.*Event(...)` call (see linhas 519-522: `after () : Collection_HashCode_staticinit() { MultiSpec_1RuntimeMonitor.Collection_HashCode_staticinitEvent(thisJoinPoint.getStaticPart().getSignature()); }`), so the source-level `Signature initsig = __STATICSIG;` is gone post-compilation. | `StaticSigGrammarTest.staticSigAbsorbedByJavaMopCompiler` — asserts (a) source-level count via `DemandCounter.countMop("__STATICSIG")` ≥ 1 in `generic_new`; (b) `DemandCounter.countCompiledAj("__STATICSIG")` == 0 in BOTH `jca` AND `generic_new` compiled aspects; (c) the compiled aspect contains `thisJoinPoint.getStaticPart().getSignature()` invocations on the corresponding line ranges (the inlined absorption pattern) | Round-7 planned §4.S `StaticSigEmitter` (~80 LOC) to detect `__STATICSIG` in advice body source. The compiler had already expanded it for BOTH corpora. The round-8 archive precondition is **SATISFIED** as of 2026-05-26 audit; the contingency placeholder in tasks §4.S'.3 (reintroduce §4.S) is NOT activated. |
| **C. `adviceexecution()`** real semantics | 1 jca + 1 generic_new (in `commonPointcut`: `!adviceexecution()`) | **dexlib2 inline-call emission model** — dexlib2 emits `invoke-static *RuntimeMonitor.*Event(...)` directly at the matched call site rather than synthesising AJC-style advice methods (`ajc$before$...`/`ajc$after$...`). The semantics of `!adviceexecution()` ("not while inside an advice body") is vacuously true because advice-body executions do not exist as separate join points in this model. | APK inspection of `results/gh53_smoke_ajc/instrumented_apks/cryptoapp.apk` confirms AJC-instrumented APKs have `ajc$after$...` methods (`smali/mop/MultiSpec_1MonitorAspect.smali`); equivalent dexlib2-instrumented APKs have no such methods (dexlib2 emits `invoke-static` calls inline at the matched site instead of synthesising advice methods). The `commonPointcut` JSON's `!adviceexecution()` clause is therefore satisfied trivially by the dexlib2 emission model. | `AdviceExecutionGrammarTest.adviceExecutionVacuouslyTrueInDexlib2InlineModel` — asserts (a) the descriptor's `commonPointcut` contains `!adviceexecution()`; (b) dexlib2 emits no synthetic advice methods (proven by scanning the woven DEX string pool for method names containing `ajc$before$`/`ajc$after$` and asserting zero hits); (c) the matched join points are all call sites (not advice executions), so the negation is satisfied without explicit matcher logic | Round-7 planned §4.A `AdviceExecutionPC` (~80 LOC) filtering on AJC-style advice method names. Those names do not exist in the dexlib2 output. Implementing the matcher to look for them would either always-fail (correct but useless) or require synthesising fake advice method names purely to satisfy the matcher. The instrumentation model itself absorbs the construction. |
| **D. AspectJ runtime substrate** (`br.unb.cic.rv.aspectjlang.*`, ~600 LOC) | Coverage.aj imports `org.aspectj.lang.Signature`, `org.aspectj.lang.reflect.MethodSignature` (1 aspect site) | **`coverage-weaver` module** — already shipped in dexlib2 (`coverage-weaver/src/main/java/br/unb/cic/rv/coverage/`); semantically equivalent to Coverage.aj per the module's own javadoc | `coverage-weaver/src/main/java/br/unb/cic/rv/coverage/CoverageWeaver.java:23-32` javadoc: "Semantically equivalent to the AspectJ rule in `Coverage.aj`". `coverage-weaver/src/main/java/br/unb/cic/rv/coverage/SignatureFormatter.java:14-17` javadoc: "reproduces it byte-for-byte so the Layer-5 RVSEC-COV recall check comparing ajc output against dexlib2 output lines up without per-apk tweaks." `experimento-20260508/RELATORIO.md` §3.2/§7.2 confirms: 190 APKs, JCA spec, `RV_INSTRUMENTATION_VARIANT=dexlib2`, all coverage data via `mop.Coverage.log()` (the runtime helper that `CoverageWeaver` invokes). | `RuntimeSubstrateGrammarTest.aspectJSubstrateAbsorbedByCoverageWeaver` — asserts (a) `coverage-weaver` module produces a synthetic `mop.Coverage` runtime class in the dexlib2 build output; (b) no MOP advice in the JavaMOP-compiled `.aj` references `org.aspectj.lang.*` (only `Coverage.aj` does, and `Coverage.aj` is not consumed by the dexlib2 pipeline in production builds); (c) the `experimento-20260508` artefact contains zero `org.aspectj.lang.*` references in the instrumented DEX | Round-7 planned §4.RT to ship six minimal local substrate classes (~600 LOC) under `br.unb.cic.rv.aspectjlang.*` + an `AspectJRuntimeRemapper` (~150 LOC) that rewrites `org.aspectj.lang.*` references in the woven bytecode. The only consumer of those references is `Coverage.aj`, and `Coverage.aj` itself is absorbed by `coverage-weaver`. The substrate has no caller after the absorption. |
| **E. `thisJoinPoint*` bindings** end-to-end (~250 LOC) | 1 Coverage.aj `thisJoinPointStaticPart` + 3 generic_new `thisJoinPoint` via `__STATICSIG` | **JavaMOP compiler (for `__STATICSIG`)** + **`coverage-weaver` (for `thisJoinPointStaticPart`)** | APK inspection of `results/gh53_smoke_ajc/instrumented_apks/cryptoapp.apk`: 115/115 MOP advices in `smali/mop/MultiSpec_1MonitorAspect.smali` have zero `Lorg/aspectj/lang/JoinPoint;` references (`grep -c 'JoinPoint\|Signature\|thisJoinPoint\|ajc$tjp_\|__STATICSIG' smali/mop/MultiSpec_1MonitorAspect.smali` → 0). Only `Coverage` aspect uses `JoinPoint.StaticPart` (`ajc$tjp_N` static fields + `Factory.makeSJP(...)`). The MOP path simply does `invoke-static *RuntimeMonitor.*Event(args_tipados)`. | `ThisJoinPointGrammarTest.thisJoinPointBindingsAbsorbedByJavaMopAndCoverageWeaver` — asserts (a) the compiled `.aj` for JCA contains zero `thisJoinPoint*` references; (b) the `__STATICSIG` macro is expanded upstream (see entry B above); (c) the only remaining consumer (`Coverage.aj`) is absorbed by `coverage-weaver` (see entry D above) | Round-7 planned §4.JP `ThisJoinPointEmitter` (~250 LOC) to emit binding bytecode for `thisJoinPoint`/`thisJoinPointStaticPart`/`thisEnclosingJoinPointStaticPart`. Both consumers (MOP via `__STATICSIG`, Coverage.aj directly) are absorbed upstream. The closure has no use in the post-absorption pipeline. |
| **F. `Coverage.aj` end-to-end weaving** | 1 aspect (the file itself) | **`coverage-weaver` module** | See entry D above. The `coverage-weaver` module emits `invoke-static {vX}, Lmop/Coverage;->log(Ljava/lang/String;)V` at every non-excluded method entry, with the Soot-style signature pre-computed at weave time as a constant string. The output is byte-for-byte equivalent to what AJC would weave from `Coverage.aj`, but without the `JoinPoint`/`Signature`/`reflect.MethodSignature` runtime objects (zero reflection, zero GC pressure). | `CoverageAjAbsorptionGrammarTest.coverageAjAbsorbedByCoverageWeaver` — asserts (a) the `coverage-weaver` module is present in the dexlib2 build output; (b) instrumented APKs contain `Lmop/Coverage;->log(Ljava/lang/String;)V` invocations at method entries; (c) the RVSEC-COV recall metric on the experimento-20260508 sample matches the AJC-variant recall to within 0% (the byte-for-byte equivalence claim); (d) no `Lorg/aspectj/lang/JoinPoint$StaticPart;` or `Lorg/aspectj/runtime/reflect/Factory;` references appear in the instrumented DEX | Round-7 planned §4.CV `CoverageAjEndToEndTest` (synthesising an Android-shaped fixture, weaving `Coverage.aj` against it, asserting that ART verification passes and `thisJoinPointStaticPart.getSignature()` resolves correctly). The `coverage-weaver` module already delivers the same operational outcome (RVSEC-COV log lines) without weaving `Coverage.aj` at all. The `Coverage.aj` source is retained as a semantic reference (per the `CoverageWeaver` javadoc) but the dexlib2 pipeline does not consume it. |
| **G. `within(*..Log)` suffix-wildcard + `within(Coverage+)` `T+`-in-positive-within** | 1 aspect (Coverage.aj only) | **`coverage-weaver`** — the package-exclusion logic that round-7 §4.WW would have implemented for Coverage.aj is delivered by `coverage-weaver/PackageFilter.java` instead | `coverage-weaver/PackageFilter.java` (~61 LOC) hard-codes the exclusion list (`android..*`, `java..*`, `kotlin..*`, `mop..*`, `aspectj..*`, etc.) that Coverage.aj's `excludedPackages()` named pointcut declared. The dexlib2 pipeline does not need to evaluate `within(*..Log)` or `within(Coverage+)` because the only consumer of those forms is Coverage.aj, which is absorbed. | `WithinExtensionsGrammarTest.withinSuffixAndTPlusAbsorbedByCoverageWeaver` — asserts (a) `coverage-weaver/PackageFilter.java` exists and excludes the expected packages; (b) the simple positive `within(pkg..*)` form is still in-change via §4.W (other consumers exist in JCA/generic_new); (c) only the suffix-wildcard and `T+`-in-positive-within sub-forms are absorbed | Round-7 planned §4.WW (~50 LOC) extending §4.W to suffix-wildcards (`*..Log`) and subtype-aware matching (`Coverage+`). Both sub-forms have a single consumer (Coverage.aj); that consumer is absorbed. The simple `pkg..*` form remains in-change because other consumers exist (JCA descriptor `commonPointcut`, generic_new specs). |

**Archive precondition for entry B** — **SATISFIED 2026-05-26**. The `generic_new` audit (tasks §4.S'.1) was completed by subagent inspection of `$RVSEC_HOME/rvsec/rvsec-mop/src/main/resources/generic_new/MultiSpec_1MonitorAspect.aj` (592 LOC). Grep counts: `__STATICSIG`=0, `toLongString`=0, `thisJoinPointStaticPart`=0. The contingency placeholder in tasks §4.S'.3 is NOT activated; entry B archives as documented.

#### 2.2.2 Round-7 inherited entries (unchanged from round-7)

| AspectJ syntax | Upstream absorption | Assertion test FQN | Rationale |
|---|---|---|---|
| `handler(...)` | `PointcutExpressionParser-NamedRefPC-fallback` — exception-handler join points have no DEX-level analogue; source-level absorbed by matcher's family-of-pointcuts decision | `HandlerGrammarTest.handlerAbsorbedByNamedRefPC` | Zero source demand. Source-level present but pipeline-level absent. Reclassified from SILENT-GAP-permanent to NOT-NEEDED β in round-5. |
| `declare precedence: A, B;` | `MonitorRuntime-dispatch-loop` — runtime monitor-dispatch property, not weaver concern | `AspectDeclarationGrammarTest.declarePrecedenceAbsorbedByDispatchLoop` | Zero source demand. The `MonitorRuntime` serialises monitor invocations through a deterministic dispatch loop; AspectJ "precedence" is realised by the loop ordering. |
| `aspect Foo { ... }` declaration syntax | `JavaMOP-descriptor-emit + DescriptorReader.java:13-15` — `.aj` source consumed by JavaMOP compiler; `PointcutExpressionParser` never sees `aspect`/`pointcut` source tokens | `AspectDeclarationGrammarTest.aspectFooAbsorbedByDescriptorReader` | Source-level non-zero (aspect=1, jca=2, generic_new=2) but pipeline demand = 0. |
| `pointcut p(): ...` named declaration | Same path β as `aspect Foo` | `AspectDeclarationGrammarTest.namedPointcutDeclarationAbsorbedByDescriptorReader` | Source-level non-zero (aspect=2, jca=1, generic_new=1) but pipeline demand = 0. The `Coverage.aj` named pointcuts (`excludedPackages()`, `traced()`) are absorbed together with Coverage.aj itself (see §2.2.1-F). The JCA `BaseAspect.notwithin()` named ref remains in-change via §4.B. |
| Aspect inheritance `aspect Bar extends Foo` | `JavaMOP-descriptor-emit` — inheritance resolved upstream; flattened descriptor emitted | `AspectDeclarationGrammarTest.aspectInheritanceAbsorbedByDescriptorReader` | Zero source demand. The pipeline reads `AspectDescriptor`; inheritance is upstream concern. |
| Abstract aspect + concrete subaspect | `JavaMOP-descriptor-emit` — same upstream-absorption as inheritance | `AspectDeclarationGrammarTest.abstractAspectAbsorbedByDescriptorReader` | Zero source demand. The `BaseAspect.notwithin()` macro is in-change via §4.B as a `NamedRefPC` expansion. |
| Privileged aspect | `JavaMOP-descriptor-emit` — access resolved upstream | `AspectDeclarationGrammarTest.privilegedAspectAbsorbedByDescriptorReader` | Zero source demand. |
| ~~`execution(...)` real matcher consumer in dexlib2~~ | **ROUND-8 REVISION**: §4.E is RESTORED as a full in-change closure (matcher + emitter, ~230 LOC) per user decision 2026-05-26. The "no positive consumer in JCA/generic_new" observation remains true today, but the user opted for defensive shipping so future specs that use `execution(...)` positively do not require re-opening the change. See §4.E in tasks.md (in-change) and proposal.md "In-change closures (round-8)" list. | n/a (entry retracted) | This entry from the round-8 NOT-NEEDED β draft is RETRACTED. The corresponding matrix row carries `Verdict = COVERED` with the §4.E closure as evidence. |

---

## 3. Out of scope by construction (not in matrix, not here)

For completeness — these AspectJ productions are AUSENTE-JUSTIFICADO in design.md and do NOT appear in the matrix's closed enumeration. They are not deferred (a deferral implies the construction is in scope but explicitly skipped); they are excluded from scope.

- `lock`/`unlock` (OQ3) — AspectJ extensions, zero corpus demand.
- Per-clauses (`issingleton`, `perthis`, `pertarget`, `percflow`, `percflowbelow`, `pertypewithin`) (OQ4) — aspect-instantiation models, JavaMOP defaults to implicit `issingleton`.
- Inter-Type Declarations (`declare parents`, method/field/constructor introductions) (OQ4) — JavaMOP does not emit ITDs.
- `declare warning`/`declare error`/`declare soft` (OQ4) — compile-time mechanisms with no runtime weaving footprint.
- Annotation-style aspect syntax (`@Aspect`, `@Before`, etc.) (OQ4) — alternative AspectJ 5 surface; corpora exclusively use code-style.
- Generics in type patterns (`<T extends X>`, parameterised method signatures) (OQ4) — JavaMOP targets erasure-level signatures.

If any of these surface in a future corpus, the matrix amendment workflow opens by adding the row + the closure sub-change.

---

## 4. AJC parity diff (Follow-up — out of round-8 scope by design)

Round-5 cross-LLM review surfaced "AspectJ weaver parity diff vs. AJC offline reference" as an oracle-of-behaviour. After round-8 absorption, the equivalent question becomes "does the post-closure dexlib2 weaver produce byte-for-byte equivalent DEX to AJC's offline weaver for the corpus patterns?" — a separate engineering effort to wire AJC into CI as a comparative oracle for high-stakes rows. Deferred to a future Follow-up sub-change; out of gh62 scope (the goal is correct AspectJ semantics in dexlib2 for the constructions that reach the pipeline, not byte-for-byte equivalence with AJC's output for constructions absorbed upstream).

**Note**: the `coverage-weaver`/`Coverage.aj` equivalence claim made in §2.2.1-D and §2.2.1-F IS a byte-for-byte parity claim, but it is asserted against the `SignatureFormatter` javadoc and the RVSEC-COV recall metric, not against AJC weaving runs. A future Follow-up could harden this by running AJC against `Coverage.aj` and diffing the woven DEX against the `coverage-weaver` output.

---

## Appendix: The Three Empirical Audits

The round-8 reclassifications above rest on three audits run on 2026-05-26 against the live `origin/modules` branch. The audit reports are preserved at `docs/analise_sintese_macro.md` (synthesis) and the individual evidence files cited inline above. Summary:

### A.1 APK AJC-instrumentado real

**Target**: `results/gh53_smoke_ajc/instrumented_apks/cryptoapp.apk`, decompiled via `apktool d`.

**Question**: what does AJC actually emit in the woven bytecode for MOP advices, and for `Coverage.aj`?

**Finding**: 115/115 MOP advices in `smali/mop/MultiSpec_1MonitorAspect.smali` are trivial pass-through (`invoke-static *RuntimeMonitor.*Event(args_typed)`), with zero `thisJoinPoint`/`__STATICSIG`/`condition`/`Signature` references. Only `Coverage.aj` uses `JoinPoint.StaticPart` (via `ajc$tjp_N` static fields built by `Factory.makeSJP(...)` in each instrumented class's `<clinit>`). Implication: the round-7 closures targeting runtime-reflection-bearing constructions in MOP advices were attacking nothing — the JavaMOP compiler had already reduced them to pass-through calls.

### A.2 `.aj` gerado pelo JavaMOP

**Target**: `results/gh53_smoke_dexlib2/monitors/MultiSpec_1MonitorAspect.aj` (JCA-derived) and `results/gh53_smoke_dexlib2/monitors/Coverage.aj`.

**Question**: which AspectJ constructions survive the `.mop` → `.aj` compilation step?

**Finding**:
- `condition(...)` — **completely removed**. The MOP source `condition(isValid(transformation))` at `CipherSpec.mop:32` becomes `after (...) returning (...) : CipherSpec_g1(transformation) { MultiSpec_1RuntimeMonitor.CipherSpec_g1Event(transformation, c); ... }` at `MultiSpec_1MonitorAspect.aj:212-218` — the condition logic moved into the runtime monitor body.
- `__STATICSIG` — **zero occurrences** in the compiled JCA `.aj`.
- Named pointcuts (`namedPointcuts` schema) — the `AspectDescriptor` JSON consumed by dexlib2 today carries only `commonPointcut` (singular). `Coverage.aj` declares two named pointcuts (`excludedPackages()`, `traced()`) that the current schema cannot represent; if `Coverage.aj` were in-pipeline, §4.D would be needed. Since `Coverage.aj` is absorbed (§2.2.1-D/F), §4.D is needed only for `BaseAspect.notwithin()` (§4.B's input), and the schema extension is correspondingly smaller in scope (one `commonPointcut` + at most one named ref per descriptor for the JCA workload).
- Construction inventory of the compiled `.aj`: `pointcut`, `before`, `after`, `after returning`, `call`, `target`, `args`, `!within`, `&&`, `||`, `!adviceexecution()`. **Nothing else**.

### A.3 `coverage-weaver` vs `Coverage.aj`

**Target**: `rvsec-android/rvsec-instrumentation-dexlib2/coverage-weaver/src/main/java/br/unb/cic/rv/coverage/*` and `rvsec-mop/src/main/resources/aspect/Coverage.aj`.

**Question**: is `coverage-weaver` semantically equivalent to `Coverage.aj`?

**Finding**: YES, and the equivalence is explicit in the module's javadoc:
- `CoverageWeaver.java:23-32` — "Semantically equivalent to the AspectJ rule in `Coverage.aj`".
- `SignatureFormatter.java:14-17` — "reproduces it byte-for-byte so the Layer-5 RVSEC-COV recall check comparing ajc output against dexlib2 output lines up without per-apk tweaks".
- `experimento-20260508/RELATORIO.md` §3.2 / §7.2 — the 190-APK production experiment ran `RV_INSTRUMENTATION_VARIANT=dexlib2` exclusively; all 2.78M coverage rows came from `mop.Coverage.log()` (the `coverage-weaver`-emitted runtime helper), NOT from `Coverage.aj`.

The implication is unambiguous: shipping the AspectJ runtime substrate (~600 LOC under `br.unb.cic.rv.aspectjlang.*`) + the `thisJoinPoint` binding emitter (~250 LOC) + the `execution(...)` emitter (~150 LOC) to support `Coverage.aj` would buy zero net capability. `coverage-weaver` already delivers the equivalent operational outcome with better thread-safety (`ConcurrentHashMap.newKeySet()` vs `HashSet<String>` in the aspect), better runtime cost (string-pool constant vs reflective `Method.getDeclaringClass().getName()` per call), and zero AspectJ runtime dependencies.

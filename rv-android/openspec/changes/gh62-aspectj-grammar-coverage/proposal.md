# Proposal: gh62-aspectj-grammar-coverage

## Why

The dexlib2 instrumenter (`rvsec-android/rvsec-instrumentation-dexlib2/`) has accumulated five incremental fixes (`gh52`, `gh56`, `gh57`, `gh59`, `gh61`), each targeting a single observed gap surfaced by a downstream symptom (a `VerifyError`, a missing `g2` event, a wide-slot binding bug). A cross-LLM artifact review of `gh61` (Codex GPT-5, DeepSeek, Gemini 2.5 Pro, Claude Opus 4.7) and a follow-up review of an earlier draft of this very change revealed that this reactive posture leaves **silent gaps** in production: pointcuts that parse without error, weave without crashing, and emit nothing — invisible without an external oracle.

Four classes of silent failure are confirmed against the current `HEAD` of `origin/modules`:

1. **Matcher always-match for unmodelled designators.** `PointcutMatcher.java:109-114` collapses `IfPC`, `NamedRefPC`, and `WithinPC` into `Match.empty(pe)` regardless of the underlying expression. Every pointcut that the parser cannot model becomes a `NamedRefPC` (`PointcutExpressionParser.java:131-132`), which means `this(...)`, `withincode(...)`, `cflow(...)`, `cflowbelow(...)`, `handler(...)`, `initialization(...)`, `preinitialization(...)`, `get(FieldPattern)`, `set(FieldPattern)`, and `adviceexecution()` are all silently always-match. The AspectJ 5 annotation family (`@annotation`, `@target`, `@this`, `@args`, `@within`, `@withincode`) reaches the parser through a different failure mode: `PointcutExpressionParser.isIdentPart()` rejects `@` as an identifier character, so the parser raises `PointcutParseException` before reaching the `NamedRefPC` fallback. The end-state verdict is still SILENT-GAP (the gap surfaces as a parse-time exception that callers catch and downgrade), but the matcher always-match description does not apply to that family — the matrix rows for `@*` MUST cite the parser-crash anchor, not the matcher always-match anchor.
2. **Strict `.equals()` against unmodelled descriptors for partially-modelled forms.** `T+` in `call()` owner position (`PointcutMatcher.java:153-157` — `if (!expectedOwner.equals(actualOwner) && !cpsAwareOwnerMatch(...))`), `T+` in return type, `*` wildcard in method name (`PointcutMatcher.java:161-167` — `if (!expectedName.equals(mr.getName()))`), and trailing-mixed `(T, ..)` varargs (`PointcutExpressionParser.java:271-273` — `isVarargs` only accepts `..` as the sole element) fail to match because the matcher does literal string equality without expanding subtype/wildcard/varargs semantics. The earlier draft framed this as "malformed descriptors like `Ljava/io/OutputStream+;` being constructed" — that narrative is pedagogically convenient but not what the code does. The real defect is missing subtype/wildcard expansion before the `.equals()` (`cp.declaringType()` preserves the `+` suffix and feeds it unchanged into the comparison).
3. **Silent emitter no-op.** `DexWeaver.java:560-566` has `case TRY_CATCH_WRAP: case REPLACE: break;` — `after() throwing(...)` plans are generated by `AfterThrowingEmitter` and silently discarded by the weaver with no log, no counter, no exception. The standard test surface — green bar against the JCA fixtures — cannot detect any of this.
4. **Silent advice-body reflective API gap (behavioural parity with AspectJ).** The dexlib2 instrumenter exists because AspectJ's offline weaver breaks on newer Android apps; for it to be a drop-in replacement, the *runtime contract* of advice bodies must match AspectJ. A typical MOP-generated advice does `Object[] a = thisJoinPoint.getArgs(); Signature s = thisJoinPoint.getSignature(); MonitorRuntime.dispatch(s.getName(), a);` — if dexlib2 weaves the advice but does not populate `thisJoinPoint`, the monitor compiles, executes, and emits empty events. The corpora use `thisJoinPoint` (3 in `generic_new/`), `.getSignature()` (4 across `aspect/`+`generic_new/`), `Signature` subtypes (`MethodSignature`/`ConstructorSignature`/`FieldSignature` — 7 in `aspect/Coverage.aj` template), `aspect Foo { ... }` declaration syntax (5), and named `pointcut p(): ...` declarations (4). This class also covers `proceed(...)` in around bodies and aspect-declaration mechanics (abstract aspects, inheritance, `declare precedence`) — the bridge between *grammar that parses* and *instrumentation that produces the events the monitors actually consume*.

Corpus demand (**provisional — to be regenerated by `DemandCounter` per task §3.4 before matrix population**; cross-LLM Round-5 review independently re-grepped the four corpora and found ~11/17 designators diverge from the values below by 2-4×, driven by (a) Java `if()` statements in advice bodies contaminating the pointcut `if()` count, (b) `.aj` template files in `generic_new/` like `MultiSpec_1MonitorAspect.aj` containing 55-115 `pointcut p():` declarations vs. the 1 declared, (c) `grep -c` line-counting vs. `-oc` occurrence-counting). The table below is preserved as the initial baseline for traceability; the canonical values land in the matrix after `DemandCounter.countAll()` runs in §3.4. Patterns distinguish *pointcut* uses from *method-name* uses; `DemandCounter` scans both `.mop` AND `.aj` files (the cross-LLM review showed restricting to `.mop` undercounts substantially).

| Designator / form | aspect | jca | generic | generic_new |
|---|---:|---:|---:|---:|
| `get(FieldPattern)` / `set(FieldPattern)` *(true field-access pointcut)* | 0 | 0 | 0 | 0 |
| `(T, ..)` trailing-mixed varargs in `call()` | 0 | 14 | 0 | 4 |
| `T+` in `call()` owner/return | 0 | 0 | 0 | 135 |
| `execution(...)` (placeholder matcher) | 1 | 23 | 0 | 27 |
| `if(...)` (matcher always-match; emitter has `IfGuardEmitter` scaffold) | 8 | 16 | 0 | 37 |
| `this(...)` (any sub-semantic, as pointcut) | 0 | 0 | 0 | 0 |
| `target(...)` (binding + type) | 0 | 128 | 356 | 108 |
| `args(...)` (binding + type + mixed) | 0 | 159 | 96 | 47 |
| `adviceexecution()` (parsed as `NamedRefPC`) | 0 | 1 | 0 | 1 |
| `within(...)` positive | 24 | 13 | 0 | 13 |
| AspectJ 5 `@*` family (6 designators) | 0 | 0 | 0 | 0 |
| `after() throwing(...)` end-to-end | 0 | 0 | 0 | 2 |
| `thisJoinPoint` binding (advice-body API) | 0 | 0 | 0 | 3 |
| `thisJoinPointStaticPart` binding | 1 | 0 | 0 | 0 |
| `JoinPoint.getSignature()` + `Signature` subtypes | 8 | 0 | 0 | 3 |
| `aspect Foo { ... }` declaration | 1 | 2 | 0 | 2 |
| `pointcut p(): ...` named declaration | 2 | 1 | 0 | 1 |

Two prior baselines were corrected against the live grep: (a) earlier draft claimed 356/158 `get/set` field-access pointcuts in `generic/`/`generic_new/` — that count conflated `call(* Foo.get(...))` method-name calls with `get(FieldPattern)` field-access; corrected to **zero**. (b) v1 draft claimed `if(...) = 18 in generic_new`, `(T,..) = 4+ in jca`, `this(...) present in generic_new`, `execution(...) = 0 in jca`; verified counts are 37 / 14 / 0 / 23 respectively. The bucket assignments below reflect the corrected baseline.

This change moves from per-bug fixes to a **grammar-anchored coverage matrix**: one authoritative map of the AspectJ pointcut grammar against the dexlib2 parser/matcher/emitter, with demand counts from the four `.mop` corpora and verdicts (COVERED / SILENT-GAP / EXPLICIT-NO-OP / NOT-NEEDED) for every production. The matrix becomes the contract that future closures reference.

**Scope expansion (round-5 cross-LLM review)**: in addition to documenting the surface, this change implements closures for two row families whose silent failures are too systemic to defer:

1. **JoinPoint Reflective API family** — rows for `thisJoinPoint`, `JoinPoint.getSignature()` and its `MethodSignature`/`ConstructorSignature`/`FieldSignature` subtypes, `.getArgs()`, `.getTarget()`/`.getThis()`, `.getKind()`/`.getSourceLocation()`, and `org.aspectj.lang.JoinPoint` runtime linkage. Currently SILENT-GAP because `ThisJoinPointEmitter.signatureFor():22-30` returns the raw pointcut expression string (e.g. `"call(* Hashtable.get(Object)) && target(h) && args(o)"`) as if it were a `Signature` object — the emitter populates a wrong payload, not an empty one. Closure: ship local equivalents of `org.aspectj.lang.{JoinPoint,Signature,MethodSignature,ConstructorSignature,FieldSignature,SourceLocation}` in the instrumented DEX, extend `MonitorInvokeBuilder` to emit real `Signature` construction from the matcher `Context`, and replace `ThisJoinPointEmitter`'s string-return with register-based JoinPoint passing. Rows flip SILENT-GAP → COVERED in-change.
2. **Matcher Correctness family** — rows `target(name)` and `args(name)` *standalone* (currently `PointcutMatcher.java:106-108` returns `Match.empty(pe)` regardless; binding is only effective in `&&`-composition with `call()`); `!` negation rows beyond `!within` (currently `parseUnary` only specialises `!within` — `!handler`, `!cflow`, `!if` collapse to `NamedRefPC` always-match); named-pointcut references (currently `NamedRefPC` is the dominant always-match path — the closure resolves named pointcuts defined in the same aspect against the aspect's pointcut table instead of unconditionally matching). Closures ship real `TargetPC`/`ArgsPC` standalone matching, full `parseUnary` negation handling, and a `NamedRefPC` resolver. Rows flip SILENT-GAP → COVERED in-change.

The remaining SILENT-GAP rows (trailing-varargs, `T+` in `call()` owner/return, `execution(...)` real matcher, `if(...)` semantic evaluation, `after() throwing` weaver wiring, `target(Type)`/`args(Type)` type-matching, `adviceexecution()`, `BaseAspect.notwithin()` expansion, `withincode`/`cflow`/`cflowbelow`/`initialization`/`preinitialization`, AspectJ 5 `@*` family, signature-pattern modifiers, positive `within(...)` weaver-side filter, `T+` inside `!within`, `thisJoinPointStaticPart`/`thisEnclosingJoinPointStaticPart` bindings, abstract aspect, aspect inheritance, privileged aspect) remain in the scope ledger as Fix-now or Follow-up entries, scheduled as separate sub-changes. The smali property bump (`smali-dexlib2` 3.0.8 → 3.0.9) ships in §0 gated by an empirical `dexdump` diff (see §0.3a in tasks).

GitHub Issue: [#62](https://github.com/PAMunb/rvsec/issues/62)

## What Changes

**Matrix document (`docs/aspectj_grammar_coverage.md`)** — one row per AspectJ pointcut designator, advice form, type-pattern modifier, composition operator, advice-body reflective API element, aspect-declaration mechanic, and AspectJ runtime linkage requirement. For each row: normative syntax from the AspectJ Programming Guide §"Pointcuts" + AspectJ 5 quick reference, per-corpus demand counts obtained via a deterministic Java helper (`DemandCounter`, in the `grammar-tests` module — see below), parser/matcher/emitter state in dexlib2 anchored to `file:line`, and a verdict from the four-value vocabulary `{COVERED, SILENT-GAP, EXPLICIT-NO-OP, NOT-NEEDED}`. Verdicts MUST be anchored to evidence: every COVERED row cites a passing test in `grammar-tests/`; every SILENT-GAP row cites a `@Disabled` test naming the gap; every EXPLICIT-NO-OP row cites both an `UnsupportedOperationException`-asserting test AND the `file:line` of the no-op declaration.

The matrix SHALL cover **exactly** (not "at minimum") the following productions, enumerated by `AspectJDesignators.DESIGNATORS` in `grammar-tests`:

- **Classical pointcut designators (single-form rows)**: `call`, `execution`, `withincode`, `cflow`, `cflowbelow`, `if`, `handler`, `get`, `set`, `staticinitialization`, `initialization`, `preinitialization`, `adviceexecution`, named-pointcut references.
- **`target` / `this` / `args` sub-semantic rows** (one row per sub-semantic — the dexlib2 matcher diverges per sub-form):
  - `target(name)` value-binding (binds the receiver register; dominant use across `jca/`/`generic/` — e.g. `call(* Hashtable.get(Object)) && target(h) && args(o)`); `target(Type)` type-matching (matcher always-match at `PointcutMatcher.java:106-108`).
  - `this(name)` value-binding; `this(Type)` type-matching. Both currently fall into `NamedRefPC` always-match (`PointcutExpressionParser.java:131-132`); no `ThisPC` class exists.
  - `args(name)` value-binding; `args(Type)` type-matching; `args(*, name, ..)` mixed.
- **Within-family per-stage delegation rows**: `within(...)` positive (matcher always-match at `PointcutMatcher.java:109-114`, weaver-side filter required), `!within(...)` (matcher implements via `NotWithinPC`).
- **AspectJ 5 annotation pointcut designators**: `@annotation`, `@target`, `@this`, `@args`, `@within`, `@withincode`.
- **Advice forms** (each as a row, because the dexlib2 weaver/emitter pipeline diverges per form): `before`, `after`, `after returning`, `after throwing`, `around`. Note: `returning(Id)` and `throwing(Id)` are advice modifiers (`after() returning(name): ...`), NOT pointcut designators — they appear only inside the advice-form rows, not as standalone matrix rows.
- **Type-pattern modifiers** (one row per position because matcher diverges per position):
  - `T+` in `call()` param position; `T+` in `call()` owner position; `T+` in `call()` return position; `T+` inside `!within(...)` (currently exact-match-after-strip at `PointcutMatcher.java:343-359`).
  - `*` wildcard; `..` standalone varargs; `..` trailing-mixed `(T, ..)` (distinct row); dot-glob (`..*`); single-level glob (`.*`); arrays (`T[]`, `T[][]`); inner-class (`Outer.Inner` vs `Outer$Inner`).
- **SignaturePattern modifiers**: positive visibility (`public`/`private`/`protected`), negated visibility (`!public`), `static`, `final`, `throws ExceptionPattern`. The matcher currently strips positive modifiers (`PointcutExpressionParser.java:200-215`); negated and non-visibility modifiers are unmodelled.
- **Composition operators**: `&&`, `||`, `!`, parentheses.
- **Advice-body reflective API** (behavioural-parity rows — what the advice body reads at runtime; if dexlib2 weaves the advice but does not populate these the monitor silently produces empty events): `thisJoinPoint` binding; `thisJoinPointStaticPart` binding; `thisEnclosingJoinPointStaticPart` binding; `JoinPoint.getArgs()`; `JoinPoint.getSignature()` (+ `MethodSignature` / `ConstructorSignature` / `FieldSignature` subtype accessors `.getName()` / `.getDeclaringType()` / `.getParameterTypes()` / `.getReturnType()`); `JoinPoint.getTarget()` / `.getThis()`; `JoinPoint.getKind()` / `.getSourceLocation()`.
- **Around-advice mechanics**: `proceed(...)` keyword inside around body (one row; consistent with `around` itself being EXPLICIT-NO-OP at the emitter level — a future closure of `around` MUST simultaneously close this row).
- **Aspect declaration mechanics**: `aspect Foo { ... }` declaration syntax; named-pointcut declaration `pointcut p(): ...` (distinct from named-pointcut *reference* under classical designators); abstract-aspect + concrete-subaspect pattern (the JavaMOP `BaseAspect` idiom relies on this); aspect inheritance (`aspect Bar extends Foo`); `declare precedence: A, B;` (advice ordering across aspects — without it, two aspects matching the same join point emit events in non-deterministic order); privileged aspect (access to private members of woven types).
- **Runtime linkage**: `org.aspectj.lang.JoinPoint` (and `JoinPoint.StaticPart`, `Signature` subtypes) class availability in the instrumented bytecode — without this row the dependent advice-body rows have no runtime substrate.

New AspectJ versions or new corpora add new rows; new always-match or malformed-descriptor paths in production code WITHOUT a corresponding row are forbidden.

**Grammar test corpus (`rvsec-android/rvsec-instrumentation-dexlib2/grammar-tests/`)** — a new Maven test-only submodule containing one test method per matrix row. Every COVERED row has a passing fixture today; every SILENT-GAP has a `@Disabled("gh62 SILENT-GAP: <one-line>")` fixture whose assertion describes the correct AspectJ behaviour. As future closures ship, the closure's commit removes the `@Disabled` annotation AND flips the matrix row's verdict atomically — partial flips are blocked by `MatrixIntegrityTest` (see §Bidirectional enforcement below).

**`DemandCounter` Java helper** (in `grammar-tests`) — replaces the originally-proposed inline shell grep recipe. `DemandCounter.count(Designator, Corpus)` walks `$RVSEC_HOME/rvsec/rvsec-mop/src/main/resources/{aspect,jca,generic,generic_new}/` with `Files.walk()` and a compiled `Pattern` per designator. Determinism is portable (no `bash -lc`, no `LC_ALL`, no shell quoting), `ProcessBuilder` is removed, and `MatrixIntegrityTest.testDemandCountsReproducible` invokes the helper directly. The matrix document quotes the `Pattern` per designator inline so reviewers can read it without opening Java code.

**Bidirectional enforcement** — `MatrixIntegrityTest` asserts BOTH directions:
- *Matrix → tests*: every COVERED row has an enabled test; every SILENT-GAP row has a `@Disabled` test (already in the original proposal).
- *Tests → matrix*: every enabled (non-`@Disabled`) test method in `grammar-tests/` resolves to a `COVERED` or `EXPLICIT-NO-OP` row (the latter is an enabled passing test asserting `UnsupportedOperationException` or equivalent); every `@Disabled` test resolves to a `SILENT-GAP` row; the skip count equals the `SILENT-GAP`-row count. A `@Disabled` test that begins to pass silently (gap closed accidentally), or an orphan test with no matrix row, breaks CI.

**Scope ledger (`openspec/changes/gh62-aspectj-grammar-coverage/ledger.md`)** — a three-bucket schedule with per-entry `Owner` and `Target milestone`. Bucket assignments are tentative pending the `DemandCounter`-driven re-count in §3.4 (executed BEFORE matrix population in §5); the canonical assignment lives in tasks §1.3a/§1.3b/§1.3c.

The four-value verdict vocabulary is unchanged. Round-5 review eliminated the SILENT-GAP-permanent sub-bucket: `handler(...)` and `declare precedence` are now classified `NOT-NEEDED` via INV-INS-89 path β (handler joinpoints have no DEX-level analogue — the production "absorbs" them at the source level before reaching the matcher; `declare precedence` is a runtime ordering property of the monitor dispatch loop, not a weaver concern — also out-of-pipeline). `aspect Foo { ... }` and `pointcut p(): ...` are likewise `NOT-NEEDED` path β: the dexlib2 pipeline reads JSON `AspectDescriptor` produced upstream by the JavaMOP toolchain (`DescriptorReader.java`); `.aj` source never reaches `PointcutExpressionParser`. These four rows still appear in the matrix with `Verdict = NOT-NEEDED`, carry enabled passing tests asserting equivalent-descriptor production regardless of upstream syntax, and do NOT appear in the ledger (NOT-NEEDED rows are not open work; they are bucket-free by construction).

- **Fix-now** (active demand in at least one corpus + scheduled this milestone): trailing-varargs `(T, ..)` in `call()` params; `T+` in `call()` owner; `T+` in `call()` return; `execution(...)` real matcher; `BaseAspect.notwithin()` named-ref expansion; **`after() throwing(...)` end-to-end** (re-classified from EXPLICIT-NO-OP — `DexWeaver.java:560-566` discards the plan silently); `target(Type)` type-matching; `args(Type)` type-matching; `if(...)` matcher semantic evaluation (the `IfGuardEmitter` scaffold is wired via `EmitterDispatch.java:70-74` but the matcher remains always-match); `adviceexecution()` real semantics (currently parsed as `NamedRefPC` at `PointcutExpressionParser.java:131` → always-match).
- **Follow-up** (real work but no current corpus demand, or non-blocking): `get(FieldPattern)`/`set(FieldPattern)` field-access matcher + emitter (zero demand after corrected grep, completeness); `this(name)` / `this(Type)` (zero corpus demand confirmed; no `ThisPC` class — matrix completeness only); `withincode(...)`; `cflow`/`cflowbelow`; `initialization(...)`/`preinitialization(...)`; AspectJ 5 `@*` family (6 designators, zero demand); positive `within(...)` matcher-side filter; `T+` inside `!within(...)` (exact-match-after-strip); SignaturePattern modifiers (positive visibility currently stripped; `!public`/`static`/`final`/`throws` unmodelled); `thisJoinPointStaticPart` / `thisEnclosingJoinPointStaticPart` bindings (completeness for parity); aspect inheritance (`aspect Bar extends Foo`); abstract-aspect + concrete-subaspect pattern (zero demand in corpora today but relevant for `BaseAspect` idiom); privileged aspect; **AspectJ weaver parity diff vs. AJC offline reference** (gold-standard behavioural oracle for SILENT-GAP `@Disabled` tests — surfaces closures that pass the disabled-test assertion but diverge from real AspectJ; deferred per cross-LLM review as oracle-of-behaviour, not oracle-of-grammar).
- **Deferred-by-design** (production code raises `UnsupportedOperationException` AND a passing test asserts the throw — INV-INS-89-compliant `EXPLICIT-NO-OP`): `around` advice (`EmitterDispatch.java:61-65` + `EmitterDispatchTest.java:54-59`); `proceed(...)` keyword in around body (EXPLICIT-NO-OP consistent with `around` itself — the around row's UOE covers it by reflection).

The reflective-API family rows (`thisJoinPoint`, `JoinPoint.getSignature()` + subtypes, `getArgs()`, `getTarget()`/`getThis()`, `getKind()`/`getSourceLocation()`, runtime linkage) and the matcher-correctness rows (`target(name)`/`args(name)` standalone, `!` negation beyond `!within`, named-pointcut references) are **NOT in the ledger** because they ship as COVERED rows in this change (see §"Scope expansion" above).

Note: `lock`/`unlock` and other AspectJ extensions are NOT in the ledger because they are NOT in the closed enumeration (zero demand AND outside the scope decision — see design.md OQ3). Per-clauses, ITDs, `declare warning/error/soft`, annotation-style aspects, and generics in type patterns are AUSENTE-JUSTIFICADO (design.md OQ4) — also outside the closed enumeration.

**Bump `smali-dexlib2` 3.0.8 → 3.0.9** in `rvsec-android/rvsec-instrumentation-dexlib2/pom.xml:32`. The pin was set in the initial gh52 commit and never bumped; 3.0.9 is the latest published. The bump is an isolated commit (§0 in tasks) gated by `mvn package` AND a behavioural diff: 5 APKs from the INV-INS-31 baseline are re-instrumented pre/post-bump and `dexdump` output is diffed; non-trivial divergence reverts the bump within gh62 and tracks a separate `chore(deps)` issue. This is a runtime-dependency change (the shaded `instr-cli.jar` ships the new `smali-dexlib2` bytes); the §0.3a `dexdump` diff is the behavioural gate that pairs the bump with this otherwise documentation-only change.

**Other production code changes ship in this change** — see §"Scope expansion" above. Specifically, `advice-emitter/` gains the JoinPoint reflective API implementation (new shipped runtime classes + `MonitorInvokeBuilder` Signature-construction emit + `ThisJoinPointEmitter` register-based passing); `pointcut-engine/` gains matcher correctness fixes (standalone `TargetPC`/`ArgsPC` matching, full `parseUnary` negation handling, `NamedRefPC` resolver). Sub-changes opened off the ledger ship the remaining closures and reference back to gh62 in their proposals.

## Capabilities

### New Capabilities
*(none — the artefacts produced by this change live in `docs/`, `rvsec-android/.../grammar-tests/`, `rvsec-android/.../advice-emitter/`, `rvsec-android/.../pointcut-engine/`, and `openspec/changes/<this>/`. No new domain capability is introduced; existing `instrumentation` capability is extended.)*

### Modified Capabilities
- `instrumentation`: three additions.
  1. The dexlib2 instrumenter's AspectJ surface SHALL be **documented as a grammar coverage matrix anchored to demand evidence**; any production parser/matcher/emitter behaviour MUST appear as a row in that matrix with a current verdict. The matrix is the contract; production code SHALL NOT introduce new always-match or malformed-descriptor paths without a corresponding row. Enforced by `MatrixIntegrityTest` at every CI run.
  2. The dexlib2 instrumenter SHALL implement the **AspectJ JoinPoint reflective API** end-to-end so that advice bodies receive a populated `JoinPoint` with a real `Signature` object (one of `MethodSignature`/`ConstructorSignature`/`FieldSignature`), `getArgs()`, `getTarget()`/`getThis()`, `getKind()`, and `getSourceLocation()` — behavioural parity with the AspectJ offline weaver. Required so MOP monitors that read the join-point context (e.g. `MonitorRuntime.dispatch(s.getName(), a)`) receive non-empty events.
  3. The dexlib2 pointcut matcher SHALL implement **correctness fixes** for four matcher paths that are currently always-match: `target(name)`/`args(name)` standalone binding (without `&&`-composition with `call()`); `!` negation specialization beyond `!within` (`!handler`, `!cflow`, `!if`); resolution of named-pointcut references against the aspect's pointcut table.

## Impact

**Affected modules (rv-android uv workspace):**

- *(no Python module is affected.)*

**Affected modules (sibling rvsec repo, `rvsec-android/rvsec-instrumentation-dexlib2/`):**

- New Maven submodule `grammar-tests/` (Maven artifact `br.unb.cic.rv:grammar-tests:0.9.0-SNAPSHOT`). Test-only — no `main/java/`, excluded from the `instr-cli` shade plugin. Depends on `pointcut-engine`, `advice-emitter`, `dex-mutator`, JUnit 5, JUnit Platform Launcher (for skip-count discovery), `org.junit-pioneer:junit-pioneer:2.3.0` (test scope, for `@SetEnvironmentVariable` used in `RobustnessTest`), `org.commonmark:commonmark:0.24.0` + `org.commonmark:commonmark-ext-gfm-tables:0.24.0` (test scope, for matrix Markdown parsing). All GAVs pinned in `dependencyManagement` of `rvsec-instrumentation-dexlib2/pom.xml`.
- `advice-emitter/` — new shipped runtime classes (local equivalents of `org.aspectj.lang.{JoinPoint,Signature,MethodSignature,ConstructorSignature,FieldSignature,SourceLocation}`); `MonitorInvokeBuilder` gains Signature-construction emit (`new`-`invoke-direct` populating the Signature object with name / declaring-type / parameter-descriptors / return-descriptor derived from the matcher `Context`); `ThisJoinPointEmitter.signatureFor()` rewritten to emit register-based JoinPoint passing instead of returning the raw pointcut expression string; `BeforeEmitter`/`AfterEmitter`/`AfterReturningEmitter` extended to wire the JoinPoint register through advice-body invocations.
- `pointcut-engine/` — `PointcutMatcher` gains standalone `TargetPC`/`ArgsPC` real binding (replacing `Match.empty(pe)` at `:106-108`); `PointcutExpressionParser.parseUnary()` extended to specialise `!handler`/`!cflow`/`!if` beyond just `!within`; `NamedRefPC` matcher resolves named pointcuts against the aspect's pointcut table (`AspectDescriptor.namedPointcuts()`) instead of unconditional always-match.
- `pom.xml:32` — single-line property bump `<smali.version>3.0.8</smali.version>` → `<smali.version>3.0.9</smali.version>`. Propagates to every submodule declaring `smali-dexlib2` / `smali-baksmali`.

**Affected documentation (rv-android):**

- New `docs/aspectj_grammar_coverage.md` — the matrix document.
- New `openspec/changes/gh62-aspectj-grammar-coverage/ledger.md` — the scope ledger.

**Pipeline impact:**

- `grammar-tests/` is excluded from the `instr-cli.jar` shade plugin; the matrix and ledger documents do not enter the jar.
- **`instr-cli.jar` ships behaviour changes** for the JoinPoint reflective API + matcher correctness families. Pre-change advice bodies that read `thisJoinPoint.getSignature().getName()` received the raw pointcut expression string and produced corrupted monitor events; post-change they receive the matched method's actual name. Pre-change `target(name)` standalone produced `Match.empty` (no binding); post-change it binds the receiver register. The two changes flip ~10 matrix rows from SILENT-GAP to COVERED. Behavioural equivalence on the pre-existing surface (call-pointcut matching, before/after weaving, register allocation post-gh61) is gated by the smali bump's `dexdump` diff (§0.3a, 5 APKs from the INV-INS-31 baseline) AND by the existing per-module test bars (`pointcut-engine`, `advice-emitter`, `dex-mutator`, `coverage-weaver`).
- **Smoke validation on the JCA-226 dataset** (5 representative APKs): pre-change and post-change instrumentations re-run; MOP events are compared; the post-change event stream MUST contain non-empty `Signature.getName()` payloads at the join points where the pre-change stream had empty/malformed payloads. Other monitor properties (event count parity, no new VerifyError) MUST hold.
- The `phtcosta/rvandroid:0.9.0` Docker image is rebuilt with the new jar. No full 190-APK re-instrumentation is required for this change (the 5-APK smoke validation is sufficient evidence of correctness; a full re-run would be a separate experiment).
- No APE experiment re-run is needed.

**PRD references:**

- **FR02** (APK instrumentation): behaviour change — the dexlib2 instrumenter now produces populated JoinPoint objects and correct standalone binding semantics. The matrix codifies the new surface; the rows that flip SILENT-GAP→COVERED in this change document the behavioural delta.
- **NFR03** (Testability): improved — the `grammar-tests` module is the first executable oracle for the AspectJ surface of the dexlib2 instrumenter; `MatrixIntegrityTest` enforces bidirectional integrity at every CI run.

**Cross-module dependencies:**

- None new. The matrix document and the `grammar-tests/` module are self-contained. The new shipped runtime classes in `advice-emitter/` are local equivalents (`br.unb.cic.rv.aspectjlang.*`) — they do NOT depend on `org.aspectj.lang.*` at runtime, avoiding a new transitive dep on `aspectjrt.jar` in the instrumented APKs.

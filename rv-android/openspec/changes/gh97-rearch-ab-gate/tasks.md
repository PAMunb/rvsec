<!-- Ordering is the point of this list, not a convenience.
     Group 1 captures leg A's provenance and MUST complete before anything else — the baseline image
     ID is destroyed by a `docker prune` and is unrecoverable afterwards.
     Group 5 (the freeze) MUST complete before Group 6 (the jar and image) and Group 8 (the campaign).
     Nothing after the freeze may edit the pre-registration; an unplanned analysis is exploratory
     by definition and is reported as such.
     Groups 2, 3 and 4 are independent of each other and may run in any order after Group 1.
     Critical path: 1 -> 4 -> 5 -> 6 -> 7 -> 8 -> 9 -> 10. -->

## 1. Baseline provenance — capture before anything can destroy it

- [x] 1.1 Record leg A's image identity: tag `phtcosta/rvandroid:0.9.3`, ID `sha256:b2904fdfc3ddfc81ad455abd5e5685ddc97666c9411c4d994fec9111311aedec`, created `2026-08-01T11:47:43-03:00`, into the change's working notes. `:latest` points at the same ID — record that too, so a later retag cannot be mistaken for a rebuild.
- [x] 1.2 Record leg A's jar sha256 (`386ce08d1846a4088755a8d755e5b70391af3b42add091d231dbcc52aed24e69`) and the baseline commit (`5dcf2259…`), and confirm the deployed jar still hashes to that value.
- [x] 1.3 Compute and record the SHA-256 of `calibracao/subset40.txt` — this is the `corpus_basis` digest both legs are declared against.
- [x] 1.4 Confirm `experimento-e3-decisiva/per_apk_paired.csv` has 40 rows and the three expected arms, and record its sha256 as leg A's frozen input.

## 2. Module code the gate adds

<!-- This group was headed "the only code the gate adds" while corpus basis was the whole of it.
     Amendment 02 (2026-08-06) added 2.8–2.9: the completion check the campaign's two silent
     truncations showed to be missing. Those two tasks cannot affect the running campaign — the
     container's Python is a frozen image clone at 19ae3da1 — so they gate nothing here and are
     ordered with the rest of the module code rather than on the campaign's critical path. -->


- [x] 2.1 Add `corpus_basis → ape.corpusBasis` to `APERV_PROPERTY_MAPPING` in `modules/aperv-tool/src/aperv_tool/tools/aperv/tool.py`.
- [x] 2.2 Validate the value in `configure()` against `^[A-Za-z0-9._-]+:[0-9a-f]{64}$`, raising `ConfigurationError` naming the key and the rejected value, before any device interaction.
- [x] 2.3 Emit `ape.corpusBasis=<value>` from `_push_properties()` when configured; omit the key entirely when absent (INV-APV-56) — no placeholder, no warning.
- [x] 2.4 Add unit tests: the line is written with the configured value byte-identical; an absent basis emits no key; a malformed basis raises before any push.
- [x] 2.5 Add the source-sweep test proving no module under `modules/aperv-tool/src` reads `RUN_START` (INV-APV-57).
- [x] 2.6 Pre-check the DSL seam locally — `RV_TOOLS: "aperv:mop_on_llm_off:mop_off_llm_off:mop_on_llm_70@corpus_basis=subset40:<sha256>"` — by inspecting the generated `ape.properties` on a single local run. `_parse_single_tool_spec` emits a plural `variants` shape its own `TODO(FR15)` calls dead against `ToolConfig(variant: str)`. This is the cheap check, not the authoritative one: the seam is verified over the chain the campaign actually uses in task 7.2. If either check fails, fall back to a per-arm `overrides` entry set by the campaign configuration and record the deviation here.
- [x] 2.7 Run `/rv-test-run aperv-tool`
- [x] 2.8 Stop `tool.py` reporting a success it did not establish (INV-APV-60). Introduce `APERV_TEARDOWN_GRACE_S = 45`, replacing the literal at `_build_command` (`tool.py:1059`) so the grace is stated once, and on the non-timeout return path raise `RVToolExecutionError` naming the elapsed time and the budget when the exploration returned more than that grace short of it. `tool.py:1298` currently logs `APE-RV execution completed successfully` having inspected nothing. **Do not branch on the exit code**: a non-zero exit is normal for APE-RV — it exits non-zero on an application crash during exploration — so exit 255 from a vanished device and exit 1 from a crashing application carry the same information. Elapsed time is the only signal that separates them. The timeout path, its `RVToolTimeoutError` and its trace compression are unchanged: a timeout remains the expected, successful ending.
- [x] 2.9 Unit tests for 2.8: a return short of the budget raises and names both numbers; a return at the budget with a non-zero exit does **not** raise; the timeout path still raises `RVToolTimeoutError` and still compresses. No device.

## 3. Margins and premises — derived from leg A, before any plan is frozen

- [x] 3.1 Compute per-outcome replica dispersion from `experimento-e3-decisiva/results/e3_decisiva_*/coverage.csv` at `(apk, rep, tool)` granularity, for `cov_method`, `cov_act`, `cov_mop`, `mop_unique` and `mop_total`.
- [x] 3.2 Derive the G1 margin per outcome from 3.1, floored at 1.5 pp for the percentage outcomes (twice the documented −0.743 pp between-campaign drift). Record the derivation, not only the result.
- [x] 3.3 Settle whether `mop_total` gates or is descriptive, on the evidence of 3.1 — it counts violation lines rather than distinct violations and is the noisiest of the five. Decide before the freeze; the decision is not revisitable after.
- [x] 3.4 Compute the expected substrate displacement for G3: per-application flagged-widget delta over the 40 applications, old parser against `derive_mop_artifact.py`, using the pinned static-analysis corpus.
- [x] 3.5 Re-confirm host-side that no application in the corpus would have hit the pre-`gh96` footprint guard, against the guard's actual threshold. Leg A shows no arm with `cov_method < 5`, which is evidence but not the threshold itself. Any affected application is declared before the campaign, never excluded after.
- [x] 3.6 Record `cov_act`'s ceiling (median 100.0 in both guided arms) as a declared premise of G2, with G2 stated one-sided.

## 4. Campaign scaffold and the adapted scripts

- [x] 4.1 Create `experimento-rearch-aperv/` with its `README.md` (design, how to run, partitioning), modelled on `experimento-e3-decisiva/README.md`.
- [x] 4.2 Generate `filters/batch_00.txt` … `batch_07.txt` — 5 applications each, deterministic alphabetical split of `calibracao/subset40.txt`. Assert union == subset, no duplicate, no loss. **Superseded for leg B by amendment 01 (2026-08-05), which does not invalidate what this task did.** The 8×5 split stands, byte-identical to leg A's, and is kept in `filters/` as that leg's partition and therefore evidence. Leg B runs the same deterministic alphabetical rule in blocks of 4, as `filters10/batch_00..09.txt`, asserted the same way — union == subset, no duplicate, no loss. Only the block size and the container count changed; the rule did not.
- [x] 4.3 Write `docker-compose.yml`: 8 containers on `phtcosta/rvandroid:0.9.3-rearch`, each running all three arms over its own 5 applications, `RV_TIMEOUTS=1800`, `RV_REPETITIONS=3`, the three skip flags, `RVSMART_LLM_MODE=true` with the `sglang` service, and the jar bind-mount `../modules/aperv-tool/src/aperv_tool/tools/aperv/ape-rv.jar:…:ro` following the `docker/docker-compose.cmpft2.yml` precedent. **Amended 2026-08-05 (amendment 01): 10 containers over `filters10/`, 4 applications each, and no `sglang` service.** The LLM service is no longer declared here: the host GPU holds leg A's own server at 12.6 GiB of 15.47 GiB, so a second instance dies in CUDA OOM before serving, and the campaign joins the external network `experimento-e3-decisiva_default`, whose `sglang` alias is the hostname the entrypoint's socat bridge targets. Reusing it also means both legs' LLM arms are served by the same backend. The jar bind-mount, the `corpus_basis` DSL token, `RV_TIMEOUTS=1800`, `RV_REPETITIONS=3` and the skip flags are unchanged and were verified present on all ten services after the file was regenerated.
- [x] 4.4 Copy `consolidate_cal.py`, `verify_iteration.py`, `multiarm_stats.py` and `stats_utils.py` into the campaign's `scripts/`. Do not edit the originals under `experimento-cal/scripts/` (`gh94` INV-APV-55).
- [x] 4.5 Adapt only the trace-reading paths of the copies onto `aperv_tool.analysis.trace_ndjson` (`gh94`'s deliverable). Leave the logcat and `tasks.json` paths untouched — they produce `per_apk_paired.csv`, and changing them would break the pairing the gate rests on. `stats_utils.py` stays byte-identical.
- [x] 4.6 Write `scripts/preflight_runstart.py`: reads the first line of one trace per arm with `json.loads`, checks `props_digest`, `preset` + `params` against the arm's declared overrides, `build.sha` against the built `rearch` commit, and `corpus_basis` against the digest from 1.3. The `corpus_basis` check distinguishes two blocking failure modes and names them separately in the report — **absent** means the DSL parameter path dropped the value before it reached `ape.properties`, **mismatched** means it arrived carrying the wrong list. Reports every check PASS/FAIL and exits 1 on any FAIL. It is an operator script and never runs on an execution path (INV-RUN-03, `gh95` D1).
- [x] 4.7 Write `scripts/compare.py`: G1 paired against leg A on the control arm, G2 as the within-campaign contrast on `cov_act`, G3 descriptive with the 3.4 displacement, all via `stats_utils.paired_bootstrap_ci` (B=10,000, seed 42).
- [x] 4.8 Add unit tests for `compare.py` over synthetic paired vectors with known answers.
- [x] 4.9 Run `/rv-doc-code` on each new script.
- [x] 4.10 Run `/rv-test-run aperv-tool`

## 5. Pre-registration and freeze — nothing downstream may change it

- [x] 5.1 Write the pre-registration document under `docs/`, modelled on `docs/20260730_preregistro_corrida_decisiva.md`: the grid, the arms, the corpus and its digest, the seeds, both image IDs, both jar shas, both git shas, the CSV each outcome is read from, G1/G2/G3 with the margins from 3.2, the declared premises from 3.5/3.6, and the tie rule.
- [x] 5.2 State the substrate confound explicitly, with the direction the change is expected to move and the reason no magnitude is predicted, so no later reading can present it as a discovery.
- [x] 5.3 Declare what counts as a blocking regression: a CI excluding zero in the harmful direction **and** |Δ| above the derived margin, on G1; and G2's contrast losing its sign or its CI including zero.
- [x] 5.4 Freeze the document: record its sha256 in `calibracao/journal.jsonl` with a `FREEZE-PREREGISTRO` state, before the jar is built. Everything after this point that was not planned here is exploratory and is reported as such.

## 6. The jar and the image — only after both sides are complete

- [x] 6.1 Confirm `ape` stages `rearch-03` through `rearch-07` are complete and `gh94` and `gh95` are applied here. This gate is not advisory: an unapplied `gh94` leaves no NDJSON reader, and an unapplied `gh95` leaves nothing for the pre-flight's `preset` check to verify.
      **Confirmed 2026-08-05, and the reading matters as much as the result.** This gate asks for *applied*, not *archived*, and its own justification is what settles it: "leaves no NDJSON reader" and "leaves nothing for the `preset` check to verify" are both claims about code being in the tree, not about a directory having moved into `archive/`. Read as requiring archives it would deadlock the whole line, because three of the changes it names cannot archive until this campaign runs — `rearch-04` 9.1 and 9.2 are delegated to task 7.2b, `rearch-07`'s device verification to 7.2a, and `gh95` 7.6's evidence-retirement half waits on this change having executed. The cycle dissolves on the reading, not on the ordering; nothing was archived to satisfy this task.
      Each clause against measured state:
      - **`gh94` applied** — satisfied twice over: archived at `openspec/changes/archive/2026-08-05-gh94-ndjson-trace-reader/`, with `analysis/trace_ndjson.py` (the reader the adapted scripts import) and `analysis/clock_logcat_join.py` both present under `modules/aperv-tool/src/aperv_tool/`.
      - **`gh95` applied** — 56/57. All code landed; the single open box is 7.6, an owner sign-off on retiring the migration's *evidence*, which touches no code this change runs. `tool.py` resolves arms as `preset` + `overrides`, which is what gives the pre-flight's check 2 something to verify.
      - **`rearch-03` complete** — archived (`2026-08-05-rearch-03-decision-pipeline`). Its 8.4 on-device smoke is folded into task 7.1/7.3 (design D11).
      - **`rearch-04` complete** — 75/78. Open: 9.1 and 9.2 (both delegated here) and 12.4 (its own archive command). Its 9.1a, still owed when this gate was written, closed on 2026-08-05 (`ape` `9e948102`): 310 and 306 steps pre-change against 335 post-change, so the new write path is 8.77 % **above** the pre-change mean, the throughput gate does not bind and `dec.mopx` stays. That was the last obligation on the `ape` side that was neither delegated here nor a heap measurement.
      - **`rearch-05` complete** — archived (`2026-08-05-rearch-05-thin-python-arms`).
      - **`rearch-06` complete** — 25/29. Open: 2.4 (JVM unit tests, no device) and group 5 (the heap series), which design D11 places explicitly **out of scope** for this smoke — a different harness, a different granularity, and not a gate.
      - **`rearch-07` complete** — 50/51. The one open box is 9.5, a by-hand `## Purpose` check that runs *after* its own archive.
      **Every open box on the `ape` side is either delegated to this campaign or an owner-executed measurement; none is unfinished implementation.** The chain design D9/D11 calls a precondition is therefore closed, and group 6 may proceed.
- [x] 6.2 Build the jar from the `ape-rearch` worktree (branch `rearch`) and copy it to `modules/aperv-tool/src/aperv_tool/tools/aperv/ape-rv.jar`. Record its sha256 and the built commit. **The build must supply the revision stamp itself (design D10)** — `git-commit-id-maven-plugin` cannot read a worktree's `HEAD` and stamps `../ape`'s master instead (`ape` `docs/20260803_procedimento_worktree_rearch.md` §2; reproduced 2026-08-05: worktree HEAD `0675f67a` → `BuildInfo.GIT_SHA = c638142`), which would leave check 3 green and blind, since the image's own jar is cloned from that same master. **This task is the first deployment of a worktree-built jar, so it is where that document's "no worktree jar is deployed, so no delivered jar carries the wrong stamp" stops being true** — amend its closing sentence as part of this task:
      ```
      mvn -o package -Dmaven.gitcommitid.skip=true \
          -Dgit.commit.id.abbrev=$(git rev-parse --short HEAD) \
          -Dgit.build.time=$(date -u +%Y-%m-%dT%H:%M:%SZ)
      ```
      **Done 2026-08-05.** Built from the `ape-rearch` worktree at branch `rearch`, HEAD `9e948102875519ada533e02681fa012e1e4db937` (short **`9e948102`**), with the D10 recipe verbatim plus one addition the procedure doc §2 already prescribes and this environment needs: `d8` is not on the `PATH`, so the build is prefixed `PATH="$ANDROID_HOME/build-tools/35.0.1:$PATH"`. Without it the build dies in the `package` phase *after* compiling, which looks like a code failure and is not. `BUILD SUCCESS` in 11.3 s.
      **The stamp was verified before the jar was deployed, which is the whole point of D10.** Built jar sha256 `a7eddf5a776ce20f7299911d7d9acb3a0f1342cdc1512b3e28aa00488e582a94`; `strings -a classes.dex` over the unzipped jar reads `GIT_SHA = 9e948102` — the worktree's own HEAD — and `JAR_BUILT = 2026-08-05T17:22:00Z`, not the `unknown` that omitting `git.build.time` would leave. Content markers as the task specifies: `ApeRvHb` present, `NdjsonSink` present (×2), `RUN_START` present, **`APE-STEP` absent**. Deployed, and the digest recomputed at the destination rather than assumed from the copy: `a7eddf5a…`.
      **The premise this task was written on had already expired, and the evidence is physical.** D10 says 6.2 is the *first* deployment of a worktree-built jar, so it is where the procedure doc's *"nenhum jar entregue carrega o carimbo errado"* stops being true. It stopped being true earlier: the jar this task displaced was **not** the `5cebabc5…` that `rearch-04` 9.1a's evidence file still describes as deployed, but `605b4174…` dated 13:49 — a later rebuild by another session — and its `GIT_SHA` is **`c638142`**, `master`'s HEAD, exactly the defect §2 predicts. So a mis-stamped worktree jar had already been deployed before this task ran. It is preserved as the counter-example at `backup/gh97-6.2-superseded/` with its own README, and §2's closing sentence was amended to be dated to *that* deployment rather than to this one. Nothing measured is affected: 9.1a established jar identity by recomputed sha256 before each launch and says so explicitly. Its own measured article, `5cebabc5…`, was confirmed intact at `backup/rearch-04-9.1a/ape-rv.jar.stage4` **before** this overwrite, precisely because its evidence file pointed at the deployed path for it.
      `manifest.json`'s `build.expected_sha` filled with `9e948102`, taken from `git rev-parse --short HEAD` in the worktree and never read back off the built jar. Check 3 therefore stops reporting "manifest declares no `build.expected_sha`" and starts discriminating: the image's own jar is cloned from `master` and would present `c638142`, which is not `9e948102`.
      `mvn install` is **not** the shape here: `rearch-06` 6.1 records that it would overwrite the `ape-rv.jar` that `master` shares with rv-android. Package, then copy explicitly. The manifest's `build.expected_sha` — `experimento-rearch-aperv/manifest.json`, currently `null`, which is why check 3 reports "manifest declares no `build.expected_sha`" until this task fills it — is taken from `git rev-parse --short HEAD` **in the worktree** and never read back off the built jar; comparing the jar against its own stamp is a tautology that always passes. Confirm the built jar before deploying it: `strings -a classes.dex` over the unzipped jar shows `ApeRvHb`, `NdjsonSink` and `RUN_START` present and `APE-STEP` absent (the jar is dex-only, so `unzip -l` shows nothing either way).
- [x] 6.2a **Verify the pre-change jar was preserved — the copy itself is made by `gh94` 4.0.** This task was written to make that copy, on the assumption that 6.2 would be the first thing to overwrite `modules/aperv-tool/src/aperv_tool/tools/aperv/ape-rv.jar`. It is not: the owner decision of 2026-08-05 that gave `gh94` 4.1 its own device run made **that** change's 4.0 the first overwrite, and a jar can only be preserved once — before it is gone. What is owed here is the check, not the copy. Confirm `backup/gh97-prechange-jar/` holds a file whose sha256 is `386ce08d1846a4088755a8d755e5b70391af3b42add091d231dbcc52aed24e69` — the legacy build, `APE-STEP` present, no heartbeat — and note the path in `rearch-04` 9.1a. That digest is not merely *a* pre-change jar: `ape` `docs/20260803_procedimento_worktree_rearch.md` §2 records it as **byte-identical to the jar measured in the E3 decisive run**, which is leg A of this very campaign, so it is the only deployed copy of the artifact 9.1a needs for the two pre-change legs of its throughput gate (INV-SNK-13), and the physical article behind the baseline this change compares against. Do not `git add` the backup (`backup/` is untracked here despite the top-level `CLAUDE.md` describing it as ignored). **If the file is absent or the digest differs, stop and say so**: losing it fails nothing loudly, it silently makes the only measurement of the new per-step write path's wall-clock cost unrunnable, and by this point in the sequence it cannot be recovered from the deployment path.
      **Verified 2026-08-05, by recomputation and not by reading the digest next to the file.** `backup/gh97-prechange-jar/` holds `ape-rv.jar` (277,084 bytes), `SHA256SUMS` and a `README.md`; `sha256sum` over the file returns `386ce08d1846a4088755a8d755e5b70391af3b42add091d231dbcc52aed24e69`, matching both the stored sum and the digest this task names. The preserved article is the E3 one, so leg A's physical baseline is intact.
      Two things this task expected are no longer true, both in the harmless direction. The cross-reference it owes — "note the path in `rearch-04` 9.1a" — **is already there**, written by that change itself, together with its own independent recomputation of the same digest; nothing was added, because a second note would only restate it. And the risk this check exists to catch **has already passed without materialising**: 9.1a executed on 2026-08-05 and consumed exactly this file for its two pre-change legs (310 and 306 steps against 335 post-change, PASS at +8.77 %). This verification therefore confirms an input that has already been spent rather than guarding one still owed — which is the outcome it was written to make possible, recorded here so a later reader does not read the tick as protecting something still ahead.
- [x] 6.3 Push the rv-android commits — the image's stage-4 layer clones `PAMunb/rvsec` at build time, so unpushed work is silently absent from the image. **Push the working branch to itself — `git push origin rearch-counterparts` — and not into `modules`.** The merge into `modules` is what this whole gate's verdict decides; performing it here would settle by hand the question the campaign exists to answer. The branch is shared with other sessions, so check what is about to be published before pushing: `git log --oneline origin/rearch-counterparts..HEAD` lists it, and commits authored elsewhere travel with ours.
      **Pushed 2026-08-05**: `bfaf9f91..19ae3da1` on `rearch-counterparts`, **34 commits**, and the remote now resolves to `19ae3da10d79ce40a7c3949fa40abdf60d8c5d15` — checked against the local HEAD by `git ls-remote` rather than trusting the push output. The 34 were reviewed before publishing, as the task requires: all belong to the `gh94`/`gh95`/`gh96`/`gh97` line apart from four housekeeping commits from other sessions on this shared branch (untracking IDE and shade files, keeping 279 GB of corpora out of the index, two `scripts/` fixes). Nothing foreign, and nothing was staged that this session did not write — the tracked modifications standing in `docs/`, `modules/rvagent-tool/`, `modules/rv-tools/` and `experimento-cal/` belong to another session, were never added to the index, and did not travel.
- [x] 6.4 Build `phtcosta/rvandroid:0.9.3-rearch`. Do not reuse or move `0.9.3` or `latest`. **Do not push the image** (owner decision, 2026-08-05): the campaign runs on this host and `docker-compose.yml` declares no `pull_policy`, so Compose's default resolves a locally present image without contacting the registry, and publishing would put an unreviewed image under a shared name for no benefit the campaign collects.
      **Pass the branch explicitly, and do not use the repository's build script.** `docker/rvandroid/Dockerfile` declares `ARG RVSEC_BRANCH=modules` and clones `PAMunb/rvsec` at that branch, so a plain `docker build` yields an image containing none of `gh94`, `gh95`, `gh96` or `gh97` — every arm would run the canonical harness while the campaign recorded it as the re-architected one, which is gh71's failure moved up a layer from the jar to the harness. And `docker/rvandroid/build.sh` runs `docker build --no-cache -t $IMAGE:0.9.3 -t $IMAGE:latest`, moving both tags this task protects: `0.9.3` **is** leg A's image identity and `latest` points at the same ID, so running it would destroy the comparison's provenance in the act of building the artifact to be compared. The shape is therefore explicit, single-tagged, and branch-parameterised:
      ```
      docker build --no-cache \
          --build-arg RVSEC_BRANCH=rearch-counterparts \
          -t phtcosta/rvandroid:0.9.3-rearch \
          docker/rvandroid/
      ```
      Confirm after the build, before anything is launched, that `0.9.3` and `latest` still resolve to `sha256:b2904fdfc3ddfc81ad455abd5e5685ddc97666c9411c4d994fec9111311aedec` — a moved tag is not recoverable and the pre-registration reads that ID as leg A's. Confirm too that the image's `rvsec.branch` label reads `rearch-counterparts`, which is the one field that says which harness shipped.
      **Built 2026-08-05, not pushed.** `sha256:2cc5c3aada3dd741434d78bfb38da4dd87cded80d05ab7967bbbe725e61472d7`, created `2026-08-05T14:39:39-03:00`, label `rvsec.branch=rearch-counterparts`. Leg A's tags were re-read after the build and are **unmoved**: `0.9.3` and `latest` both still resolve to `sha256:b2904fdf…aedec`.
      **The label was not taken as proof — the contents were checked.** Running the image, `/opt/rvsec` is on branch `rearch-counterparts` at commit `19ae3da1`, which is exactly what 6.3 pushed, and it carries `analysis/trace_ndjson.py` (`gh94`'s reader, 38,604 B) and `tools/aperv/derive_mop_artifact.py` (`gh96`'s generator, 51,439 B). A label can be set by hand; a clone at the right commit carrying both deliverables is the harness itself.
      **One measured fact strengthens pre-flight check 3 beyond what the design assumed.** The image's own jar — built by the Dockerfile's unpinned clone of `phtcosta/ape`, i.e. `master` — hashes to `386ce08d1846a4088755a8d755e5b70391af3b42add091d231dbcc52aed24e69`, which is **byte-identical to leg A's jar**: `ape` `master` has not moved since the baseline commit. So if the bind-mount ever fails to win, the container does not run a *subtly* different jar, it runs the pre-stage-4 legacy one, which emits `[APE-STEP]` and no `RUN_START` at all. The pre-flight would then fail on the very first parse rather than on a field comparison — the loudest available failure, and the opposite of gh71's silence.
- [x] 6.5 Record the new image's ID beside leg A's, in the pre-registration's provenance section — an appendix of measured facts, not a change to the frozen plan.
      **Done 2026-08-05, under the rule the pre-registration itself declares in its §2** rather than under one invented here: the four pending values enter §11 as measured facts with their measurement date, nothing else in the document is touched in the same edit, and the journal entry for the appendix is separate and later than the freeze. All four are now filled — jar `a7eddf5a…`, jar commit `9e948102…`, image ID `sha256:2cc5c3aa…`, harness commit `19ae3da1…` — the last read from inside the image rather than from the local repository.
      The "nothing else touched" clause was **verified, not asserted**: `git diff -U0` over the document reports a single hunk, at the §11 table. The freeze itself is intact — the document hashed `c0ac9a7f…` when frozen on 2026-08-04 and the new `APENDICE-PROVENIENCIA` entry records the post-appendix digest `a0da2273…` beside it, so a reader can verify the plan preceded the binary by comparing the two entries. `calibracao/journal.jsonl` re-parses whole after the append.

## 7. Smoke and the pre-flight gate

- [x] 7.1 Run the smoke: a small application subset, 1 repetition, reduced timeout, all three arms.
      **Ran 2026-08-05**, 3 arms × 2 applications × 1 repetition × 300 s = 6 runs, one container, into `results_smoke/`. **Group 4 never built a vehicle for this** — the README pointed at a compose section that did not exist — so `docker-compose.smoke.yml` was written first; that gap is the reason this task could not simply be executed as written.
      **The applications were chosen by the artifact, not by `cov_mop`, and the first smoke was discarded for getting that wrong.** `cov_mop` comes from the RVSEC logcat markers and does not depend on the guidance artifact at all, so an application can post `cov_mop` 68 % with an entirely empty MOP artifact — which is what happened. The replacements were picked by deriving the artifact host-side over `subset40` and requiring `flagged`, `wtgEdges` and `mopActivities` all positive; only 4 of the 40 applications qualify. The discarded run is at `backup/gh97-smoke-descartado/`.
      Wall clock came out at 355–361 s per run against a 300 s timeout, i.e. ~57 s of install, flush and teardown overhead per run. That is the number amendment 01's ~1857 s per run at 1800 s was derived from, so the partition's budget rests on a measurement rather than an estimate.
- [x] 7.2 Run `scripts/preflight_runstart.py` over the smoke's traces. The campaign does not start until it reports 3/3 arms PASS on **the three checks that a trace can carry** — `preset`+`params`, `build.sha` and `corpus_basis`. **`props_digest` is SKIP by design, and the bar was corrected on 2026-08-05 rather than met.** Measured on that day's smoke: 9 PASS, 0 FAIL, 3 SKIP, one SKIP per arm, all of them check 1.
      The reason it cannot pass is that `_push_properties()` writes the file to a temporary path and unlinks it after the push (`finally: os.unlink`), so nothing survives for the digest to be compared against. Reconstructing it was attempted through the tool's own code path with the push intercepted, and it did **not** reproduce any of the three echoed digests under any plausible variation of the inputs — while the same reconstruction executed *inside the campaign image* produced bytes identical to the local tree, which rules out drift between the harness and the image and leaves the difference unexplained. That loose end is recorded rather than closed.
      **The reason it does not need to pass** is that the failure mode it was written for is already caught, more strongly, by the jar itself. From stage 2 onward an unknown key, a retired key, or a non-neutral value of an inactive feature **aborts the run before step 1** — so "the harness sent something it should not have" fails loudly at the jar rather than silently, and a digest comparison adds nothing to that. What remains legitimately checkable from the trace alone is what the other three checks read: the arm's `preset` and every declared override present in `params`, the `build.sha` that says which of the two jars in the container ran, and the `corpus_basis` recomputed from the list file.
      **Retaining the pushed file was considered and rejected** (owner decision, 2026-08-05). It would have meant one `.properties` artifact per task — the device path is fixed, so every task overwrites it and only a per-run name would work — added to the tool purely to feed a check whose failure mode the jar already rejects. That is a new artifact bought to serve a redundant check, which P1 forbids. The delta spec drafted for it was withdrawn. A `build.sha` mismatch here means the image's own default-branch jar won the mount — the gh71 failure mode, caught before 24 hours are spent. An **absent** `corpus_basis` here is the authoritative verdict on the DSL parameter seam of task 2.6: it exercises the whole `RV_TOOLS` → `_parse_single_tool_spec` → `configure()` fold → `ape.properties` → `RUN_START` chain over the exact configuration the campaign will run, which the local pre-check cannot do.
      **Re-run 2026-08-05 against the stored traces, and the numbers above reproduce exactly: 9 PASS · 0 FAIL · 3 SKIP, exit 0, `GATE: the campaign starts.`** Per arm — `mop_on_llm_off` `preset=mop` with 4 keys compared, `mop_off_llm_off` `preset=mop` with 4 keys of which 2 sit at the jar default (`ape.mopWeightOpenMenu`, `ape.mopWeightWtg`), `mop_on_llm_70` `preset=llm_mop` with 9 keys. `build.sha` reads `9e948102` on all three, which is the `rearch` worktree commit and not master's `c638142` — so the bind-mounted stage-4 jar won, and the gh71 failure mode did not recur. `corpus_basis` reads `subset40:b60903adf4c8fca07e014e3655db158a220184d112f2f995a181fd98dd3d48d4` on all three: present, not absent, so the DSL parameter path carried the value end to end.
- [x] 7.2a **The `ape` side's device verification, delegated here (owner decision 2026-08-05).** Stage `rearch-07`'s own end-to-end smoke, its Docker verification and the deployed half of its skew drill were all removed on the rule that the APE-RV side executes exactly once, in this change. A delegation is only real if this smoke checks what those tasks would have, so read from the same traces as 7.2, per MOP arm (`mop_on_llm_off`, `mop_on_llm_70`):
      - `MOP_DATA` is present with `status=loaded`, `formatVersion=1` and a non-empty `sourceDigest` — the three fields that distinguish a compact-artifact load from every prior format, and the only evidence on the device side that the cutover actually took;
      - `wtgEdges > 0` and `mopActivities > 0`, so a load that succeeded on an artifact carrying nothing is not read as a pass;
      - at least one step with a MOP boost — the guidance mechanism firing, which is what `rearch-07` 8.1 called "boost fires" and what a green `status=loaded` alone does not establish;
      - on the control arm `mop_off_llm_off`, **no boost and no `decision_source=MOP` in any step** — the skew drill's negative half, stated as influence rather than as presence. **This clause was corrected on 2026-08-05, having been written wrong and caught by the smoke it gates.** It read "**no** `MOP_DATA` record with `status=loaded` and no boost", which is false by construction and would have failed the pre-flight and blocked the campaign on a defect in the check rather than in the deploy: `mop_off_llm_off` declares `"mop_data": "static_analysis"` like the other two arms, and D1 of this change's own design says so in as many words — *"keeping `mop_data` present, so it loads the substrate and scores nothing by it"* — as does INV-APV-29, under which MOP-off means the document is present with the weights zeroed and **never** an omitted document. The control is not a non-MOP arm; it is a MOP arm whose five scoring weights are `0`. So the artifact reaching it is expected, and what must not reach it is *effect*. Measured on the smoke: the control loaded the same artifact as the guided arms (`sourceDigest` identical) and produced **0 boosts and 0 `decision_source=MOP` across 303 steps**, against 17 and 15 on `mop_on_llm_off` — the property the drill is about, holding exactly. This is the same predicate validity gate 9.1 applies to the full campaign, which is a second reason to state it this way: the smoke now checks in miniature what the campaign checks at scale, instead of checking something the campaign never checks.
      **Re-measured 2026-08-05 through `aperv_tool.analysis.trace_ndjson` — the reader `consolidate.py` and `verify.py` use, so this is what the analysis will see — and every clause holds on _both_ applications, not only the one the first record carried:**

      | Application | Arm | `MOP_DATA` | `fmt` | `sourceDigest` | `wtgEdges` | `mopActivities` | steps | boosts | `decision_source=MOP` |
      |---|---|---|---|---|---|---|---|---|---|
      | smartpack | `mop_off_llm_off` *(control)* | loaded | 1 | `4798542d9844…` | 23 | 8 | 303 | **0** | **0** |
      | smartpack | `mop_on_llm_70` | loaded | 1 | `4798542d9844…` | 23 | 8 | 234 | 15 | 2 |
      | smartpack | `mop_on_llm_off` | loaded | 1 | `4798542d9844…` | 23 | 8 | 307 | 17 | 15 |
      | freeotpplus | `mop_off_llm_off` *(control)* | loaded | 1 | `ffede8bd9a92…` | 86 | 7 | 292 | **0** | **0** |
      | freeotpplus | `mop_on_llm_70` | loaded | 1 | `ffede8bd9a92…` | 86 | 7 | 227 | 150 | 66 |
      | freeotpplus | `mop_on_llm_off` | loaded | 1 | `ffede8bd9a92…` | 86 | 7 | 273 | 185 | 88 |

      Within each application the control's `sourceDigest` is **identical** to the guided arms', which is the property that makes the negative half meaningful: the control received the same artifact and produced no effect from it — 0 boosts and 0 `decision_source=MOP` across 303 and 292 steps. No arm reported `status=rejected`. `rearch-07` 8.1 is discharged here and nowhere else.
      **One number published by the 2026-08-05 session did not survive re-measurement, and is corrected here rather than carried.** That session's handoff recorded `mop_on_llm_70` on smartpack as 108 steps, 2 boosts, 0 `decision_source=MOP`; the trace gives **234 / 15 / 2**. The `mop_on_llm_off` row of the same table reproduced exactly, so the error was confined to one row. The corrected figures are consistent three independent ways — `len(steps)` from the reader, `RUN_END.steps`, and the logcat heartbeat count all read 234 — which the published figure was not. The direction of the finding is unchanged and strengthened.
      A `status=rejected` on a MOP arm is the deployed skew failure and blocks the campaign like any other pre-flight FAIL. This is a deployment sanity check, not an analysis: it touches no outcome, no margin and no G1/G2/G3 clause, so it does not reopen the group-5 freeze — record it as a pre-flight check, in the pre-flight report, never in the pre-registration.
- [x] 7.2b **Evidence harvested for the `ape` side — recorded, never gating (design D11).** Unlike 7.2a this blocks nothing: `scripts/consolidate.py` and `scripts/verify.py` read traces through `aperv_tool.analysis.trace_ndjson` and never touch heartbeats or `clock_logcat_join`, so none of it can invalidate an outcome this change measures. Letting it block the campaign would give a telemetry-only observation authority over an experiment that does not read it. From the same smoke:
      - **Heartbeats (INV-SNK-14) — confirmation across three arms, no longer the only source.** `gh94` 4.0–4.2 now captures the primary record on its own run, ahead of this smoke (design D11), so what this bullet adds is breadth: the standalone run exercises one arm, this exercises `mop_off_llm_off`, `mop_on_llm_off` and `mop_on_llm_70`. Grep each arm's `task.result.logcat_file` for tag `ApeRvHb`; record the line count per arm and compare it against that run's `StepRecord` count, with the `s` values agreeing. Append the per-arm counts to `openspec/changes/archive/2026-08-05-gh94-ndjson-trace-reader/heartbeat-evidence.md`, beside 4.2's record rather than replacing it — that file is `gh94` 4.2's deliverable and stays there. **The path is the archived one**: this task was written while `gh94` was open, and that change archived on 2026-08-05, moving the file. Appending to the pre-archive path would silently create a second file under a directory that no longer exists as a change, so the record would be written and then lost. **A disagreement, or an arm with no heartbeat line at all, is a finding: report it.** It does not gate this campaign (below), and by this point `gh94` 5.5–5.10 will already have been decided on 4.2's evidence — but an arm that produced no heartbeat where another did is a difference between arms, which is exactly the kind of thing this change exists to notice.
      - **Sample-trace inventory (discharges `rearch-04` 9.1).** Confirm the smoke's traces carry a MOP boost (already asserted by 7.2a), at least one LLM call on `mop_on_llm_70`, a flushed final step, and `RUN_END`. That trace is also `rearch-04` 9.2's input, so record where it was kept. The remaining two clauses of 9.1 — an LLM **error** and a `no_match reason=dead_pair` — are recorded **if present and not required**: no arm can be made to produce either inside a reduced-timeout smoke, and both are already asserted at schema level by `gh94`'s golden-fixture tests, which is where a format claim belongs.
      **Heartbeats — done 2026-08-05, no finding to report.** Counted the same four ways `gh94` 4.2 used, because they fail differently: raw `ApeRvHb` grep, parsed count, distinct heartbeat `s`, `StepRecord` count, distinct step `s`. All five agree in all six runs — 303 / 234 / 307 on smartpack and 292 / 227 / 273 on freeotpplus — with the `s` sets **identical** and contiguous `1..N`, no gap and no duplicate. The last heartbeat's `t` matches `RUN_END.t_last_step` (smartpack `mop_on_llm_off`: `s=307 t=299892` against `"t_last_step": 299892`). **No arm produced fewer heartbeats than another, and none produced none** — the difference-between-arms this task exists to catch is absent. INV-SNK-14 holds across the MOP-off control and the LLM arm as well as the reference arm, so the mechanism is not sensitive to arm. Appended to `openspec/changes/archive/2026-08-05-gh94-ndjson-trace-reader/heartbeat-evidence.md`, the **archived** path, as an appendix beside 4.2's record rather than replacing it.
      **Sample-trace inventory — and `rearch-04` 9.1 discharges completely, which the design did not expect.** MOP boost present (7.2a above); LLM calls on `mop_on_llm_70` — 142 on smartpack, 92 on freeotpplus; flushed final step, evidenced by `len(steps)` equalling `RUN_END.steps` in all six runs; `RUN_END` present in all six, `reason=timeout`. The two clauses D11 wrote off as un-guaranteeable inside a reduced-timeout smoke — an LLM **error** and a `no_match reason=dead_pair` — **both appeared**: `result=breaker_open` twice with `trips` 1 and 2 plus `reason=timeout` twice on freeotpplus `mop_on_llm_70`, and **37 `dead_pair`** across the two `llm_70` runs. D11's provision was "recorded if present and not required", so this is that provision firing rather than a contradiction of it; the practical consequence is that 9.1 rests on device evidence for every clause instead of leaning on `gh94`'s golden-fixture tests for the last two. Those tests remain the right home for the schema-level assertion.
      Traces kept at `experimento-rearch-aperv/results_smoke/rearch_aperv_smoke/rearch_aperv_smoke/<apk>/<apk>__1__300__aperv:<arm>.trace`, each with a `.ndjson.gz` beside it — that is also `rearch-04` 9.2's input. The tree is untracked and root-owned and is never `git add`ed.
- [x] 7.3 Confirm the smoke's tasks are COMPLETED with coverage > 0 and no `VerifyError` in the logcats. This is also what discharges `rearch-03` 8.4, whose "on-device smoke at the next scheduled rebuild" is this rebuild.
      **Re-measured 2026-08-05: 6/6 identity-distinct COMPLETED, coverage > 0 everywhere, zero `VerifyError`, zero detected errors.** Counted on `(apk_name, tool_config.name, tool_config.variant, repetition, timeout)` — never by grepping `tasks.json` for `COMPLETED`, which double-counts through `result.state_transitions[]`, the miscount learned from 2026-06-19. Note `variant` lives under `tool_config`, not directly in `config`.

      | Application | Arm | `method_coverage` | `activities_coverage` | `methods_mop_reachable` |
      |---|---|---|---|---|
      | smartpack | `mop_off_llm_off` | 34.84 % | 81.82 % | 51.32 % |
      | smartpack | `mop_on_llm_70` | 22.19 % | 72.73 % | 26.32 % |
      | smartpack | `mop_on_llm_off` | 33.07 % | 100.0 % | 48.68 % |
      | freeotpplus | `mop_off_llm_off` | 28.33 % | 42.86 % | 21.65 % |
      | freeotpplus | `mop_on_llm_70` | 38.93 % | 100.0 % | 30.30 % |
      | freeotpplus | `mop_on_llm_off` | 39.40 % | 100.0 % | 31.17 % |

      `coverage.csv` carries 2,852 rows. **Zero `VerifyError` is the load-bearing half of this task**: it says the instrumented APKs verify under ART in the campaign image, which is the failure that would have made every coverage number above meaningless. `rearch-03` 8.4 is discharged — this rebuild was its "next scheduled rebuild".
- [x] 7.4 Record the pre-flight report and a `PREFLIGHT` entry in `calibracao/journal.jsonl`. The report SHALL carry 7.2a's checks alongside 7.2's, and SHALL name them as the `ape` side's delegated device verification — otherwise `rearch-07` archives pointing at a smoke whose record does not show it verified what was delegated. It SHALL also carry 7.2b's observations, marked as recorded-not-gating, so a later reader cannot mistake a heartbeat finding for a passed gate or read the absence of the two optional 9.1 clauses as a failure.
      **Written 2026-08-05 to `docs/20260805_preflight_gate_rearch.md`**, named to sit beside `20260804_preregistro_gate_rearch.md` since it is the same family — the plan, then the record that the plan's gate opened. Every SHALL above is met and the report is structured around them: it opens by labelling its three kinds of statement with their different authority, §4 carries 7.2a and names it in its heading as the `ape` side's delegated device verification with `rearch-07` 8.1 called out as discharged there and nowhere else, and §7 carries 7.2b under the heading "Recorded, never gating" with D11's reasoning restated so the distinction survives without the reader having to find the design.
      The last SHALL was written against an expectation that turned out false — it guards against reading "the absence of the two optional 9.1 clauses as a failure", but neither was absent (7.2b). §7 therefore records their **presence** and what it buys, which serves the same purpose: the clause exists so a reader draws no wrong conclusion from that row, and the wrong conclusion available now is the opposite one.
      `PREFLIGHT` appended to `calibracao/journal.jsonl` with the report's sha256 `a4b1a7592936feda855210231d09f9577cbfb0962729df5f1b872f07a5b99825`; the file re-parses whole at 15 entries. The entry's `nota` records the gate result, the corrected smartpack figure and the complete discharge of 9.1, and states explicitly that **the entry does not authorise the launch** — that waits on the owner's explicit go-ahead, which is a separate decision from this gate.

## 8. The campaign

- [x] 8.1 Launch: `docker compose up -d` from the campaign directory. `rv-platform` owns the emulator lifecycle throughout — no manual emulator management, in any context, without exception.
      **Launched 2026-08-05 16:20** on the owner's explicit go-ahead, which is a decision separate from the 7.4 gate and was given only after the pre-launch state was re-derived. All ten containers `running` on `phtcosta/rvandroid:0.9.3-rearch` = `sha256:2cc5c3aa…`, RV_DELAY laddered 0→90 s so ten emulators do not contend for CPU at boot.
      Three identities were verified **against the running containers**, not against the host tree, because that is where gh71's failure hid: the bind-mounted jar reads `a7eddf5a…` **inside** `rearch_aperv_00` — the stage-4 jar stamped `9e948102`, not the image's own master-built one; the image ID is identical on all ten; and the LLM backend serves `Qwen/Qwen3-VL-4B-Instruct`, the stock upstream weights with no fine-tune.
      **The sglang process serving leg B is the same process that served leg A.** It started `2026-08-01T15:41:36Z`, two minutes before the E3 decisive run began (15:43Z), and has not restarted since — `Up 4 days (healthy)`. So the LLM backend is not merely an equivalent configuration across the two legs, it is numerically the same server with the same loaded weights, which removes it as a candidate explanation for any difference the gate measures.
- [x] 8.2 Monitor with the campaign's monitor script, counting identity-distinct completed work per container, never by grepping `tasks.json` for COMPLETED (which double-counts through `state_transitions`).
      Fourteen hourly iterations, 2026-08-05 17:50 to 2026-08-06 12:14 -03, cron job `8bf536b0`, since cancelled. Total wall clock 19 h 55 m against the ~18,6 h the amendment-01 budget predicted. Cadence held at ~20 runs/h for nine hours, dipping to 17/h twice when containers fell out of phase — never a stall, so no container was ever restarted for hanging. Final tally, counted identity-distinct: **360/360 COMPLETED**, 40/40 APKs, 120/120 in each of the three arms, durations min 1859 s / median 1872 s / max 1909 s against an 1800 s budget.
      **Nine identities were lost during the run, in two classes the loop could see unequally.** Seven failed loudly — `adb: device offline` during APK install, ~60 s each, spread across `mop_off_llm_off` (×4), `mop_on_llm_off` (×2) and `mop_on_llm_70` (×1), so no arm bias — each stored `ERROR` with its full message and each recovered by the ordinary resume with no operator judgement. Two failed silently and are the finding this group's other tasks answer; the loop could not see them, because nothing it read looked at duration.
      SGLang was never touched and stayed `healthy` throughout, which is what preserves the provenance claim recorded under 8.1.
      The containers' stdout is the only record of all of this, and it does not survive a `docker compose down`; it is preserved under 8.4, so these counts can be re-derived rather than taken on this note's word.
- [x] 8.3a Implement the admissibility predicate and Gate 6 (INTEGRITY) in `scripts/verify.py`, applied per identity over 100% of tasks, blind to arm and to the direction of the effect, before any aggregation. Six criteria (design D12): **C1** `state == COMPLETED` with an empty `error_message`; **C2** `execution_time_seconds >= timeout`; **C3** a trace carrying at least one step beyond `RUN_START`; **C4** at least one distinct `RVSEC-COV` signature in the logcat; **C5** `cov_method > 0` **and** `cov_act > 0`; **C6** the count of admissible identities equal to the manifest's `predicted_identities`. C4's floor is **1** — structural, not fitted — and the observed distribution is reported so a later change can calibrate it on evidence rather than on this campaign's own numbers. Add the unit tests of the design's testing table: each criterion must fail alone.
- [x] 8.3b Write `scripts/repair_tasks.py`. `--dry-run` by default. It copies the inadmissible run's `.logcat`, `.trace`, `.trace.ndjson.gz` and the container's original `tasks.json` to `backup/gh97-truncated-runs/<container>/` with a `SHA256SUMS` **before** mutating anything — the re-execution writes to the same filenames, so skipping this destroys the evidence of the defect in the act of repairing it. Then it sets `result.state` to `"ERROR"` (the enum **name**, which is what `TaskState[...]` reads back — `task.py:473`) with an `error_message` naming the criterion that failed. It imports the predicate from `verify.py` and never the reverse. `backup/` is not gitignored here and is never `git add`ed.
- [x] 8.3c Register **amendment 02** in `docs/20260804_preregistro_gate_rearch.md` and append its digest entry to `calibracao/journal.jsonl`. This SHALL be done before task 9.5 and before any outcome is read: an admissibility criterion decides which data enters the analysis, so registering it after the numbers are visible is choosing the ruler with the score in view. The amendment states the six criteria, their arm-blindness, and that C2's floor is structural rather than derived from the campaign.
- [x] 8.3 Run `repair_tasks.py` (dry-run, review, apply), then one final resume pass (`docker compose up -d`) to recover both the transient `adb install` failures and the repaired truncated runs, then confirm identity-distinct **admissible** completions equal 360 — not merely 360 `COMPLETED`.
      **Executed 2026-08-06.** The two truncated runs had already been repaired by hand and re-executed before `repair_tasks.py` existed, so the script's first run against the real tree is its regression test rather than its first use: `uv run python scripts/repair_tasks.py --iter-dir .` reports **360 identities scanned, 0 inadmissible**, and writes nothing. That is Gate 6's six criteria applied to 100% of the campaign, so the count confirmed here is 360 identity-distinct **admissible** completions, not 360 `COMPLETED` — which the tree also was on 2026-08-05, while carrying two runs that had not happened.
      **The distinction is not hypothetical and is why this task was rewritten.** Leg B lost nine identities in two classes. Seven failed loudly — all seven `adb: device offline` during install, spread across `mop_off_llm_off` (×4), `mop_on_llm_off` (×2) and `mop_on_llm_70` (×1), each stored `ERROR` with its full message and each recovered by the ordinary resume with no operator judgement. Two failed silently: `rearch_aperv_05` `de.lukasneugebauer.nextcloudcookbook_62.apk` rep 3 `mop_on_llm_off` at **1284 s** of 1800 (emulator died, `adb` exit 255, `emu kill` refused on port 5554) and `rearch_aperv_07` `net.pfiers.osmfocus_1009013.apk` rep 1 `mop_on_llm_off` at **1012 s** (exit 1, emulator alive, APE-RV stopped on its own). Both were stored `COMPLETED` with `error_message` null, so no resume could reach them — a container reporting `36/36 tasks completed (100.0%)` was carrying one.
      **Both were repaired by hand on 2026-08-06**, ahead of the script, because both containers had already exited and the campaign could not wait: artifacts and `tasks.json` copied to `backup/gh97-truncated-runs/rearch_aperv_0{5,7}/` with `SHA256SUMS`, the C2 predicate applied over all 36 records of each (one match each), `state→ERROR` with the criterion named, then `docker compose up -d`. Both produced `Resume: skipped 35 already-completed tasks (1 remaining)` against the `36/36 (100.0%)` the same log reported a line earlier. 8.3b describes this validated procedure rather than proposing one.
      **Count per identity `(apk_name, tool_config.name, tool_config.variant, repetition, timeout)`, never per record.** A resume **appends** a task record instead of overwriting the failed one, so counting records double-counts every recovery — during this campaign that turned 3 outstanding tasks into an apparent 8. This is the 2026-06-19 lesson in a second form.
- [x] 8.4 Extract the traces before any `docker compose down` — device artifacts are ephemeral.
      **The traces were never at risk, and the thing that was is not the trace.** Each container bind-mounts `./results/rearch_aperv_NN` onto its results directory, so every artifact the run writes lands on the host as it is written: 360 `.trace`, 360 `.logcat` and ten `experiment_completion.json` are on disk, and Gate 6 passing C3 and C4 on all 360 is an independent proof that each one is present and readable. `docker diff` shows `out/` created and empty inside the container — the monitor, instrument and static-analysis phases were skipped by design — and nothing else under `/opt/rvsec/rv-android` outside the mount.
      What a `down` would destroy is the containers' **stdout**: the per-task summaries, the `adb install` failures, the resume lines and the `36/36 tasks completed (100.0%)` that a repaired container printed one line before `Resume: skipped 35 already-completed tasks (1 remaining)`. That is the only record of how the campaign ran, as opposed to what it produced, and 8.2's counts are re-derivable only from it. All eleven container logs — the ten plus the smoke — are extracted to `experimento-rearch-aperv/container_logs/*.log.gz` (19,4 MB raw, 1,6 MB compressed) with a `SHA256SUMS`. Never `git add` it.
      The containers themselves are left `Exited (0)` and intact. Nothing here required a `down`, and none was run.

## 9. Validity gates, then outcomes — in that order

- [x] 9.1 Gate: control arm is clean — `decision_source=MOP` == 0 and `mop=` == 0 in every step of `mop_off_llm_off`, anchored on `(?<![a-z_])mop=` so the pattern does not also match the tail of `activity_has_mop=1`.
      **PASS.** Implemented as Gate 7 (CONTROL) in `verify.py` and run over the campaign: **120 control runs, 214.448 steps, 0 selected by a MOP-derived source and 0 carrying a MOP-widget boost.** `mop_frontier` is also 0 throughout, reported but not gated on, because the control keeps `frontier_boost_weight` at 200 so navigation survives (INV-APV-30) and gating a boost that shares a producer with the surviving one risks failing on intended behaviour.
      **The task's wording is against the retired format and was translated, not weakened.** `decision_source=MOP` and `mop=` with the `(?<![a-z_])mop=` anchor describe a text scan over `[APE-STEP]` lines, where a bare search for `mop=` also matched the tail of `activity_has_mop=1`. Leg B's traces are stage-4 NDJSON and the reader returns `decision_source`, `mop` and `activity_has_mop` as separate named fields, so the ambiguity the anchor guarded against cannot arise. The source test matches any source whose name carries `MOP`, case-insensitively, rather than one literal: a clean control arm emits no MOP token to read the spelling off, so pinning a literal would silently stop matching if the jar renamed a stage. The vocabulary actually observed is `SATA`, `Coverage`, `Form`, `Budget` and `Component`.
- [x] 9.2 Gate: jar and preset — the pre-flight's checks hold across the full campaign, not only the smoke, on a sampled basis per arm.
      **PASS**, over the full campaign rather than by sampling — Gate 1 (CONFIG) audits 360/360 tasks, so the per-arm sample the task allowed for was not needed.
      **This gate failed on its first run and was corrected; the correction is recorded here rather than absorbed.** It reported 240 mismatches in 360 tasks. All 240 were the same two keys — `ape.mopWeightWtg` and `ape.mopWeightOpenMenu` — absent from `RUN_START.params` in all 120 `mop_off_llm_off` runs, and absent because the control declares them at **0**, which is the `mop` preset's own value: the jar echoes only what departs from the preset. There were **zero value disagreements anywhere in the campaign**. So the gate was failing 120 healthy control runs for having complied.
      **No threshold was moved and no semantics was invented.** `preflight_runstart.check_plan` — the pre-flight the pre-registration's §10 declares blocking, frozen 2026-08-04 before the campaign, PASS on 3/3 arms — already states the rule: *"a declared key ABSENT from `params` is not a mismatch ... the jar omits a key that sits at its own default"*. `_gate_config` was the less careful copy, and it now applies the same rule. **The pre-flight's anti-vacuity guard came with it**, deliberately: an arm whose every declared key is absent compared nothing and FAILS, so absence cannot become a way to pass without checking anything. Keys compared per run after the correction: control 4 (2 at preset value), reference 4 (0), LLM arm 9 (0). The report names the uncompared keys instead of leaving them to be inferred from a silent pass.
- [x] 9.3 Gate: arm attribution — every run's effective plan matches its declared arm, 40/40 per arm.
      **PASS — 120/120 per arm**, which is 3 replicas × 40 applications and exceeds the 40/40 the task asked for, since Gate 1 audits every task rather than one per application. Every run's echoed `preset` and every compared override matches the arm it is filed under. Gate 2 (COLLISIONS) independently confirms no identity maps to more than one arm, which is the failure mode an `@override` arm would produce.
- [x] 9.4 Gate: task integrity — every identity **admissible** under Gate 6's six criteria (8.3a), not merely `COMPLETED`. `COMPLETED` is a claim the harness makes; admissibility is the claim checked. Report the count of identities failing each criterion and name them; lost runs are reported, never silenced. Include the distribution of distinct `RVSEC-COV` signatures so C4's floor can be calibrated later on evidence. Count per identity, never per record — a resume appends rather than overwrites.
      **PASS — 360/360 identities admissible**, not merely COMPLETED. Failures per criterion: C1=0, C2=0, C3=0, C4=0, C5=0, C6=0, against the manifest's 360 `predicted_identities`. Counted per identity: the tree holds **369 task records for 360 identities**, and those 9 extra records are exactly the 9 recoveries of 8.2 — the arithmetic that would have reported 369 is the double-count this task warns about.
      **Distribution of distinct `RVSEC-COV` signatures**, reported so C4's structural floor can be calibrated later on evidence rather than on this campaign's own numbers: n=360, **min 46**, median 4.414,5, max 23.711, and **0 identities at or below the floor of 1**. The nearest run to the floor exceeds it by a factor of 46, so no identity in this campaign was anywhere near the boundary — which is the fact a later calibration needs, and the reason raising the floor on this evidence would still be choosing a number against the campaign's own distribution.
- [ ] 9.5 Run `scripts/verify.py` and confirm it is clean. No outcome is read before 9.1–9.5 pass; a failed gate invalidates what it protects, and the analysis is not adjusted to route around it.
- [ ] 9.6 Run `scripts/consolidate.py` to produce `per_apk_paired.csv` and `tel_proxies.csv`.

## 10. Verdict

- [ ] 10.1 Run `scripts/compare.py` and produce the report: G1, G2 and G3 with point estimates and CIs for every outcome, including the ones that do not gate.
- [ ] 10.2 Write the result document — whatever it says, including a tie, with its CIs and the declared premises restated. A tie is a valid and useful outcome.
- [ ] 10.3 List every analysis that was not in the frozen plan and label it exploratory. **One entry is already owed and is not an analysis but an observation**, so it is named here rather than discovered at write-up time: the monitored-operation substrate is structurally absent over most of the corpus — `flagged > 0` in 8 of 40 applications, `wtg > 0` in 15 of 40, and all three of `flagged`/`wtg`/`mopActivities` together in only 4 of 40, with the corpus's two most-flagged applications carrying an empty WTG. It was measured on 2026-08-05, **after** the 2026-08-04 freeze, so it is reported under this task rather than folded into the pre-registration's declared premises, and it modifies no margin, arm or decision rule. It belongs here because G3 reports displacement and 36 of the 40 applications cannot produce any, which a reader would otherwise take for a null result of the rewrite rather than an absence in the substrate.
- [ ] 10.4 Record the merge verdict for the `rearch` line, and a `DECIDE` entry in `calibracao/journal.jsonl`.
- [ ] 10.4a Carry the admissibility criteria out of this campaign's directory, where the next campaign would have to re-derive them. `.claude/skills/rv-experiment-compare/SKILL.md` gains them as a standard step of any multi-tool comparison, with the counting rule. `modules/rv-platform/CLAUDE.md` records that a `COMPLETED` task is not by itself a valid one, and what the resume contract does with each state. `modules/aperv-tool/CLAUDE.md` gains the counterpart to its existing "non-zero exit is normal" gotcha: the exit code cannot be the authority, because a dead emulator and an application crash are indistinguishable by it, which is why INV-APV-60 reads elapsed time instead.
- [ ] 10.5 Run `/rv-qa-lint-fix aperv-tool`
- [ ] 10.6 Run `/rv-verify aperv-tool`
- [ ] 10.7 Invoke `/rv-code-reviewer` via Skill tool
- [ ] 10.8 Run `/rv-docs-sync aperv-tool`

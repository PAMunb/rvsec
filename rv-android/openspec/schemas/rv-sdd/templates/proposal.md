## Why

<!-- Explain the motivation for this change. What problem does this solve? Why now? Reference the GitHub Issue if one exists (GitHub Issue: #N). -->

## What Changes

<!-- Describe what will change. Be specific about new capabilities, modifications, or removals. Mark breaking changes with **BREAKING**. -->

## Capabilities

### New Capabilities
<!-- Capabilities being introduced. Use kebab-case identifiers matching the 7 RV-Android domains (core, platform, experiment, agent, instrumentation, analysis, tools). Each creates specs/<name>/spec.md -->
- `<name>`: <brief description of what this capability covers>

### Modified Capabilities
<!-- Existing capabilities whose REQUIREMENTS are changing (not just implementation).
     Only list here if spec-level behavior changes. Each needs a delta spec file.
     Use existing spec names from openspec/specs/. Leave empty if no requirement changes. -->
- `<existing-name>`: <what requirement is changing>

## Impact

<!-- Affected modules (list from the 14-module uv workspace), APIs, dependencies, cross-module interfaces. Reference FRs/NFRs from docs/PRD.md. -->

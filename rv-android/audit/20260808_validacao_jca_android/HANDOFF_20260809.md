# Retomada — validação das specs jca_android (2026-08-09)

Doutorado (RV-Android): auditoria adversarial e reproduzível das 23 specs JavaMOP
`jca_android` contra as regras CrySL que as originaram. O PLANO MESTRE é o protocolo
`docs/20260808_validar_specs_jca_android.md` — siga rigorosamente a ordem da seção 19.
Não improvise fora dele.

## Documentos que governam o trabalho

1. Protocolo (plano mestre): `docs/20260808_validar_specs_jca_android.md`
2. Pré-registro: `audit/20260808_validacao_jca_android/fase0/pre_registro.md`
3. Modelo semântico: `audit/20260808_validacao_jca_android/fase0/modelo_semantico.md`
4. Manifesto do corpus: `audit/20260808_validacao_jca_android/fase0/manifesto.md`
5. Desvios do pré-registro: `audit/20260808_validacao_jca_android/fase0/desvios.md`

## O que já foi feito (seção 19, itens 1–3: completos)

- Fase 0 congelada (`fase0/manifesto.md`, `fase0/toolchain_ambiente.md`).
- Piloto (CipherSpec, GCMParameterSpecSpec) ENCERRADO em 2026-08-09: 3 revisores,
  juiz, refutação, respostas e decisão final. Vereditos: **ambas REPROVADAS** no
  escopo coberto. Score descritivo 55,90 (rev. 2). Tudo em `pilot/`:
  decisão final = `pilot/juiz_sintese.md` §8; claims = `pilot/juiz_claims_resolvidos.csv`;
  re-soma reproduzível = `python3 pilot/juiz_rescore.py`.
- Desvios D-piloto-1..4 formalizados em `fase0/desvios.md`. A verificação única do
  Xtext upstream (D-piloto-1) já foi executada: vírgula é o operador mais externo do
  ORDER (leitura A); evidência congelada em `fase0/upstream_CrySL_e92f5607.xtext`.
- Decisão do pesquisador (2026-08-09): `RandomStringPassword.mop` está EXCLUÍDA da
  rodada (sem regra CrySL); registrar a exclusão no manifesto e no juízo global.

## Próximos passos (seção 19, item 4 em diante)

1. Auditar as 20 specs restantes em lotes, 3 revisores independentes por lote, como
   no piloto. Pareamento spec↔regra: `fase0/inventario_pareamento.md`. Os prompts dos
   revisores DEVEM incorporar os desvios D-piloto-1..4 (`fase0/desvios.md`) e os
   ajustes de processo do juiz (fim da §6 de `pilot/juiz_sintese.md`).
   Lote A sugerido (pequenas): DHGenParameterSpecSpec, HMACParameterSpecSpec,
   PBEParameterSpecSpec, IvParameterSpecSpec, SecretKeySpecSpec (ver pareamento).
2. Verificações de conjunto (protocolo §19.5) e análise do errors.csv (hipóteses
   H1–H7 em `pilot/gama_historico.md` — testar, nunca inferir do zero).
3. Juízo global, refutação, vereditos e matriz de gates (protocolo §§15–17).
4. SÓ DEPOIS: propor correções das specs e evolução da skill rv-analyze-spec, em
   patches separados (nunca misturar descoberta e reparo).

## Regras operacionais (aprendizados — obrigatórios)

- NUNCA editar este handoff, o protocolo ou qualquer prompt do usuário. Progresso se
  reporta na conversa e nos artefatos de saída sob `audit/`, somente.
- Novos documentos da auditoria em INGLÊS; os existentes ficam como estão.
- Subagentes: prompts autocontidos com caminhos absolutos; cada revisor escreve só
  arquivos com seu prefixo (alfa_/beta_/gama_/juiz_/refutacao_); revisores não leem
  os pareceres uns dos outros antes da primeira rodada.
- Gerar monitores sempre em diretório scratch, nunca na árvore de specs; conferir
  stderr (exit 0 não garante sucesso). Comandos: ver
  `modules/rv-monitor-generator/src/rv_monitor_generator/runtime_verification_generator.py`
  (~linhas 211 e 267).
- Emulador Android: somente via rv-experiment/rv-platform, nunca manualmente.
- MetaCrySL e o conjunto `jca` são somente leitura. Não commitar nada sem pedido
  (working tree tem ~549 arquivos de outros trabalhos).
- pytest do repo: sempre `--import-mode=importlib -o "addopts="`.

Comece pelo passo 1 dos próximos passos.

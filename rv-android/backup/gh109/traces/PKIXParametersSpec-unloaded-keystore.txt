# gh109 task 2.R, the separating half of the REQUIRES-reading producer shape (task 2.10).
# The key store is obtained and never loaded, so no `generatedKeyStore` was ever written for
# it and the read returns NOT_OBSERVED rather than VIOLATED. That distinction is the whole
# point of the pair: the specification draws PKIXPARAMETERS-NOBS-00 and not
# PKIXPARAMETERS-CONSTR-00, because what it saw is an absence of evidence and not evidence
# of a store the rule refuses.
#
# The separating trace is PKIXParametersSpec.txt, identical but for the `load`, which draws
# nothing. Whether the NOBS branch is worth keeping over a real APK corpus is what task 7.3
# measures; this trace is what makes the branch reachable and checkable in the meantime.
#
# `preparedCertPathParameters` is not staged either way but for different reasons: here the
# read failed, there it succeeded.
KeyStore.getInstance("PKCS12") -> ks
new PKIXParameters(ks) -> params

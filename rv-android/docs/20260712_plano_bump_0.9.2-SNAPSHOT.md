# Plano — Bump de versão 0.9.1 → 0.9.2-SNAPSHOT

Data: 2026-07-12
Branch: `modules`
Status: **investigação/mapeamento — NÃO implementado ainda**
Track: **Quick Path** (`quick-path` schema) — mudança mecânica, sem decisões de design
(WORKFLOW.md §3: bump multi-arquivo é Quick Path, não FF/Full SDD)

## Objetivo

Subir a versão do projeto de `0.9.1-SNAPSHOT` → **`0.9.2-SNAPSHOT`** (POMs Maven, mantendo o
sufixo `-SNAPSHOT`) e as tags de imagens Docker `0.9.1` → **`0.9.2`** (forma release, sem
`-SNAPSHOT`), além dos scripts de runtime/build ativos.

**Método:** usar o `versions-maven-plugin` (`mvn versions:set`) para os POMs do reator;
edições manuais para scripts, Dockerfiles, compose, defaults Python e asserts de teste.

**Princípio orientador (mantido do bump anterior): NÃO mexer nos históricos.** Experimentos
passados, relatórios, OpenSpec archives, docs (`*.md`) e reatores com versão própria
divergente permanecem na versão/tag antiga.

## Convenção de versão

| Contexto | Antes | Depois |
|---|---|---|
| POMs Maven | `0.9.1-SNAPSHOT` | `0.9.2-SNAPSHOT` |
| Caminhos de JAR Maven em scripts (`RV_MONITOR_VERSION`, `validator-*.jar`) | `0.9.1-SNAPSHOT` | `0.9.2-SNAPSHOT` |
| Tags de imagem Docker | `0.9.1` | `0.9.2` |

---

## O que MUDOU desde o bump 0.9.0→0.9.1 (simplificações)

1. **Órfãos `rvsec-gesda` (+common/core) e `rvsec-reachability` sumiram do tree ativo** —
   foram movidos para `backup/` (`backup/rvsec-gesda/*`, `backup/rvsec-reachability/*`, os
   4 POMs restantes em `0.9.0-SNAPSHOT`). O tratamento especial "deixar em 0.9.0" **não é
   mais necessário**: eles estão em `backup/` (regra "nunca tocar backup"). **A árvore ativa
   está uniformemente em `0.9.1-SNAPSHOT`.**
2. **Nova ocorrência de default de imagem** não existente no plano anterior:
   `rv-android/.claude/skills/rv-experiment-compare/scripts/gen_compare.py:202`.
3. **`experimento-20260706/` agora existe e está ATIVO** (run JCA 219 APKs, 4 VMs gcloud,
   pinado na imagem `0.9.1`) — ponto de decisão crítico (ver seção "Decisão pendente").

---

## Mapa do reator Maven

`mvn versions:set -DnewVersion=0.9.2-SNAPSHOT` rodado da **raiz** alcança **45 POMs** do reator
(todos herdam `br.unb.cic:rvsec-parent:0.9.1-SNAPSHOT`):

- raiz `rvsec-parent`
- `rv-monitor` + `rv-monitor-rt` + `logicrepository` + `plugins_logicrepository` (+10 plugins) + `rv-monitor`
- `javamop`
- `mop-maven-plugin`
- `rvsec` + (`rvsec-mop`, `rvsec-mop-extractor`, `rvsec-mop-defsuses`, `rvsec-core`, `rvsec-logger-csv`, `rvsec-agent`, `rvsec-android`)
- `rvsec-android` → `rvsec-apk`, `rvsec-logger-logcat`, `rvsec-gator` (+commons/sootandroid/client), `rvsmart`, `rvsec-frame-computer`, `rvsec-instrumentation-dexlib2` (+10 filhos)

**NÃO alcançado pelo `versions:set` da raiz** (comentado no reator — `pom.xml:73`
`<!-- <module>rv-android</module> -->`), porém ativo e em `0.9.1-SNAPSHOT`:

| POM | Decisão |
|---|---|
| `rv-android/pom.xml` (`<version>` linha 10 + `<parent><version>`) | **BUMP explícito → 0.9.2-SNAPSHOT** |

Total: **46 POMs ativos** → `0.9.2-SNAPSHOT` (45 via plugin + 1 explícito).

**Históricos / versão própria (NÃO tocar):**

| POM / grupo | Versão | Motivo |
|---|---|---|
| `backup/rvsec-gesda/*` (3) + `backup/rvsec-reachability/pom.xml` | `0.9.0-SNAPSHOT` | Em `backup/` — regra "nunca tocar backup" |
| `crylogger/*` (3 POMs) | `0.3.0-SNAPSHOT` / `0.0.1` | Reator próprio, fora do rvsec |
| `rvsec/docker/mop` | `1.2.8` | Artefato de terceiros |
| `rv-monitor/docs` + `installer` | `1.4-SNAPSHOT` | groupId upstream |
| `teste-sootup` | `0.6.0-SNAPSHOT` | Órfão de teste |

> Nota: nenhum POM ativo tem dependência hardcoded em `0.9.1-SNAPSHOT` (todos usam herança de
> parent / `${project.version}`). `rvsec/rvsec-android/rvsmart/dependency-reduced-pom.xml` é
> **gerado** pelo shade-plugin (regenerado no build) — **não editar à mão**.

---

## Arquivos a alterar (fora dos POMs)

### 1. Scripts de runtime — caminhos de JAR Maven (mantêm `-SNAPSHOT`)

| Arquivo | Linha | Mudança |
|---|---|---|
| `configure.sh` (raiz) | 13 | `RV_MONITOR_VERSION=0.9.1-SNAPSHOT` → `0.9.2-SNAPSHOT` |
| `rvsec/config.sh` | 13 | `RV_MONITOR_VERSION=0.9.1-SNAPSHOT` → `0.9.2-SNAPSHOT` |
| `rv-android/scripts/run_phase5_validators.sh` | 80 | `validator-0.9.1-SNAPSHOT.jar` → `validator-0.9.2-SNAPSHOT.jar` |

### 2. Docker — chain de build ativo (tag release `0.9.2`, sem `-SNAPSHOT`)

**Build scripts** (`VERSION=` / `IMAGE_TAG=` `0.9.1` → `0.9.2`; comentários `push` na l.15 também):
- `build_docker_image.sh:12` (`IMAGE_TAG="0.9.1"`)
- `rv-android/docker/base/build.sh:5` (+comentário `:15`)
- `rv-android/docker/android/build.sh:5` (+comentário `:15`)
- `rv-android/docker/tools/build.sh:5` (+comentário `:15`)
- `rv-android/docker/rvandroid/build.sh:5` (+comentário `:15`)
- `rv-android/docker/rvandroid_dev/build.sh:3`

**Dockerfile `FROM`** (encadeamento de camadas → `0.9.2`):
- `rv-android/docker/android/Dockerfile:1` (`FROM phtcosta/rvsec_base:0.9.1`)
- `rv-android/docker/tools/Dockerfile:1` (`FROM phtcosta/rvsec_android:0.9.1`)
- `rv-android/docker/rvandroid/Dockerfile:1` (`FROM phtcosta/rvandroid_tools:0.9.1`)
- `rv-android/docker/rvandroid_dev/Dockerfile:1` (`FROM phtcosta/rvandroid_tools:0.9.1`)

**Run helpers ativos** (`docker run ... :0.9.1` → `0.9.2`):
- `rv-android/docker/base/run.sh:3`
- `rv-android/docker/android/run.sh:3`
- `rv-android/docker/tools/run.sh:17` (+comentário `:47`)
- `rv-android/docker/rvandroid/run.sh:3`

### 3. Compose canônico/ativo (→ `0.9.2`)

- `rv-android/docker/docker-compose.yml:20` (`phtcosta/rvandroid:0.9.1`, hardcoded — entrypoint principal)
- `rv-android/docker/docker-compose.dexlib2-validation.template.yml:34,55` (template genérico; +comentários `:6,:21`)

### 4. Python — defaults de imagem em scripts/tooling ativos (→ `0.9.2`)

- `rv-android/scripts/baseline_docker.py:252-253` (`default="phtcosta/rvandroid:0.9.1"` + help)
- `rv-android/scripts/preprocess_docker.py:271-272`
- `rv-android/scripts/calibration_orchestrator.py:584-585`
- `rv-android/.claude/skills/rv-experiment-compare/scripts/gen_compare.py:202` **(NOVO — não estava no bump anterior)**

### 5. Testes (asserts de nome de JAR — mantêm `-SNAPSHOT`)

Os nomes `*-0.9.1-SNAPSHOT.jar` são apenas asserts; o runtime (`instrumenter.py`) resolve via
`glob("*.jar")`, então **runtime não quebra**, mas os testes falharão até atualizar:

- `rv-android/modules/rv-instrumentation-core/tests/test_instrumenter.py:97-99`
- `rv-android/modules/rv-instrumentation-dexlib2/tests/test_dexlib_instrumentation.py:536-538,546-548,580-582,594-596`

→ 3 nomes de jar (`rv-monitor-rt`, `rvsec-core`, `rvsec-logger-logcat`) → `-0.9.2-SNAPSHOT.jar`.

---

## 🔒 DECISÃO CONFIRMADA — NÃO tocar `experimento-20260706/` nem históricos

**Decisão do usuário (2026-07-12): não tocar no experimento nem em coisas históricas.**

`experimento-20260706/` está **pinado na imagem `0.9.1`** (experimento ativo, 219 APKs, 4 VMs).
Mudar a tag para `0.9.2` quebraria `pull`/reprodutibilidade e violaria a regra "nunca alterar
config de experimento sem autorização". **Ficam intactos em `0.9.1`:**
- `rv-android/experimento-20260706/docker-compose.gcp.yml:25` (+comentário `:6`)
- `rv-android/experimento-20260706/docker-compose.smoke.yml:19`
- `rv-android/experimento-20260706/scripts/build_and_push_091.sh` (6 refs; script **nomeado** para 0.9.1)
- `rv-android/experimento-20260706/scripts/copy_dataset_to_vms.sh:9` (comentário)

Estes arquivos **entram na verificação como exceções esperadas** (Fase 6) — o `git grep` residual
deve conter APENAS `experimento-20260706/`, `backup/`, `experimento-20260604/`,
`experimento-20260508/`, `para05/`, `data/` e `*.md`.

---

## NÃO TOCAR (históricos)

- Compose de experimentos antigos e tudo em `experimento-20260604/`, `experimento-20260508/`,
  `para05/`, `data/`, `backup/`.
- Documentação e relatórios (`*.md`), OpenSpec `changes/` e `archive/`.
- Reatores com versão própria (crylogger, docker/mop, rv-monitor docs/installer, teste-sootup).
- `experimento-20260706/` (ver "Decisão pendente" — recomendação: manter em 0.9.1).

---

## Sequência de execução (quando autorizado)

**Fase 0 — Higiene de git.** A árvore já tem mudanças não commitadas (`.project`, jars, docs).
O commit do bump deve ser **escopado apenas aos arquivos do bump**.

**Fase 1 — Maven (reator), via plugin.** Da raiz:
```bash
mvn versions:set -DnewVersion=0.9.2-SNAPSHOT -DgenerateBackupPoms=true
```
Validar com `git diff --stat` que **só** os 45 POMs do reator mudaram (zero alterações em
crylogger / rv-monitor docs+installer / docker-mop / teste-sootup / `backup/*`).

**Fase 2 — `rv-android/pom.xml` (órfão ativo, comentado no reator).** Bump explícito
(`<version>` linha 10 + `<parent><version>`) → `0.9.2-SNAPSHOT`.

**Fase 3 — Scripts runtime.** `configure.sh`, `rvsec/config.sh`, `run_phase5_validators.sh`.

**Fase 4 — Docker.** Build scripts + Dockerfile `FROM` + run helpers + compose canônico +
defaults Python (itens 2–4 acima). **Excluir `experimento-20260706/`** (decisão pendente).

**Fase 5 — Testes (OBRIGATÓRIO: tudo green).** Atualizar os asserts de nome de jar nos 2
arquivos e **rodar as suítes afetadas com o contrato de CI** até passarem:
```bash
cd rv-android
uv run pytest modules/rv-instrumentation-core/tests/test_instrumenter.py \
              modules/rv-instrumentation-dexlib2/tests/test_dexlib_instrumentation.py \
              --import-mode=importlib -o "addopts="
```
Critério: **0 failed** nesses arquivos (os asserts `*-0.9.2-SNAPSHOT.jar` batem com a nova versão).

**Fase 6 — Verificação (green obrigatório).**
- **Grep residual:** `git grep -n "0.9.1-SNAPSHOT"` e `git grep -n ":0.9.1"` →
  confirmar que **só** restam ocorrências nas exceções esperadas (`backup/`,
  `experimento-20260706/`, `experimento-20260604/`, `experimento-20260508/`, `para05/`,
  `data/`, `*.md`). **Zero** ocorrência em POM/scripts/Docker/Python/testes ativos.
- `mvn versions:commit` (remover `*.versionsBackup`).
- **Build de sanidade Maven:** `mvn -o -q -DskipTests validate` no reator → **EXIT=0**
  (prova que a resolução de parents/deps não quebrou; no bump anterior deu EXIT=0).
- **Testes Python green:** comando da Fase 5 → **0 failed**.

**Fase 7 — Commit.** Mensagem escopada, ex.:
`chore(release): bump 0.9.1-SNAPSHOT -> 0.9.2-SNAPSHOT (poms reator + rv-android + scripts/docker ativos)`.

---

## Riscos

- **Imagens Docker `0.9.2` precisam ser efetivamente reconstruídas/publicadas** (`build.sh` de
  cada camada + push) para os compose/scripts atualizados funcionarem. **Bump de tag ≠ build** —
  sem rebuild, o `pull :0.9.2` falha.
- **Testes Python** falharão até atualizar os asserts (Fase 5).
- **`experimento-20260706/` pinado em `0.9.1`** — decisão confirmada: **NÃO tocar**. Se for
  bumpado por engano, o experimento em curso perde reprodutibilidade.
- **`backup/*` em `0.9.0-SNAPSHOT`** referenciando um parent que deixará de existir — irrelevante
  (não é buildado; regra "nunca tocar backup").

---

## Inventário bruto (para conferência)

- **46 POMs ativos** em `0.9.1-SNAPSHOT` (45 reator + `rv-android`).
- **4 POMs em `backup/`** em `0.9.0-SNAPSHOT` (gesda×3 + reachability) — ignorar.
- **~29 arquivos não-POM** com `0.9.1`, dos quais ~25 a bumpar (scripts/docker/python/testes) e
  ~4 a preservar (`experimento-20260706/`).
</content>
</invoke>

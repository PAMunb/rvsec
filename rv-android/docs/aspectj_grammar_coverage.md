# AspectJ Grammar Coverage Matrix — dexlib2 instrumenter

> **This document is the authoritative contract** for the AspectJ pointcut surface that the
> DEX-native dexlib2 instrumenter (`rvsec-android/rvsec-instrumentation-dexlib2/`) weaves
> correctly today. It supersedes `docs/AJ_CONSTRUCTIONS_INVENTORY.md` and
> `docs/AJ_TO_DEXLIB2_MAPPING.md` (INV-INS-102). The matrix is materialised as executable
> tests in the `grammar-tests/` Maven submodule; `MatrixIntegrityTest` breaks the build if the
> matrix and the code/tests move independently.

## References

- **AspectJ Programming Guide §"Pointcuts"** — https://eclipse.dev/aspectj/doc/latest/progguide/semantics-pointcuts.html (snapshot 2026-05-29)
- **AspectJ 5 Quick Reference** — https://eclipse.dev/aspectj/doc/latest/quick5.pdf (snapshot 2026-05-29)
- **smali-dexlib2 version** — `3.0.9` (verified against `https://maven.google.com/com/android/tools/smali/group-index.xml`, §0.4; pinned in `rvsec-instrumentation-dexlib2/pom.xml`)

## Verdict vocabulary

Every matrix row carries exactly one `Verdict` from the closed set `{COVERED, SILENT-GAP, EXPLICIT-NO-OP, NOT-NEEDED}` (INV-INS-89). After gh62 archives there are **zero `SILENT-GAP` rows** (INV-INS-91).

- **COVERED** — the construction is woven correctly by dexlib2 today. Evidence is an enabled, passing test FQN in `grammar-tests/`.
- **SILENT-GAP** — the construction reaches the instrumenter with non-zero pipeline demand but is dropped without an error. **No row may carry this verdict post-archive** — it must be closed (flip to COVERED) or reclassified (EXPLICIT-NO-OP / NOT-NEEDED). Retained in the vocabulary only so `MatrixIntegrityTest.testNoSilentGapRowsRemain` has a value to forbid.
- **EXPLICIT-NO-OP** — the construction is deliberately not woven and the code raises `UnsupportedOperationException` at the weave attempt. Evidence cites BOTH the UOE-asserting test FQN AND the `file:line` of the no-op. (Currently only `around` / `proceed(...)`.)
- **NOT-NEEDED** — the construction need not be woven, via one of two paths:
  - **path α** — `DemandCounter.countMop == 0` across all four corpora AND no parser/matcher/emitter implementation. Zero demand everywhere; no absorber needed.
  - **path β** — `DemandCounter.countMop ≥ 1` (source demand exists) BUT `DemandCounter.countCompiledAj == 0` (pipeline demand is zero — the construction is absorbed by a named upstream stage before reaching dexlib2). Evidence cites both demand counts, the named absorber, the empirical evidence, and an enabled passing assertion test.

## Demand counting

Demand is counted by the portable Java helper `DemandCounter` in `grammar-tests/src/test/java/br/unb/cic/rv/grammar/util/DemandCounter.java` (no shell, no `ProcessBuilder`):

- `countMop(designator, corpus)` — **SourceDemand**: walks `$RVSEC_HOME/rvsec/rvsec-mop/src/main/resources/{aspect,jca,generic,generic_new}/` matching **only `*.mop` files plus `aspect/Coverage.aj`** (never the git-ignored `-s` stray `*.aj` build artifacts in those dirs).
- `countCompiledAj(designator, corpus)` — **PipelineDemand**: walks the committed `empirical-monitors/{jca,generic,generic_new}/MultiSpec_1MonitorAspect.aj` snapshot (byte-identical to a fresh `rv-monitor-generator` run WITHOUT `-s`). **PipelineDemand is the authoritative scope signal**: closures ship in-change when PipelineDemand ≥ 1, not when SourceDemand ≥ 1.

Each designator's `java.util.regex.Pattern` is the audit trail and is pinned in `DemandCounter`'s regex map; `MatrixIntegrityTest.testSourceDemandCountsReproducible` and `.testPipelineDemandCountsReproducible` assert the matrix columns against the helper's output at every CI run (INV-INS-93). The per-row counting rule (per-occurrence vs per-line, negated-form ownership) is pinned per INV-INS-93 so the counts reproduce deterministically.

## Matrix

| AspectJ syntax | SourceDemand (aspect,jca,generic,generic_new) | PipelineDemand (compiled .aj) | Parser | Matcher | Emitter | Verdict | Evidence | Deferral note |
|---|---|---|---|---|---|---|---|---|
| `call(MethodPattern)` | 0,many,many,many | many,many,many | `PointcutExpressionParser.java:parse`/`parseCallBody` → `CallPC` | `PointcutMatcher.java:117,308-406` (`matchCall`) | `EmitterDispatch.java:56-76` → `Before`/`AfterEmitter` inline `invoke-static *RuntimeMonitor.*Event` | COVERED | `br.unb.cic.rv.grammar.CallPatternGrammarTest#baseCallPatternMatchesExactSignature` | The dominant pointcut form (every JCA + generic spec). Pattern: `(?<![A-Za-z0-9_])call\s*\(` (per-occurrence). |
| `execution(MethodPattern)` | 1,0,0,0 | 0,0,0 | `PointcutExpressionParser.java` → `ExecutionPC` | `PointcutMatcher.java:510-514` (`Match.empty` at method entry; coverage-weaver catch-all) | n/a (no MOP execution() emitter) | NOT-NEEDED β | `br.unb.cic.rv.grammar.ExecutionPointcutGrammarTest#executionPositiveAbsorptionAssertion` | Absorber `coverage-weaver` (R11.2). `EXECUTION_POSITIVE` source = 1 (`aspect/Coverage.aj:50` `execution(* *.*(..))`), pipeline 0,0,0 — JavaMOP emits `execution()` verbatim; the sole consumer is Coverage.aj, absorbed by `coverage-weaver`. deferred.md §2.2.1-H. Pattern: `(?<![A-Za-z0-9_!])execution\s*\(`. |
| `target(name)` binding | 0,0,0,many | many | `PointcutExpressionParser.java` → `TargetPC` (binding) | `PointcutMatcher.java:220-222` (`matchTarget` binding form = always-match collector) | `Before`/`AfterEmitter` resolve the bound register at injection | COVERED | `br.unb.cic.rv.grammar.TargetGrammarTest#targetBindingFormAlwaysMatches` | The binding form `target(o)` is an inert collector on an established call-site match (carries no type filter). |
| `target(Type)` type-matching | 0,0,0,8 | 0,0,8 | `PointcutExpressionParser.java` → `TargetPC` (type) | `PointcutMatcher.java:220-243` (`matchTarget` type form — declared-type subtype via `InheritanceResolver`, V-decision) | n/a (gate, not emit) | COVERED | `br.unb.cic.rv.grammar.TargetGrammarTest#targetTypeMatchesByReceiverType` | §4.TT. `TARGET_TYPE` pipeline 0,0,8 (positive only; negated `!target` owned by §4.N row). Pattern: `(?<![A-Za-z0-9_!])target\s*\(\s*[A-Z]`. |
| `this(name)` binding | 0,0,0,0 | 0,0,0 | MISSING (no `ThisPC` class) | MISSING | MISSING | NOT-NEEDED α | `DemandCounter.countMop == 0` across all four corpora; no parser/matcher/emitter impl | deferred.md §2.1: zero demand; `target(name)` covers the receiver-binding case the corpora exercise. No backing test (path-α structural verdict; §6.2 checks demand+MISSING, not an FQN). |
| `this(Type)` type-matching | 0,0,0,0 | 0,0,0 | MISSING (no `ThisPC` class) | MISSING | MISSING | NOT-NEEDED α | `DemandCounter.countMop == 0` across all four corpora; no parser/matcher/emitter impl | deferred.md §2.1: zero demand. No backing test (path-α structural verdict). |
| `args(name)` binding | 0,0,0,many | many | `PointcutExpressionParser.java` → `ArgsPC` | `PointcutMatcher.java:268-305` (`matchArgs` binding = always-match collector) | `Before`/`AfterEmitter` resolve bound arg registers | COVERED | `br.unb.cic.rv.grammar.ArgsGrammarTest#argsBindingFormAlwaysMatches` | The pure-binding form `args(o)` carries no type filter — always matches on an established call-site match. |
| `args(Type)` type-matching | 0,0,0,3 | 0,0,3 | `PointcutExpressionParser.java` → `ArgsPC` | `PointcutMatcher.java:268-305` (`matchArgs` positional declared-type, V-decision) | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.ArgsGrammarTest#argsTypeMatchesByArgumentType` | §4.AT. `ARGS_TYPE` pipeline 0,0,3 (positive only; negated `!args` owned by §4.N row). Pattern: `(?<![A-Za-z0-9_!])args\s*\(\s*[A-Z]`. |
| `args(*, name, ..)` mixed | 0,0,0,3 | 0,0,3 | `PointcutExpressionParser.java` → `ArgsPC` (mixed positions) | `PointcutMatcher.java:268-305` (`matchArgs`: `"*".equals(expected)` at `:292`, Type pin, trailing `..`) | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.ArgsGrammarTest#argsLeadingWildcardTypeAndTrailingRest` | Leading `*` accepts any single arg, a Type pins the next position, trailing `..` accepts the rest. |
| `withincode(MethodPattern)` | 0,0,0,0 | 0,0,0 | MISSING (parser routes unknown keyword to `NamedRefPC` always-match) | `PointcutMatcher.java:158-159` (`NamedRefPC` always-match) | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.WithincodeGrammarTest#withincodeHasZeroCorpusDemand` | deferred.md §2.1: zero demand (would filter join points by enclosing method signature). `WITHINCODE` = 0 in every corpus (source AND pipeline). |
| `cflow(Pointcut)` | 0,0,0,0 | 0,0,0 | MISSING (`NamedRefPC` fallback) | `PointcutMatcher.java:158-159` (always-match) | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.CflowGrammarTest#cflowHasZeroCorpusDemand` | deferred.md §2.1: zero demand (runtime flow tracking, substantial cost). `CFLOW` = 0 in every corpus. |
| `cflowbelow(Pointcut)` | 0,0,0,0 | 0,0,0 | MISSING (`NamedRefPC` fallback) | `PointcutMatcher.java:158-159` (always-match) | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.CflowGrammarTest#cflowBelowHasZeroCorpusDemand` | deferred.md §2.1: zero demand. `CFLOWBELOW` = 0 in every corpus. |
| `if(BooleanExpression)` | 0,0,0,5 | 0,0,3 | `PointcutExpressionParser.java` → `IfPC.java:11` (raw `javaExpression`) | `PointcutMatcher.java:117` family (`IfPC` treated as match-true; weaver emits the guard) | `IfGuardEmitter.java:53` (2-shape lowering: `o==null`→`if-nez`, `!Thread.holdsLock(o)`→`invoke-static`+branch; fail-loud default) | COVERED | `br.unb.cic.rv.grammar.IfGrammarTest#nullCheckShapeLowersToIfNez` | §4.I (R11.5, fork-free in-weaver). `IF_PCD` pipeline 0,0,3 (generic_new). Pattern: `(?:^\|&&\|\|\|)\s*if\s*\(`. |
| `handler(TypePattern)` | 0,0,0,0 | 0,0,0 | MISSING (`NamedRefPC` fallback) | `PointcutMatcher.java:158-159` (always-match) | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.HandlerGrammarTest#handlerAbsorbedByNamedRefPC` | deferred.md §2.1 (round-8 P-decision: moved from β to α — zero source demand; no DEX-level exception-handler join point). `HANDLER` = 0 in every corpus. |
| `get(FieldPattern)` | 0,0,0,0 | 0,0,0 | MISSING (no `FieldAccessPC`) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.FieldAccessGrammarTest#getFieldAccessHasZeroCorpusDemand` | deferred.md §2.1: zero demand (the earlier 356/158 count conflated `call(* Foo.get(...))` with field access). `GET_FIELD` = 0 in every corpus. Pattern: `(?:^\|&&\|\|\|)\s*get\s*\(`. |
| `set(FieldPattern)` | 0,0,0,0 | 0,0,0 | MISSING (no `FieldAccessPC`) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.FieldAccessGrammarTest#setFieldAccessHasZeroCorpusDemand` | deferred.md §2.1: zero demand. `SET_FIELD` = 0 in every corpus. Pattern: `(?:^\|&&\|\|\|)\s*set\s*\(`. |
| `staticinitialization(TypePattern)` | 0,0,0,3 | 0,0,3 | `PointcutExpressionParser.java`/`parseStaticInitBody` → `StaticInitPC` | `PointcutMatcher.java:517-528` (`matchStaticInit` — class `<clinit>`, `T+` subtype via `InheritanceResolver`) | `StaticInitializationEmitter.java:56` + `StaticInitSynthesizer.java:54` (synthesize `<clinit>` when absent) | COVERED | `br.unb.cic.rv.grammar.StaticInitializationGrammarTest#staticinitializationTSubtype` | §4.Y. 3 generic_new sites; weaver synthesizes a `<clinit>` when the matched class has none. |
| `initialization(ConstructorPattern)` | 0,0,0,0 | 0,0,0 | MISSING (`NamedRefPC` fallback) | `PointcutMatcher.java:158-159` (always-match) | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.InitializationGrammarTest#initializationHasZeroCorpusDemand` | deferred.md §2.1: zero demand (`staticinitialization` is in-change via §4.Y; constructor-init variants have zero demand). `INITIALIZATION` = 0 in every corpus. |
| `preinitialization(ConstructorPattern)` | 0,0,0,0 | 0,0,0 | MISSING (`NamedRefPC` fallback) | `PointcutMatcher.java:158-159` (always-match) | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.InitializationGrammarTest#preInitializationHasZeroCorpusDemand` | deferred.md §2.1: zero demand. `PREINITIALIZATION` = 0 in every corpus. |
| `adviceexecution()` | 0,0,0,0 | 1,1,1 | `PointcutExpressionParser.java` → `NamedRefPC` (`!adviceexecution()` in commonPointcut) | `PointcutMatcher.java:158-159` (`adviceexecution` family vacuously true) | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.AdviceExecutionGrammarTest#adviceExecutionVacuouslyTrueInDexlib2InlineModel` | Absorber `dexlib2-inline-emission-model`. `ADVICEEXECUTION` pipeline 1,1,1 (the `!adviceexecution()` clause in `commonPointcut`); vacuously satisfied because the inline-call model emits no synthetic `ajc$*` advice methods. deferred.md §2.2.1-C. |
| named-pointcut reference | 0,jca,0,gn | jca,_,gn | `PointcutExpressionParser.java` → `NamedRefPC.java:14`; `BaseAspectExpander.java:25` expands `BaseAspect.notwithin()` | `PointcutMatcher.java:158` (`NamedRefPC`; resolved exclusions matched as `NotWithinPC` AND-chain) | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.NamedReferenceGrammarTest#baseAspectNotwithinExpandsTwelveExclusionsList` | §4.B/§4.D. `BaseAspect.notwithin()` resolves against `descriptor.baseAspectExclusions` (canonical 12-entry list) into an AND-chain of `!within(...)`. |
| `condition(BooleanExpression)` | 0,64,0,10 | 0,0,0 | n/a (absorbed before `.aj`) | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.ConditionGrammarTest#conditionAbsorbedByRuntimeMonitor` | Absorber `JavaMOP-compiler`. `CONDITION` source 0,64,0,10 / pipeline 0,0,0 — the JavaMOP compiler folds the predicate into the `*RuntimeMonitor.*Event(...)` body before emitting the `.aj`. deferred.md §2.2.1-A. Pattern: `(?<![A-Za-z0-9_])condition\s*\(`. |
| `__STATICSIG` macro | 0,0,0,3 | 0,0,0 | n/a (absorbed before `.aj`) | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.StaticSigGrammarTest#staticSigAbsorbedByJavaMopCompiler` | Absorber `JavaMOP-compiler`. `STATICSIG` source 0,0,0,3 / pipeline 0,0,0 — the compiler inlines it as `thisJoinPoint.getStaticPart().getSignature()`. deferred.md §2.2.1-B. Pattern: `__STATICSIG`. |
| `within(pkg..*)` positive simple | 24,0,0,0 | 0,0,0 | `PointcutExpressionParser.java` (not exercised positively) | n/a (positive within absorbed) | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.WithinPositiveGrammarTest#withinPositiveAbsorptionAssertion` | Absorber `coverage-weaver` (R11.2). `WITHIN_POSITIVE` source 24 (Coverage.aj `excludedPackages()` only) / pipeline 0,0,0. deferred.md §2.2.1-I. Pattern: `(?<![A-Za-z0-9_!])within\s*\(`. |
| `within(*..Log)` suffix-wildcard | 1,0,0,0 | 0,0,0 | n/a (Coverage.aj only) | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.WithinExtensionsGrammarTest#withinSuffixAndTPlusAbsorbedByCoverageWeaver` | Absorber `coverage-weaver`. Suffix-wildcard `within(...)` sub-form; sole consumer is `Coverage.aj`, whose package exclusions are reproduced by `coverage-weaver/PackageFilter.java:20`. deferred.md §2.2.1-G. |
| `within(T+)` T+-inside-positive | 1,0,0,0 | 0,0,0 | n/a (Coverage.aj only) | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.WithinExtensionsGrammarTest#withinSuffixAndTPlusAbsorbedByCoverageWeaver` | Absorber `coverage-weaver`. `within(Coverage+)` subtype-aware positive-within sub-form; sole consumer is `Coverage.aj`. deferred.md §2.2.1-G. |
| `!within(TypePattern)` | 0,0,0,0 | 13,13,13 | `PointcutExpressionParser.java:75-101` (`!within` is the dominant negation) → `NotWithinPC` | `PointcutMatcher.java:178-183` (`matchNotWithin`, prefix/literal type-pattern) + `matchesTypePattern:557` | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.NamedReferenceGrammarTest#baseAspectNotwithinExpandsTwelveExclusionsList` | `NOT_WITHIN` pipeline 13,13,13 (the `commonPointcut` RVMObject/baseAspect exclusions); flows via §4.B/§4.D. Pattern: `!\s*within\s*\(`. |
| `@annotation(AnnotationType)` | 0,0,0,0 | 0,0,0 | MISSING (parser rejects `@` as ident char) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.AnnotationPointcutGrammarTest#atAnnotationHasZeroCorpusDemand` | deferred.md §2.1: zero demand across all four corpora; no `AnnotationPC`. |
| `@target(AnnotationType)` | 0,0,0,0 | 0,0,0 | MISSING (parser rejects `@`) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.AnnotationPointcutGrammarTest#atTargetHasZeroCorpusDemand` | deferred.md §2.1: zero demand; no impl. |
| `@this(AnnotationType)` | 0,0,0,0 | 0,0,0 | MISSING (parser rejects `@`) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.AnnotationPointcutGrammarTest#atThisHasZeroCorpusDemand` | deferred.md §2.1: zero demand; no impl. |
| `@args(AnnotationType)` | 0,0,0,0 | 0,0,0 | MISSING (parser rejects `@`) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.AnnotationPointcutGrammarTest#atArgsHasZeroCorpusDemand` | deferred.md §2.1: zero demand; no impl. |
| `@within(AnnotationType)` | 0,0,0,0 | 0,0,0 | MISSING (parser rejects `@`) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.AnnotationPointcutGrammarTest#atWithinHasZeroCorpusDemand` | deferred.md §2.1: zero demand; no impl. |
| `@withincode(AnnotationType)` | 0,0,0,0 | 0,0,0 | MISSING (parser rejects `@`) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.AnnotationPointcutGrammarTest#atWithincodeHasZeroCorpusDemand` | deferred.md §2.1: zero demand; no impl. |
| `before()` advice | 0,jca,_,gn | jca,_,gn | `PointcutExpressionParser.java` (advice position from descriptor) | n/a (advice form, not a pointcut) | `EmitterDispatch.java:78-80` → `BeforeEmitter` (`invoke-static` at `BEFORE` insertion point) | COVERED | `br.unb.cic.rv.grammar.AdviceFormGrammarTest#beforeAdviceEmitsMonitorInvokeAtEntry` | 18 jca + 43 generic_new pipeline sites; emits the monitor invoke at method/call entry. |
| `after()` advice | 0,jca,_,gn | jca,_,gn | `PointcutExpressionParser.java` | n/a | `EmitterDispatch.java:81` → `AfterEmitter` (`invoke-static` at `AFTER`) | COVERED | `br.unb.cic.rv.grammar.AdviceFormGrammarTest#afterAdviceEmitsMonitorInvokeAtExit` | 97 jca + 14 generic_new sites; emits at the exit insertion point. |
| `after() returning(Id)` advice | 0,jca,_,gn | jca,_,gn | `PointcutExpressionParser.java` (returning binding) | n/a | `EmitterDispatch.java:86` → `AfterReturningEmitter` (wrapper-substitution, no inline scratch) | COVERED | `br.unb.cic.rv.grammar.AdviceFormGrammarTest#afterReturningAdviceEmitsAtExitWithoutInlineScratch` | 64 jca + 8 generic_new sites; binds `$return` to the move-result destination register. |
| `after() throwing(Id)` advice | 0,0,0,1 | 0,0,1 | `PointcutExpressionParser.java` (throwing binding) | n/a | `AfterThrowingEmitter.java:27` + `DexWeaver.java:798-820` (`TRY_CATCH_WRAP` install with range-splitting) | COVERED | `br.unb.cic.rv.grammar.AfterThrowingGrammarTest#installsTryRangeAndHandler` | §4.T. Installs a try-range over the matched invoke; handler delivers the monitor event then re-throws. |
| `around()` advice | 0,0,0,0 | 0,0,0 | `PointcutExpressionParser.java` (advice flagged around) | n/a | `EmitterDispatch.java:61-65` — `throw new UnsupportedOperationException("around advice is out of scope...")` | EXPLICIT-NO-OP | `br.unb.cic.rv.grammar` — no-op anchor `EmitterDispatch.java:61-65`; UOE assertion `br.unb.cic.rv.emitter.EmitterDispatchTest#aroundAdviceRejected` (`EmitterDispatchTest.java:54-58`) | deferred.md §1: zero demand; `around` needs wrapper-method synthesis with `proceed()`, a substantial DEX feature with no current use case. |
| `T+` in `call()` param | 0,jca,_,gn | jca,_,gn | `PointcutExpressionParser.java`/`parseCallBody` → `CallPC.ParamSpec.isSubtype` | `PointcutMatcher.java:377-396` (param subtype via `InheritanceResolver`) | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.CallPatternGrammarTest#parameterPositionTSubtypeMatchesSubtypeArg` | gh61 + §4.O param-subtype path; `T+` in a parameter position matches a subtype argument. |
| `T+` in `call()` owner | 0,0,0,71 | 0,0,64 | `PointcutExpressionParser.java`/`parseCallBody` → `CallPC.declaringType` retains trailing `+` | `PointcutMatcher.java:326-347` (owner subtype via `InheritanceResolver`, FQN form) | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.CallPointcutGrammarTest#callTSubtypeInOwner` | §4.O. `CALL_OWNER_TSUBTYPE` pipeline 0,0,64 (per-occurrence of the `+.` owner token). Pattern: `[A-Za-z0-9_]\+\.`. |
| `T+` in `call()` return | 0,0,0,0 | 0,0,0 | MISSING (return-descriptor subtype branch never built, R11.3) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.CallPointcutGrammarTest#returnPositionTSubtypeNotNeeded` | §4.R' (R11.3). `CALL_RETURN_TSUBTYPE` = 0 in `.mop`, `Coverage.aj`, and all 3 pipeline `.aj`; all subtype polymorphism is OWNER-position (§4.O). deferred.md §2.1. Pattern: `call\s*\(\s*[A-Za-z_][A-Za-z0-9_.]*\+\s+`. |
| `T+` inside `!within(...)` | 0,0,0,0 | 0,0,0 | MISSING (`matchesTypePattern` strips `+` before exact-match) | `PointcutMatcher.java:557` (`matchesTypePattern` strips `+`) | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.TypePatternGrammarTest#tPlusInsideNegativeWithinHasZeroCorpusDemand` | deferred.md §2.1: zero demand (`countMop == 0` across all four corpora AND `countCompiledAj == 0`); no parser/matcher impl for the negated `T+` form (positive `within(T+)` is itself β/Coverage.aj-only; the negated form has no use). The assertion test pins the path-α zero-demand condition. |
| `*` wildcard | 0,0,356,74 | 0,240,67 | `PointcutExpressionParser.java`/`parseCallBody` → `CallPC.returnType == "*"` | `PointcutMatcher.java:353-364` (`matchCall` skips the return-equality gate when the pattern return is `*`) | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.CallReturnWildcardGrammarTest#returnWildcardMatchesAnyReturnOnRealCorpusForm` | §4.RW return-type `*` (`call(* Owner.name(..))`): `matchCall` skips the return-equality gate when the pattern return is `*`, symmetric to the args/name `*` handling. `CALL_RETURN_WILDCARD` pipeline 0 jca / 240 generic / 67 generic_new. Pattern: `call\(\* `. |
| `..` standalone varargs | 0,jca,_,gn | jca,_,gn | `PointcutExpressionParser.java`/`parseCallBody` → `CallPC.varargs` (empty head) | `PointcutMatcher.java:366-376` (varargs: actual arity ≥ head size; empty head = match-anything) | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.CallPatternGrammarTest#standaloneVarargsMatchesAnyParams` | `(..)` matches any parameter list. |
| `(T, ..)` trailing-mixed | 0,5,0,0 | 6,0,0 | `PointcutExpressionParser.java`/`parseCallBody` → `CallPC.ParamList { head; trailingVarargs }` | `PointcutMatcher.java:366-396` (pins the head, accepts any tail) | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.CallPointcutGrammarTest#trailingMixedVarargsMatchHeadAndAcceptRest` | §4.V. `CALL_TRAILING_VARARGS` source 0,5,0,0 (`.mop` jca) / pipeline 6,0,0 (the JavaMOP compile splits one multi-clause `.mop` site into 2 `call()` sites). Pattern: `call\([^)]*, \.\.\)`. |
| `..*` dot-glob | 0,_,_,_ | _,_,_ | `PointcutExpressionParser.java` → `NotWithinPC` type pattern | `PointcutMatcher.java:557` (`matchesTypePattern` prefix match on `pkg..*`) | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.CallPatternGrammarTest#packageGlobsMatchViaNotWithin` | `pkg..*` package glob, exercised through the `!within(...)` path (the dominant corpus consumer). |
| `.*` single-level glob | 0,_,_,_ | _,_,_ | `PointcutExpressionParser.java` → `NotWithinPC` type pattern | `PointcutMatcher.java:557` (`matchesTypePattern` single-level match on `pkg.*`) | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.CallPatternGrammarTest#packageGlobsMatchViaNotWithin` | `pkg.*` excludes a direct member but not a deeper-nested class, via the `!within(...)` path. |
| `T[]` / `T[][]` arrays | 0,_,_,_ | _,_,_ | `PointcutExpressionParser.java`/`parseCallBody` → `CallPC.ParamSpec` array descriptor | `PointcutMatcher.java:377-396` (`expectedDesc.contentEquals(actual)` on the `[B`/`[[…]` descriptor) | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.CallPatternGrammarTest#arrayParameterMatchesArrayDescriptor` | Array descriptors in a signature match exactly (`byte[]` → `[B`). |
| `Outer.Inner` inner-class qualifier | 0,0,0,0 | 0,0,0 | n/a (no `Outer.Inner`-vs-`Outer$Inner` disambiguation; owners resolve as plain descriptors) | n/a | n/a | NOT-NEEDED α | `DemandCounter.countMop == 0` across all four corpora; no parser/matcher impl distinguishing `Outer.Inner` from `Outer$Inner` | deferred.md §3 / spec §"Type-pattern modifiers": no corpus pointcut spells a nested inner-class owner with the `.`-qualifier form; nested owners that do appear are matched by their plain dex descriptor. No backing test (path-α structural verdict). |
| positive visibility (`public`) | 0,jca,_,_ | jca,_,_ | `PointcutExpressionParser.java`/`parseCallBody` strips the leading visibility modifier | `PointcutMatcher.java:308-406` (resolves name/owner/return after the modifier is stripped) | n/a | COVERED | `br.unb.cic.rv.grammar.SignatureModifierGrammarTest#positiveVisibilityModifierStrippedAndCallStillResolves` | `call(public ...)` parses with the modifier stripped; the underlying method still matches. Real JCA advice always spells `public` (25+ sites). |
| negated visibility (`!public`) | 0,0,0,0 | 0,0,0 | MISSING (no modifier-matching predicate) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.SignatureModifierGrammarTest#negatedVisibilityHasZeroCorpusDemand` | deferred.md §2.1: zero demand; modifiers are stripped, never used as a discriminating predicate. |
| `static` signature modifier | 0,0,0,0 | 0,0,0 | MISSING (descriptive prefix stripped; no exclusive-static filter) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.SignatureModifierGrammarTest#staticModifierHasZeroCorpusDemand` | deferred.md §2.1: zero demand for modifier discrimination; bare `static` is stripped (and the positive-visibility leg covers the strip path). |
| `final` signature modifier | 0,0,0,0 | 0,0,0 | MISSING (descriptive prefix stripped) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.SignatureModifierGrammarTest#finalModifierHasZeroCorpusDemand` | deferred.md §2.1: zero demand (same rationale as `static`). |
| `throws ExceptionPattern` | 0,0,0,0 | 0,0,0 | MISSING (no throws-clause matching) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.SignatureModifierGrammarTest#throwsModifierHasZeroCorpusDemand` | deferred.md §2.1: zero demand; the `throws` signature clause appears in no corpus pointcut. |
| `&&` composition | 0,jca,_,gn | jca,_,gn | `PointcutExpressionParser.java` (`and := unary ('&&' unary)*`) → `CombinedPC.AND` | `PointcutMatcher.java` (`CombinedPC` AND with merged bindings) | n/a | COVERED | `br.unb.cic.rv.grammar.CallPatternGrammarTest#compositionAndGroupingMatch` | Every corpus pointcut AND-composes `call()` with `target`/`args`/`!within`. |
| `\|\|` composition | 0,_,_,_ | _,_,_ | `PointcutExpressionParser.java` (`or := and ('\|\|' and)*`) → `CombinedPC.OR` | `PointcutMatcher.java` (`CombinedPC` OR with merged bindings) | n/a | COVERED | `br.unb.cic.rv.grammar.CallPatternGrammarTest#compositionAndGroupingMatch` | `\|\|` inside parentheses matches either alternative. |
| `!` negation | 0,0,0,16 | 0,0,16 | `PointcutExpressionParser.java:75-101` (`!within`/`!target`/`!args` specialization, §4.N) → `NotWithinPC`/`NegationPC` | `PointcutMatcher.java:178-199` (`matchNotWithin`, negation inversion) | n/a (gate) | COVERED | `br.unb.cic.rv.grammar.CompositionGrammarTest#negativeTargetArgsParserSpecialization` | §4.N. `!target(T)`/`!args(T)` invert the inner type verdict. `NOT_TARGET`+`NOT_ARGS` pipeline 14+2=16 (generic_new). Patterns: `!\s*target\s*\(`, `!\s*args\s*\(`. |
| parentheses grouping | 0,_,_,_ | _,_,_ | `PointcutExpressionParser.java` (`primary := '(' or ')'`) | `PointcutMatcher.java` (grouped sub-expression matched as a unit) | n/a | COVERED | `br.unb.cic.rv.grammar.CallPatternGrammarTest#compositionAndGroupingMatch` | Parenthesised grouping parses and composes correctly (`(call(...) \|\| call(...))`). |
| `thisJoinPoint` binding | 1,0,0,0 | 0,0,0 | n/a (Coverage.aj only) | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.JoinPointReflectiveApiGrammarTest#reflectiveApiFamilyAbsorbedByCoverageWeaver` | Absorber `coverage-weaver`. Sole consumer is `Coverage.aj`; `coverage-weaver` injects `Lmop/Coverage;->log(...)` referencing no `org.aspectj.lang.*` type. deferred.md §2.2.1-D/F. |
| `thisJoinPointStaticPart` binding | 1,0,0,0 | 0,0,0 | n/a (Coverage.aj only) | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.JoinPointReflectiveApiGrammarTest#reflectiveApiFamilyAbsorbedByCoverageWeaver` | Absorber `coverage-weaver`. deferred.md §2.2.1-D/F. |
| `thisEnclosingJoinPointStaticPart` binding | 1,0,0,0 | 0,0,0 | n/a | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.JoinPointReflectiveApiGrammarTest#reflectiveApiFamilyAbsorbedByCoverageWeaver` | Absorber `coverage-weaver`. Member of the `org.aspectj.lang.JoinPoint` reflective family (family source = 1, the `thisJoinPoint`/`thisJoinPointStaticPart` site in `aspect/Coverage.aj`, the sole consumer); absorbed by `coverage-weaver`, which references no `org.aspectj.lang.*` type. deferred.md §2.2.1-D/F. |
| `JoinPoint.getArgs()` | 1,0,0,0 | 0,0,0 | n/a (Coverage.aj only) | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.JoinPointReflectiveApiGrammarTest#reflectiveApiFamilyAbsorbedByCoverageWeaver` | Absorber `coverage-weaver`; the reflective accessor never appears in woven DEX. deferred.md §2.2.1-D/F. |
| `JoinPoint.getSignature()` | 0,0,0,3 | 0,0,3 | `PointcutExpressionParser.java` (staticinit advice carries the literal token) | `PointcutMatcher.java:517-528` (`matchStaticInit`) | `StaticInitializationEmitter.java:56` delivers `org.aspectj.lang.ClassSignature` (`rvsec-core`) to `*staticinitEvent(Signature)` | COVERED | `br.unb.cic.rv.grammar.StaticInitializationGrammarTest#signatureDeliveryForStaticinitEvent` | §4.Y (round-10 AC / R11.5). The one reflective accessor that survives the pipeline (3 generic_new staticinit sites); delivered fork-free via the `rvsec-core` Signature substrate (only `getDeclaringType()` exercised). |
| `JoinPoint.getTarget()` | 1,0,0,0 | 0,0,0 | n/a (Coverage.aj only) | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.JoinPointReflectiveApiGrammarTest#reflectiveApiFamilyAbsorbedByCoverageWeaver` | Absorber `coverage-weaver`. deferred.md §2.2.1-D/F. |
| `JoinPoint.getKind()` | 1,0,0,0 | 0,0,0 | n/a (Coverage.aj only) | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.JoinPointReflectiveApiGrammarTest#reflectiveApiFamilyAbsorbedByCoverageWeaver` | Absorber `coverage-weaver`. deferred.md §2.2.1-D/F. |
| `proceed(...)` | 0,0,0,0 | 0,0,0 | n/a (subsumed by `around` deferral) | n/a | `EmitterDispatch.java:61-65` (the `around` rejection covers `proceed` — the around emit never runs) | EXPLICIT-NO-OP | `br.unb.cic.rv.grammar` — no-op anchor `EmitterDispatch.java:61-65`; UOE assertion `br.unb.cic.rv.emitter.EmitterDispatchTest#aroundAdviceRejected` (`EmitterDispatchTest.java:54-58`) | deferred.md §1: subsumed by `around` deferral; the matrix row cites the same anchor (EXPLICIT-NO-OP by reflection). |
| `aspect Foo { ... }` | 1,2,_,2 | 0,0,0 | n/a (consumed by JavaMOP descriptor emit; parser never sees `aspect` token) | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.AspectDeclarationGrammarTest#aspectFooAbsorbedByDescriptorReader` | Absorber `DescriptorReader`. Source ≥ 1 (Coverage.aj declares one aspect); every `AdviceDescriptor.expression` is a pure pointcut expression. deferred.md §2.2.2. |
| `pointcut p(): ...` declaration | 2,1,_,1 | 0,0,0 | n/a (named pointcut inlined at descriptor-emit time) | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.AspectDeclarationGrammarTest#namedPointcutDeclarationAbsorbedByDescriptorReader` | Absorber `DescriptorReader`. Source ≥ 1 (Coverage.aj declares two named pointcuts); flattened descriptor `expression` fields carry no `pointcut` declaration token. deferred.md §2.2.2. |
| `abstract aspect` + concrete subaspect | 0,0,0,0 | 0,0,0 | MISSING (no parser path) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.AspectDeclarationGrammarTest#abstractAspectHasZeroCorpusDemand` | deferred.md §2.1 (round-8 P-decision): zero source demand across all corpora. |
| aspect inheritance | 0,0,0,0 | 0,0,0 | MISSING (no parser path) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.AspectDeclarationGrammarTest#aspectInheritanceHasZeroCorpusDemand` | deferred.md §2.1 (round-8 P-decision): zero source demand. |
| `declare precedence` | 0,0,0,0 | 0,0,0 | MISSING (no parser path) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.AspectDeclarationGrammarTest#declarePrecedenceHasZeroCorpusDemand` | deferred.md §2.1 (round-8 P-decision): zero source demand; `MonitorRuntime` dispatch loop would govern precedence if a spec declared one. |
| privileged aspect | 0,0,0,0 | 0,0,0 | MISSING (no parser path) | MISSING | MISSING | NOT-NEEDED α | `br.unb.cic.rv.grammar.AspectDeclarationGrammarTest#privilegedAspectHasZeroCorpusDemand` | deferred.md §2.1 (round-8 P-decision): zero source demand. |
| `org.aspectj.lang.JoinPoint` family | 1,0,0,0 | 0,0,0 | n/a (runtime linkage; sole consumer Coverage.aj) | n/a | n/a | NOT-NEEDED β | `br.unb.cic.rv.grammar.RuntimeSubstrateGrammarTest#aspectJSubstrateAbsorbedByCoverageWeaver` + `br.unb.cic.rv.grammar.CoverageAjAbsorptionGrammarTest#coverageAjAbsorbedByCoverageWeaver` | Absorber `coverage-weaver`. The `JoinPoint`/`StaticPart` runtime linkage's sole consumer is `Coverage.aj`, whose end-to-end weaving is absorbed by `coverage-weaver` (emits `Lmop/Coverage;->log(...)` with the signature pre-computed at weave time, referencing no `org.aspectj.lang.*` type — `CoverageAjAbsorptionGrammarTest` asserts the §4.CV' end-to-end absorption, deferred.md §2.2.1-F). `org.aspectj.lang.Signature` is NOT part of this row — COVERED via §4.Y (`rvsec-core`). deferred.md §2.2.1-D/F. |

<!-- §5 populated (round-8 source+pipeline columns): every row carries a verdict + evidence.
     Demand columns: PipelineDemand quotes the REAL DemandCounter.countCompiledAj values for the
     designators with a registered regex key (condition, __STATICSIG, adviceexecution,
     execution_positive, within_positive, not_within, if_pcd, target_type, args_type,
     not_target+not_args, call_return_tsubtype, call_owner_tsubtype, call_name_glob,
     call_trailing_varargs, call_return_wildcard — see §1.2 DEMAND BASELINE in tasks.md and the
     DemandCounter.PATTERNS map). Rows whose designator has no registered DemandCounter key
     (e.g. the base call()/before()/after() forms, package globs, composition operators) quote a
     qualitative volume marker (many / jca / gn / _ for "present, not separately keyed") rather than
     a fabricated integer — only the §1.2-baseline keyed designators have reproducible counts that
     MatrixIntegrityTest.testPipelineDemandCountsReproducible (§6.8) can assert against.

     Verdict tally (73 rows): COVERED 28 | EXPLICIT-NO-OP 2 (around, proceed) |
     NOT-NEEDED β 16 | NOT-NEEDED α 27. Zero TBD cells, zero SILENT-GAP.

     Evidence FQNs: every COVERED and every NOT-NEEDED β / α row that has a dedicated grammar test
     cites a REAL enabled passing method in br.unb.cic.rv.grammar.*GrammarTest (verified against the
     live suite). Twelve NOT-NEEDED α rows have NO dedicated test (this(name), this(Type),
     withincode, cflow, cflowbelow, handler, get, set, initialization, preinitialization,
     T+ inside !within(...), Outer.Inner) — their Evidence states the path-α structural condition
     (countMop == 0 AND no parser/matcher impl), which is exactly what INV-INS-89 path α requires;
     no FQN is fabricated for them.

     The "AspectJ syntax" column is the stable key: it MUST stay in 1:1 set-equality with
     AspectJDesignators.DESIGNATORS (INV-INS-88). 73 rows. -->

[tool.poetry]
name = "rv-evaluator"
version = "0.1.0"
description = "Basic LLM models evaluation for RV-Android"
authors = ["RV-Android Team"]
readme = "README.md"
packages = [{include = "rv_evaluator", from = "src"}]

[tool.poetry.dependencies]
python = ">=3.12,<4.0"
# Dependências de todos os módulos (orquestrador final)
rv-platform = {path = "../rv-platform", develop = true}
rv-android-core = {path = "../rv-android-core", develop = true}
rv-static-analysis = {path = "../rv-static-analysis", develop = true}
rv-screen-parser = {path = "../rv-screen-parser", develop = true}
rv-coverage = {path = "../rv-coverage", develop = true}
rv-llm = {path = "../rv-llm", develop = true}
rv-tools = {path = "../rv-tools", develop = true}
rvandroid-tool = {path = "../rvandroid-tool", develop = true}
pydantic = "^2.9.0"
# Dependências específicas para orquestração
matplotlib = "^3.9.0"  # Para silenciar logs verbosos no CLI
pandas = "^2.3.1"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"
pytest-cov = "^6.0.0" 

[tool.poetry.scripts]
rv-experiment = "rv_evaluator.__main__:main"

[tool.pytest.ini_options]
addopts = "--cov=src"


[tool.coverage.run]
source = ["src"]
omit = [
    "*/tests/*",
    "*/test_*.py",
]

[tool.coverage.report]
# show_missing = true
# fail_under = 80


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
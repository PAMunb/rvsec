# Resolved at build time by the parent's resources filtering. The drift test fails when this
# directory does not exist, rather than skipping: a schema check that can silently not run is a
# green with nothing behind it.
committed.schemas.dir=${committed.schemas.dir}

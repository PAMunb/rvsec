# Interpolated by Maven at generate-test-resources, so these are the values of the EFFECTIVE pom,
# not the text of any single pom file. DependencyDisciplineTest reads them back.
guava.version=${guava.version}
scala.version=${scala.version}
